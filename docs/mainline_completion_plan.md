# Mainline completion plan — M-1 through M-4

**Date:** 2026-07-23. **Status:** brief frozen; construction only, no
new law. Everything below executes under the adopted 25-item ledger.
Standing rules for every phase: create-new artifacts with replay and
mutation falsifiers; sealed bytes never edited or reformatted; drifts
disclosed as testimony, never repaired; no value, bar, hash, or
enumeration order as selector anywhere; register tags per
`nu-register-split-v1` (F-NR6) on every published number; any
required semantics beyond adopted rules → named gap, versioned
adjudication, stop. The BI-4 v2 disposition table
(`blake3:75a9dcc0…`) fixes every quantifier: cone-level = demand
ladder, halt at 15, semantic O(16) = ∅, F1 exclusion, full-instance
Theorem 12, per-stage ledgers 5–15; branch-indexed = Stage-4 act and
ν, Σν, bar trajectories, WB-1 tables, provenance identities.

## M-1 — Transport-map export (gate for F-UC4)

Successor to `semantic_nu_invariance_v1` (which proved finite
family-bijection invariance but declined to export the maps).

- Export the typed cross-package transport maps as first-class
  certificate content: for each proved bijection, the typed map, its
  inverse, and identity/composition coherence, replayable.
- Regression: re-verify the v1 invariance result *through* the
  exported maps (the bijections must be exactly the ones the maps
  induce; count agreement alone is insufficient).
- No new semantics: maps are assembled from the adopted equality,
  naturality, and transport machinery. A map needing more → named
  gap, stop.
- Deliverable: `docs/semantic_nu_transport_maps_v2.json` + result doc.

## M-2 — UC-1 scoring artifact (after M-1)

Executes the scoring that `docs/univalent_collapse_hypothesis.md`
registered, against sealed artifacts only (BI-1/BI-1b/BI-2/BI-4
certificates and the correspondence diagnostic; nothing recomputed,
nothing new observed).

- **P1** (ledger identity 5–15): score from the BI-4 G3a verdict.
- **P2** (winner identity modulo fork shadow): stages 8–15 from the
  byte-identity record; stages 5–7 by the registered relabeling
  check — winners compared after canonical relabeling of Stage-4
  references. This is the one comparison not yet in a certificate;
  it is mechanical over sealed telescopes.
- **P3** (margin confinement): from the sealed R-T2 scheme sets and
  the branch demand records — verify every enacted live demand fell
  in the pairwise-matched scheme subsets.
- **F-UC4** (with M-1 maps): assess whether semantic-family counts
  are invariant under the adopted equivalence class; apply to the
  order axis (ν = 2 vs 3). If invariance holds, any order-axis
  equivalence is refuted and **UC-1a burns**; recorded per the
  registration's burn semantics (F-UC6: no rescue equivalences).
- **Zone assignment, honestly bounded:** Z-COLLAPSE versus Z-HEAL
  requires the U-T2 disposition (the act-level Π/Σ equivalence
  construction or its certified obstruction), which has never been
  attempted. The scoring artifact reports every decidable verdict
  and names `U_T2_UNATTEMPTED` as the sole residual — it does not
  guess the zone. U-T2 itself is post-mainline unless the adopter
  says otherwise.
- Deliverable: `docs/UC1_SCORING_V1_RESULT.md` + certificate.

## M-3 — Bridge (E-7/E-8; parallel-safe with M-1/M-2)

Implement the bridge per its existing spec (the rewritten Step-15
completion document), upgraded with cone-aware quantifiers:

- Every bridge statement declares its quantifier from the BI-4
  disposition table — cone-level claims quantify over all four
  branches; branch-level claims name their branch and register.
- The bridge consumes the semantic register only for law-level
  content; structural quantities appear solely as tagged testimony.
- UC-1 is neither cited nor anticipated (its scoring status is
  recorded in M-4, not used in M-3).
- Regression gate: the bridge machinery applied to the enacted
  branch must reproduce the sealed E-5/BI-2 results exactly before
  any cross-branch statement issues (F-BI5 pattern).
- Deliverable: bridge result doc + certificate per the E-7/E-8 spec,
  with the same stop semantics (named gaps, no improvisation).

## M-4 — The final certificate (after M-2 and M-3)

One create-new artifact, the theory's completion record:

- Binds by digest: the 25 adjudications, the certified halt chain
  (E-5 → BI-0 v6 → BI-1/1b → BI-2 → BI-4), the UC-1 scoring
  artifact, the bridge, and the drift-disclosure testimony.
- States the two-law theory in its adopted final form: generative
  law (nothing without demand); two-register gating (debt gates
  guarded stages, certified value gates the open band); constitutive
  bootstrap; explicit nondeterminism at Stage 4 (choice secured
  against value, not proof); halt at debt-freedom, cone-level.
- Splits every claim per the disposition table, tags every number's
  register, and lists explicitly what remains open: U-T2, WB-1's
  W-T1/W-T2, the parked conjecture, and the external-falsifier
  frontier. Silence is not disposition.
- Mutation falsifiers: flipping any verdict, digest, quantifier, or
  register tag must invalidate replay.
- Deliverable: `docs/FINAL_CERTIFICATE_V1.md` +
  `docs/final_certificate_v1.json`.

## Order and gating

M-1 → M-2. M-3 parallel with both. M-4 after M-2 and M-3. Any stop
in any phase returns to the adopter with the named gap; no phase
proceeds past a failed regression rung. Parked and non-gating
throughout: WB-1's tests, U-T2, the R2 injective-join strengthening,
the diagnostic-policy question — none may be smuggled in as a side
effect of mainline work.
