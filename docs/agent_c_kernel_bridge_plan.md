# Agent C — KERNEL-BRIDGE: the three blocking premises (long pole)

> **Successor update (2026-07-19):** TRUNC-ER subsequently closed the
> registered d = 1 endpoint-sensitive realizer, and HIST-CERT v3 consumed
> all four exact-prefix bundles to discharge F-B2. General C6,
> candidate-level C8, intended-schema classification, and F-T1 remain open.
> See `docs/TRUNC_ENDPOINT_REALIZER_RESULT.md` and
> `docs/HIST_CERT_V3_RESULT.md`. The earlier execution snapshot below is
> archival.

> **Execution update (2026-07-19):** Addendum 2 has been executed. The
> registered v3 C7 bindings and the Step 5/7/8 exact-prefix constant-boundary
> C6 bridges replay; general C6, the Trunc endpoint-sensitive realizer,
> candidate-level C8, and F-B2 remain open. See
> `docs/KERNEL_BRIDGE_V3_ELEMENT_OVERLAY_RESULT.md` and
> `docs/ip1_candidate_verdict_join_v5_element_overlay.json`.

> **Addendum 2 (2026-07-19, post KERNEL-BRIDGE v4):** the boundary axiom
> in force is now
> `pathcon-attachment-declared-bound-boundary-theory-axiom-v3-element-overlay`
> (adopted, `docs/element_overlay_adjudication.md`), which contains v2
> verbatim plus `historical-element-declaration-overlay-v1` (three
> registered entries: S¹/S²/S³ base clauses read as `El` of their
> formation clauses, each requiring a replayable operational-role
> witness). C-6/C-7 resume under v3; obstruction
> `C-7-historical-point-clause-not-typed-as-owner-element-v1` is retired
> only by replayed witnesses; falsifiers F-O1..F-O3 (overlay) and the
> sharpened F-B2 bind this work. Conditionality flags advance to
> `conditional_on_boundary_axiom_v3`.

> **Addendum 2026-07-19 (post boundary adjudication — ADOPTED axioms now
> in force).** `docs/boundary_adjudication_proposal.md` records the
> adoption of `pathcon-attachment-declared-bound-boundary-theory-axiom-v2`
> (including its four registered historical diagrams) and
> `boundary-charge-zero-reference-only-v1`. This expands Agent C's scope:
>
> - **C-6 (V2 term-level completion).** The five obligations from
>   BOUNDARY-AUDIT "Remaining proof obligations": (1) elaborated
>   interval/cofibration contexts with typed point, face, and overlap
>   terms; (2) typed `coe`/`hcom` and the motive-typed `PathCon`
>   eliminator (starting stock: TDC v3 `pen-type::cubical`); (3)
>   weakening, substitution, and naturality for the parameterized Trunc
>   squash family; (4) typed realizers for the beta/principal-transport/
>   ordered-naturality keys; (5) independence and exhaustiveness proofs
>   before any presentation is used as a semantic classifier or basis.
>   Promote `boundary_variants` V2 from feature-gated audit code to the
>   adopted rule's implementation path; the V1 realizer remains as the
>   constant-boundary special case.
> - **C-7 (base-binding witnesses).** Replayable typed witnesses
>   identifying the implicit cubical base with the corresponding
>   historical point clause for S1, S2, S3 (mandatory-binding clause 2 of
>   the adopted axiom).
> - **C-8 (charging enforcement).** All basis formulas cite 1 + d² with
>   c(b) = 0 under `boundary-charge-zero-reference-only-v1`, by name and
>   version. Elaboration must enforce reference-only boundaries: a
>   boundary term not resolving to a sealed clause or context variable is
>   an elaboration error that names the hoisting rule.
> - **Ownership transfer.** `pen-type::cubical` passes from Agent A to
>   Agent C for the V2 phase (HIST-CERT v1 is shipped; Agent A's rerun
>   waits on C's realizers). Deliver realizer APIs so Agent A can rerun
>   HIST-CERT create-new — the rerun itself is Agent A's, not C's.
> - **Falsifiers F-B1..F-B3** (adjudication doc) bind this work: a
>   registered diagram that fails term-typing is reported verbatim — the
>   interpretation is revised by the user in a versioned successor axiom;
>   the checker is never weakened to admit it.

**Standing:** construction task. Admissible context:
`docs/IP1_REPAIRED_BOUNDARY_RESULT.md` (schema-3 artifact and its false
flags are the requirements list), `docs/ip1_tdc1_plan.md`, the T4 frozen
law. Forbidden: the bar value in any derivation path (the ρ-vs-Bar
comparison remains a separate post-certificate replay step); weakening any
A5 semantics; any silent promotion of conditional diagnostics to scores.

## Mission

The IP-1 v3 artifact is an operational checker result because three
candidate-level premises are false:

1. sort-preserving typed substitution/instance theorem,
2. complete naturality basis (depth-two fragment),
3. whole-telescope elaboration/fuel compositionality.

Build them. These same three items are what TDC-1 needs for
intended-schema classification — this is the single kernel investment both
tracks converge on. Success upgrades `ip1_candidate_verdict_join` from
checker-relative toward typed-semantic grade and unblocks any future
non-Z4 TDC verdict.

## Owned modules

Core `pen-type::{infer,normalize,equality}` (currently placeholders),
new `pen-type::substitution`, `pen-search::candidate_join` (schema-4).
Do **not** touch `pen-type::cubical` internals (Agent A extends them) or
`boundary_variants` (Agent B). Coordinate through types, not edits.

## Work items

1. **C-1 (substitution).** Typed substitution and instance operations on
   the kernel IR; sort-preservation theorem, mechanized as exhaustive
   checks over the finite clause grammar plus property tests; mirror the
   finite statements in Agda (`pen-agda` pattern) where feasible.
2. **C-2 (naturality basis).** Enumerate the naturality generators for the
   depth-two fragment, scoped to the classes that actually appear in the
   schema-3 quotient rows (63/72/78 rows at κ = 2/3/4). Completeness proof
   per class or a named gap. A class with a gap **stays unrankable** —
   its flags stay false; there is no partial promotion.
3. **C-3 (fuel compositionality).** Clause-local elaboration fuel bounds
   composed to a whole-telescope termination bound; fail-closed where
   composition cannot be proved.
4. **C-4 (rewire).** `candidate_join` schema 4 (create-new; schema 3 stays
   byte-for-byte archival): consume C-1..C-3, set
   `implemented_egp_bridge_returns_bound` and companions true exactly
   where earned, per row. Full mutation battery inherited from schema 3
   plus mutations of each new theorem's witness data.
5. **C-5 (P5 window re-audit).** Re-verify the public-lift boundary under
   the new substitution machinery: does the `App(Lib(14), Lib(13))`
   witness's status change? Does any *new* public token route open inside
   the raw leaf surface? The window claim ("unrankability is enforced by
   the two-layer window") must be re-earned, not assumed. If a route
   opens, that is F-IP2 firing again — report verbatim; the repaired
   boundary statement is revised or dies, and the artifact says which.

## Falsifiers / exit conditions

- Any bridge theorem failing on a quotient row → the row becomes a named
  obstruction; never dropped, never averaged away.
- C-5 finds an in-surface token route → report as F-IP2; do not patch the
  surface definition to close it (that would be tuning the domain to save
  the theorem).
- If C-2 completeness is unprovable for a class family after honest
  effort, deliver the named gap and stop; Z4-style honesty applies to
  bridges too.

## Done

Schema-4 artifact replays with per-row bridge flags earned or named gaps;
Agda mirror checks pass; schema-3 archival untouched; C-5 verdict recorded
either way. This agent does not emit any Step-16 acceptance conclusion —
that composition (bridge + A5) is a separate replay owned by the user.
