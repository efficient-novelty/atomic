# Agent E continuation — GRAMMAR-COMPLETE: the intended classes for steps 9–15

**Date:** 2026-07-20. **Standing:** construction plus interpretation
registration. Admissible context: all archival results through
`SCHEMA2_V4_RESULT.md` and `SCHEMA2_M1_SWEEP_RESULT.md`; the frozen spec
(`step_15_completion_open_problem.md`); the sealed trace; the full adopted
axiom/rule stack including `endpoint-premise-ledger-api-rule-v1` (public
endpoint-premise API now in force). Forbidden: the bar value; archived
counts as grounds for any constructor decision (F-E4); **pending membership
verdicts or sweep outcomes as grounds for any generator** (the adopted
verdict-blindness firewall — every new generator's recorded justification
must cite the sealed trace and frozen spec only, and must state explicitly
that no verdict, count, or sweep result was grounds).

## Mission

Schema2 currently speaks Genesis 1–8 and is mute about 9–15. Complete the
intended constructor inventory — Modal, Synthesis, and the Map/Axiomatic
remainder — by registering typed signatures for the sealed steps 9–15
vocabulary (the H-space-signature pattern, generalized), extend the v4
class-indexed induction over the new constructors, and prove the total
typed raw-candidate classifier. Success makes global E-4 completeness a
finite assembly rather than an open definition, unblocking E-2b and
everything downstream.

## Work items

- **G-1 (sealed-vocabulary survey — first, and gating).** Reconstruct the
  steps 9–15 telescopes from the reference trace (exact predecessor
  digests, as v2 did for Step 8); enumerate their raw clause shapes; and
  produce **one batched registered-signature proposal document** covering
  all seven steps, with per-signature grounds from the trace and spec.
  Where the trace underdetermines a signature, emit an ADJUDICATION
  REQUIRED entry — batched into the same document, so the user signs once,
  not seven times. No constructor work proceeds on an unadjudicated
  signature.
- **G-2 (Modal).** Typed constructors for the Step-10 modal shell (the
  modality string): formers, units/counits, composition laws as the trace
  exports them. Bias disclosure, recorded as such: J3-adjacent material is
  expected to be law-like and therefore generated-heavy; the expectation
  decides nothing.
- **G-3 (Temporal).** The Step-15 kernel is already characterized by the
  Guard-Rail's J3: two operator-formers, a comparison map, an interaction
  clause, two exchange laws, an eliminator, a composition law. Register
  these as typed constructors verbatim from that characterization — this
  is the one class whose signature list is effectively pre-adjudicated by
  an adopted theorem's hypotheses. Any mismatch between J3's list and the
  sealed Step-15 telescope is a report-verbatim event (it would touch
  Guard-Rail F3).
- **G-4 (Axiomatic/Map remainder).** Steps 9, 11–14 (axiomatic bundle,
  connection shell, curvature shell, operator bundle, Hilbert functional):
  the adjoint/mate/interchange grammar and the natural-transformation /
  exported-support-action grammar the v4 audit names as incomplete.
- **G-5 (Synthesis).** Typed constructors from the registered P6 /
  combinatorial-synthesis material, under the same trace-grounded
  signature discipline. If the registered material cannot ground typed
  rules, the class stays empty with a named gap — an empty class honestly
  held is a result.
- **G-6 (unit-coherence decision, verdict-blind).** Assess whether the
  sealed trace and frozen spec justify a typed unit-coherence
  constructor/generator — on their own terms. The recorded grounds must
  quote trace/spec material only; the M1 sweep outcome is recorded as
  non-grounds by explicit statement. Either outcome is fine: a
  trace-grounded generator enters the grammar (and the pending left-unit
  verdict then falls wherever E-4 puts it), or no generator is justified
  and the left-unit's fate rests with the completed basis. What is not
  fine is deciding this question by looking at what it does to the
  verdict.
- **G-7 (induction extension, rolling).** Every new constructor receives
  the v4 treatment: typed normalization/naturality tokens under a genuine
  non-renaming substitution; endpoint-dependent constructors use the
  public API (12-form induction); Agda mirrors under `--safe --without-K`,
  no postulates.
- **G-8 (total classifier, last).** The theorem retiring
  `E34_TOTAL_TYPED_RAW_CANDIDATE_TO_SCHEMA2_CLASSIFIER_NOT_PROVED`: every
  raw candidate within the frozen caps elaborates to Schema2 classes, or
  receives a named exclusion; Unknown rows end only by typed
  classification or named obstruction, never by omission.

## Falsifiers

- **F-G1.** A registered signature fails term-level typing → the
  interpretation is revised in a versioned successor; the checker is never
  loosened (F-B1 pattern).
- **F-G2.** Any constructor or generator whose recorded justification
  traces to a count, verdict, sweep, or bar → invalid on its face (adopted
  firewall + F-E4).
- **F-G3.** A sealed vocabulary item provably inexpressible in the
  intended grammar → a spec-level discovery about the record, reported
  verbatim; neither the grammar nor the record is bent to fit.
- **F-G4.** The classifier finds a raw candidate outside every class →
  the Unknown row persists, named (F-E1 pattern); exhaustion may not be
  claimed over it.
- **Standing.** All archival digests replay byte-stable; create-new
  artifacts; full mutation batteries; no E-2b output, stage count, F-Q2
  evaluation, or halt/continuation conclusion from this brief.

## Sequencing

G-1 first and alone (its output is the batched signature adjudication for
the user); G-2..G-6 after signature adoption, parallelizable; G-7 rolling
alongside; G-8 strictly last. On completion, the lawful sequence is:
global E-4 assembly → the five pending membership verdicts → E-2b (counts
as outputs, F-Q2) → Agent A's F-T1 discharge → E-5 (F1 live) → E-7/E-8 →
the preregistered reselection fork.
