\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsfonts,amsthm,mathtools}
\usepackage{booktabs,longtable,array,tabularx}
\usepackage{enumitem}
\usepackage[margin=1in]{geometry}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue!60!black,citecolor=blue!60!black,urlcolor=blue!60!black]{hyperref}
\usepackage{microtype}

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{principle}[theorem]{Principle}
\newtheorem{axiom}[theorem]{Axiom}

\theoremstyle{remark}
\newtheorem{notation}[theorem]{Notation}
\newtheorem{remark}[theorem]{Remark}

\newenvironment{proofsketch}{\begin{proof}[Proof sketch]}{\end{proof}}

\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcommand{\mname}[1]{\begingroup\ttfamily\footnotesize #1\endgroup}

\newcommand{\CL}{\mathrm{CL}}
\newcommand{\SLaw}{\mathrm{SL}}
\newcommand{\PEN}{\mathrm{PEN}}
\newcommand{\DCT}{\mathrm{DCT}}
\newcommand{\MBTT}{\mathrm{MBTT}}
\newcommand{\ATwoTT}{\mathrm{A2TT}}
\newcommand{\CCHM}{\mathrm{CCHM}}
\newcommand{\B}{\mathcal{B}}
\newcommand{\A}{\mathcal{A}}
\newcommand{\I}{\mathcal{I}}
\newcommand{\U}{\mathcal{U}}
\newcommand{\Ob}{\mathcal{O}}
\newcommand{\Hist}{\mathsf{Hist}}
\newcommand{\Cand}{\mathsf{Cand}}
\newcommand{\Real}{\mathsf{Real}}
\newcommand{\Supp}{\operatorname{Supp}}
\newcommand{\Aut}{\operatorname{Aut}}
\newcommand{\IsEquiv}{\operatorname{isEquiv}}
\newcommand{\ua}{\operatorname{ua}}
\newcommand{\Schemas}{\operatorname{Schemas}}
\newcommand{\KernelClauses}{\operatorname{KernelClauses}}
\newcommand{\SelBar}{\operatorname{Bar}}
\newcommand{\Str}{\operatorname{Str}}
\newcommand{\coiter}{\operatorname{coiter}}
\newcommand{\Spec}{\operatorname{Spec}}
\newcommand{\Ric}{\operatorname{Ric}}
\newcommand{\vol}{\operatorname{vol}}
\newcommand{\isSet}{\operatorname{isSet}}
\newcommand{\dom}{\operatorname{dom}}
\newcommand{\Disc}{\operatorname{Disc}}
\newcommand{\Shape}{\operatorname{Shape}}
\newcommand{\Flat}{\flat}
\newcommand{\Sharp}{\sharp}
\newcommand{\Next}{\bigcirc}
\newcommand{\Eventually}{\Diamond}
\newcommand{\argmin}{\operatorname*{arg\,min}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\D}{\mathbb{D}}
\newcommand{\HH}{\mathbb{H}}
\newcommand{\OO}{\mathbb{O}}
\newcommand{\SM}{SU(3)\times SU(2)\times U(1)}
\newcommand{\Tr}{\operatorname{Tr}}

\title{\textbf{The Answer Is Two}\\
\large A Technical Synthesis of the Two-Law Framework}
\author{Halvor S. Lande}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
The theory states that every determinate, history-bearing world requires exactly two primitive explanatory roles. The Constitutive Law determines admissible coherent structure; the revised Selective Law governs selection, exact realization, and entry into shared public history. Its formal face is two-register acceptance: at guarded stages (nonempty windowed obligation profile), acceptance is total discharge of the live demands with typed receipts, and no value quantity legislates; on the debt-free open band, acceptance requires certified earned novelty, which a provenance ceiling (\(\nu\le4\kappa\)) forecloses. The Principle of Efficient Novelty survives as the law's evidentiary standard --- novelty counts only when anchored to paid clauses or discharged demands --- and as the open band's foreclosing wall, never as a guarded-stage selector; where genuinely distinct total payments coexist, the law's silence is certified rather than broken, and history is lawfully a cone. Its realization face is free sealing: once an API is selected, the new layer is its least term-model completion, containing no unforced generators or equations and initial among exact realizations of that selected API. This relative initiality must not be confused with the false stronger claim that PEN always selects the initial completion of a weaker requirement fixed in advance. Its physical face is Internal Record Actualization plus Public Confluence: a formal seal becomes a public event only through a stable decodable internal record, and compatible public records must admit a joint nondemolition refinement; finite-capacity failure is ``common seal'' or ``no common seal,'' never contradictory sealed facts on one overlap. The Two Laws do not determine a universal public-event extractor \(\mathcal Q_p\), tolerance, metric clock calibration, or resource entropies. They do, however, support a complete model-level bridge: in an explicit seven-cell thermal repetition observer, the dynamics uniquely forces Hamming weight as the minimal sufficient statistic for the retained event claim, the optimal no-abstention seal has error \(521/15625\), all public coarse-grainings glue by exact nondemolition refinement, and the measured second-Renyi resources are \(n_p=4.887399\) bits and \(n_g=3.894753\) bits, giving \(K_{\rm cap}=3.307335\) bits. This closes the immediate finite-observer construction problem without converting an observer-specific dynamics into a universal law. The exact obligation-depth package gives \(d_{\mathrm{obl}}=2\), \(d_\mu\le2\), a two-layer chronological window, affine Fibonacci scaling (descriptive interface arithmetic, gating nothing), and the certified fifteen-step Genesis Sequence ending in a temporal-cohesive shell: a demand chain with exactly one live obligation per stage from 4 through 15, a machine-certified unique total discharger at every guarded stage except Step 4 --- where four genuinely distinct payments survive the full quotient, making lawful history a four-branch cone whose futures heal (per-stage ledgers identical from Step 5, structures byte-identical from Step 8, only cumulative diagnostics permanently branch-indexed) --- and a certified debt-free halt: the Step-16 obligation ledger is empty at full instance granularity on every branch, the demanded-but-underdetermined falsifier is executed and excluded on every branch, and the halt is therefore a theorem of the laws rather than of the enacted choice. Value bookkeeping is register-disciplined: the enacted engine's structural totals are sealed testimony, and all law-level counts are act-local, proof-bearing semantic-family values. A reparameterization no-go shows that the structural ledger does not calibrate this runtime to seconds or redshift. A second underdetermination result is equally important: constructive admissibility, composition, Genesis availability, free sealing, and Public Confluence do not uniquely select complex Born quantum theory. Classical simplices, real and quaternionic quantum fragments, and generalized probabilistic models survive appropriate subsets of these requirements. The desired endpoint is therefore a reconstruction theorem deriving, rather than postulating, spectral dagger/filter completeness, a primitive central monoidal phase, free environment completion, and public-interface descent; these would respectively target homogeneous self-dual/Jordan structure, complex phase-balanced composition, purification, and gravity-compatible tomography. The Harlow--Usatyuk--Zhao closed-universe observer construction supplies a conditional public-ledger capacity crossover, while current particle-physics audits give an exact \(\mathbb Z_6\) point-field kernel and conditional \(q_\nu=0\), \(g_{\rm tree}=2\), one-loop controls, and a minimal Higgs carrier; the complete global gauge form, one-Higgs multiplicity, Koide equipartition, and numerical constants retain explicit bridge premises. The framework is consequently strongest as a meta-kinematic and architectural theory: modern physics can be represented naturally inside it, but necessity and sufficiency for the unique modern kinematic framework remain the central open theorem.
\end{abstract}

\tableofcontents

\section{Theorem Roadmap}

The formal spine below consolidates the two-level framework \cite{lande-foundations}, the coherence-depth theorem package \cite{lande-coherence,lmcs-coherence}, the PEN library-growth formalization \cite{lande-pen}, the generative calculus \cite{lande-generative}, the synthetic framework abstraction theorem \cite{lande-synthetic}, and the runtime/cosmological completion \cite{lande-bigbang}. The physics, mind, consciousness, artificial-system, and moral-status sections use the current book appendices as their normalization layer \cite{lande-book-appendices}.

The dependency chain of the theory is:
\[
\begin{aligned}
\text{two primitive roles}
&\Rightarrow
(\CL,\SLaw)
\Rightarrow
(\CCHM,\PEN)
\Rightarrow
d_{\mathrm{obl}}=2
\\
&\Rightarrow
\text{Fibonacci scaling}
\Rightarrow
\text{Genesis}
\Rightarrow
\DCT
\Rightarrow
\text{ordered internal runtime},
\\
\substack{\DCT+\mathsf{Closed}(U)\\[-0.2em]{}+\mathsf{IRA}+\mathsf{PC}+\mathcal{Q}_p}
&\Rightarrow
\text{observer-indexed public physics}.
\end{aligned}
\]

\begin{enumerate}[leftmargin=*]
\item The primitive explanatory roles are exhaustive: admissibility and actualization.
\item The Constitutive Law is realized by constructive, univalent, bounded-depth, canonical structure.
\item The Selective Law operates in two registers: obligation gates the guarded stages (total discharge with typed receipts; value quantities are diagnostics there), and certified earned novelty gates only the debt-free open band. PEN is the evidentiary standard for what counts as novelty, not a guarded-stage selector. Relative free sealing realizes the selected API without junk or extra equations. Physical public actualization additionally requires a stable internal record satisfying Public Confluence.
\item Exact depth two follows from a binary univalence obstruction and cubical horn contractibility above depth two.
\item Depth two induces a two-step recurrence, hence Fibonacci scaling.
\item The two laws applied to the empty library yield the fifteen-step Genesis Sequence relative to the fixed grammar, admissibility lane, and canonical quotient: three constitutive founding acts, then a certified demand chain with a unique total discharger at every guarded stage except Step 4, whose four-way certified tie makes lawful history a four-branch cone; universality beyond the disclosed lane remains open.
\item The Step-15 shell is forced grammar growth: at \(H_{14}\) the resident grammar cannot express any discharger of the live temporal demand, and the guarded modal completion is the unique cost-minimal coherent extension (Minimal-Extension Theorem, restated demand-side; the engine-register stall comparison survives as corroborating diagnostic).
\item The terminal temporal-cohesive step internalizes dynamics and yields runtime ignition; its semantic completion is governed by the DCT consistency trichotomy (strict fusion refuted, fragments modeled, L\"ob repaired along the jet filtration).
\item The temporal shell derives an oriented internal clock class, not a unique metric clock: every monotone reparameterization preserves the structural trace, so any calibration to seconds, scale factor, or redshift is a declared bridge.
\item Continuation past Step 15 is foreclosed, and the closure is now certified rather than contested: directive debt is exhausted at Step 16 (\(\mathrm{O}(16)=\emptyset\) at full instance granularity, all 89 window instances decided; the demanded-but-underdetermined falsifier executed and excluded), internal continuations score zero by univalent closure, and external earning is bounded by the act-local provenance ceiling \(\nu\le4\kappa\), far below any standard a completed field sets. The former ``contested'' reading is resolved: the formula-maximizing survivors rode engine-register counting rules (\(d^2\), \(r^2\), telescopic inheritance) that the semantic register refuses as credit. The halt holds on all four branches of the Stage-4 cone --- a theorem of the laws, not of the enacted choice. Runtime begins where debt ends.
\item The evaluator itself is derived: the per-step counting rules decompose into a six-principle Schema Calculus, with the derived evaluator reproducing the strict trace value for value.
\item The universe is adopted as spatially closed. Conditional on this axiom, the exterior fundamental Hilbert account is one-dimensional and real, while rich quantum descriptions are observer-indexed and entropy-limited.
\item Internal Record Actualization and Public Confluence are adopted as the physical sealing clauses. Neither the Two Laws nor the closed-universe observer construction chooses a universal record POVM or tolerance. For one explicit finite thermal-repetition observer, however, the observer dynamics derives the extractor, optimal tolerance, retained sufficient statistic, and exact confluence map rather than inserting them by hand.
\item The revised laws constrain the meta-kinematic envelope but do not yet classify the hidden operational theory: classical, real, complex, quaternionic, Jordan, and generalized-probabilistic countermodels prove that homogeneity/self-duality, purification, local tomography, and complex Born structure are not consequences of the present clauses alone.
\item The resulting leading public-ledger capacity is a smooth Page-like crossover at
\[
K_{\mathrm{cap}}=-\log_2(2^{-n_p}+2^{-n_g}).
\]
Equal pointer and gravity floors give the model-specific soft-min value \(K_{\mathrm{cap}}=n-1\). In the explicit \((N,q)=(7,1/5)\) observer, \((n_p,n_g,K_{\mathrm{cap}})=(4.887399,3.894753,3.307335)\) bits and the retained Hamming-weight statistic has \(H_2(W)=2.848709\) bits, below the crossover.
\item The particle-physics closure audit now separates exact representation facts from bridge choices: sequential sealing gives \(q_\nu=0\), the point-field action has exact kernel \(\mathbb Z_6\), and its faithful image is \(G_0/\mathbb Z_6\); the complete global gauge form and one-Higgs multiplicity remain conditional. Tree-level \(g=2\), the one-loop coefficients for declared matter, and \(\rho_{\mathrm{tree}}=1\) are evaluator checks rather than new selections.
\item The Koide audit leaves an exact geometric equivalence and an explicit underdetermination theorem. Permutation symmetry does not equalize the two sector weights. The value \(Q=2/3\) therefore requires a new rank-blind central-actualization bridge, phase alignment, a physical mass-seal extractor, and a hierarchy premise selecting one of two positive roots.
\item The same formal spine induces the theory's physics, mind, consciousness, artificial-system, and ethical structures.
\item The induced physics supplies a graded inventory of public-record constraints and conditional targets in the gauge, baryon, neutrino, charged-lepton-flavor, dark-sector, quantum-probability, gravitational-wave, and primordial-spectrum interfaces.
\end{enumerate}

\section{Formal Setting and Primitive Roles}

\begin{definition}[History and continuation]
A realized history is a cumulative state \(H\). A candidate continuation at \(H\) is an object \(x\) equipped with an intended update map
\[
I(H,x)=H'.
\]
The admissible cone \(\A(H)\) is the class of continuations coherent with both the Constitutive Law and the already realized history.
\end{definition}

\begin{definition}[Admissible cone as a dependent fiber]
Let \(\Hist\) be the class of realized histories and let \(\Cand(H)\) be the raw candidate family over \(H\). The constitutive predicate is
\[
\CL_H:\Cand(H)\to\mathsf{Prop},
\]
and the admissible cone is the dependent subfamily
\[
\A(H):=\sum_{x:\Cand(H)}\CL_H(x).
\]
The integration map is partial on raw candidates and total on admissible selected candidates:
\[
\I_H:\A(H)\to\Hist.
\]
\end{definition}

\begin{definition}[Determinate world]
A determinate world is a sequence
\[
H_0 \xrightarrow{x_0} H_1 \xrightarrow{x_1} H_2 \to \cdots,
\qquad
H_{n+1}=I(H_n,x_n),
\]
where \(x_n\in\A(H_n)\) is selected and integrated at each stage.
\end{definition}

\begin{principle}[Two primitive roles]
The primitive architecture consists of:
\[
\begin{aligned}
\CL &: \text{what coherently counts as admissible},\\
\SLaw &: \text{what becomes actual and how it is integrated}.
\end{aligned}
\]
\end{principle}

\begin{theorem}[Two-role exhaustiveness]
Every primitive explanatory role in a determinate world is classified either under admissibility or under actualization and integration.
\end{theorem}

\begin{proofsketch}
If only admissibility is given, there is a class of possible structures but no selected branch. If only actualization is given, there is no coherent candidate domain over which actualization can operate. A proposed third primitive role either constrains whether \(x\in\A(H)\), hence belongs to \(\CL\), or constrains whether and how \(x\) is selected and integrated, hence belongs to \(\SLaw\). No further primitive explanatory type is generated.
\end{proofsketch}

\begin{proposition}[Primitive law factorization]
Any primitive law \(L\) on determinate histories factors extensionally as
\[
L_H=(C_H,S_H),
\]
where \(C_H\subseteq\Cand(H)\) is its admissibility component and
\[
S_H:C_H\rightharpoonup \Hist
\]
is its actualization/integration component.
\end{proposition}

\begin{proofsketch}
The observable effect of \(L\) at \(H\) is exhausted by which raw candidates remain live and which live candidate is sealed into the next history. The first datum is the characteristic predicate of \(C_H\). The second datum is the partial next-history map \(S_H\). Any additional clause in \(L\) either changes \(C_H\), changes \(S_H\), or is inert with respect to determinate history.
\end{proofsketch}

\section{Constitutive Law}

\begin{principle}[Constitutive Law]
An admissible candidate is a coherently realizable, equivalence-invariant, locally satisfiable, canonically executable structure.
\end{principle}

\begin{definition}[Constitutive burdens]
The four constitutive burdens are:
\begin{enumerate}[leftmargin=*]
\item coherent realizability: existence is witness-bearing;
\item equivalence-invariance: equivalent presentations are identical in being;
\item local satisfiability: coherence obligations are discharged at bounded depth;
\item canonicity: closed constructions have determinate realization.
\end{enumerate}
\end{definition}

\begin{definition}[Constitutive judgment]
For history \(H\) and raw candidate \(x:\Cand(H)\), the judgment
\[
H\vdash_{\CL} x\ \mathsf{adm}
\]
means that \(x\) carries:
\[
\begin{array}{ll}
\mathrm{CL1} & \text{a witness-bearing realization type } \Real_H(x),\\
\mathrm{CL2} & \text{transport along all structural equivalences in its public interface},\\
\mathrm{CL3} & \text{a finite local family of coherence obligations } O_H(x),\\
\mathrm{CL4} & \text{canonical evaluation of closed generated terms.}
\end{array}
\]
Thus \(\CL_H(x)\) is the conjunction of CL1--CL4.
\end{definition}

\begin{proposition}[Formal shadow of \(\CL\)]
The canonical formal shadow of the Constitutive Law is a CCHM-style cubical homotopy type theory with dependent types, univalence, Kan composition, and computational canonicity.
\end{proposition}

\begin{proofsketch}
Witness-bearing existence is represented by judgments \(\Gamma\vdash a:A\). Equivalence-invariance is represented by univalence,
\[
(A\simeq B)\simeq (A=_{\U}B).
\]
Bounded local satisfiability is represented by structural integration obligations over finite public interfaces. Canonicity with univalence requires computational path constructors and filler operations; cubical Kan operations supply the required execution rules.
\end{proofsketch}

\begin{proposition}[Constructivity, univalence, and cubical canonicity]
CL1 forces constructive witnesses; CL2 forces identity to respect equivalence; CL4 forces the chosen univalent calculus to compute closed canonical forms.
\end{proposition}

\begin{proofsketch}
A witness-free existence predicate would make \(H\vdash_{\CL}x\ \mathsf{adm}\) insensitive to realization and would not supply data for integration. A presentation-sensitive identity predicate would distinguish equivalent public structures, violating CL2. Book-style univalence without computational path and filler rules supplies equivalence-invariance but not the canonical execution demanded by CL4. CCHM-style cubical structure supplies paths, transport, composition, and normalization in one formal shadow.
\end{proofsketch}

\section{Selective Law and PEN}

\begin{definition}[Selective burdens]
The revised Selective Law carries ten burdens: localizability, commitment, admissibility, valuation, critical sufficiency, constructive primeness, exact realization, coherent integration, internal recordability, and public confluence.
\end{definition}

\begin{principle}[Selective Law]
The Selective Law operates in two registers. At guarded stages --- whenever the live window exports unmet obligations --- the accepted continuation is the admissible total discharger of those obligations, with typed provenance; value quantities are audited but select nothing, and where genuinely distinct total dischargers coexist after the canonical quotient, the law's silence is certified and history is lawfully a cone. On the debt-free open band, acceptance requires certified earned novelty, which the provenance ceiling forecloses. PEN is the law's evidentiary standard --- what novelty \emph{is} and what earning would require --- not its guarded-stage selector. The selected API is realized by its free sealing completion and folded into further history without unforced generators or equations.
\end{principle}

\begin{principle}[Strengthened physical Selective Law]
The Selective Law has three interfaces that must not be conflated. Two-register acceptance fixes which admissible API is sealed (obligation at guarded stages, certified earning on the open band, with PEN as the audit standard). Free sealing realizes the chosen API. A formally integrated continuation becomes a \emph{public physical event for an internal context \(p\)} only when it is actualized as a stable, decodable record inside that context. Compatible public records on an overlap must admit a joint nondemolition refinement; incompatible records yield no common seal, not two contradictory sealed facts. Selection without such a record is structural evolution, not yet a public event for \(p\).
\end{principle}

This strengthening does not add a third primitive role. Free realization, record formation, and public confluence constrain how a selected admissible continuation becomes determinate history and therefore belong to the actualization/integration side of \(\SLaw\). They do, however, expose structure that the value audit alone does not contain.

\noindent The PEN formalization is the library-growth model of \cite{lande-pen}. Write \(\B_H\) for the sealed public library contained in the realized history \(H\).

\begin{definition}[PEN state and candidate]
A PEN state is a monotone context \(\B_H\) closed under definitional derivability. A candidate over \(H\) is a triple
\[
x=(x_{\mathrm{core}},\mathcal{S}_x,\mathcal{G}_x),
\]
where \(x_{\mathrm{core}}\) is the definitive type-theoretic data, \(\mathcal{S}_x\) is a finite specification by atomic clauses, and \(\mathcal{G}_x\) is the obligation graph of atomic coherence witnesses required to seal \(x\) against the active history.
\end{definition}

\begin{definition}[Definitional derivability and irreducible clause]
Fix \(\B_H\) with operational reduction relation \(\rightsquigarrow_H\) generated by its eliminators, computation rules, and judgmental equalities. An operation or clause \(c\) is definitionally derivable over \(H\) when there is a closed term \(t_c\) in the signature of \(\B_H\) such that
\[
t_c\rightsquigarrow_H^{*} v_c
\]
for a canonical value \(v_c\), with all required computation behavior inherited from the existing eliminator fragment. A clause \(c\) is constructively irreducible over \(H\) when every attempted realization of \(c\) contains a new neutral head or stuck eliminator relative to \(\rightsquigarrow_H\), equivalently when orienting the intended behavior requires adding a new operational, equation, or API clause.
\end{definition}

\begin{definition}[API dependency graph]
Let \(\mathcal{S}_x=\{c_1,\ldots,c_m\}\). Its dependency graph has vertices \(c_i\). An edge \(c_i\to c_j\) records dependence on a symbol introduced by \(c_j\). A structural bundle is an induced strongly connected subgraph closed under well-typedness over \(\B_H\). A specification \(\mathcal{S}_x\) is minimal complete when:
\[
\begin{array}{ll}
\mathrm{Irr} & \text{every clause is constructively irreducible over }H,\\
\mathrm{Comp} & \text{all irreducible operations required for the package are present},\\
\mathrm{Unity} & \text{the clauses form one structural bundle},\\
\mathrm{Min} & \text{no proper structural sub-bundle is complete and independently acceptable.}
\end{array}
\]
\end{definition}

\begin{definition}[Irreducible integration cost]
The irreducible integration cost of \(x\) at \(H\) is
\[
\kappa_H(x):=
\left|\mathrm{Irr}_H(\mathcal{S}_x)\right|,
\]
where \(\mathrm{Irr}_H(\mathcal{S}_x)\) is the set of constructively irreducible atomic clauses in a minimal complete specification for \(x\). Definitionally derivable clauses contribute \(0\). Nonzero admissible candidates satisfy \(\kappa_H(x)>0\).

The auxiliary \(\MBTT\) audit length \(K_H(x)\) is the prefix-free bit length of the same specification, including \(O(\log i)\) pointer cost for references to existing library entries \(L_i\). \(K_H\) is not the denominator of \(\rho_H\); it is a representation audit and deterministic tie-break layer.
\end{definition}

\begin{definition}[Sealing latency]
The sealing latency of \(x\) at \(H\) is
\[
\Delta_H(x):=|\mathcal{G}_x|.
\]
It counts the normalized atomic coherence witnesses required to integrate \(x\) against the active historical interface. Thus \(\kappa_H(x)\) charges irreducible specification clauses, while \(\Delta_H(x)\) charges the interface debt incurred by sealing those clauses into public history.
\end{definition}

\begin{definition}[Generative gain]
Let \(\Schemas(\B_H)\) be the set of introduction, elimination, projection, computation, Kan, and path-reduction schemas available in \(\B_H\), quotiented by definitional equality and canonical renaming. The generative gain of \(x\) at \(H\) is the marginal expansion
\[
\nu_H(x):=
\left|
\Schemas(\B_H[x])\setminus
\langle \Schemas(\B_H)\rangle_H
\right|,
\]
where \(\B_H[x]\) is the library obtained by adding the specification of \(x\), and \(\langle \Schemas(\B_H)\rangle_H\) is the closure of inherited schemas under definitional derivability in the old library.
\end{definition}

\begin{definition}[Components of generative gain]
For admissible \(x\),
\[
\nu_H(x)=\nu_{G,H}(x)+\nu_{C,H}(x)+\nu_{P,H}(x),
\]
where \(\nu_{G,H}\) counts new formation and introduction schemas, \(\nu_{C,H}\) counts elimination, projection, destructor, and cross-interface schemas, and \(\nu_{P,H}\) counts computation, Kan, path-constructor, and path-reduction schemas. Opaque constants without structured interaction have at most formation gain and negligible cross-interface gain.
\end{definition}

\begin{proposition}[Telescopic API gain]
Let \(\mathcal{S}_x\) be a minimal complete API specification whose direct imported library interfaces are \(\mathrm{Refs}(\mathcal{S}_x)=\{L_1,\ldots,L_m\}\). If these imports form a dependency telescope with unique maximal imported interface \(L_{\max}\), then
\[
\nu_{C,H}(x)=\nu_H(L_{\max})+\kappa_H(x)+(m-1).
\]
\end{proposition}

\begin{proofsketch}
The maximal import contributes the inherited eliminative surface, since the dependent telescope lifts its derivation schemas without summing duplicated ancestry. Each irreducible field of \(\mathcal{S}_x\) contributes one internal projection, destructor, or computation-facing accessor. Fusing \(m\) imported interfaces into one structural bundle requires \(m-1\) bridge equations, exchange laws, or naturality squares.
\end{proofsketch}

\begin{definition}[Efficiency and diagnostic ledger quantities]
For \(x\in\A(H)\),
\[
\rho_H(x)=\frac{\nu_H(x)}{\kappa_H(x)}.
\]
For a realized sequence \(H_0\to\cdots\to H_n\), write
\[
\Omega_{n-1}:=
\frac{\sum_{i=1}^{n-1}\nu_{H_{i-1}}(x_i)}
{\sum_{i=1}^{n-1}\kappa_{H_{i-1}}(x_i)},
\qquad
\Phi_n:=\frac{\Delta_{n}}{\Delta_{n-1}},
\qquad
\SelBar(H_n)=\Phi_n\Omega_{n-1}.
\]
All three composite quantities are \emph{diagnostics}: recorded at every stage, register-tagged, and legislatively inert. The enacted engine used \(\SelBar\) as an acceptance threshold; the completed audit of the certified trace established that it never performed selective work at any guarded stage (the sole discharger either cleared it incidentally or, at the semantic Stage~8, failed it and lawfully advanced), and the adopted law assigns value quantities no guarded-stage authority. Cumulative diagnostics are additionally branch-indexed permanently: they are the one place the Stage-4 choice leaves a trace.
\end{definition}

\begin{definition}[Obligation profile and guarded acceptance]
The demand window at stage \(n\) is the pair \((S_n,S_{n-1})\) of most recently sealed interfaces; the live obligation profile is
\[
\mathrm{O}(n+1) \;=\; C(S_n,S_{n-1})\setminus D(\B_n),
\]
the window-generated demands not derivable in the current library. A stage is \emph{guarded} when \(\mathrm{O}\neq\emptyset\) and \emph{open} when \(\mathrm{O}=\emptyset\). At a guarded stage, a candidate is \emph{acceptable} iff it is admissible and totally discharges \(\mathrm{O}\), each demand answered by a typed discharge witness. No value quantity, threshold, or presentation order may select among acceptable candidates: if exactly one exists it is selected (the certified trace realizes this at every guarded stage except Step~4); if several genuinely distinct acceptable candidates survive the full canonical quotient, the law's silence is itself certified, each founds a lawful branch, and the enacted branch is identified indexically. On the open band, acceptance requires certified earned novelty under the provenance discipline below; the accounting bound \(\nu\le4\kappa\) forecloses it.
\end{definition}

\begin{definition}[Constructive primeness]
An admissible \(x\in\A(H)\) is constructively prime, written \(P_H(x)\), when no proper terminal structural sub-bundle of \(G_{\mathcal{S}_x}\) is itself well-typed, constructively irreducible, admissible over \(H\), and independently acceptable. Equivalently, \(x\) is not presentation-equivalent over \(H\) to a gratuitous bundle of independently acceptable continuations. Primeness is an audit discipline (anti-bundling in the value accounting), not a selection filter: at guarded stages the acceptance question is payment, and a bundle either is or is not the total discharge the window demands.
\end{definition}

\begin{definition}[Local horizon]
A local horizon is an increasing filtration of candidate cones
\[
\A_0(H)\subseteq \A_1(H)\subseteq\cdots\subseteq\A(H).
\]
The active horizon is the least \(r\) for which \(\A_r(H)\) contains a sufficient constructively prime candidate.
\end{definition}

\begin{axiom}[Cumulative growth]
Selected integration is monotone:
\[
H_{n+1}=\I_{H_n}(x_n),
\qquad
\B_{H_n}\subseteq \B_{H_{n+1}},
\]
and all definitional consequences of \(\B_{H_n}\) remain available in \(\B_{H_{n+1}}\).
\end{axiom}

\begin{axiom}[Horizon policy]
PEN evaluates candidates inside the active local horizon. After a realization the horizon resets to the least finite cone containing the new public interface; after an idle expansion it widens by one filtration step. Selection is performed in the least horizon containing a sufficient constructively prime candidate.
\end{axiom}

\begin{axiom}[Admissibility gate]
A candidate \(x\) belongs to \(\A_r(H)\) only if \(\mathcal{S}_x\) is well-typed over \(\B_H\), nonredundant under \(\rightsquigarrow_H\), minimal complete, constructively irreducible clausewise, compatible with the current coherence window, and inside the horizon bound \(r\).
\end{axiom}

\begin{axiom}[Two-register acceptance]
At a guarded stage (\(\mathrm{O}(H)\neq\emptyset\)), the accepted continuation is the total discharger of \(\mathrm{O}(H)\) with typed provenance, unique in the certified trace at every stage but one; where several genuinely distinct total dischargers survive the canonical quotient, no clause of the law selects among them --- the silence is certified, and lawful history branches. No bar, efficiency comparison, parsimony order, or deterministic presentation key participates in guarded acceptance; all such quantities are recorded as diagnostics. On the open band (\(\mathrm{O}(H)=\emptyset\)), acceptance requires a complete certified audit whose earned novelty justifies extension; by the provenance accounting bound this requires \(\rho>4\), which no certified candidate attains. The historical engine encoding (bar-clearing with minimal positive overshoot, cost and canonical-key tie-breaks) is retained in the record as enactment-conservative testimony: on the certified trace it selected the same winners, because every winner was the unique acceptable discharger; its tie-break key is inadmissible as law and is precisely where the enacted Stage-4 branch was fixed by artifact rather than by clause.
\end{axiom}

\begin{axiom}[Coherent integration]
When \(x^*\) is selected, its free sealing completion is sealed as a new public layer. Its resolved obligation graph \(\mathcal{G}_{x^*}\) is exported as an opaque interface, the score tuple
\[
(\nu_H(x^*),\kappa_H(x^*),\Delta_H(x^*),\rho_H(x^*))
\]
is appended to history, and later admissibility may reference the new layer only through its sealed public interface.
\end{axiom}

\begin{definition}[Relative free sealing]
Let \(\mathcal S_{x^*}\) be the normalized generators, recursors, computation rules, and coherence equations of the selected API. Define
\[
F_{x^*}(\B_H)
:=
\mathrm{Term}(\B_H;\mathcal S_{x^*})/{\equiv_{x^*}},
\]
where \(\equiv_{x^*}\) is the least substitution-, composition-, and public-interface-stable congruence generated by the old equations and the equations forced by \(\mathcal S_{x^*}\). Integration is
\[
\B_{H^+}=F_{x^*}(\B_H).
\]
\end{definition}

\begin{theorem}[Relative free-sealing theorem]
Assume the generated reduction system is lawful. In the category of exact realizations of the already selected API \(\mathcal S_{x^*}\) over \(\B_H\), the completion \(F_{x^*}(\B_H)\) is initial. Equivalently,
\[
\mathrm{Map}_{\B_H,x^*}
\bigl(F_{x^*}(\B_H),C\bigr)
\simeq\mathbf1
\]
for every lawful exact realization \(C\), with the mapping type read strictly in the executable lane and homotopically when the recursion, \(\eta\)-, and coherence data are retained.
\end{theorem}

\begin{proofsketch}
Interpret old generators by the given map from \(\B_H\), new generators by their specified realizations in \(C\), and extend recursively. The interpretation respects \(\equiv_{x^*}\) because \(C\) satisfies every forced equation. Any second interpretation agrees on all generators and therefore on all generated terms by structural induction. The retained \(\eta\)- and coherence rules upgrade uniqueness to contractibility in the intensional formulation.
\end{proofsketch}

\begin{proposition}[Selection is not general initiality]
The PEN ranking does not imply that \(x^*\) is the initial completion of an independently fixed weaker requirement. Minimal positive overshoot and categorical leastness are different orders.
\end{proposition}

\begin{proofsketch}
At the Step-8 comparison, the live window demand requires an operative lift of the two-dimensional path vocabulary; the sealed record certifies that a bare \(S^3\) cannot discharge it and never enters the acceptable field, while the H-space \(S^3\) is the unique total payment. Relative initiality applies afterward to the selected H-space API --- the free completion \emph{of that API}, not of some weaker ``sphere-like'' requirement fixed in advance. Defining the weaker requirement retrospectively from the winner would make the stronger claim circular; the demand register blocks the circularity by fixing what was required before any candidate is inspected.
\end{proofsketch}

\begin{axiom}[Internal Record Actualization]
In physical runtime there is no external ledger or external actualizer. A candidate outcome \(x\) is actualized as a public event for an internal context \(p\) only if there exists an internal record carrier \(R_p\), a distinguishable record state \(r_x:R_p\), and a comparison interface to the prior public history such that:
\begin{enumerate}[leftmargin=*]
\item \(r_x\) is correlated with the realized alternative by the Born-weighted internal dynamics;
\item \(r_x\) is recoverable by an admissible internal decoder to the declared tolerance;
\item the record is locked into later admissibility --- it persists until replaced by a declared sufficient statistic proved lossless for every retained public claim, rather than being silently overwritten; and
\item admissible comparisons with other internal contexts preserve the record up to their stated observer-code error.
\end{enumerate}
\end{axiom}

\begin{definition}[Public-event extractor]
For an internal context \(p\), a public-event extractor is a partial map
\[
\mathcal{Q}_p:
(H_{\mathrm{pub}},\rho_{pW},x)
\rightharpoonup
\mathsf{Rec}_p,
\]
where \(\rho_{pW}\) is the joint context/world state and \(x\) is a candidate event. The value \(\mathcal{Q}_p(H_{\mathrm{pub}},\rho_{pW},x)=r_x\) is defined only when the record is constitutively admissible, Born-supported, decodable at the declared tolerance, compatible with prior lock-in, mutually comparable with other observer ledgers, and inside the active record-capacity bound. Otherwise \(\mathcal{Q}_p\) returns no public seal for \(p\); the dynamics may continue, but the event remains unsealed or must be exported through an explicitly declared abstraction proved lossless for the retained public claims.
\end{definition}

\begin{axiom}[Public Confluence]
Let \(r_p\) and \(r_q\) be separately repeatable, copyable, and future-stable records concerning a common public overlap. If they are operationally compatible, there exists a joint nondemolition refinement \(r_{pq}\), unique up to public operational equivalence, whose comparison maps recover both records. If no such refinement exists, no public history contains both as facts about that overlap. Thus finite-capacity failure has the form
\[
\text{common seal}
\qquad\text{or}\qquad
\text{no common seal},
\]
not two mutually contradictory sealed facts.
\end{axiom}

\begin{proposition}[Public Confluence does not determine the hidden kinematics]
Public Confluence constrains the common record layer, not the full private state cone or composite rule. Classical simplex theories, real and quaternionic quantum fragments, and generalized probabilistic theories can each be equipped with a confluent classical public-record interface. Consequently the revised Two Laws do not by themselves derive homogeneity/self-duality, purification, local tomography, or complex Born quantum structure.
\end{proposition}

\begin{proofsketch}
Finite stochastic theory is constructive, compositional, homogeneous, self-dual, locally tomographic, and publicly confluent, but a nontrivial mixed distribution is not the marginal of a pure classical bipartite state, so purification fails. Real quantum theory is homogeneous, self-dual, purifying, and publicly confluent, but two-rebit states possess an antisymmetric--antisymmetric global direction invisible to all local symmetric product effects, so local tomography fails. Rational polyhedral GPT cones give constructive compositional models without homogeneous self-dual cones. None of these differences is visible to a confluence condition imposed only on stable classical records.
\end{proofsketch}

\begin{proposition}[PEN does not determine \(\mathcal{Q}_p\)]
The structural acceptance data --- obligations, discharges, and the audited value tuple --- determine which admissible continuation is sealed, but they do not determine a record POVM, a decoding tolerance, a pointer coarse-graining, or a physical clock calibration. These data are invariantly invisible to \((\nu,\kappa,\Delta,\rho)\). The public-event extractor is therefore a bridge axiom or derived theorem of additional observer dynamics, not a hidden consequence of the existing score.
\end{proposition}

\begin{proofsketch}
Fix the same formally selected continuation and the same score tuple. Distinct admissible POVMs or coarse-grainings on the observer record can produce different public partitions while leaving every PEN quantity unchanged. Likewise, a strictly increasing reparameterization of the physical record times changes throughput but not selection order or score. Hence models agreeing on \(\CL\), PEN, and the structural trace can disagree on \(\mathcal{Q}_p\) and on clock calibration.
\end{proofsketch}

\begin{proposition}[PEN operationalizes the formal face of \(\SLaw\)]
The five PEN rules are cumulative growth, horizon policy, admissibility gate, efficient overshoot, and coherent integration. Relative free sealing fixes the exact realization of the selected API. Internal Record Actualization, Public Confluence, and \(\mathcal{Q}_p\) govern the additional passage from formal integration to observer-relative public physical sealing.
\end{proposition}

\begin{proofsketch}
Localizability is supplied by evaluating only \(\A(H)\). Admissibility is supplied by the gate \(x\in\A(H)\). Valuation is supplied by \(\rho=\nu/\kappa\). Critical sufficiency is supplied by \(\SelBar(H)\). Constructive primeness is supplied by \(P_H(x)\) and the minimal-overshoot rule. Commitment and exact coherent integration are supplied by \(H'=F_x(H)\). Internal Record Actualization supplies public recordability, and Public Confluence supplies overlap consistency.
\end{proofsketch}

\begin{center}
\small
\begin{tabularx}{\textwidth}{l Y Y}
\toprule
Rule & Formal content & Selective burden discharged \\
\midrule
Cumulative growth & \(H_{n+1}=I(H_n,x_n)\) and \(H_n\subseteq H_{n+1}\) at the public-interface level & commitment, integration \\
Horizon policy & evaluate the live local continuation cone before widening search & localizability, critical sufficiency \\
Admissibility gate & require \(x\in\A_r(H)\) before scoring & admissibility \\
Discharge and earning & total payment of the live obligations at guarded stages; certified earning (foreclosed by the ceiling) on the open band & acceptance, valuation as audit, anti-bundling \\
Coherent integration & freely realize and export the selected API as sealed formal history & exact realization, path dependence, future admissibility \\
Internal Record Actualization & stable decodable record in an internal context & public physical eventhood \\
Public Confluence & joint nondemolition refinement for compatible overlap records & common seal or no common seal; never contradictory public facts \\
\bottomrule
\end{tabularx}
\end{center}

\begin{proposition}[Ratio scoring]
The ratio \(\rho=\nu/\kappa\) is the selective efficiency invariant; a difference score \(\nu-\kappa\) is not invariant under gratuitous bundling.
\end{proposition}

\begin{proofsketch}
If two independent moves are bundled, raw gain can increase faster than the penalty in a difference score while adding unnecessary integration burden. A ratio score compares generative leverage per unit cost. Minimal positive overshoot above \(\SelBar(H)\) then selects sufficient gain without rewarding opaque payload packing.
\end{proofsketch}

\begin{proposition}[Path dependence without global teleology]
PEN is history-sensitive and non-oracular: the selected \(x_n\) depends on \(H_n\) and its live filtered cone, not on a completed future trajectory.
\end{proposition}

\begin{proofsketch}
The quantities \(\nu_H,\kappa_H,\SelBar(H)\), constructive primeness, and the horizon filtration are all indexed by realized history. After integration, \(H_{n+1}=\I_{H_n}(x_n)\) changes the next cone and the next bar. No term in the selection rule quantifies over future selected histories except through candidates presently admissible at \(H_n\).
\end{proofsketch}

\section{Kinematic Scope and Reconstruction Target}

\begin{proposition}[Current meta-kinematic result]
Modern physics admits a natural realization satisfying the revised Two Laws: coherent states and processes form the admissible field; dynamics and measurement update realized history; stable classical records satisfy Public Confluence. The converse and uniqueness directions are not proved. The revised laws presently determine a meta-kinematic envelope, not the unique complex quantum--relativistic theory inside it.
\end{proposition}

\begin{definition}[Genesis operational-completion package]
The proposed nonnumerical reconstruction package consists of four theorem targets:
\begin{enumerate}[leftmargin=*]
\item \emph{Genesis-to-Jordan:} derive spectral decomposition, a positive dagger/state--effect correspondence, frame symmetry, and constructive filter completeness, hence homogeneous self-dual cones and Euclidean Jordan structure.
\item \emph{Natural monoidal phase:} derive a faithful primitive central \(U(1)\) action on pure-process carriers, natural under all processes and balanced under composition, thereby excluding presentation-dependent real complexifications and noncentral quaternionic phase.
\item \emph{No fundamental mixing:} prove that the mixed operational category is the free environment completion of the pure Genesis category, hence every mixed state has a pure dilation unique up to reversible environmental equivalence.
\item \emph{Public descent:} prove that public records glue over shared interfaces. Ordinary local tomography is recovered only when the overlap carries no additional gauge, edge, or gravitational record data.
\end{enumerate}
\end{definition}

\begin{principle}[Open Two-Laws-to-modern-kinematics reconstruction conjecture]
For finite irreducible compositional operational theories with stable public histories, the Constitutive Law, the revised Selective Law, and the Genesis Sequence derive the Genesis operational-completion package; its resulting pure category is equivalent to finite-dimensional complex Hilbert spaces with phase-balanced tensor product, and its causal completion is equivalent to the corresponding completely positive operational theory, with public-interface descent in gravitational sectors.
\end{principle}

The conjecture has explicit falsifiers. A constructive publicly confluent countermodel satisfying the four derived completion targets but not operationally equivalent to complex quantum theory refutes sufficiency. A finite stable-history operational theory lacking any admissibility--actualization factorization refutes necessity. Failure to derive any target from the Genesis candidate competition identifies that target as genuine bridge content rather than permitting it to be silently inserted into Step 14.

\section{Exact Coherence Depth Two}

\begin{definition}[Obligations and historical arity]
For a globally acting structural candidate \(X\) sealed against a depth-\(k\) historical interface, let
\[
O^{(k)}(X)
\]
be the normalized set of atomic structural integration obligations. For \(o\in O^{(k)}(X)\), let \(\Supp(o)\) be the set of earlier sealed layers whose exported schemas occur irreducibly in the normalized typing derivation of \(o\). The historical arity is \(a(o)=|\Supp(o)|\).
\end{definition}

\begin{definition}[Depth invariants]
The obligation depth \(d_{\mathrm{obl}}\) is the least \(d\) such that \(O^{(k)}(X)\simeq O^{(d)}(X)\) for every \(k\ge d\). The minimal-signature depth \(d_\mu\) is the least \(d\) such that every admissible surface declaration is presentation-equivalent to a normalized public signature with no irreducible primitive historical field of arity above \(d\).
\end{definition}

\begin{definition}[Sealed structural extension]
A sealed structural extension is a globally acting candidate \(X\) whose payload changes the active reduction, elimination, modal, or equality interface of the core calculus. It is exported through an opaque public signature
\[
\Sigma_X=(K_X,T_X),
\]
where \(K_X\) is payload data and \(T_X\) is structural trace data. A trace field is primitive when it is not computed by lower-interface data; it is derived when it is canonically generated by the cubical filler operations from already exported boundary data.
\end{definition}

\begin{definition}[Horn fiber]
For a fixed lower boundary \(\partial\), write the depth-\(k\) marginal horn-extension fiber as
\[
H_k(\partial)
:=
\sum_{\gamma:\Gamma_{\mathrm{miss}}(\partial)}
\mathrm{Fill}_{\partial}(\gamma),
\]
where \(\gamma\) is the missing face and \(\mathrm{Fill}_{\partial}(\gamma)\) is the type of cubical fillers compatible with the visible faces.
\end{definition}

\begin{definition}[Obligation status]
An obligation occurrence has one of four normalized statuses:
\[
\begin{array}{ll}
\mathrm{present} & \text{a normalized obligation type occurs in }O^{(k)}(X),\\
\mathrm{contractible} & \text{the obligation type occurs but its fiber is canonically contractible},\\
\mathrm{eliminable} & \text{presentation equivalence removes it from }\mu\text{-minimal public form},\\
\mathrm{absent} & \text{no normalized obligation of that kind occurs.}
\end{array}
\]
The depth theorem uses all four statuses: higher obligations may be present, contractible, and eliminable without being absent.
\end{definition}

\begin{theorem}[Depth-two theorem package]
In the fully coupled CCHM-style cubical extension calculus of globally acting structural extensions sealed behind opaque public signatures:
\begin{enumerate}[leftmargin=*]
\item \(d_{\mathrm{obl}}=2\);
\item \(d_\mu\le 2\);
\item primitive historical support factors through the previous two normalized signatures;
\item under uniform payload size \(c\), the presentation-invariant sealing cost satisfies
\[
\mu_{n+1}=\mu_n+\mu_{n-1}+2c.
\]
\end{enumerate}
\end{theorem}

\begin{proofsketch}
The lower bound is supplied by a binary univalence obstruction. Let \(2=\mathbf{1}+\mathbf{1}\) and let \(\mathrm{swap}:2\to2\) exchange the summands. Univalence yields
\[
p_{\mathrm{swap}}:=\ua(\mathrm{swap}):2=_{\U}2.
\]
A globally acting candidate such as a family of endomaps \(f:\prod_{A:\U}A\to A\) requires coherence of its action over \(p_{\mathrm{swap}}\). Unary data specify action at each exported type but not binary comparison over the univalent path; hence \(d_{\mathrm{obl}}\ge2\).

For the upper bound, the marginal datum at depth \(3\) is not a closed three-sphere filler but an open-box extension fiber over already fixed lower boundary data. Since \(\IsEquiv(f)\) is a mere proposition, equivalence-proof data do not generate an independent tower. Cubical operations \(\mathrm{hcomp}\) and \(\mathrm{hfill}\) compute missing faces and fillers from visible boundaries, making the remote horn fiber contractible. Iteration gives \(O^{(k)}(X)\simeq O^{(2)}(X)\) for all \(k\ge2\).

Contractible higher factors are computationally derived in canonical normalized public syntax. Replacing them by generated horn witnesses preserves presentation equivalence and removes them from \(\mu\)-minimal primitive trace. Surviving binary trace fields factor through the previous two signatures by composition of intervening unary bridges and binary traces. Thus the exact obligation depth is two and the public chronological window is two.
\end{proofsketch}

\begin{proposition}[Primitive/derived separation]
For \(k\ge3\), every higher structural trace field in a normalized surface presentation is either rejected as an inadmissible naked higher face or packaged as a horn boundary whose witness is derived from lower public data.
\end{proposition}

\begin{proofsketch}
The fixed raw structural syntax classifies action clauses as unary, comparison clauses as binary, and higher material as horn-shaped. A higher field without boundary data is not a structural trace field for the sealed extension. A higher field with boundary data elaborates to an open-box problem, and the cubical Kan structure computes the witness. Thus no higher field survives as primitive trace in a \(\mu\)-minimal public signature.
\end{proofsketch}

\begin{proposition}[Binary lower bound]
There exists a structural extension \(X\) for which \(O^{(2)}(X)\not\simeq O^{(1)}(X)\).
\end{proposition}

\begin{proofsketch}
Take the two-point type and the automorphism \(\mathrm{swap}:2\simeq2\). By univalence, \(\mathrm{swap}\) determines \(p_{\mathrm{swap}}:2=_{\U}2\). A globally acting extension must preserve its public action under this path. The required square compares two uses of the extension across one structural equivalence. This comparison has historical arity two and cannot be reconstructed from independent unary action data.
\end{proofsketch}

\begin{proposition}[Horn upper bound]
For each \(k\ge3\) and fixed depth-\((k-1)\) boundary \(\partial\), the marginal remote horn factor \(H_k(\partial)\) is canonically contractible.
\end{proposition}

\begin{proofsketch}
The visible boundary determines the admissible missing-face problem. Cubical composition computes a composite face, and cubical filling packages the compatible filler. Substitution stability of Kan operations identifies any two admissible remote completions over the same boundary. Hence the projection from the total depth-\(k\) obligation to the depth-\((k-1)\) boundary is an equivalence with contractible fiber.
\end{proofsketch}

\begin{proposition}[Minimal-signature elimination]
Every arity-\(k\ge3\) structural trace field is absent from \(\mu\)-minimal public syntax.
\end{proposition}

\begin{proofsketch}
Surface normalization splits trace fields into unary, binary, and horn-shaped forms. Horn-shaped fields carry explicit derivation obligations from their lower boundary. Since the horn fiber is contractible and computationally filled, replacing an explicit higher field by the generated filler preserves presentation equivalence and lowers or preserves primitive trace cost. Minimality therefore excludes the explicit higher primitive field.
\end{proofsketch}

\begin{proposition}[Chronological Markov blanket]
At stage \(n+1\), primitive structural obligations factor through the interface generated by stages \(n\) and \(n-1\). Earlier stages contribute only by derived composites through this two-layer interface.
\end{proposition}

\begin{proofsketch}
Unary trace records present action against the immediately active public interface. Binary trace records comparison between the candidate and the previous active interface. Any comparison with a remote layer factors through a telescope of adjacent unary bridges and binary comparison traces. The remote dependency is therefore present only as a derived composite, not as primitive historical support.
\end{proofsketch}

\section{Affine Fibonacci Scaling}

\begin{theorem}[Universal affine recurrence]
If structural integration coherence has obligation depth \(d\), then the maximal full-coupling presentation cost satisfies
\[
\mu_{n+1}
=
\sum_{j=0}^{d-1}(\mu_{n-j}+\kappa_{n-j}).
\]
\end{theorem}

\begin{definition}[Dependency footprint]
The dependency footprint \(F_{n+1}\subseteq\{n,n-1,\ldots,n-d+1\}\) is the subset of the chronological window whose public interfaces are actually used by the primitive trace of stage \(n+1\).
\end{definition}

\begin{proposition}[Full-coupling envelope]
Sparse dependency costs are bounded by the full-coupling recurrence:
\[
\mu_{n+1}^{\mathrm{sparse}}
\le
\sum_{j=0}^{d-1}(\mu_{n-j}+\kappa_{n-j}).
\]
Equality holds in the maximally coupled case \(F_{n+1}=\{n,\ldots,n-d+1\}\).
\end{proposition}

\begin{proofsketch}
The chronological window theorem restricts primitive support to the last \(d\) layers. A sparse candidate may use a proper subset of those interfaces, so its charge is the corresponding sub-sum. The full envelope is the supremum over admissible footprints inside the same window and therefore bounds all sparse cases.
\end{proofsketch}

\begin{corollary}[Depth-two Fibonacci law]
For \(d_{\mathrm{obl}}=2\),
\[
\mu_{n+1}=\mu_n+\mu_{n-1}+\kappa_n+\kappa_{n-1}.
\]
If \(\kappa_n=c\), then
\[
U_n:=\mu_n+2c
\qquad\Rightarrow\qquad
U_{n+1}=U_n+U_{n-1}.
\]
Consequently \(U_{n+1}/U_n\to\varphi=(1+\sqrt5)/2\).
\end{corollary}

\begin{proofsketch}
At stage \(n+1\), primitive obligations are generated by the active public interfaces in the preceding \(d\) layers. Full coupling charges both inherited opaque cost \(\mu\) and current payload \(\kappa\) for each layer in the window. Substituting \(d=2\) gives the two-step affine recurrence. A constant shift by \(2c\) removes the affine term, leaving the Fibonacci recurrence.
\end{proofsketch}

\begin{definition}[Diagnostic bar (engine register)]
Let \(\Delta_n=\mu_n\) after canonical normalized public interfaces are fixed. Define
\[
\Phi_n=\frac{\Delta_n}{\Delta_{n-1}},
\qquad
\Omega_{n-1}=\frac{\sum_{i<n}\nu_i}{\sum_{i<n}\kappa_i},
\qquad
\SelBar_n=\Phi_n\Omega_{n-1}.
\]
These are descriptive quantities of the interface ledger: recorded, branch-indexed, and legislatively inert under the adopted law. The intrinsic descriptive form is the two-step memory bar \(\widehat{\SelBar}_n=(\nu_{n-1}+\nu_{n-2})/(\kappa_{n-1}+\kappa_{n-2})\), which carries the window's own memory depth and forgets the Stage-4 branch choice from \(n=7\) onward --- the cumulative form above remembers it forever, which is why cumulative diagnostics are permanently branch-indexed.
\end{definition}

\begin{proposition}[Golden-ratio asymptotics of the debt ledger]
\(\Phi_n\to\varphi\): the shifted sealing-cost sequence is Fibonacci, so adjacent debt ratios converge to the golden ratio --- the fingerprint of width-two anticipation, and a statement about the \emph{debt} arithmetic alone. Composite limits such as \(\SelBar_n\to\varphi\,\Omega_\infty\) hold under the displayed stability hypotheses and inherit the diagnostic, branch-indexed status of their factors; no selective consequence follows from them.
\end{proposition}

\begin{remark}[Retired: the \(\Omega\)-ratchet]
Earlier formulations proved an ``\(\Omega\)-ratchet'': acceptance forces \(\rho_n>\Omega_{n-1}\), so cumulative efficiency increases strictly along accepted steps, with corollaries excluding declining tails, idle repetition, and average-matching steps. The proposition is retired, and honestly: its premise is false under the adopted law (guarded acceptance never consults \(\rho\); the certified semantic Stage~8 winner \emph{failed} the diagnostic bar and lawfully advanced), and its corollaries are refuted in the lawful register --- the audited worth column of the certified trace is non-monotone and contains accepted steps with \(\nu=0\) (the Hopf and curvature payments: pure discharge, reorganization without minting). The engine-register \(\Omega\)-trajectory of the enacted trace remains sealed testimony; the picture of a world climbing an efficiency gradient does not survive the audit and is not part of the theory. What replaces it is stronger and simpler: the uniqueness census --- exactly one acceptable payment at every guarded stage but one --- so the ascent needs no gradient to climb.
\end{remark}

\begin{proposition}[Runtime richness from incompleteness]
Once the arithmetic-hosting strata are sealed, the library's derivability closure is incomplete (G\"odel), so content independent of \(\B_n\) exists at every stage. Under the adopted law this bears on the halt in exactly one way: it shows the halt is \emph{not} an exhaustion of expressible content. The sequence stops because \(\mathrm{O}(16)=\emptyset\) --- nothing further is owed --- while independent content continues to exist and is simply unowed; on the debt-free field it cannot be accepted, because certified earning is bounded by the provenance ceiling. The halt is completion of obligation amid inexhaustible possibility, which is precisely what makes the completed library a rich runtime rather than a finished catalogue.
\end{proposition}

\begin{proposition}[Discrete non-responsiveness]
Extensional or \(h\)-set-like branches have depth-one coherence and no higher path content. Their exclusion from the certified ascent is not a pricing outcome: from Step~5 onward the live window demands are demands for structured identity, operative form, and interface discipline, and flat-identity candidates do not discharge them. A discrete candidate is not an inefficient rival; it is a non-payment --- it answers a debt nobody has incurred.
\end{proposition}

\section{Generative Calculus}

\begin{definition}[MBTT cost]
\(\MBTT\) is the fixed minimal specification grammar used to count irreducible candidate descriptions. The clause cost of \(X\) over \(\B_n\) is \(\kappa_{\mathrm{cl}}(X\mid\B_n)\). The bit-level audit cost is \(\kappa_{\mathrm{bit}}(X\mid\B_n)\).
\end{definition}

\begin{definition}[Charged kernel clauses]
The charged integration cost is
\[
\kappa(X\mid\B_n)
=
\left|\KernelClauses(X\mid\B_n)\right|.
\]
\(\KernelClauses(X\mid\B_n)\) contains only irreducible API commitments not already definitionally supplied by \(\B_n\). Definitional completion, projections, aliases, adjoint completions, and generated eliminators add executable telescope structure but zero additional charged \(\kappa\).
\end{definition}

\begin{definition}[Schema novelty]
Novelty is literal schema difference:
\[
\nu(X\mid\B_n)
=
|\Schemas(\B_n+X)\setminus\Schemas(\B_n)|.
\]
It decomposes as
\[
\nu=\nu_G+\nu_C+\nu_H,
\]
where \(\nu_G\) counts formation and introduction schemas, \(\nu_C\) counts cross-interface and elimination schemas, and \(\nu_H\) counts homotopical, computation, Kan, and path schemas.
\end{definition}

\begin{proposition}[Topological amplification]
Higher-inductive and cohesive candidates can generate homotopical and cross-interface schemas unavailable to discrete candidates, creating superlinear novelty relative to irreducible specification cost.
\end{proposition}

\begin{proofsketch}
Discrete candidates add formation and elimination content but no genuine path constructor. A higher-inductive candidate adds point data, path data, eliminators, computation rules, and compatibility obligations. Since these schemas are generated by a small constructor package, \(\nu_H\) and \(\nu_C\) can increase without proportional growth in \(\kappa\).
\end{proofsketch}

\begin{proposition}[Step 5 circle selection]
At the first geometric transition, \(S^1\) with constructors
\[
\mathsf{base}:S^1,
\qquad
\mathsf{loop}:\mathsf{base}=_{S^1}\mathsf{base}
\]
is the certified unique total discharger of the live window demand --- the inhabitation of structured identity that the sealed dependent grammar exports. Engine-register signature \((\nu,\kappa,\rho)=(7,3,2.33)\); audited semantic worth \(6\) on \(\kappa=3\), the ascent's richest minting until the terminal step.
\end{proposition}

\begin{proofsketch}
Discrete competitors have flat identity and do not discharge a demand for structured identity: they are non-payments, not inefficient rivals. Higher-dimensional packages presuppose path vocabulary the library does not yet export and fail admissibility against the live window. The circle is the least structure that pays, and the census certifies it is the only one.
\end{proofsketch}

\begin{definition}[Schema Calculus]
The per-class counting formulas of the reference evaluator decompose into six principles:
\begin{enumerate}[leftmargin=*,label=P\arabic*.]
\item generator counting: one schema per new formation or introduction generator;
\item adjoint completion: elimination behavior forced by the interpreting adjunction is definitionally free;
\item HIT universal property: the fixed universal-property surcharge for higher-inductive packages;
\item the \(d^2\) law: a dimension-\(d\) path constructor contributes \(d\) monodromy schemas plus \(d(d-1)\) variation fillers;
\item connectivity dichotomy: record access telescopes (Telescopic Elimination); morphism transport contributes \(2\kappa+r^2\) over \(r\) referenced interfaces;
\item uniform action over the referenced base.
\end{enumerate}
\end{definition}

\begin{proposition}[Derived evaluator]
Every component of the reference evaluator is forced, proven, derived, or gauge: ten of sixteen formula components are forced outright by the definitions of \(\nu\) and \(\kappa\); the \(d^2\) law (Lemma L1) and the \(r^2\) transport law (Lemma L2) are proven at the level of careful sketch; the H-space chaining term is resolved by canonical \((\mathrm{operation},\mathrm{coherence})\) pairing; and global \(\nu\)-rescaling is gauge (only cross-class ratios enter selection). The evaluator rebuilt from the derived rules reproduces the strict fifteen-step Genesis trace value for value and hash-identically in the accepted lane.
\end{proposition}

\begin{proofsketch}
L1: the \(d\) monodromy schemas and \(d(d-1)\) ordered variation fillers are the depth-two self-coherence data of a \(d\)-cell; the rival rules \(2d\) and \(\binom{d}{2}\) are refuted at \(d=1\) by inspection of the circle, trace-independently. L2: per-clause forward and lifting faces give \(2\kappa\) (lifting purchased by the charged fibration structure, explaining the bare-arrow \(\kappa=1\) carve-out); \(r\) diagonal monodromies (live by admissibility: trivial self-transport would trivialize the candidate) plus \(r(r-1)\) directed couplings (ordered because inverses are propositional, not definitional) give \(r^2\). The corrected law charges \(\sum_i d_i^2\) per constructor rather than \(\max_i d_i^2\) per telescope; applying the correction engine-wide leaves all fifteen selected steps unchanged and repairs over-pruning off-trace. A register caution now governs this cluster: the \(d^2\) and \(r^2\) laws are engine-register counting rules --- exact descriptions of the enacted evaluator, sealed as testimony --- and the act-local audit refuses their outputs as independent law-level credit (a lifted site or transport coupling is an instance or completion unless it carries its own anchor). The earlier slogan that Born's exponent, \(d^2\), \(r^2\), and Fibonacci are ``four shadows of ordered self-pairing within the depth-two window'' therefore overstates by two: what survives register-independently is the pair rooted directly in width-two structure --- the Fibonacci debt recurrence and the depth bound \(d_{\mathrm{obl}}=2\) --- and any bridge from that depth to the Born exponent must be built on those alone. L1 and L2 remain worth machine-checking as evaluator derivations, but nothing law-level now rides on them: the Step-16 load they once carried is discharged by the certified halt package.
\end{proofsketch}

\section{Genesis Sequence}

\begin{definition}[Genesis trajectory]
The Genesis Sequence is the trajectory of the two laws from the empty library \(\B_0=\varnothing\): three constitutive founding acts, then at each guarded stage
\[
\B_{n+1}=I(\B_n,X_n),
\qquad
X_n \;=\; \text{the total discharger of } \mathrm{O}(n) \text{ with typed provenance},
\]
unique at every guarded stage except Step 4, where four genuinely distinct dischargers survive the canonical quotient and the enacted branch is fixed indexically. (The enacted engine realized this trajectory through its bar-and-overshoot encoding; on the certified trace the two prescriptions select identical winners, and the encoding is retained as testimony.)
\end{definition}

\begin{center}
\small
\begin{tabular}{r l r r r r r r}
\toprule
Step & Selected shell & \(\Delta_n\) & \(\nu^{\mathrm{eng}}\) & \(\nu^{\mathrm{sem}}\) & \(\kappa\) & \(\rho^{\mathrm{eng}}\) & \(\SelBar^{\mathrm{diag}}\) \\
\midrule
1  & Universe \(\U_0\) & 1   & 1   & 1 & 2 & 0.50  & 0.50 \\
2  & Unit type \(\mathbf{1}\) & 1   & 1   & 0 & 1 & 1.00  & 0.50 \\
3  & Witness \(\star:\mathbf{1}\) & 2   & 2   & 1 & 1 & 2.00  & 1.33 \\
4  & \(\Pi/\Sigma\) shell & 3   & 5   & 3 & 3 & 1.67  & 1.50 \\
5  & Circle \(S^1\) & 5   & 7   & 6 & 3 & 2.33  & 2.14 \\
6  & Propositional truncation & 8   & 8   & 3 & 3 & 2.67  & 2.56 \\
7  & Sphere \(S^2\) & 13  & 10  & 1 & 3 & 3.33  & 3.00 \\
8  & H-space \(S^3\) & 21  & 18  & 1 & 5 & 3.60  & 3.43 \\
9  & Hopf fibration & 34  & 17  & 0 & 4 & 4.25  & 4.01 \\
10 & Cohesion/modal shell & 55  & 19  & 4 & 4 & 4.75  & 4.46 \\
11 & Connection shell & 89  & 26  & 1 & 5 & 5.20  & 4.91 \\
12 & Curvature shell & 144 & 34  & 0 & 6 & 5.67  & 5.42 \\
13 & Endomorphic operator bundle & 233 & 46  & 2 & 7 & 6.57  & 5.99 \\
14 & Hilbert-functional shell & 377 & 62  & 3 & 9 & 6.89  & 6.68 \\
15 & Temporal-cohesive shell & 610 & 103 & 6 & 8 & 12.88 & 7.40 \\
\bottomrule
\end{tabular}
\end{center}

\noindent\emph{Register key.} \(\nu^{\mathrm{eng}}\), \(\rho^{\mathrm{eng}}\), and \(\SelBar^{\mathrm{diag}}\) are the enacted engine's structural ledger --- sealed testimony, exact as a record of the enacted evaluator, with no law-level authority; the engine's mechanism-audited total is \(\sum\nu^{\mathrm{eng}}=358\). \(\nu^{\mathrm{sem}}\) is the audited semantic-family register: act-local, receipt-bearing, the sole lawful value column, with \(\sum\nu^{\mathrm{sem}}=32\). The step-8 engine entry reflects the audited mechanism correction; the two semantic zeros (Steps 9 and 12) are certified pure discharges --- synthesis and consequence mint nothing. \(\nu^{\mathrm{sem}}\) at Step 4 is the enacted branch's value; the cone's minima carry \(2\). All cumulative columns are permanently branch-indexed.

\begin{center}
\small
\begin{tabularx}{\textwidth}{r l Y Y}
\toprule
Step & Phase & New primitive capacity & Dominant rejection pressure \\
\midrule
1--4 & Bootstrap & typed universe, inhabitance, witness, dependent formation and pairing & untyped or witnessless candidates fail coherent realizability \\
5 & Geometric ascent & first nontrivial path constructor & flat-identity candidates do not discharge the structured-identity demand \\
6 & Geometric ascent & propositional truncation and proof-irrelevant compression & raw constructive content is too costly when only support is needed \\
7 & Geometric ascent & two-dimensional homotopy payload & lower-dimensional path structure cannot express surface coherence \\
8 & Geometric ascent & \(S^3\) action and H-space multiplication & the bare sphere cannot discharge the operative-lift demand (sealed exclusion) \\
9 & Geometric ascent & Hopf fibration and bundle prototype & unrelated higher spheres lack the fiber/base interface gain \\
10 & Framework abstraction & cohesive modal adjunctions & purely discrete or purely spatial formalisms fail cross-aspect reuse \\
11 & Framework abstraction & transport/connection structure & cohesive shape without parallel transport cannot compare fibers locally \\
12 & Framework abstraction & curvature as failure of transport around loops & flat transport leaves loop-dependent information unmeasured \\
13 & Framework abstraction & metric/frame/endomorphic operator bundle & connection data without metric comparison lacks measurement interface \\
14 & Framework abstraction & Hilbert-functional/operator shell & metric geometry without operator state space lacks quantum-functional dynamics \\
15 & Terminal synthesis & temporal-cohesive guarded runtime & kinematic libraries without temporal exchange cannot execute internal flow \\
\bottomrule
\end{tabularx}
\end{center}

\begin{definition}[Synthetic signatures for Steps 10--15]
The late selected shells have the following synthetic signatures:
\[
\begin{array}{rcl}
10&:&\Pi_{\infty}\dashv \Disc\dashv \Flat\dashv \Sharp,\\[0.2em]
11&:&\nabla \text{ acting on cohesive families},\\[0.2em]
12&:&R_{\nabla}=d\nabla+\nabla\wedge\nabla,\\[0.2em]
13&:&(g,\mathrm{Fr}(M),\star,\Delta,\mathrm{Ric}) \text{ as a coupled metric/frame package},\\[0.2em]
14&:&(\mathcal{H},\mathcal{B}(\mathcal{H}),\mathrm{Spec}) \text{ as Hilbert-functional shell},\\[0.2em]
15&:&(\Next,\Eventually,\text{cohesive exchange},\text{guarded coherence}).
\end{array}
\]
\end{definition}

\begin{definition}[Late charged shell clauses]
The framework-abstraction phase charges the following irreducible API clauses:
\begin{center}
\small
\begin{tabularx}{\textwidth}{r Y}
\toprule
Step & Charged API clauses \\
\midrule
10 & \((\Flat,\Sharp,\Disc,\Shape)\) \\
11 & \((\nabla,\mathrm{transport},\mathrm{covariant\ lift},\mathrm{horizontal\ lift},\mathrm{Leibniz})\) \\
12 & \((R,\mathrm{Bianchi},\mathrm{holonomy},\mathrm{ChernWeil},\mathrm{charclass},\mathrm{curvature\ transport})\) \\
13 & \((g,\mathrm{LeviCivita},\mathrm{geodesic},\vol_g,\star_g,\Delta_g,\Ric_g)\) \\
14 & \((\langle-,-\rangle,\mathrm{complete},\perp\mathrm{Dec},\Spec,C^*\mathrm{Alg},\delta/\delta\phi,\mathrm{metric\ bridge},\mathrm{curvature\ bridge},\mathrm{connection\ bridge})\) \\
15 & \((\Next,\Eventually,c,\mathrm{temporal\ action},e^\Flat,e^\Sharp,\Eventually\mathrm{-elim},\mu_{\Next})\) \\
\bottomrule
\end{tabularx}
\end{center}
The displayed lists have cardinalities \(4,5,6,7,9,8\), matching the canonical \(\kappa\)-ledger.
\end{definition}

\begin{proposition}[Late-stage novelty accounting]
The selected late shells have novelty decompositions
\[
\begin{array}{c|c|c|c|c}
n & \nu_G & \nu_C & \nu_H & \nu_n\\
\hline
10&4&15&0&19\\
11&2&24&0&26\\
12&1&33&0&34\\
13&4&42&0&46\\
14&5&57&0&62
\end{array}
\]
in the engine register --- sealed testimony of the enacted evaluator, with the diagnostic \(\rho_n>\SelBar_n\) recorded for each \(10\le n\le14\). The audited semantic worths for the same stages are \(4,1,0,2,3\): the telescopic inheritance visible in the middle column is exactly the credit class the act-local register refuses, which is why the two columns differ by an order of magnitude.
\end{proposition}

\begin{proofsketch}
Step 10 contributes four modal carriers and their unit/counit/geometric interaction schemas against the Hopf-loaded interface. Step 11 adds two new carrier families and imports the Step 10 cohesive novelty by the telescopic elimination law, giving \(2+(19+5)=26\). Step 12 adds one curvature carrier and depends on connection and cohesion, giving \(1+(26+6+1)=34\). Step 13 adds four metric/frame carrier families and depends on curvature plus connection, giving \(4+(34+7+1)=46\). Step 14 adds five Hilbert-functional carrier families and couples metric, curvature, and connection, giving \(5+(46+9+2)=62\). These identities describe the enacted evaluator exactly and carry no law-level credit; at law level each selected shell is the certified unique total discharger of its stage demand.
\end{proofsketch}

\begin{proposition}[Real-cohesive scalar provenance]
The scalar line object used by the metric shell is derived from the cohesive interpretation of the circle-cover interface and is not charged as an independent late-stage primitive.
\end{proposition}

\begin{proofsketch}
After Step 10, the shape/discrete/cohesive modalities act on the earlier circle. The universal-cover fiber of the shape unit supplies the semantic line-object interface in real-cohesive models. Since the scalar carrier is obtained from the already selected circle and cohesive shell, bottom-up scalar analysis would duplicate derived structure and incur an unnecessary surcharge; the metric shell is charged only for the metric/frame API over that scalar interface.
\end{proofsketch}

\begin{theorem}[Exact Step 15 novelty count (engine register)]
The temporal-cohesive shell has exact engine-register decomposition
\[
\nu^{\mathrm{eng}}_{15}=\nu_G+\nu_H+\nu_C=2+15+86=103,
\qquad
\rho^{\mathrm{eng}}_{15}=\frac{103}{8}=12.875,
\]
sealed testimony of the enacted evaluator (the diagnostic bar stood at \(7.40\)). The audited semantic worth is \(\nu^{\mathrm{sem}}_{15}=6\) on \(\kappa=8\): six independent receipt-bearing families, tied with the circle for the ascent's richest minting; the remaining ninety-seven engine units are temporal action on existing layers and exchange with existing interfaces --- instances and completions under the act-local audit, not independent credit. At law level the step is selected by neither number: it is the certified unique total discharger of the temporal demand among thousands of stated candidates.
\end{theorem}

\begin{proofsketch}
The two new unary formers \(\Next\) and \(\Eventually\) give \(\nu_G=2\). Temporal action on the previous fourteen layers plus guarded self-composition gives \(\nu_H=14+1=15\). Cross-interface novelty has three parts: three temporal action/comparison schemas against each of fourteen earlier layers, giving \(42\); two cohesive exchange laws acting generatorwise on the Step 10 cohesive interface of novelty \(19\), giving \(38\); and six residual comparison, observation, guarded-composition, and depth-two naturality schemas. Thus \(\nu_C=42+38+6=86\), and the efficiency inequality is immediate.
\end{proofsketch}

\begin{theorem}[Stagewise Genesis optimality]
Relative to the fixed \(\MBTT\) grammar, depth-two bar dynamics, admissibility gate, and constructive-prime rule, the selected shells above form the current canonical fifteen-step trajectory.
\end{theorem}

\begin{proofsketch}
Steps 1--3 are the constitutive founding acts; the third exports the first window demand. Steps 4--9 pay the demand chain of grammar, structured identity, control, enclosure, operative lift, and binding; Steps 10--14 pay the framework chain of aspect, transport, route-dependence, magnitude, and analysis; Step 15 pays the temporal demand. At each guarded step the selected shell is admissible and is the certified unique total discharger of the live demand --- except Step 4, where the certified four-way tie founds the cone and the enacted branch continues indexically. The enacted engine's bar-and-overshoot encoding reproduced exactly this trajectory, winner for winner, which is the enactment-conservativity of the two-register law.
\end{proofsketch}

\begin{theorem}[Minimal Temporal Extension]
Let \(G_{14}\) be the grammar earned through Step 14 and \(\mathbf{Ext}_{14}\) the groupoid of finite normalized constructor extensions over \(G_{14}\) with univalent signature equivalences as morphisms. Then:
\begin{enumerate}[leftmargin=*]
\item (Forced growth.) No \(G_{14}\)-expressible candidate discharges the live temporal demand at \(H_{14}\): the resident grammar cannot state any total payment of the obligation its own analytic completion exported. Continuation requires grammar growth. (Engine-register corroboration: the resident diagnostic supremum was \(\rho_{14}=62/9\approx6.89\) against a recorded bar of \(7.40\) --- the stall was visible in the enacted bookkeeping too.)
\item (Minimality.) The class of extensions admitting an admissible discharger of the temporal demand is nonempty, and every cost-minimal member has \(\kappa=8\).
\item (Identification.) Every cost-minimal good extension is, up to univalent signature equivalence, the free coherent guarded modal completion of the resident cohesive base.
\item (Uniqueness.) That extension is unique up to equivalence in \(\mathbf{Ext}_{14}\).
\end{enumerate}
The extension is defined without temporal vocabulary; only after the four clauses are proved is it read as the temporal-cohesive shell. Step 15 is therefore forced grammar growth, not assembly from a pre-stocked temporal alphabet.
\end{theorem}

\begin{proofsketch}
For the stall: a pure-\(G_{14}\) candidate introduces no new global modality or type former, so it can express only interaction among resident structure --- and the temporal demand asks for succession, which no resident interaction supplies; the discharge fails at expressibility, before any question of worth. For minimality and identification: the eight charged clauses of the guarded modal completion are pairwise non-redundant, and any competitor either omits a coherence clause (failing the Constitutive Law over the cohesive base) or adds a distinguishing clause that the audit prices at zero. For uniqueness: the declared category of good guarded-modal extensions has a free completion; its initiality leaves no junk and no choice, so any two cost-minimal good extensions are identified by the induced equivalence of normalized public signatures. This is a Step-15-specific theorem under that declared category, not a general consequence of the acceptance rule.
\end{proofsketch}

\begin{proposition}[Genesis status after the July 2026 audits]
The current Genesis claim separates into four grades:
\begin{enumerate}[leftmargin=*]
\item \emph{Reproduced:} the fixed evaluator and the independently derived Schema Calculus recover Steps 1--15 value for value.
\item \emph{Derived at the terminal grammar boundary:} the resident \(H_{14}\) grammar cannot discharge the live temporal demand, and its unique cost-minimal coherent extension admitting a discharger is the guarded modal completion later interpreted temporally.
\item \emph{Lane-conditional:} uniqueness of the entire fifteen-step course is relative to the fixed grammar, admissibility policy, horizon, quotient, and scoring definitions; invariance under every honest enlargement of that lane is not proved.
\item \emph{Certified after ignition:} directive debt is exhausted at Step 16 at full instance granularity, the demanded-but-underdetermined falsifier is executed and excluded, and the semantics-deep credit bound the earlier contested reading demanded has been derived and adopted: act-local, proof-bearing valuation with the accounting ceiling \(\nu\le4\kappa\). The former formula-maximizing survivors rode engine-register counting rules with no law-level standing; under the lawful register the open band is foreclosed, on every branch of the Stage-4 cone.
\end{enumerate}
Thus the Step-15 grammar objection is closed and terminality is certified on the disclosed lane, on all four branches of the cone; what remains open is universal course uniqueness --- invariance under every honest enlargement of the lane.
\end{proposition}

\begin{theorem}[Temporal-cohesive terminal fixed point]
Step 15 internalizes the generative interface into a temporal-cohesive runtime. Subsequent distinct candidates are either internally derivable from the DCT shell, with zero novelty up to univalent equivalence, or external continuations evaluated on the debt-free open band described in the Guard-Rail package below.
\end{theorem}

\begin{proofsketch}
The temporal-cohesive shell supplies temporal modalities, exchange laws with cohesion, guarded coherence, and the tangent interpretation of update. These structures make candidate generation and internal evolution expressible inside the selected library, so internal continuations have zero primitive novelty as external steps; this internal half is derived and has passed adversarial engine probing. The fate of external continuations is settled by the certified package of the runtime section: demand exhaustion at instance granularity, the executed and excluded falsifier, and the act-local provenance ceiling on the open band. The halt holds on all four branches of the Stage-4 cone.
\end{proofsketch}

\section{Runtime Ignition and Cosmogenesis}

\begin{definition}[Two clocks]
Algorithmic time \(\tau\) indexes external library assembly. Physical time \(t\) is the internal continuous geometric parameter arising after temporal-cohesive completion. They are related by runtime ignition, not by temporal succession inside \(t\).
\end{definition}

\begin{definition}[Minimal internal clock class]
For a guarded history \(s\in\Str(A)\), a clock representative is a map \(c\) into an ordered clock object \(T\) such that a nontrivial guarded tail advances the reading:
\[
c(\mathsf{tail}(s))>c(s).
\]
For continuous flow this becomes monotonicity along the chosen time orientation. Two representatives are equivalent when \(c' = f\circ c\) for a strictly increasing reparameterization \(f\). The minimal clock supplied by the temporal shell is the equivalence class
\[
\mathcal{C}_{\mathrm{ord}}=[c]_{c\sim f\circ c},
\]
not a preferred numerical representative.
\end{definition}

\begin{theorem}[Minimal clock result and calibration no-go]
The Step-15 guarded modality, its jet filtration, and the tangent carrier \(\D\) determine succession, orientation, and local infinitesimal update well enough to define \(\mathcal{C}_{\mathrm{ord}}\). They do not determine a metric calibration from Genesis steps, clause counts, \(\nu\), \(\kappa\), or \(\Delta\) to seconds, scale factor, temperature, or redshift.
\end{theorem}

\begin{proofsketch}
The guard orders a state before its tail, and the tangent interpretation supplies the internal infinitesimal direction; therefore the ordinal/relational clock class is already present. Now assign any strictly increasing timestamps to a fixed realized structural trace and compose them with any other strictly increasing function. The selected histories, score tuples, cumulative bar, obligation windows, and guarded order are unchanged, while all throughputs and physical durations change. A unique metric clock would have to distinguish these models, but the two-law structural data do not. Any such calibration is a physical bridge and must declare its own observable anchor.
\end{proofsketch}

\begin{definition}[Compilation epoch]
The compilation epoch is the sequence \((\B_n)_{n\le14}\) equipped with algorithmic ticks
\[
\tau_n=\sum_{i=1}^{n}\Delta_i.
\]
It contains kinematic structure but no internal dynamical trajectory.
\end{definition}

\begin{definition}[Runtime ignition]
Runtime ignition is the Step 15 transition in which temporal modalities \(\Next,\Eventually\), spatial-temporal exchange, and guarded coherence convert the kinematic library \(\B_{14}\) into the Dynamical Cohesive Topos.
\end{definition}

\begin{theorem}[Kinematic-dynamic equivalence]
In the temporal-cohesive shell,
\[
\Next X\simeq X^{\D}.
\]
The logical next modality is interpreted as tangent exponentiation.
\end{theorem}

\begin{proofsketch}
The pre-temporal library contains cohesive, metric, curvature, and Hilbert-functional structure but no internal flow. Step 15 introduces guarded temporal structure and exchange laws with cohesion. The infinitesimal object \(\D\) turns the temporal next operator into a tangent endofunctor, converting discrete admissible update into internal continuous displacement.
\end{proofsketch}

\begin{theorem}[DCT no-go]
In any nondegenerate topos, the unrestricted L\"ob fixpoint rule is inconsistent with the strict identification \(\Next X\simeq X^{\D}\) for pointed \(\D\).
\end{theorem}

\begin{proofsketch}
The point \(0:\D\) induces a natural evaluation \(\mathrm{ev}_0:\Next A\to A\). Since \(\varnothing^{\D}\cong\varnothing\), applying L\"ob to \(\mathrm{ev}_0\) at \(A=\varnothing\) produces a global element \(1\to\varnothing\), contradicting nondegeneracy. The strict semantic completion of Step 15 --- all eight clauses, the strict identification, and unrestricted L\"ob jointly --- therefore has no model.
\end{proofsketch}

\begin{proposition}[DCT consistency trichotomy]
The Step-15 semantic completion resolves into three graded fragments:
\begin{enumerate}[leftmargin=*]
\item the strict fusion (eight clauses, \(\Next X\simeq X^{\D}\), unrestricted L\"ob) is inconsistent by the no-go theorem;
\item the coiteration fragment --- eight clauses plus the strict identification, productivity by guarded coiteration only, no L\"ob --- is modeled by the Cahiers topos with \(\Next:=(-)^{\D}\);
\item the filtered fragment --- L\"ob under the weakened identification \(\Next\simeq\blacktriangleright((-)^{\D})\) --- is modeled by the tower of jet-truncated stages over the Cahiers topos, where L\"ob holds stage-wise and self-composition \(\Next\Next A\to\Next A\) provably fails.
\end{enumerate}
The repair principle: nilpotency generates a well-founded jet filtration, and L\"ob is licensed as contractive fixpoints along that filtration --- the discrete staircase does not disappear but moves inside the instant as the Taylor tower. The no-go closes the gap between the fragments: no model recovers the strict fusion. Step-15 selection arithmetic (\(\nu_{15}=103\), \(\kappa_{15}=8\)) is unaffected, since it prices the clause package, not the semantic completion. Status: the no-go is proven in full; the model fragments are at careful-sketch level with one cell (\(\Sharp\Eventually\)-exchange) unverified and mechanization pending.
\end{proposition}

\begin{proposition}[Type-theoretic irreversibility]
Guarded corecursion induces a forward execution asymmetry even when internal geometric equations admit reversible local descriptions.
\end{proposition}

\begin{proofsketch}
A guarded fixpoint has type \(\mathrm{fix}:(\Next A\to A)\to A\). Construction of the next state is mediated by the guard. Removing the guard would require accessing the result without temporal delay, collapsing the guarded typing discipline. Thus runtime execution has a typed forward direction. By the consistency trichotomy above, such fixpoints are licensed along the jet filtration (or replaced by coiteration in the strict-identification fragment); the forward asymmetry is present in both consistent fragments.
\end{proofsketch}

\begin{proposition}[Compressed ignition state]
The initial physical state is the public export of the selected temporal-cohesive library, not a point on a pre-existing physical time line. Its integration debt is \(\Delta_{15}=610\) in the current canonical ledger.
\end{proposition}

\begin{proofsketch}
Physical time is internal to Step 15, so no \(t<0\) physical slice is available. The pre-runtime sequence is counted by \(\tau\), and its selected public interface is the complete kinematic-plus-temporal shell. The low initial algorithmic burden is the cost of that selected interface rather than a thermodynamic microstate sampled from a prior physical ensemble.
\end{proofsketch}

\begin{definition}[Guarded streams and internal flow]
For a type \(A\), guarded streams satisfy
\[
\Str(A)\simeq A\times \Next\Str(A).
\]
A guarded step map \(s:A\to\Next A\) induces
\[
\coiter_A(s):A\to\Str(A).
\]
Under \(\Next X\simeq X^{\D}\), a synthetic vector field \(v:\U\to\U^{\D}\) becomes a guarded step map \(\theta_\U^{-1}\circ v:\U\to\Next\U\).
\end{definition}

\begin{theorem}[Internalization of meta-evolution]
After Step 15, continuous structural evolution on the connected component of the univalent universe containing \(\B_{15}\) is expressible internally:
\[
\mathrm{Flow}_v
:=
\coiter_\U(\theta_\U^{-1}\circ v):\U\to\Str(\U).
\]
\end{theorem}

\begin{proofsketch}
The tangent interpretation \(\theta_X:\Next X\simeq X^{\D}\) converts an internal vector field into a guarded next-state map. Guarded coiteration then produces an internal stream of universe states. Since all terms used in the construction belong to the temporal-cohesive shell, the evolution no longer requires external PEN compilation steps.
\end{proofsketch}

\begin{theorem}[Univalent horizon]
The compilation epoch \((\B_n)_{n<15}\) is not internally observable as a physical past of \(\B_{15}\). Physical observables are terms in the DCT runtime and therefore remain inside the connected component generated after runtime ignition.
\end{theorem}

\begin{proofsketch}
Internal observation is a DCT term: a path, section, functional, flow, or record map defined after Step 15. By univalence, internal paths in \(\U\) identify structures by equivalence inside the runtime component. The external algorithmic sequence \(\tau_1,\ldots,\tau_{14}\) is not a path in physical time \(t\); it is the metatheoretic compilation trace. A physical observable crossing from \(t\) into pre-runtime \(\tau\) would require an internal path whose endpoints belong to different explanatory registers, which the temporal-cohesive typing discipline does not supply.
\end{proofsketch}

\begin{definition}[Two debts]
The framework carries two distinct debts that the original halt argument conflated. The quantitative debt \(\Delta_n=F_n\) prices integration and never vanishes. The directive debt \(\mathrm{O}(n)\) is the set of unmet window demands --- obligations of the two most recent sealed interfaces not yet discharged by the derivability closure --- and it shapes the admissible field: throughout Genesis, formula-maximizing shapes were excluded by directive debt (the bare-\(S^3\) record at Step 8 is the sealed instance), never by pricing.
\end{definition}

\begin{theorem}[Obligation locality and debt exhaustion]
From cumulativity, monotone derivability, and depth-two admissibility: discharge persists and \(\mathrm{O}(n+1)\) is window-generated, so selection dynamics is a guarded recursion with directive debt as guard. Moreover, because the specifically declared Step-15 guarded-modal API is realized by its relative free completion, every demand of its window is derivable, and therefore
\[
\mathrm{O}(16)=\emptyset:
\]
Step 16 is the first debt-free stage in the sequence. The exposed ``open band'' at Step 16 is therefore a theorem of the framework, not an engine artifact, and at Step 16 pricing must act alone for the first time.
\end{theorem}

\begin{theorem}[Relocated halt criterion]
The sequence halts at Step 15 if and only if
\[
\sup_{Z\in\text{open band}}\rho(Z)\ <\ \SelBar_{16}
=\Phi_{16}\,\Omega_{15}
=\frac{987}{610}\cdot\frac{359}{64}
=\frac{354333}{39040}\approx 9.077.
\]
The condition is implied by an extraction-guarded valuation --- univalent internality (definable constructions over sealed entries score \(\nu=0\)) plus linearly bounded opaque credit (\(\nu\le\alpha\kappa+\beta\) without binding obligation) --- and fails under any valuation sustaining superlinear open-band credit (\(r^2\), \(d^2\), or telescopic inheritance on mere reference).
\end{theorem}

\begin{proposition}[Halt status, July 2026]
The internal half of the halt --- \(\nu=0\) up to canonical identification for internally derivable continuations --- is derived and passed adversarial engine probing. The external half, formerly contested, is now certified, and the resolution followed exactly the route the contested record demanded. The adversarial probe's seven formula-maximizing survivors (\(\Pi/\Sigma\)-weaves exploiting the \(r^2\) transport law, high-dimension path constructors exploiting the \(d^2\) law, axiomatic candidates exploiting telescopic inheritance) rode counting rules that the subsequently adopted act-local register refuses as credit: under receipt-bearing valuation --- every counted family anchored to a paid clause role or a discharged pre-existing demand, derivable from the candidate and its sealed prefix alone --- the \(d^2\), \(r^2\), and inheritance channels mint nothing, and the accounting bound \(\nu\le4\kappa\) caps every certified open-band candidate at \(\rho\le4\). Reading B's condition (``admissible only if the internality gate is derived blind from the definitions with Genesis 1--15 conservativity as regression gate'') was met: the semantic register was adopted on grounds predating the probe, and its extraction re-derives the certified trace's operational history exactly. The finer refinement anticipated here was likewise executed: instance-level enumeration of the \((S_{15},S_{14})\) window decides all 89 demand instances, \(\mathrm{O}(16)=\emptyset\) at full granularity, and the demanded-but-underdetermined falsifier --- the one channel that could have restored a sixteenth step --- was executed and excluded, separately on each of the four branches of the Stage-4 cone. The halt's narrative is therefore the one the contested record held in escrow: the sequence stops not because continuation is priced beyond reach but because its last constructive act paid off every obligation. Runtime begins where debt ends.
\end{proposition}

\section{Quantum Theory and Born Weights}

\begin{proposition}[No-cloning to quantum kinematics]
An efficient physical runtime forbids unrestricted cloning of live state structure; linear quantum kinematics preserves higher path novelty while preventing free duplication of \(\nu\).
\end{proposition}

\begin{proofsketch}
Unrestricted cloning would copy a live state without paying the integration cost of independently constructing it, inflating \(\nu/\kappa\). In type-theoretic terms, global cloning drives states toward flat \(h\)-set behavior, suppressing higher path structure. Quantum no-cloning preserves the non-flat cohesive structure needed for interference and homotopical novelty.
\end{proofsketch}

\begin{center}
\small
\begin{tabularx}{\textwidth}{l Y Y}
\toprule
Regime & Structural condition & Physical reading \\
\midrule
Classical & identity truncated to public \(h\)-set data & copyable records and deterministic public control \\
Quantum & live cohesive/path data remains unflattened & amplitudes, interference, entanglement, no-cloning \\
Measurement & application of a flat public-record modality & path-structured support becomes copyable outcome data \\
\bottomrule
\end{tabularx}
\end{center}

\begin{definition}[Finite/tame phase-proof family]
For a finite or countable public outcome type \(X\), a finite/tame phase-proof family is a map \(p:\Psi\to X\) such that each homotopy fiber
\[
\Psi_x:=\sum_{\psi:\Psi}(p(\psi)=x)
\]
is \(\pi\)-finite, together with a phase labeling
\[
\theta_x:\pi_0(\Psi_x)\to U(1)
\]
constant on isomorphism classes. Countable families are required to have absolutely convergent phased sums. The object \(\Psi_x\) is the structured proof support for \(x\): witnesses, paths between witnesses, and automorphism groups are retained until public flattening.
\end{definition}

\begin{definition}[Proof fiber and groupoid cardinality]
For a preparation \(p:\Psi\to X\), the proof space over \(x:X\) is
\[
\Psi_x:=\sum_{\psi:\Psi}(p(\psi)=x).
\]
Its ordinary symmetry-aware cardinality is
\[
|\Psi_x|
=
\sum_{[w]\in\pi_0(\Psi_x)}
\frac{1}{|\Aut(w)|}.
\]
Automorphism division identifies internally symmetric presentations as less than independent support.
\end{definition}

\begin{definition}[Phased homotopy cardinality]
The phased homotopy cardinality, or local amplitude, of \(x\) is
\[
\psi(x):=|\Psi_x|_\theta
=
\sum_{[w]\in\pi_0(\Psi_x)}
\frac{e^{i\theta_x([w])}}{|\Aut(w)|}.
\]
\end{definition}

\begin{definition}[Conjugate phase-proof family]
The conjugate family \(\overline{\Psi}\to X\) has the same underlying fibers and conjugated phase labels. If \(|\Psi_x|_\theta=\psi(x)\), then
\[
|\overline{\Psi_x}|_{\overline{\theta}}
=
\overline{\psi(x)}.
\]
The decohered self-pairing over outcomes is
\[
\mathcal{D}:=\Psi\times_X\overline{\Psi},
\qquad
\mathcal{D}_x\simeq\Psi_x\times\overline{\Psi_x}.
\]
\end{definition}

\begin{theorem}[Born weight]
Let \(\mathcal{D}:=\Psi\times_X\overline{\Psi}\). Then
\[
w(x)=|\mathcal{D}_x|_{\theta\otimes\overline{\theta}}
=\psi(x)\overline{\psi(x)}
=|\psi(x)|^2,
\]
and
\[
\Pr(x)=\frac{|\psi(x)|^2}{\sum_y|\psi(y)|^2}.
\]
\end{theorem}

\begin{proofsketch}
The public scalar support for \(x\) must be phase-neutral. The constructively prime phase-neutral object built functorially from a single directed proof fiber is the conjugate self-pairing \(\Psi_x\times\overline{\Psi_x}\). Phased groupoid cardinality is multiplicative over products, and the conjugate fiber has cardinality \(\overline{\psi(x)}\). Normalization of public weights gives the probability rule.
\end{proofsketch}

\begin{proposition}[Uniqueness of the Born square]
Among public scalars built functorially from one phase-proof fiber, every phase-blind monoidal scalar has the form
\[
|\psi(x)|^{2k}\qquad(k\ge0).
\]
The least nontrivial constructively prime scalar is therefore \(|\psi(x)|^2\).
\end{proposition}

\begin{proofsketch}
A monoidal scalar built from \(m\) copies of \(\Psi_x\) and \(n\) copies of \(\overline{\Psi_x}\) has value \(\psi(x)^m\overline{\psi(x)}^{\,n}\). A global phase shift \(\psi(x)\mapsto e^{i\alpha}\psi(x)\) multiplies this by \(e^{i(m-n)\alpha}\), so phase-blindness for all \(\alpha\) forces \(m=n\). Thus the scalar is \(|\psi(x)|^{2k}\). For \(k>1\) the construction bundles \(k\) copies of the same proof/conjugate-proof pair; constructive primeness rejects this duplication. The first nonconstant case \(k=1\) is the Born weight.
\end{proofsketch}

\begin{proposition}[Interference order law]
For coherent alternatives with amplitudes \(\psi_i\) contributing to one unresolved outcome channel,
\[
w_{\mathrm{coh}}
=
\left|\sum_i\psi_i\right|^2
=
\sum_i|\psi_i|^2+\sum_{i\ne j}\psi_i\overline{\psi_j}.
\]
For alternatives first separated into distinct public records,
\[
w_{\mathrm{dec}}=\sum_i|\psi_i|^2.
\]
Thus interference is precisely the noncommutation of coherent summation and conjugate self-pairing.
\end{proposition}

\begin{proofsketch}
The disjoint sum of coherent proof families is formed before applying public extraction, so phased cardinalities add and the conjugate self-pairing produces cross-terms. If the measurement interface first records the alternatives separately, each branch is paired with its own conjugate before classical summation. The latter operation eliminates cross-terms because distinct public records are orthogonal channels.
\end{proofsketch}

\begin{proposition}[Entanglement as proof-space nonfactorization]
For a product outcome interface \(X\times Y\), separability requires a factorization
\[
\Psi_{x,y}\simeq \Psi^X_x\otimes\Psi^Y_y
\]
compatible with phase labels and automorphism weights. Entanglement is failure of this factorization in the phase-proof family.
\end{proposition}

\begin{proofsketch}
A product of independent proof families yields an amplitude matrix of rank one, \(\psi(x,y)=\psi_X(x)\psi_Y(y)\), and public weights factor after self-pairing. If the fiber over \((x,y)\) has irreducible paths, phases, or automorphisms linking the two coordinates, no such tensor decomposition exists. The public record then carries nonfactorizing Born weights.
\end{proofsketch}

\begin{definition}[Continuous phase-proof interface]
A continuous public outcome interface is a sigma-finite measured space \((X,\Sigma,\mu)\). A continuous phase-proof preparation is represented by a measurable field \(x\mapsto\Psi_x\) whose local phased cardinalities form an amplitude representative
\[
\psi\in L^2(X,\mu).
\]
If \(d\mu'=f\,d\mu\) with \(f>0\) almost everywhere, the half-density transformation
\[
\psi'=\frac{\psi}{\sqrt f}
\]
preserves the public measure:
\[
|\psi'|^2\,d\mu'=|\psi|^2\,d\mu.
\]
\end{definition}

\begin{definition}[Continuous Born event measure]
For \(\phi,\psi\in L^2(X,\mu)\), define the event cross-pairing
\[
C_{\phi,\psi}(B):=\int_B\overline{\phi(x)}\,\psi(x)\,d\mu(x),
\qquad B\in\Sigma.
\]
The diagonal public support measure is
\[
\mathsf{Born}_\psi(B):=C_{\psi,\psi}(B)
=\int_B|\psi(x)|^2\,d\mu(x).
\]
For \(\psi\ne0\), the normalized probability measure is
\[
\Pr_\psi(B)=
\frac{\int_B|\psi(x)|^2\,d\mu(x)}
{\int_X|\psi(x)|^2\,d\mu(x)}.
\]
\end{definition}

\begin{proposition}[Events are integrated after fiberwise pairing]
For an event \(B\in\Sigma\),
\[
\Pr_\psi(B)\propto \int_B|\psi(x)|^2\,d\mu(x),
\]
not
\[
\left|\int_B\psi(x)\,d\mu(x)\right|^2.
\]
\end{proposition}

\begin{proofsketch}
The public measurement interface assigns distinct \(x\in B\) to orthogonal record channels. The self-pairing is therefore local in \(x\), and event probability is the integral of local public support. The squared integral would instead treat all points of \(B\) as one coherent unresolved channel before public record separation.
\end{proofsketch}

\begin{theorem}[Continuous \(L^2\) Born completion]
Let \((X,\Sigma,\mu)\) be sigma-finite. Let \((\mathcal{P}_n)\) be refining finite measurable partitions of a finite-measure exhaustion, and let finite/tame coarse-grainings of a phase-proof preparation produce simple amplitudes
\[
\psi_n=\sum_i c_{n,i}\frac{1_{B_{n,i}}}{\sqrt{\mu(B_{n,i})}}
\in L^2(X,\mu).
\]
If \(\psi_n\to\psi\) in \(L^2(X,\mu)\), then
\[
|\psi_n|^2\,d\mu \to |\psi|^2\,d\mu
\]
in total variation. If \(\psi\ne0\), the normalized finite Born measures converge to \(\Pr_\psi\). If \(\phi_n\to\phi\) and \(\psi_n\to\psi\) in \(L^2\), then
\[
\lim_n\int_X\overline{\phi_n(x)}\psi_n(x)\,d\mu(x)
=
\int_X\overline{\phi(x)}\psi(x)\,d\mu(x).
\]
\end{theorem}

\begin{proofsketch}
The normalized indicators \(1_{B_{n,i}}/\sqrt{\mu(B_{n,i})}\) are orthonormal, so finite Born weights \(|c_{n,i}|^2/\sum_j|c_{n,j}|^2\) are exactly the event measures of the embedded simple functions. Inner-product convergence follows from Cauchy-Schwarz. Diagonal measure convergence follows from
\[
\bigl||a|^2-|b|^2\bigr|\le |a-b|(|a|+|b|)
\]
and hence
\[
\||\psi_n|^2-|\psi|^2\|_{L^1}
\le
\|\psi_n-\psi\|_{L^2}
\bigl(\|\psi_n\|_{L^2}+\|\psi\|_{L^2}\bigr)\to0.
\]
Normalization is continuous away from zero norm.
\end{proofsketch}

\begin{proposition}[Spectral-measure Born form]
For a self-adjoint observable \(A\) with projection-valued measure \(E_A\),
\[
\Pr_\psi(A\in B)=\langle\psi,E_A(B)\psi\rangle.
\]
In a spectral direct-integral representation
\[
\mathcal{H}\simeq\int_X^\oplus K_x\,d\mu(x),
\]
this is
\[
\Pr_\psi(B)=\int_B\|\psi(x)\|_{K_x}^2\,d\mu(x).
\]
The scalar \(L^2(X,\mu)\) case is \(K_x\simeq\C\).
\end{proposition}

\begin{proofsketch}
The projection \(E_A(B)\) is multiplication by \(1_B\) in the spectral representation. The inner product therefore evaluates the fiberwise self-pairing over \(B\). The fiber \(K_x\) is the completed local amplitude space of proof-support channels over outcome \(x\), with finite phased groupoid cardinality as its tame one-dimensional shadow.
\end{proofsketch}

\begin{proposition}[Measurement as public flattening]
Measurement is the production of a flat public record:
\[
\mathrm{meas}:Q\to\Flat X,
\]
where \(Q\) is a live cohesive/path-structured quantum state and \(\Flat X\) is the copyable record interface of outcomes.
\end{proposition}

\begin{proofsketch}
Before measurement, the proof family over outcomes carries phase, automorphisms, and higher path dependence. Applying the flat public-record modality discards non-record path structure and exports a stable outcome type whose elements can be copied and compared. The Born weight supplies the phase-neutral support assigned to that public record.
\end{proofsketch}

\begin{definition}[Measurement chain]
A measurement chain is a finite sequence
\[
Q_0\to Q_1\to\cdots\to Q_n\to C,
\]
where \(Q_0\) is the measured quantum system, \(Q_1,\ldots,Q_n\) are physical apparatus, amplification, sensory, or neural links, and \(C\) is an active context. A physical link \(Q_i\) is absorbable when replacing \(Q_0\) by \(Q_0\times Q_i\) preserves the same Born-weighted public record statistics.
\end{definition}

\begin{proposition}[Observer-terminal flattening]
If every \(Q_i\) in a measurement chain is absorbable, then no physical subchain supplies a distinguished terminal flat record. The terminal interface is an active context \(C\) that converts the branch-weighted record family into an integrated public/private update.
\end{proposition}

\begin{proofsketch}
Absorbability means that moving the cut along the physical chain leaves the quantum description invariant up to the same outcome weights. Hence the detector, amplifier, display, sensory organ, and neural relay do not by themselves select a privileged endpoint. A terminal record must be a flat update of an active context: it is the point at which a branch-weighted family is integrated into a history-bearing perspective and made available for public comparison or later report.
\end{proofsketch}

\begin{proposition}[Exchange statistics from proof-space symmetry]
Bosonic and fermionic statistics are determined by the action of permutation symmetries on phase-proof families: symmetric action preserves phased cardinality, while sign-twisted action reverses phase under transposition.
\end{proposition}

\begin{proofsketch}
For indistinguishable \(n\)-particle proof families, the symmetric group \(S_n\) acts on the fiber. Taking homotopy orbits with trivial character yields symmetric support and therefore bosonic accumulation. Taking the sign character on transpositions twists the phase labeling by \(-1\), forcing antisymmetric support and exclusion of identical diagonal states. The distinction is therefore a representation-theoretic property of the proof-space symmetry exported to the Hilbert-functional shell.
\end{proofsketch}

\section{Spacetime, Gravity, and Gauge Structure}

\begin{theorem}[Four-dimensional stage]
The efficient public spacetime stage is four-dimensional: quaternionic associative control avoids non-native coherence overhead, and only in dimension four does the Hodge star close on two-forms,
\[
\star:\Omega^2(M^4)\to\Omega^2(M^4).
\]
\end{theorem}

\begin{proofsketch}
Quaternionic geometry supplies associative \(S^3\)-control inside the depth-two window. Octonionic patching introduces extra coherence payload.

In four dimensions the metric and Hodge infrastructure already paid for at the metric shell yields
\[
\Omega^2=\Omega^2_+\oplus\Omega^2_-
\]
with no change of form degree. Lower dimensions lack this closure. Higher dimensions lose it while increasing patching cost.
\end{proofsketch}

\begin{proposition}[Native associativity ceiling]
The efficient primitive control algebra for public spacetime reaches its native ceiling at quaternionic \(S^3\)-control.
\end{proposition}

\begin{proofsketch}
Pure differential geometry permits \(GL(n,\R)\) for arbitrary \(n\), so dimension is not capped by manifold syntax alone. PEN evaluates primitive synthesis cost. Quaternionic control is associative and remains inside the depth-two coherence window. Octonionic control introduces non-associative patching and explicit \(A_\infty\)-style coherence payload that cannot be discharged natively at depth two. The last high-yield associative stage is the quaternionic Hopf pattern \(S^3\hookrightarrow S^7\to S^4\).
\end{proofsketch}

\begin{proposition}[Hodge endomorphism spike]
For a \(d\)-dimensional metric stage,
\[
\star:\Omega^2(M^d)\to\Omega^{d-2}(M^d).
\]
The only dimension in which \(\star\) is an endomorphism of two-forms is \(d=4\), where it yields self-dual and anti-self-dual projectors at no new primitive metric cost.
\end{proposition}

\begin{proofsketch}
The metric/frame shell has already paid for \(\star\). In \(d=4\), \(\star^2\) acts on \(\Omega^2\) and splits curvature-relevant two-forms into \(\Omega^2_+\oplus\Omega^2_-\). This creates instanton, chirality-sensitive, and curvature-factorization elimination channels, contributing a concentrated \(\nu_C\) gain. In other dimensions the Hodge star changes degree on two-forms, so the same endomorphic reuse is absent.
\end{proofsketch}

\begin{proposition}[Lorentzian modal separation]
Lorentzian signature preserves the distinction between temporal flow and spatial cohesion.
\end{proposition}

\begin{proofsketch}
In Euclidean signature, isotropy can rotate a designated temporal tangent direction into a spatial direction, and univalence turns coherent geometric paths into type equivalences, collapsing \(\Next\) toward cohesive modality. Lorentzian signature partitions tangent vectors into timelike, spacelike, and null classes. The null cone prevents a causal-class-preserving path from identifying flow with spatial cohesion.
\end{proofsketch}

\begin{corollary}[No Lorentz-violation regime]
Any fundamental regime that removes Lorentzian causal-class separation reopens the univalent modal-collapse channel between temporal flow and spatial cohesion.
\end{corollary}

\begin{proofsketch}
The Step 15 temporal modality is represented by tangent infinitesimal update, while cohesion governs spatial/shape structure. If the metric signature permits continuous symmetry paths from temporal to spatial tangent generators, univalence identifies the corresponding modal roles. Lorentzian null-cone separation is the minimal geometric barrier preventing that identification.
\end{proofsketch}

\begin{proposition}[Einstein-Hilbert minimality]
Among local scalar curvature actions yielding second-order divergence-compatible four-dimensional dynamics, the Einstein-Hilbert action
\[
S_{\mathrm{EH}}=\int (R-2\Lambda)\sqrt{-g}\,d^4x
\]
has minimal \(\MBTT\)-AST specification cost.
\end{proposition}

\begin{proofsketch}
The scalar curvature \(R\) is the shortest curvature scalar using the metric and Levi-Civita structure already selected. Quadratic and higher curvature terms add AST nesting and derivative or contraction burden without an equal primitive schema gain in the second-order sector. The minimal admissible curvature action is therefore Einstein-Hilbert.
\end{proofsketch}

\begin{center}
\small
\begin{tabularx}{\textwidth}{l Y Y}
\toprule
Candidate action term & Additional MBTT burden & PEN status \\
\midrule
\(R\) & single curvature scalar from paid metric/connection data & selected core \\
\(R^2\) & extra multiplication and higher-order scalar nesting & higher \(\kappa\), no second-order gain \\
\(R_{\mu\nu}R^{\mu\nu}\) & tensor contraction plus quadratic nesting & rejected in minimal sector \\
\(\nabla R\nabla R\) & derivative tower above curvature scalar & rejected by second-order filter \\
\bottomrule
\end{tabularx}
\end{center}

\begin{theorem}[Standard Model local gauge alphabet]
The efficient internal factor and Lie-algebra alphabet is
\[
G_0=\SM.
\]
The product ordering is conventional. This theorem concerns the three-factor local alphabet and does not by itself choose its global form.
\end{theorem}

\begin{proofsketch}
Hopf and division-algebra structure supply the available high-yield bundle symmetries. Complex unit phase gives \(U(1)\). Unit quaternions give \(SU(2)\). Octonionic automorphism structure, constrained by preservation of the complex quantum phase, yields \(SU(3)\). Larger unification groups add definitional and breaking cost without corresponding primitive topological gain at the active stage.
\end{proofsketch}

\begin{center}
\small
\begin{tabularx}{\textwidth}{l l Y}
\toprule
Rung & Efficient symmetry & Role \\
\midrule
\(\C\) & \(U(1)\) & phase symmetry for complex amplitudes \\
\(\HH\) & \(SU(2)\) & smallest rich non-abelian internal orientation language \\
\(\OO\) & \(SU(3)\) & color symmetry from stabilized octonionic structure preserving complex phase \\
\bottomrule
\end{tabularx}
\end{center}

\begin{corollary}[Empty-desert constraint]
Gauge enlargement beyond \(G_{\mathrm{SM}}\) is rejected when its extra representation and breaking data increase \(\kappa\) without adding primitive efficient novelty at the active public interface.
\end{corollary}

\begin{proofsketch}
A grand-unified group must encode the larger algebra, add breaking structure back to the observed gauge product, and carry additional heavy mediators or selection rules. If the already selected Hopf/division-algebra product supplies the required internal alphabet, the larger group is an inefficient rebundling rather than a constructively prime continuation.
\end{proofsketch}

\begin{corollary}[Proton stability]
If baryon-violating decay channels require unselected grand-unified mediator structure, then proton decay is absent in the efficient Standard Model alphabet.
\end{corollary}

\begin{proofsketch}
Grand-unified proton decay requires additional gauge bosons or comparable mediator geometry beyond \(G_{\mathrm{SM}}\). The empty-desert constraint rejects precisely this larger symmetry package when it only breaks back to the selected product group with extra overhead. With no selected mediator interface, the corresponding baryon-violating channel is not part of the primitive public physics.
\end{proofsketch}

\begin{proposition}[Weak mixing at the structural normalization point]
Conditional on the registered sealing-order premise for the Dirac extension, the sequentially sealed matter content of one Standard Model generation determines
\[
\sin^2\theta_W
=\frac{\Tr T_3^2}{\Tr Q^2}
=\frac{2}{16/3}
=\frac{3}{8}
\]
at the equal-trace normalization point, with no grand-unified embedding: the traces \(\Tr T_3^2=2\), \(\Tr Y^2=10/3\) (whence the \(5/3\) hypercharge normalization factor), and \(\Tr Q^2=16/3\) are properties of the selected representation content itself.
\end{proposition}

\begin{proofsketch}
The computation is a verified trace identity over the derived matter content; historically the same value was obtained only by embedding the generation in \(SU(5)\), but the embedding is not used --- anomaly cancellation over the selected alphabet fixes the hypercharges up to one qualification recorded below, and the hypercharges fix the traces. The two declared obligations have since moved (July 2026 placement audit; registered record in the constants program). The equal-trace bridge principle now has a registered candidate in pair form: the two factors sharing the photon enter public bookkeeping at the matter-interface normalization, an unequal weighting being a declined dial of the tier-three profile; no triple coincidence with the strong coupling is claimed, and none is observed. The matching placement was adjudicated by a frozen two-loop audit under pre-registered kill conditions: the Planck-scale placement is dead --- it forces \(\sin^2\theta_W(M_Z)\approx 0.153\) against the measured \(0.2312\), a worse failure than minimal \(SU(5)\)'s \(\approx 0.21\), which belongs to the triple-unification demand this framework never makes --- and under measured running the equal-trace crossing of the pair sits at \(\mu_X \approx 1.1\times10^{13}\)~GeV. The claim accordingly refiles to its normalization-only form: the traces, the \(5/3\), and the \(3/8\) at the crossing are structural, and the crossing's existence in the desert is verified, but the framework does not compute the low-energy value \(0.2312\). The remaining open item is a blind structural derivation of the matching scale, whose target is now public and therefore retrodiction-grade on the scale alone; it is a bridge-tier claim, not a core one, and its value half stays closed until that derivation exists.

The qualification (adversarial review, July 2026) is load-bearing. With right-handed neutrinos included simultaneously, \(B-L\) is anomaly-free and the equations admit \(Y_\epsilon=Y+\epsilon(B-L)\), with \(\epsilon\sim-1-\epsilon\); anomaly and Yukawa equations alone therefore do not select \(\epsilon=0\). The frozen exact enumeration verifies the standard six-slot ray and this simultaneous family. Once those six slots are sealed, however, adding the all-left-handed Dirac slot \(\nu^c\) makes the neutrino Yukawa equation, mixed gravitational anomaly, and cubic anomaly reduce independently to \(Y_{\nu^c}\), \(Y_{\nu^c}\), and \(Y_{\nu^c}^3\). They force \(Y_{\nu^c}=0\), hence the physical charge \(q_\nu=-Y_{\nu^c}=0\). Thus the standard traces and \(3/8\) normalization are exact relative to sequential sealing, not consequences of the simultaneous system. The remaining conditionality is precisely that registered sealing-order premise.
\end{proofsketch}

\begin{theorem}[Charge lattice and exact \(\mathbb Z_6\) kernel]
Use primitive integer hypercharge \(y=6Y\). For one all-left-handed generation and the Yukawa carrier,
\[
(Q,u^c,d^c,L,e^c,\nu^c,H)
=
(1,-4,2,-3,6,0,3).
\]
Let \(G_0=SU(3)\times SU(2)\times U(1)_y\), and let \(\rho\) be its total action on these slots. Then
\[
\ker\rho
=
\left\{(z^2I_3,z^3I_2,z):z^6=1\right\}
=
\left\langle(\omega_3,-I_2,e^{i\pi/3})\right\rangle
\cong\mathbb Z_6,
\]
and therefore
\[
G_0/\mathbb Z_6\cong\operatorname{Im}\rho.
\]
An irrep with color triality \(t\), weak parity \(d\), and integer hypercharge \(y\) descends to this faithful quotient exactly when
\[
2t+3d+y\equiv0\pmod 6.
\]
\end{theorem}

\begin{proofsketch}
If \((A,B,z)\in\ker\rho\), the \(e^c\) slot gives \(z^6=1\), the \(L\) slot gives \(B=z^3I_2\), and the \(d^c\) slot gives \(A=z^2I_3\). Conversely these six elements act trivially on every displayed slot. The descent congruence is the phase condition for the generator. The first-isomorphism map \([g]\mapsto\rho(g)\) is then a group isomorphism, which the structure-identity principle turns into equality of the quotient and effective-action presentations.
\end{proofsketch}

\begin{proposition}[Physical global-form boundary]
The sealed point-field representation proves the exact kernel and the faithful local action above; it does not by itself select the complete physical global gauge group. Without an \emph{Effective Gauge Presentation} principle, the same local fields permit
\[
G_0/\Gamma,
\qquad
\Gamma\in\{1,\mathbb Z_2,\mathbb Z_3,\mathbb Z_6\}.
\]
The full quotient \(G_{\rm phys}=G_0/\mathbb Z_6\) follows only if transformations identical on every complete physical probe are presentation-identical and the admitted line, bundle, and topological probes contain no further detector of the residual kernel. Univalence identifies \(G_0/\ker\rho\) with \(\operatorname{Im}\rho\); it does not identify non-isomorphic global groups merely because their local point fields agree \cite{tong-lines}.
\end{proposition}

\begin{proposition}[Lowest-risk perturbative controls]
For minimally coupled Dirac matter, the Pauli reduction gives \(g_{\psi,{\rm tree}}=2\); for the charged \(W\), the Yang--Mills three-vector vertex gives \(g_{W,{\rm tree}}=2\). These are tree-level grammar controls: an independent dipole operator or radiative correction changes the measured value. With
\[
\mu\frac{dg_i}{d\mu}=\frac{b_i}{16\pi^2}g_i^3,
\qquad
g_1=\sqrt{\frac53}\,g_Y,
\]
three sealed generations and \(N_H\) complex \((\mathbf1,\mathbf2)_{1/2}\) scalar doublets give
\[
(b_1,b_2,b_3)
=
\left(4+\frac{N_H}{10},-\frac{10}{3}+\frac{N_H}{6},-7\right).
\]
Thus the one-doublet checksum is
\[
\boxed{(b_1,b_2,b_3)=\left(\frac{41}{10},-\frac{19}{6},-7\right)},
\]
or \((b_Y,b_2,b_3)=(41/6,-19/6,-7)\) without GUT normalization. The coefficient calculation checks declared field content; it does not select that content.
\end{proposition}

\begin{proposition}[Minimal Higgs carrier, multiplicity gap, and \(\rho\)]
Assume elementary scalars and direct dimension-four Yukawa masses. Since the right-handed charged fermions are weak singlets, the Yukawa contractions force at least one color-singlet carrier
\[
H\sim(\mathbf1,\mathbf2)_{1/2}
\]
up to conjugation. One such doublet is sufficient, but anomaly cancellation, the charge lattice, and the \(\mathbb Z_6\) kernel do not fix its multiplicity: a second identical or inert doublet is an explicit countermodel. Hence \(N_H=1\) requires a registered scalar-minimality or no-unpaid-duplicate premise. For one doublet with \(\langle H^0\rangle=v/\sqrt2\),
\[
m_W^2=\frac{g^2v^2}{4},
\qquad
m_Z^2=\frac{(g^2+g_Y^2)v^2}{4},
\qquad
\boxed{\rho_{\rm tree}=1}.
\]
The converse fails: any number of \(Y=1/2\) doublet vacuum values, and other custodially arranged scalar sectors, can also give \(\rho_{\rm tree}=1\).
\end{proposition}

\begin{proposition}[Three-generation floor]
The efficient fermion replication count is \(N=3\): the least generation count whose flavor sector carries an unremovable CP-odd invariant.
\end{proposition}

\begin{proofsketch}
With \(N\) generations the mixing interface carries \(\tfrac{1}{2}N(N-1)\) angles and \(\tfrac{1}{2}(N-1)(N-2)\) physical phases after field rephasing. For \(N\le 2\) the phase count is zero, the flavor sector admits a real presentation, and matter/antimatter-asymmetric records are not expressible. \(N=3\) is the least count with one unremovable phase; \(N=4\) adds three angles and two phases with no further qualitative unlock, failing least sufficiency.
\end{proofsketch}

\begin{theorem}[Koide geometry]
Let
\[
v=(\sqrt{m_1},\ldots,\sqrt{m_N})\in\mathbb R_{>0}^N,
\qquad
v=a\,u+w,
\qquad
u=\frac{(1,\ldots,1)}{\sqrt N},
\qquad
w\perp u.
\]
Writing
\[
r=\frac{\|w\|^2}{a^2},
\qquad
p_U=\frac{\|P_Uv\|^2}{\|v\|^2},
\]
one has the exact identities
\[
Q_N
=
\frac{\sum_i m_i}{(\sum_i\sqrt{m_i})^2}
=
\frac{1+r}{N}
=
\frac{1}{Np_U}.
\]
Consequently
\[
\boxed{
Q_N=\frac2N
\iff
r=1
\iff
p_U=p_W=\frac12
\iff
\angle(v,u)=45^\circ .}
\]
This equivalence is kinematic. Interpreting \(v\) as a physical amplitude additionally requires a positive, phase-aligned square-root carrier; \(m_i=|\chi_i|^2\) alone does not imply \((\sum_i|\chi_i|)^2=|\sum_i\chi_i|^2\).
\end{theorem}

\begin{theorem}[Koide underdetermination no-go]
Permutation symmetry, nondegeneracy, univalence, Born self-pairing, and the present actualization axioms do not select \(r=1\). In particular, for
\[
u=\frac{(1,1,1)}{\sqrt3},
\qquad
w_0=\frac{(-5,-3,8)}{\sqrt{98}},
\qquad
v_r=u+\sqrt r\,w_0,
\qquad
0<r\le1,
\]
all three components are positive and distinct, the \(S_3\) carrier and stabilizer status are unchanged, but
\[
\|P_Uv_r\|^2=1,
\qquad
\|P_Wv_r\|^2=r,
\qquad
Q(v_r)=\frac{1+r}{3}.
\]
Thus \(r=1\) gives \(2/3\) while \(r=1/2\) gives \(1/2\), with no presently registered rule distinguishing the two.
\end{theorem}

\begin{proofsketch}
The commutant of the permutation representation is indeed spanned by \(P_U\) and \(P_W\), but this classifies equivariant quadratic operators, not the complete quotient \(\mathbb R^N/S_N\). The latter is continuous: symmetric higher invariants and the unordered mass spectrum distinguish points inside a Weyl chamber without choosing generation labels. The group acts transitively on chambers, not on the continuum of points inside one chamber; topological contractibility therefore does not make the public action groupoid terminal. Born self-pairing maps already prepared channel amplitudes to weights and does not prepare equal amplitudes. Accordingly, a two-channel inventory or proof-count \((1,1)\) cannot be identified with \((\|P_Uv\|,\|P_Wv\|)\) without inserting the desired result. The previous claim that the Koide residue was only R1a plus R2 is withdrawn.
\end{proofsketch}

\begin{proposition}[Born-counting correction]
Appendix H defines phased groupoid cardinality as an amplitude and then squares it to obtain public weight. Hence amplitude counts \(1:2\) give weight ratio \(1:4\), not \(1:2\); amplitudes \(1/6:1\) give \(1:36\), not \(1:6\). Under the attempted sector identification these alternatives would yield \(Q=5/3\) and \(Q=37/3\), respectively, not the earlier quoted \(1\) and \(7/3\). Equal counts conceal this level mismatch because squaring \(1:1\) leaves it unchanged. The former normalization trilemma therefore does not support equipartition.
\end{proposition}

\begin{axiom}[Rank-blind central actualization]
Let
\[
\mathcal Z=\mathbb C P_U\oplus\mathbb C P_W
\]
be the central two-channel algebra of an occupied multiplicity-free flavor decomposition. After carrier ranks and within-sector coordinates have been retained in separate records, suppose the actualized state \(\omega\) on this radial sector record is invariant under the abstract exchange \(\tau(P_U)=P_W\), \(\tau(P_W)=P_U\):
\[
\omega\circ\tau=\omega.
\]
This is new bridge content. It is not supplied by \(S_N\) or univalence, because \(U\) and \(W\) have unequal dimensions and are not equivalent representations.
\end{axiom}

\begin{proposition}[Conditional Koide completion]
Assume rank-blind central actualization and the flavor-state identification
\[
\omega(P)=\frac{\|Pv\|^2}{\|v\|^2}.
\]
Normalization and exchange invariance give \(\omega(P_U)=\omega(P_W)=1/2\), so
\[
\|P_Uv\|^2=\|P_Wv\|^2,
\qquad
\boxed{Q_N=\frac2N}.
\]
For a physical charged-lepton prediction this conditional theorem still requires the positive phase-aligned carrier, jurisdiction of the actualization rule over Yukawa settling, and a \emph{Pole-Seal Bridge} identifying a precise infrared-safe, QED-dressed mass-shell observable with the settled record. The old runtime-existence reduction did not supply these premises.
\end{proposition}

\section{Holography and Dark Sector}

\begin{proposition}[Holographic accounting]
When gravitational structure is active, integration burden is controlled by boundary area rather than bulk volume.
\end{proposition}

\begin{proofsketch}
Bulk degrees that do not change the public boundary interface add redundant interior descriptions. Gravity identifies energy, geometry, and information burden, so the minimal public account is carried by boundary data sufficient to reconstruct admissible bulk structure.
\end{proofsketch}

\begin{definition}[Boundary account]
For a gravitating region \(R\), a boundary account is a public interface
\[
\partial R \longrightarrow \mathsf{Rec}(R)
\]
such that admissible bulk fields in \(R\) are reconstructed up to the public equivalence relation from boundary data and constraint equations. Its cost is measured by the primitive boundary records, not by an independent enumeration of interior presentations.
\end{definition}

\begin{proposition}[Area-law efficiency]
If two bulk descriptions induce the same boundary record and the same admissible constraint solution class, PEN identifies the volume-level difference as redundant integration burden.
\end{proposition}

\begin{proofsketch}
The public gravitational interface sees mass-energy, causal accessibility, and boundary constraints. Interior presentations that do not alter this interface add \(\kappa\) without increasing public novelty. Since coherent gravity already couples information burden to geometry, the minimal accounting surface is the boundary interface, yielding area-controlled cost rather than independent volume storage.
\end{proofsketch}

\begin{axiom}[Closed universe]
The physical universe \(U\) is spatially closed: it is finite without spatial edge and has no asymptotic exterior boundary or external physical observer.
\end{axiom}

This is an adopted cosmological/bridge axiom, not a consequence of the Two Laws or the Genesis arithmetic. It is empirically exposed to global-curvature and topology measurements. Every closed-universe result below is conditional on it; a confidently established open infinite universe would remove this route in its present form.

\begin{definition}[Closed-universe external ledger]
For the \(U\) of the Closed Universe Axiom, its exterior ledger is the fundamental Hilbert account \(\mathcal{H}_{\mathrm{fund}}(U)\) obtained by treating \(U\) as a whole gravitational system rather than as a subsystem seen from an external observer.
\end{definition}

\begin{theorem}[Closed-universe external triviality]
In the closed-universe observer framework of \cite{harlow-observers},
\[
\mathcal{H}_{\mathrm{fund}}(U)\cong \mathbb{R}.
\]
The exterior account of the universe as a whole is one-dimensional and real.
\end{theorem}

\begin{proofsketch}
For a reference system entangled with a closed universe, the competing quantum extremal surface that includes the entire closed universe gives zero entropy for the reference in the fundamental description. Equivalently, the gravitational path integral yields a rank-one Gram matrix for closed-universe states. The absence of an exterior boundary therefore collapses the global exterior ledger to a single real line; the real structure follows from treating \(CRT\) as a gauge symmetry in the closed universe.
\end{proofsketch}

\begin{definition}[Observer-indexed effective Hilbert space]
For an observer \(\mathrm{Ob}\) inside \(U\), let \(S_{\mathrm{Ob}}\) be the coarse-grained entropy or degrees-of-freedom count of the observer. The observer-indexed effective description has state space
\[
\mathcal{H}_{\mathrm{Ob}}
\quad\text{with}\quad
\dim\mathcal{H}_{\mathrm{Ob}}\simeq e^{S_{\mathrm{Ob}}},
\]
and inner-product error
\[
\varepsilon_{\mathrm{Ob}}=O(e^{-S_{\mathrm{Ob}}})
\]
or, in the second-Renyi formulation of an entangled pointer observer,
\[
\varepsilon_{\mathrm{Ob}}=O(e^{-S_2(\omega_{\mathrm{Ob}^{\prime}})}).
\]
\end{definition}

\begin{proposition}[Observer recovery of quantum richness]
The phase-bearing quantum description of an experienced world lives in \(\mathcal{H}_{\mathrm{Ob}}\). It does not live in the one-dimensional real exterior ledger \(\mathcal{H}_{\mathrm{fund}}(U)\).
\end{proposition}

\begin{proofsketch}
The global closed-universe account has no exterior interface on which to carry phase, alternative records, or nontrivial observables. Including an observer supplies a finite pointer-indexed reference system entangled with the measured degrees of freedom. The induced map from matter degrees of freedom to the observer reference preserves inner products up to \(\varepsilon_{\mathrm{Ob}}\), so ordinary quantum mechanics is recovered at the precision available to that observer. Complex amplitudes and Born weights therefore belong to the internal effective description generated by an active context inside the closed universe.
\end{proofsketch}

\begin{proposition}[Observer--gravity public-ledger capacity]
Let \(K\) be the second-Renyi entropy in bits of \(2^K\) balanced independent candidate records, let
\[
n_p=\frac{S_2(\omega_{\mathrm{Ob}'})}{\ln 2},
\qquad
n_g=\log_2\frac{Z_1^2}{Z_2}
\]
be respectively the internal pointer entropy and gravitational Renyi-2 bottleneck, and set \(a=2^{-K}\), \(b=2^{-n_p}\), \(c=2^{-n_g}\). The leading Harlow--Usatyuk--Zhao purity law is
\[
\overline{P_2(K)}
=a+b+c+O(ab,ac,bc).
\]
Define the annealed effective ledger capacity by
\[
C_2(K):=-\log_2\overline{P_2(K)}.
\]
At leading order,
\[
C_2(K)
=K-\log_2\!\left(1+2^{K-K_{\mathrm{cap}}}\right),
\qquad
K_{\mathrm{cap}}
=-\log_2\!\left(2^{-n_p}+2^{-n_g}\right),
\]
and its continuous marginal retention is
\[
\frac{dC_2}{dK}
=\frac{1}{1+2^{K-K_{\mathrm{cap}}}}.
\]
Thus the public-ledger proxy undergoes a smooth Page-like crossover from one retained bit per proposed bit to a finite plateau. For equal floors \(n_p=n_g=n\), \(K_{\mathrm{cap}}=n-1\).
\end{proposition}

\begin{proofsketch}
For a maximally mixed record reference of dimension \(m=2^K\), \(\Tr\chi^2=1/m=2^{-K}\). Harlow--Usatyuk--Zhao's observer contribution is \(e^{-S_2(\omega_{\mathrm{Ob}'})}=2^{-n_p}\), while the gravitational saddle contributes \(Z_2/Z_1^2=2^{-n_g}\). Adding the leading purity channels gives the first display; the capacity and logistic derivative follow algebraically. The equal-floor one-bit reduction is a soft-min effect from adding two equal purity floors. It is model-specific and lies at the order-one accuracy discarded when the sum is replaced by a strict minimum.
\end{proofsketch}

\begin{proposition}[Structured real-code correction]
In the large-dimension real structured code
\[
A\in\R^{p\times g},
\qquad
B\in\R^{g\times m},
\qquad
V=AB,
\]
with independent Gaussian entries, \(m=2^K\), \(p=2^{n_p}\), and \(g=2^{n_g}\), the Appendix-B Wick contractions give
\[
\frac{\mathbb{E}\,\Tr[(V^TV)^2]}
{\bigl(\mathbb{E}\,\Tr[V^TV]\bigr)^2}
=a+b+c+ab+ac+bc+3abc.
\]
Writing
\[
A_0=b+c+bc,
\qquad
L=1+b+c+3bc,
\]
the structured moment-ratio curve is \(P_{2,\mathrm{Wick}}(K)=A_0+L2^{-K}\), with
\[
C_{2,\infty}^{\mathrm{Wick}}=-\log_2A_0,
\qquad
K_{\times}^{\mathrm{Wick}}=\log_2\frac{L}{A_0}.
\]
\end{proposition}

\begin{proofsketch}
The nine terms are the real pairings of record, pointer, and bottleneck indices. The expression is a ratio of ensemble moments; at finite dimension it is not equal to the mean of the per-realization normalized purity \(\mathbb{E}[\Tr((V^TV/\Tr V^TV)^2)]\). The two converge when normalization fluctuations concentrate. The companion calculation in \texttt{public\_ledger\_capacity/} preserves both quantities rather than conflating them.
\end{proofsketch}

\begin{definition}[Born-weighted \(\epsilon\)-public seal]
Normalize the \(m\) record columns of \(V\) to codewords \(\lvert\psi_i\rangle\). The codebook is \(\epsilon\)-publicly sealable for the internal observer when a POVM \(\{M_i\}_{i=1}^m\) satisfies
\[
P_{\mathrm{succ}}
=\frac1m\sum_{i=1}^m
\langle\psi_i\rvert M_i\lvert\psi_i\rangle
\ge 1-\epsilon.
\]
The tolerance \(\epsilon\) is part of the public-event bridge unless independently derived from observer dynamics.
\end{definition}

\begin{proposition}[Operational sealing bound and numerical record]
For the structured code above,
\[
P_{\mathrm{succ}}^{\mathrm{opt}}
\le
\min\!\left(1,\frac{\min(p,g)}{m}\right)
=
\min\!\left(1,2^{\min(n_p,n_g)-K}\right).
\]
The reproducible default simulation \((n_p,n_g)=(5,5)\) gives leading plateau \(4.000\) bits, structured plateau \(3.97763\) bits, and structured crossover \(K_\times=4.06907\). Its pretty-good-measurement success is \(0.9542\) at \(K=2\), \(0.7779\) at \(K=4\), \(0.5721\) at \(K=5\), and \(0.3303\) at \(K=6\). The exact one-stage Haar-real control agrees with its finite-size mean within \(2.24\) standard errors over the registered grid; the structured ratio-of-moments estimator agrees with the Wick expression within \(1.22\).
\end{proposition}

\begin{proofsketch}
The code rank is at most \(r=\min(p,g)\). For equiprobable pure codewords,
\[
\langle\psi_i\rvert M_i\lvert\psi_i\rangle\le\Tr M_i,
\qquad
\sum_iM_i=I_r.
\]
Summation gives \(P_{\mathrm{succ}}\le r/m\), capped at one. The Monte Carlo uses fixed seed \(250102359\), real Gaussian codes, 800 trials per one-stage point, 400 trials per structured point, and reports both annealed and quenched capacities. It demonstrates that the entropy crossover is not itself a high-fidelity sealing threshold: the latter also depends on decoder and declared tolerance.
\end{proofsketch}

\begin{axiom}[Cohesive flow preservation]
Temporal evolution \(\Phi_t\) commutes with the Shape modality:
\[
\Shape(\Phi_t X)\simeq \Phi_t(\Shape X).
\]
\end{axiom}

\begin{proposition}[Topological dark matter]
DCT-stabilized topological solitons of the gauge-Higgs geometry define a dark-matter sector without a new point-particle zoo.
\end{proposition}

\begin{proofsketch}
If a configuration has nontrivial cohesive homotopy type, flow preservation prevents continuous evolution into the trivial vacuum. The object carries stress-energy and therefore gravitational mass. Its extended topology modifies scattering by a geometric form factor
\[
\mathcal{M}(q)=\mathcal{M}_{\mathrm{point}}F_\chi(qR_\chi),
\]
suppressing hard contact signatures characteristic of point-like particles.
\end{proofsketch}

\begin{proposition}[Horizon saturation]
Late-time acceleration is represented as horizon-level response to local novelty saturation against the rising PEN bar.
\end{proposition}

\begin{proofsketch}
If local novelty density stagnates while \(\SelBar_n\) continues to rise, the efficient response is to enlarge the integration horizon rather than introduce a new local fluid. In an FRW fit this can appear as effective \(w_{\mathrm{eff}}(z)<-1\), while the typed local runtime retains microcausal structure.
\end{proofsketch}

\begin{proposition}[Trigger-indexed residual magnitude]
On the saturation reading, the cosmological term is the per-cell residual imbalance written on the trigger-epoch horizon ledger:
\[
\Lambda \;=\; 3\varepsilon\,\frac{H^2(z_t)}{c^2},
\qquad\text{equivalently}\qquad
\Lambda\,\ell_{\mathrm{P}}^2 \;=\; \frac{3\pi\,\varepsilon}{N_t},
\qquad
N_t=\frac{\pi\,c^2}{H^2(z_t)\,\ell_{\mathrm{P}}^2},
\]
with \(z_t\) the structure-formation trigger epoch, \(\varepsilon\) the order-one per-cell saturation imbalance, and \(\ell_{\mathrm{P}}\) the Planck length. For every admissible trigger \(z_t\in[0,3]\) and every \(\varepsilon\in[0.1,10]\), \(\Lambda\,\ell_{\mathrm{P}}^2\) lies in \([10^{-122.4},10^{-119}]\); the measured value is \(2.85\times10^{-122}\), implying \(\varepsilon\approx 0.31\)--\(0.50\) for \(z_t\in[0.3,0.7]\). The exponent is therefore fixed by the reading; the order-one coefficient is not.
\end{proposition}

\begin{proofsketch}
The horizon is the area-law ledger with \(N=A/4\ell_{\mathrm{P}}^2\) cells. A saturation residual is homogeneous, isotropic, and metric-proportional, so by Lovelock uniqueness in \(d=4\) its only lawful slot is the \(\Lambda\)-clause, and the only curvature scale the ledger supplies is its own radius. Non-circularity: on the asymptotic de Sitter ledger the relation \(\Lambda\,\ell_{\mathrm{P}}^2\,N_{\mathrm{dS}}=3\pi\) holds identically, so the content of the estimate is carried entirely by indexing \(N\) at the trigger epoch, which the structure-formation plateau fixes independently of \(\Lambda\); the dissolution of the coincidence problem and the magnitude estimate are one mechanism, not two. Two bridge obligations remain open: derivation of \(z_t\) from the bar dynamics (the same obligation as \(w_{\mathrm{eff}}(z)\)) and derivation of \(\varepsilon\). One coefficient candidate is pre-registered with a declared falsifier and no permitted successor fit: \(\varepsilon=1-1/\varphi=1/\varphi^2\approx 0.382\), the per-step deficit fraction of a plateaued local producer against a bar growing by \(\varphi\), which pins \(z_t=0.52\). Consonant external construction: holographic equipartition (Padmanabhan 2012), \(dV/dt=\ell_{\mathrm{P}}^2\,(N_{\mathrm{surf}}-N_{\mathrm{bulk}})\), which derives cosmic expansion from the same surface--bulk count imbalance with de Sitter as the equipartition point.

Both obligations have since been reduced (July 2026). Trigger: with the \(\varphi\)-multiplicative cadence (shared with the ringdown and \(w(z)\) fingerprints) and the ledger anchor at matter--radiation equality (M\'esz\'aros, \(\Lambda\)-independent), \((1+z_t) = (1+z_{\mathrm{eq}})/\varphi^{n^*}\); the integer \(n^* = 16\) is the unique value compatible with observed late-time acceleration (\(n^* \le 15\) overpredicts \(\Omega_\Lambda\) by \(\ge 4\times\); \(n^* \ge 17\) places the trigger in the future), giving \(z_t = 0.535 \pm 0.010\). Coefficient: the depth-two partition \(\Delta_n = \Delta_{n-1} + \Delta_{n-2}\) with admissibility-forced adjacent discharge yields \(\varepsilon = F_{14}/F_{16} = 377/987 = 1/\varphi^2 - 4.6\times 10^{-7}\) (the depth-one alternative is excluded by measurement at \(2.7\times\)). Jointly, using \(1 - 1/\varphi^2 = 1/\varphi\): \(\Omega_\Lambda/\Omega_m = (1+z_{\mathrm{eq}})^3/\varphi^{49} = 2.236 \pm 0.042\) versus measured \(2.175\) --- within \(2.8\%\) with zero free parameters. Registered engine record (\texttt{docs/lambda\_trigger\_computation\_v2.json}): the internal derivation of \(n^* = 16\) is blocked on a runtime extension of the clause calculus (no-go results exclude every frozen schema under current semantics). The extension was subsequently designed, frozen, and run blind once (\texttt{docs/runtime\_calculus\_run.json}, July 2026): the crossing landed at the second step, not the sixteenth --- a valid miss, taken as registered with no retuning. The trigger integer hereafter rests solely on the uniqueness-given-observation argument; the closed form is graded as retrodiction; the pre-registered \(w(z)\) anchors carry the forward exposure.
\end{proofsketch}

\begin{center}
\small
\begin{tabularx}{\textwidth}{l Y Y}
\toprule
Sector & Selected structure & Observational interface \\
\midrule
Dark matter & cohesive homotopy class preserved by \(\Shape\Phi_t\simeq\Phi_t\Shape\) & gravitational mass with extended form factor \\
Dark energy & horizon response to global novelty/integration imbalance & FRW-level acceleration without primitive local fluid \\
No WIMP zoo & rejection of unneeded particle multiplets & absence of contact-scale mediator signatures \\
No grand unification desert & rejection of inefficient rebundling & no forced intermediate gauge ladder \\
\bottomrule
\end{tabularx}
\end{center}

\begin{proposition}[Dark-sector compression]
The dark sector is the non-luminous public trace of already selected cohesive, gravitational, and horizon structure.
\end{proposition}

\begin{proofsketch}
Point-particle dark-sector extension requires new fields, charges, couplings, and stabilization rules. Cohesive solitons and horizon-level accounting use structures already present in the DCT runtime. The latter therefore have lower \(\kappa\) for the same gravitational and cosmological public effects, and are selected unless observations force additional primitive representation.
\end{proofsketch}

\begin{definition}[Geometric unification transition]
Unification is the transition from separate local interaction descriptions to one connection/curvature geometry over the selected Hopf/division-algebra alphabet. It is not encoded as a larger simple group \(G_{\mathrm{GUT}}\) unless that group itself clears the PEN bar as a constructively prime continuation.
\end{definition}

\begin{proposition}[Dark-sector empirical profile]
The dark-sector profile induced by the theory is:
\[
\begin{array}{ll}
\text{matter side} & \text{extended topological mass with suppressed contact scattering},\\
\text{energy side} & \text{horizon-level acceleration with local microcausality},\\
\text{unification side} & \text{no selected }SU(5)\text{ or }SO(10)\text{ mediator layer}.
\end{array}
\]
\end{proposition}

\begin{proofsketch}
The matter side follows from shape-preserving topological sectors in the existing gauge-Higgs geometry. The energy side follows from global horizon adjustment on the completed, debt-free library, where no new structural sector can ever be admitted (the completeness theorem of the halt) and adjustment must therefore be carried by what exists. The unification side follows from the Standard Model alphabet as the last internal symmetry package the demand chain paid for. These are three projections of the same completeness principle: on a debt-free field, reuse of sealed geometry is not preferred to adding primitive sectors --- it is all that is lawful.
\end{proofsketch}

\section{Computation Hierarchy}

\begin{center}
\begin{tabularx}{\textwidth}{l Y Y Y}
\toprule
Level & Native state kind & Copyability & Role \\
\midrule
Classical & Discrete public states / \(h\)-sets & copyable & flat fragment \\
Quantum & path-structured cohesive states & not generally copyable & cohesive fragment \\
Fundamental & lossless abstraction hierarchy & not primitive & runtime-construction engine \\
\bottomrule
\end{tabularx}
\end{center}

\begin{proposition}[Classical, quantum, fundamental]
Classical computation is manipulation of flattened public states; quantum computation is evolution of live cohesive path structure; fundamental computation is construction and preservation of the runtime in which both regimes exist.
\end{proposition}

\begin{proofsketch}
Flat identity permits serialization and copying. Cohesive identity preserves route information, interference, and nontrivial path composition. The Genesis/DCT layer constructs the state spaces, modalities, and update structure presupposed by classical and quantum machines.
\end{proofsketch}

\begin{definition}[Flat and cohesive computational fragments]
A classical state object is a public \(h\)-set:
\[
\isSet(X):=\prod_{x,y:X}\prod_{p,q:x=y}(p=q).
\]
Its record form is \(\Flat X\), and copying is represented by a public diagonal
\[
\Delta_{\Flat X}:\Flat X\to \Flat X\times\Flat X.
\]
A quantum state object is a live cohesive state whose identity and proof fibers need not be \(h\)-sets.
\end{definition}

\begin{proposition}[Measurement non-invertibility]
The flattening map from a cohesive quantum state to a classical record has no canonical inverse when distinct path-structured states share the same public record.
\end{proposition}

\begin{proofsketch}
\(\Flat\) preserves the copyable public distinctions and discards the higher path data not needed for the record. If \(q_1,q_2:Q\) differ only by live phase or route structure but induce the same element of \(\Flat X\), then any inverse \(\Flat X\to Q\) would have to choose one of the discarded structures without public data. Such a choice is not canonical and would reintroduce free novelty.
\end{proofsketch}

\begin{proposition}[Quantum advantage condition]
Quantum advantage occurs only for tasks whose relevant structure is carried by live path, phase, symmetry, or tensor-factorization data before flattening.
\end{proposition}

\begin{proofsketch}
Flat tasks are already represented in \(\Flat X\) and therefore gain no structural resource from preserving higher identity. A task whose objective depends on phase cancellation, constructive interference, or non-factorizing proof spaces uses information lost under \(\Flat\). Computation in the cohesive fragment can transform that structure before public extraction, so it can outperform flat enumeration precisely in those cases.
\end{proofsketch}

\section{Mind and Active Context}

\begin{definition}[Field of experience]
The field of experience at time \(t\) is a layered active presentation
\[
E_t=(F_t,M_t,B_t,A_t),
\]
where \(F_t\) is focal content, \(M_t\) marginal content, \(B_t\) background availability, and \(A_t\) anticipatory/protentive orientation. The field is bounded in focal content and open through background availability.
\end{definition}

\begin{definition}[Active context]
An active context is a bounded, sequential, integrated, history-bearing state \(C_t\) with live contents, salience weights, and an overwrite/update policy.
\end{definition}

Equivalently,
\[
C_t=(W_t,\sigma_t,\pi_t,u_t),
\]
where \(W_t\) is the current live workspace, \(\sigma_t:W_t\to\mathbb{R}_{\ge0}\) assigns salience, \(\pi_t\) encodes policy and memory constraints, and \(u_t\) is the update kernel from candidates to integrated successor contexts.

\begin{definition}[Active context over availability]
Let \(\mathsf{Avail}_t\) be the background availability space. The active workspace is a finite selected subobject
\[
W_t\hookrightarrow \mathsf{Avail}_t,
\qquad
|W_t|\le N_t,
\]
and overwrite is a partial replacement map
\[
\omega_t:W_t\times\A(C_t)\rightharpoonup W_{t+1}.
\]
\end{definition}

\begin{definition}[Local thought selection]
Thinking has PEN form:
\[
C_{t+1}=I(C_t,x_t),
\qquad
x_t\in
\argmin\{\rho_{C_t}(x)-\SelBar(C_t):x\in\A(C_t),\rho_{C_t}(x)\ge\SelBar(C_t)\}.
\]
\end{definition}

\begin{proposition}[Active context criterion]
A mind is not a static warehouse of representations but a bounded active context whose next state is selected from live candidate continuations under inherited salience and overwrite constraints.
\end{proposition}

\begin{proofsketch}
Action under delay requires retention of a small set of live alternatives. Unlimited retention destroys selectivity; no retention destroys history. The active context is the minimal structure preserving enough past to constrain the next update while remaining bounded enough for selection.
\end{proofsketch}

\begin{proposition}[Attention as local selection]
Attention is the active-context operation that raises a candidate or subcontext above the local selection bar for integration into the next context.
\end{proposition}

\begin{proofsketch}
The workspace has finite capacity and therefore cannot integrate all admissible candidates. Salience weights provide the local analogue of \(\nu\), update and interference costs provide the local analogue of \(\kappa\), and the context's threshold implements \(\SelBar(C_t)\). Attention is the resulting selection of high-ratio contents for active integration.
\end{proofsketch}

\begin{proposition}[Overwrite boundedness]
Overwrite is required for active-context determinacy: without replacement or decay of live contents, the admissible cone becomes too large for local selection.
\end{proposition}

\begin{proofsketch}
If every past live content remained equally active, \(W_t\) would grow monotonically without a usable bound. Candidate generation would then depend on an unbounded prompt, and salience comparison would lose locality. Overwrite preserves enough history for path dependence while keeping the next admissible cone finite and selectable.
\end{proofsketch}

\begin{proposition}[Mind as local lossless abstraction]
A mind is a local lossless abstraction of the public branch for action and judgment: it does not display the whole branch, but it preserves the relations needed for the next context update.
\end{proposition}

\begin{proofsketch}
The active context omits most public and mnemonic detail from focal presence. The omission is not arbitrary deletion: background availability, salience, policy, and memory constraints preserve access to the relations relevant for next-step selection. Thus the mind is local and bounded while remaining history-bearing and branch-indexed.
\end{proofsketch}

\section{Consciousness and the Shared Branch}

\begin{definition}[Hard-problem non-derivability]
Let \(F_{\mathrm{phys}}\) be any third-person functional description of discrimination, report, integration, control, and self-monitoring. The hard-problem claim is
\[
F_{\mathrm{phys}}\not\vdash \exists E\ \mathrm{AppearsFromWithin}(E)
\]
under a material-priority interpretation in which appearance is not already primitive.
\end{definition}

\begin{proposition}[Functional gap]
Functional organization entails public capacities and correlations, but it does not by itself entail appearing-as.
\end{proposition}

\begin{proofsketch}
A functional description specifies state transitions, information flow, behavioral control, and report relations. These are public structural facts. The predicate \(\mathrm{AppearsFromWithin}\) asserts first-person givenness. If givenness is not included among the primitive terms or bridge principles, no derivation from public function alone introduces it. The gap is therefore logical, not merely empirical.
\end{proofsketch}

\begin{principle}[Constructive idealism]
The primitive act of judgment,
\[
\Gamma\vdash a:A,
\]
is the joint site of form and actuality: \(A\) supplies determinate type, \(a\) supplies witness, and the judgment supplies appearing-as.
\end{principle}

\begin{definition}[Judgment decomposition]
The judgment \(\Gamma\vdash a:A\) decomposes into:
\[
\Gamma \quad\text{context},\qquad
A \quad\text{form},\qquad
a \quad\text{witness},\qquad
\vdash \quad\text{actualizing judgment}.
\]
The \(A\)-pole corresponds to constitutive form; the turnstile corresponds to selective actuality.
\end{definition}

\begin{theorem}[Logic and consciousness]
Logic maps to the Constitutive Law; consciousness maps to the Selective Law:
\[
\text{logic}\mapsto\CL,
\qquad
\text{consciousness}\mapsto\SLaw.
\]
\end{theorem}

\begin{proofsketch}
Logic supplies distinction, identity, equivalence, and admissibility. Consciousness supplies actuality, judgment, and appearing. Deriving either requires already using it: derivation presupposes logical form and occurs only as an actual judgment. The two-law architecture is therefore the formal decomposition of inquiry's irreducible presuppositions.
\end{proofsketch}

\begin{proposition}[Shared branch]
The public physical world is the stable, law-governed, collectively shareable branch produced by admissible structure under selective integration.
\end{proposition}

\begin{proofsketch}
The branch is not private stipulation because candidates must satisfy \(\CL\), and actualization is constrained by \(\PEN\). Continuity, resistance, and intersubjective agreement are effects of coherent integration into one shared history.
\end{proofsketch}

\begin{definition}[Public branch]
A public branch is a history \(H_{\mathrm{pub}}\) equipped with stable record maps
\[
r_i:H_{\mathrm{pub}}\to O_i
\]
for agents or instruments \(i\), such that admissible comparisons \(O_i\leftrightarrow O_j\) preserve the relevant typed records up to the public equivalence relation generated by \(\CL\).
\end{definition}

\begin{proposition}[Intersubjectivity]
Intersubjectivity is public-record coherence across active contexts sharing \(H_{\mathrm{pub}}\).
\end{proposition}

\begin{proofsketch}
Distinct active contexts can disagree at private salience and candidate generation. A public record is intersubjective when its comparison maps commute with the shared branch structure. Stability follows from sealing: once integrated into \(H_{\mathrm{pub}}\), the record constrains later admissible continuations.
\end{proofsketch}

\begin{definition}[Physical object]
A physical object is a stable public substructure \(P\hookrightarrow H_{\mathrm{pub}}\) whose record maps persist under admissible branch evolution and whose interactions with other public substructures are law-governed.
\end{definition}

\begin{proposition}[Objectivity from shared constraints]
Objectivity is invariance under admissible comparison between active contexts embedded in the same public branch.
\end{proposition}

\begin{proofsketch}
An objective record is not a private content but a branch-stable constraint. If two contexts \(C_i,C_j\) access record maps \(r_i,r_j\) whose comparison preserves the same public structure, then the record is invariant under perspective change. Resistance, continuity, and repeatability are consequences of the record being sealed into \(H_{\mathrm{pub}}\) and constraining later admissible updates.
\end{proofsketch}

\begin{definition}[Observer-ledger consistency]
Let \(\mathrm{Ob}_i,\mathrm{Ob}_j\) be active contexts in a closed universe, with observer-indexed descriptions \(\mathcal{H}_{\mathrm{Ob}_i}\), \(\mathcal{H}_{\mathrm{Ob}_j}\) and record maps \(r_i,r_j\). Their ledgers are consistent when the record obtained by \(i\) while treating \(j\) as part of the world agrees with the record obtained by \(j\) while treating \(j\) as observer:
\[
d_{\mathrm{pub}}\bigl(r_i|_{\mathrm{Ob}_j},r_j\bigr)
=O(e^{-\min(S_{\mathrm{Ob}_i},S_{\mathrm{Ob}_j})}).
\]
\end{definition}

\begin{proposition}[Shared branch as consistent observer family]
In a closed universe, the shared branch is the family of observer-indexed effective descriptions satisfying observer-ledger consistency.
\end{proposition}

\begin{proofsketch}
The global exterior ledger is trivial, so public richness cannot be a view from outside. Each observer obtains a nontrivial effective description by conditioning the global state through its own finite active context. Privacy is blocked because the descriptions are not independent: when observers enter one another's records, the induced public comparisons must agree up to the exponentially small errors permitted by their entropy-bounded effective descriptions. The public branch is precisely this mutually coherent family of internal ledgers.
\end{proofsketch}

\begin{proposition}[Unique public context conditional on confluence]
For any compatible family of repeatable, copyable, future-stable seals, Public Confluence yields a common nondemolition refinement unique up to public operational equivalence. Hence two observers may have different private effective descriptions, but on a shared overlap they obtain either one common public context or no common seal.
\end{proposition}

\begin{proofsketch}
Existence is the refinement clause of Public Confluence. If two common refinements existed as distinct public facts, applying confluence to the refinements would give a further common refinement preserving both; public operational equivalence identifies records with the same stable comparison behavior. Mutually contradictory records have no joint refinement and therefore cannot both enter public history. The conclusion concerns sealed records, not arbitrary private observables.
\end{proofsketch}

\begin{proposition}[Physical event-sealing gap]
The Closed Universe Axiom, the closed-universe observer code, the temporal shell, and Internal Record Actualization constrain the form and capacity of a physical seal, but they do not uniquely derive \(\mathcal{Q}_p\). In particular, they leave open the record POVM, the tolerance \(\epsilon\), and the choice of public sufficient statistic that preserves every retained claim when raw capacity saturates.
\end{proposition}

\begin{proofsketch}
The observer code controls approximate inner products and Renyi purities. Those quantities are compatible with more than one admissible decoder and more than one coarse-graining of the same pointer algebra. The temporal shell orders updates but does not select a decoder, and Internal Record Actualization states what a completed seal must accomplish without choosing which candidate record realizes it. Two models can therefore agree on every imported theorem and differ only in \(\mathcal{Q}_p\). A unique extractor must be added as a bridge axiom or derived from further observer-specific dynamics.
\end{proofsketch}

\begin{proposition}[Capacity-guarded shared branch]
Conditional on a family of extractors \(\{\mathcal{Q}_p\}\), a public branch is the maximal compatible family of internal seals satisfying five constraints: constitutive admissibility, Born-weighted decodability, append-only lock-in up to abstraction proved lossless for retained public claims, observer-ledger consistency, and Public Confluence. When a proposed raw record would exceed the active capacity, the lawful outputs are ``unsealed'' or a sufficient-statistic seal with that preservation proof; silently overwriting prior public history or sealing contradictory overlap facts is inadmissible.
\end{proposition}

\begin{theorem}[Finite thermal-repetition public-event extractor]
Let \(X\in\{0,1\}\) be balanced, let \(N=2t+1\), and let an observer contain \(N\) decohered pointer cells and \(N\) independent noise cells in
\[
\gamma_G=(1-q)\lvert0\rangle\!\langle0\rvert+q\lvert1\rangle\!\langle1\rvert,
\qquad 0<q<\frac12.
\]
The calibrated finite dynamics copies \(X\) to each pointer, applies a noise-controlled flip, decoheres in the pointer basis, and clones the resulting word \(R\), so that \(R_i=X\oplus G_i\). If the retained public claim concerns \(X\), future evidence is conditionally independent of the raw pointer given \(X\), and admissible post-seal operations preserve the weight blocks, then:
\begin{enumerate}[leftmargin=*]
\item \(W(R)=\sum_iR_i\) is the unique minimal sufficient statistic for \(X\), up to bijective relabeling;
\item the record instrument
\[
\mathcal I_w(\rho)
=
\lvert s_w\rangle\!\langle s_w\rvert_A\otimes\Pi_w\rho\Pi_w,
\qquad
\Pi_w=\sum_{r:W(r)=w}\lvert r\rangle\!\langle r\rvert,
\]
with \(s_w=(w,\widehat x(w),\varepsilon(w),c)\), defines an append-only public-event extractor \(\mathcal Q_p=\{\mathcal I_w\}_{w=0}^{N}\);
\item for symmetric unit loss and mandatory no-abstention, majority decoding is the unique optimal rule and its derived sealing-error floor is
\[
\varepsilon_{\mathrm{seal}}(N,q)
=
\sum_{j=t+1}^{N}\binom Nj q^j(1-q)^{N-j};
\]
\item every two public records \(f_p(W)\) and \(f_q(W)\) admit an exact joint nondemolition refinement, and an incompatible pair has zero projector rather than a contradictory common seal.
\end{enumerate}
For the thermal choice \(\beta\Delta=\ln4\), hence \(q=1/5\), and \(N=7\),
\[
\varepsilon_{\mathrm{seal}}=\frac{521}{15625}=0.033344,
\qquad
P_{\mathrm{succ}}^{\mathrm{opt}}=0.966656.
\]
Writing \(A=q^2+(1-q)^2\) and \(B=2q(1-q)\), the second-Renyi resource counts are
\[
n_p=-\log_2\!\left[\frac{A^N+B^N}{2}\right],
\qquad
n_g=-N\log_2 A,
\]
and therefore
\[
n_p=4.887399,\qquad
n_g=3.894753,\qquad
K_{\mathrm{cap}}=-\log_2(2^{-n_p}+2^{-n_g})=3.307335
\]
in bits. The retained statistic obeys \(H_2(W)=2.848709<K_{\mathrm{cap}}<n_p\).
\end{theorem}

\begin{proofsketch}
For a word of weight \(w\),
\[
\frac{\Pr(r\mid X=1)}{\Pr(r\mid X=0)}
=
\left(\frac{1-q}{q}\right)^{2w-N}.
\]
The factorization criterion proves sufficiency, and strict monotonicity in \(w\) proves minimality and uniqueness of the likelihood-equivalence classes. The sign of this likelihood ratio gives majority decoding; because the states commute and \(N\) is odd, it is both Helstrom/Bayes optimal and minimax, with the displayed binomial tail. For coarse-grainings \(P^p_a=\sum_{f_p(w)=a}\Pi_w\) and \(P^q_b=\sum_{f_q(w)=b}\Pi_w\), the products
\[
P^{pq}_{a,b}=P^p_aP^q_b
=
\sum_{\substack{f_p(w)=a\\f_q(w)=b}}\Pi_w
\]
form the unique least common refinement up to relabeling. They commute with and preserve the calibrated diagonal model; an empty intersection gives \(P^{pq}_{a,b}=0\). Finally, \(\operatorname{Tr}\rho_x^2=A^N\), \(\operatorname{Tr}(\rho_0\rho_1)=B^N\), and \(\operatorname{Tr}[(\gamma_G^{\otimes N})^2]=A^N\), giving the stated purities and numerical values. The hypotheses about retained claims and post-seal operations state the model's operational jurisdiction; without them, Hamming-weight compression need not preserve arbitrary future uses of the raw word.
\end{proofsketch}

\begin{proposition}[Universal bridge residue]
The theorem solves the former immediate bridge research problem for one explicit finite observer. It does not make \(\mathcal Q_p\) universal: the Two Laws still do not force real observers to implement repetition coding, independent thermal flips, the commutative weight algebra, symmetric no-abstention loss, or this gravity bottleneck. The universal problem is now to derive the admissible observer-dynamics class and its public algebra, or to isolate the additional bridge principle that selects them.
\end{proposition}

\section{Predictions}

\begin{definition}[Public-record prediction]
A public-record prediction is a tuple
\[
P=(D_P,\mathcal{S}_P,\mathcal{X}_P,\mathcal{E}_P),
\]
where \(D_P\) is the theoretical domain, \(\mathcal{S}_P\) is the selected class of public records, \(\mathcal{X}_P\) is the excluded class of public records, and \(\mathcal{E}_P\) is the experimental or mathematical interface on which the record class is compared.
\end{definition}

\begin{proposition}[Kinematic core and bridge-dependent observables]
The two-law framework presently constrains kinematic architecture more strongly than numerical observables. Its most robust outputs concern admissible state-space structure, identity, coherence depth, ordered becoming, quantum weighting, and record-capacity form. Dimensionful constants, matching scales, cosmological cadence, event-sealing tolerances, and most dimensionless values require additional bridge premises identifying formal quantities with physical clocks, records, sectors, or resource entropies. Adding such premises does not weaken the two-role ontology, but their independence must be counted rather than hidden inside \(\CL\), PEN, or the Genesis ledger.
\end{proposition}

The prediction package induced by the physics sections is the following public-record inventory. The established rows are consolidated from the audit \cite{lande-audit}; the observer-ledger row is the new conditional bridge prediction derived in the present synthesis.

{\small
\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.15\textwidth}>{\raggedright\arraybackslash}p{0.29\textwidth}>{\raggedright\arraybackslash}p{0.28\textwidth}>{\raggedright\arraybackslash}p{0.18\textwidth}@{}}
\caption{Specific physics predictions and the conditional observer-ledger bridge prediction.}
\label{tab:prediction-inventory}\\
\toprule
Domain & Selected record class \(\mathcal{S}_P\) & Excluded record class \(\mathcal{X}_P\) & Interface \(\mathcal{E}_P\) \\
\midrule
\endfirsthead
\toprule
Domain & Selected record class \(\mathcal{S}_P\) & Excluded record class \(\mathcal{X}_P\) & Interface \(\mathcal{E}_P\) \\
\midrule
\endhead
Gauge sector & Local alphabet \(G_0=SU(3)\times SU(2)\times U(1)\); exact matter-visible kernel \(\mathbb Z_6\) and faithful point-field image \(G_0/\mathbb Z_6\); the complete physical global form remains conditional on effectivity for line, bundle, and topological probes & A complete-probe detector that distinguishes a residual kernel element; or an efficient intermediate unification layer with independent public content & Collider desert and precision searches; line-operator and global-probe audit \\
Baryon number & Proton stability in all channels requiring grand-unified mediator structure & A confirmed proton-decay channel generated by an intermediate gauge mediator & Hyper-Kamiokande and related proton-decay searches \\
Neutrino sector & The PEN/minimality tier selects a Dirac interface; independently, the sequentially sealed charge solution gives \(q_\nu=0\). Neither statement follows from the other & A selected lepton-number-violating Majorana interface producing \(0\nu\beta\beta\); failure of the registered sealing-order premise reopens millicharge freedom & LEGEND-1000 and nEXO; exact charge-lattice and sealing-order audit \\
Charged-lepton flavor & Multiply conditional Koide target \(Q=2/3\): positive phase-aligned root carrier, rank-blind central actualization, a QED-safe mass seal, and the ordering \(m_3>m_\mu\). At fixed \(m_e,m_\mu\) the two positive roots are \(1776.969027\ldots\) and \(3.3173565\ldots\) MeV & A failed bridge premise or a sealed mass record incompatible with both roots refutes this bridge package, not the Two Laws & Belle II and BESIII mass precision; phase, actualization, root-ordering, and pole-seal audits \\
Signed-neutrino root exercise (withdrawn) & The numerical Dirac signed-root fit is retained only as convention-dependent phenomenology: a sign of a Dirac mass eigenvalue is removable by field rephasing & No physical falsifier is assigned until a rephasing-invariant phase carrier and extractor are supplied & Formal rephasing audit; no cosmological prediction status \\
Dark matter & Extended cohesive/topological mass with form-factor-suppressed hard scattering and cored dwarf-galaxy profiles & Clean WIMP-like contact-scattering signal from a point mediator; universal cuspy NFW dwarf profiles & XENONnT, LZ, XLZD-class direct detection; dwarf-halo kinematics \\
Dark energy & Horizon-saturation acceleration with \(w_{\mathrm{eff}}(z)\) crossing or dipping below \(-1\) while local dynamics remain microcausal & Pure constant-\(\Lambda\) record \(w=-1\) over the precision window assigned to the horizon-saturation interface & DESI, Euclid \\
Cosmological constant & Closed form \(\Omega_\Lambda/\Omega_m = (1+z_{\mathrm{eq}})^3/\varphi^{49}\) (trigger \(z_t = 0.535 \pm 0.010\), coefficient \(\varepsilon = 377/987\)); pre-registered anchors: \(w_{\mathrm{eff}} = -1\) for \(z > 0.55\), no second phantom crossing below \(z_t\), any log-periodicity at \(2\pi/\ln\varphi\) exactly & \(w \ne -1\) detected above \(z \approx 0.55\); a second crossing below the trigger; log-periodicity at another frequency; or \(\Omega_\Lambda/\Omega_m\) outside \(2.24 \pm 0.05\) at fixed \(z_{\mathrm{eq}}\) & DESI DR2/DR3, Euclid \(w(z)\) onset reconstruction; runtime-calculus derivation audit \\
Quantum probabilities & Continuous Born event-measure completion of the finite/tame Born-square rule; no finite, tame, or \(L^2\)-limit deviation from self-pairing weights & Non-Born public statistics or a failure of the stated \(L^2\)-completion hypotheses & Quantum-statistical tests; proof audit and constructive mechanization of the Born theorem \\
Observer ledger capacity & Conditional Page-like transition \(C_2(K)=K-\log_2(1+2^{K-K_{\mathrm{cap}}})\), with \(K_{\mathrm{cap}}=-\log_2(2^{-n_p}+2^{-n_g})\); the explicit seven-cell thermal observer derives \(\mathcal Q_p\), \(\varepsilon_{\mathrm{seal}}=521/15625\), \((n_p,n_g,K_{\mathrm{cap}})=(4.887399,3.894753,3.307335)\) bits, and \(H_2(W)=2.848709\) bits & Indefinitely linear retained-record capacity at fixed finite \(n_p,n_g\); any declared \(\epsilon\)-seal violating the rank bound; or failure of the stated finite-model calculation & Independent-observer and noncommutative-code simulations; universal derivation audit for the observer dynamics and public algebra \\
Black-hole ringdown & Log-periodic comb in the whitened Kerr-subtracted ringdown residual with log-frequency spacing \(\ln\varphi\) & Residual spectrum with no photon-sphere-gated peak at \(\ln\varphi\) in the Kerr near-horizon shifted variable & LIGO, Virgo, KAGRA heavy mergers; LISA supermassive mergers \\
Primordial perturbations & Low-complexity primordial spectrum describable by a small parameter set & Sharp spikes, localized non-Gaussian features, or fine structure without symmetry source & CMB-S4; LiteBIRD; structure surveys \\
\bottomrule
\end{longtable}
}

\begin{proposition}[Gauge, baryon, and neutrino status]
The selected low-energy local alphabet and the separate PEN/minimality claims have the form
\[
G_0=\SM,
\qquad
\ker\rho\cong\mathbb Z_6,
\qquad
G_0/\ker\rho\cong\operatorname{Im}\rho,
\]
and, at the distinct PEN/minimality tier,
\[
\neg G_{\mathrm{GUT}}
\quad\wedge\quad
\neg \mathrm{PD}
\quad\wedge\quad
\mathrm{Dirac}(\nu)
\quad\wedge\quad
\neg(0\nu\beta\beta),
\]
where \(G_{\mathrm{GUT}}\) denotes an intermediate simple unification layer, \(\mathrm{PD}\) denotes proton decay mediated by that layer, and \(0\nu\beta\beta\) denotes neutrinoless double-beta decay at the Majorana mass scale.
Conditional on the registered sequential-sealing premise, the charge theorem additionally gives \(q_\nu=0\).  The quotient displayed above is the faithful action on the sealed point fields; identifying it with the complete physical global group requires the separate complete-probe effectivity premise.
\end{proposition}

\begin{proofsketch}
The Hopf/division-algebra ladder selects \(U(1)\), \(SU(2)\), and \(SU(3)\) as the efficient local alphabet. The exact representation-kernel computation then supplies the faithful point-field quotient, but not the global probe policy. A larger simple group adds representation data, breaking data, and heavy mediator geometry before returning to the observed local product. PEN therefore rejects the intermediate layer, and proton-decay channels that use only its rejected mediators have no selected interface. Separately, a Majorana mass term packages lepton-number violation and self-conjugate neutrino identification through an additional bilinear or the dimension-five operator \((LH)(LH)/\Lambda\). The Dirac interface has lower primitive overhead in the declared lane. This is a PEN/minimality selection, whereas \(q_\nu=0\) is the conditional conclusion of the sequential charge seal; neutral charge neither proves Diracness nor by itself excludes a Majorana term.
\end{proofsketch}

\begin{proposition}[Charged-lepton Koide target, multiply conditional]
Assume (i) a positive phase-aligned square-root carrier, (ii) Rank-Blind Central Actualization, (iii) a gauge-invariant and infrared-safe QED-dressed mass-seal functional \(\mathcal M_\ell\), and (iv) the hierarchy label \(m_3>m_\mu\). Then the three sealed charged-lepton mass records satisfy
\[
Q=\frac{m_e+m_\mu+m_3}{\left(\sqrt{m_e}+\sqrt{m_\mu}+\sqrt{m_3}\right)^2}=\frac{2}{3}.
\]
Before premise (iv), the equation has two positive solutions:
\[
\sqrt{m_3}
=2\left(\sqrt{m_e}+\sqrt{m_\mu}\right)
\pm
\sqrt{3\left(m_e+m_\mu+4\sqrt{m_em_\mu}\right)}.
\]
At current input values these are \(m_3=1776.969027\ldots\,\mathrm{MeV}\) and \(m_3=3.3173565\ldots\,\mathrm{MeV}\).  The hierarchy premise alone selects the tau-like high root.  This proposition is a bridge-package consequence, not a consequence of the Two Laws alone.
\end{proposition}

\begin{proofsketch}
The new central actualization axiom and the state/sector-norm identification give equal \(U\)- and \(W\)-sector weights, hence the exact geometry theorem gives \(Q=2/3\) for \(N=3\). Solving the resulting quadratic in \(\sqrt{m_3}\) gives the two displayed roots; the old presentation omitted the low root. The remaining placement issue is substantive. The charged-lepton pole-mass relation is empirically close to \(2/3\), while standard running masses depart by roughly two parts in a thousand at \(M_Z\) \cite{xing-koide}; proposed cancellation mechanisms add non-Standard-Model family structure \cite{sumino-koide}. Moreover, a charged stable field in zero-temperature QED has an infraparticle branch point rather than a simple propagator pole, and unstable fermions require a gauge-independent complex-pole treatment \cite{weldon-shell,kniehl-sirlin}. Therefore ``on shell'' is not yet an extractor: \(\mathcal M_\ell\) must specify the common gauge-invariant, infrared-safe mass observable actually sealed for all three flavors. Failure of phase alignment, rank-blind actualization, jurisdiction, hierarchy, or this mass seal refiles the numerical agreement as runtime data rather than falsifying the core theory.
\end{proofsketch}

\begin{proposition}[Dark-sector predictions]
The dark-sector public record has the combined form
\[
\mathrm{DM}_{\mathrm{top}} \wedge
\neg \mathrm{WIMP}_{\mathrm{contact}} \wedge
\mathrm{Core}_{\mathrm{dwarf}}
\wedge
\mathrm{HSat}(w_{\mathrm{eff}}<-1\ \text{on a late-time interval})
\wedge
\Lambda_{\mathrm{res}}\bigl(\Lambda\,\ell_{\mathrm{P}}^2\sim 10^{-122}\bigr),
\]
where \(\mathrm{DM}_{\mathrm{top}}\) is extended topological mass, \(\mathrm{Core}_{\mathrm{dwarf}}\) is a constant-density dwarf-halo core profile, \(\mathrm{HSat}\) is horizon-saturation acceleration, and \(\Lambda_{\mathrm{res}}\) is the trigger-indexed residual magnitude of the cosmological term.
\end{proposition}

\begin{proofsketch}
Cohesive solitons in the selected gauge-Higgs geometry carry stress-energy while preserving nontrivial shape under temporal flow. Their scattering amplitude factors through an extended form factor \(F_\chi(qR_\chi)\), suppressing hard contact events. The same extended stress-energy profile yields cores rather than point-particle cusps in small halos. For the energy side, local novelty density falling below the rising PEN bar is compensated by enlarging the integration horizon. An FRW effective fit represents this horizon response by a late-time interval with \(w_{\mathrm{eff}}(z)<-1\), while the local typed runtime remains microcausal and does not contain a primitive phantom fluid. The residual magnitude follows from writing an order-one per-cell imbalance on the trigger-epoch ledger: \(\Lambda = 3\varepsilon\,H^2(z_t)/c^2\) fixes the observed exponent \(10^{-122}\) in Planck units for every admissible trigger, while \(\varepsilon\) itself and the derivation of \(z_t\) from the bar dynamics remain open bridge obligations.
\end{proofsketch}

\begin{proposition}[Born-completion prediction]
Every public quantum probability arising as a finite, tame, or continuous
\(L^2\)-limit of admissible proof families is the Born event measure
\[
\Pr_\psi(B)
=
\frac{\int_B|\psi(x)|^2\,d\mu(x)}
{\int_X|\psi(x)|^2\,d\mu(x)}
\qquad (B\in\Sigma).
\]
For normalized \(\psi\), this is \(\Pr_\psi(B)=\int_B|\psi(x)|^2\,d\mu(x)\).
Consequently the fundamental quantum interface contains no exact arbitrary-state cloning operation and no intrinsic nonlinear norm-nonpreserving update law.
\end{proposition}

\begin{proofsketch}
Finite outcome types admit a phase-blind scalar only by pairing each proof amplitude with its conjugate. Higher powers duplicate the same proof/conjugate pair and fail constructive primeness. Continuous spectra are obtained as \(L^2\)-completion of the same paired scalar measure over refinements of tame finite partitions: if \(\psi_n\to\psi\) in \(L^2(X,\mu)\), then \(|\psi_n|^2\to|\psi|^2\) in \(L^1\), so the associated Born measures converge in total variation. Cloning would require natural diagonal copying of cohesive phase-bearing states into flat public records before measurement, contradicting path dependence. Intrinsic nonlinear norm-nonpreserving evolution would replace the paired scalar by a noncanonical public measure.
\end{proofsketch}

\begin{proposition}[Holographic ringdown comb]
Let \(h(t)\) be the observed post-merger strain, let \(h_{\mathrm{Kerr}}(t)\) be the best-fit Kerr quasinormal-mode contribution, and let \(r(t)\) be the whitened residual expressed in the Kerr near-horizon shifted frequency variable. Define
\[
L(u)=|\widehat r(e^u)|^2,
\qquad
C(\tau)=\int L(u)L(u+\tau)\,du,
\]
then the holographic interface prediction is a local maximum of \(C\) at
\[
\tau=\ln\varphi \approx 0.481,
\]
with amplitude scaling approximately as \(A^{-1/2}\), where \(A\) is the horizon area of the remnant.
\end{proposition}

\begin{proofsketch}
Boundary accounting makes the horizon the efficient public interface for black-hole information. The depth-two recurrence gives the natural multiplicative microstep \(\varphi\), hence a spacing \(\Delta\log f=\ln\varphi\) in the frequency variable conjugate to horizon microstructure. Subtraction of the Kerr quasinormal background isolates the residual interface term. The photon-sphere barrier gates the observable contribution, and area dilution gives the \(A^{-1/2}\) amplitude scaling.
\end{proofsketch}

\begin{proposition}[Minimal primordial algorithmic complexity]
Let \(s_0\) be the runtime initial state at ignition and let \(\B_{15}\) be the completed kinematic library. The initial-runtime condition satisfies
\[
K(s_0\mid \B_{15})=O(1),
\]
up to the fixed parameters required by the selected geometry. The observable primordial power spectrum is therefore describable by a bounded low-dimensional parameter family; high-Kolmogorov localized structure is absent unless generated by a selected dynamical symmetry or instability.
\end{proposition}

\begin{proofsketch}
The Algorithmic Big Bang is the transition from external library construction to internal guarded corecursion. At ignition, the library already contains the admissible kinematic alphabet, so the least-cost initial runtime state is the shortest state compatible with that alphabet. Coarse-grained entropy accumulates during internal evolution and need not be present as algorithmic detail in \(s_0\). A primordial spike or localized non-Gaussian feature with no symmetry-generation source would add description length to \(s_0\) rather than arise from the selected runtime dynamics.
\end{proofsketch}

\section{Formal and Executable Realizations}

\begin{definition}[Executable library state]
An executable synthesis state is
\[
S_n=(\B_n,H_n),
\]
where \(\B_n=(e_1,\ldots,e_n)\) is the ordered sequence of sealed library entries and
\[
H_n=((\nu_1,\kappa_1),\ldots,(\nu_n,\kappa_n))
\]
is the realized score history. A sealed entry \(e_i\) consists of a public interface signature, a constructor/eliminator payload, a path-dimensional capability profile, and a canonical key. The history \(H_n\) determines the next bar \(\SelBar_{n+1}\) by the depth-two recurrence and supplies the realized denominator for cumulative efficiency.
\end{definition}

\begin{definition}[Typed executable action space]
Let \(\Cand_{\MBTT}(S_n;b,d,m)\) be the finite set of anonymous \(\MBTT\)-telescopes over \(\B_n\) generated under bit budget \(b\), abstract-syntax depth bound \(d\), and telescope-length bound \(m\). The admissible executable action space is
\[
\A_{\mathrm{exec}}(S_n)=
\{T\in \Cand_{\MBTT}(S_n;b,d,m):
\mathrm{TC}_{\B_n}(T)=1,\ \mathrm{Conn}_{\B_n}(T)=1,\ \mathrm{Ref}_{2,\B_n}(T)=1\},
\]
where \(\mathrm{TC}\) is conservative telescope type checking, \(\mathrm{Conn}\) requires non-vacuous connection to the active interface, and \(\mathrm{Ref}_2\) enforces the two-layer coherence-window reference discipline.
\end{definition}

\begin{definition}[Elaboration, normalization, and canonical quotient]
The executable candidate pipeline factors as
\[
T
\xmapsto{\mathrm{Elab}_{\B_n}}
\widehat T
\xmapsto{\mathrm{Norm}}
\widehat T^\downarrow
\xmapsto{\mathrm{Can}}
\mathrm{key}(\widehat T^\downarrow).
\]
Here \(\mathrm{Elab}\) inserts library references and desugars surface fragments, \(\mathrm{Norm}\) removes deterministic syntactic redundancy, and \(\mathrm{Can}\) assigns a stable presentation key. Define
\[
T\sim_{\mathrm{can}}T'
\quad\Longleftrightarrow\quad
\mathrm{Can}(\mathrm{Norm}(\mathrm{Elab}_{\B_n}T))
=
\mathrm{Can}(\mathrm{Norm}(\mathrm{Elab}_{\B_n}T')).
\]
The effective search surface is the quotient
\[
\overline{\A}_{\mathrm{exec}}(S_n)
=
\A_{\mathrm{exec}}(S_n)/{\sim_{\mathrm{can}}}.
\]
\end{definition}

\begin{definition}[Executable novelty and effort]
For \(T\in\overline{\A}_{\mathrm{exec}}(S_n)\),
\[
\kappa(T\mid\B_n)
=
\kappa_{\mathrm{clause}}(T)
\]
in the default structural lane, with bit-cost and entry-count variants treated as auxiliary views. The structural novelty decomposes as
\[
\nu_{\mathrm{str}}(T\mid\B_n)
=
\nu_G(T\mid\B_n)+\nu_C(T\mid\B_n)+\nu_H(T\mid\B_n),
\]
where \(\nu_G\) counts new generative schema families, \(\nu_C\) counts cross-interface consequences, and \(\nu_H\) counts homotopical or adjoint-completion consequences. The efficiency is
\[
\rho(T\mid\B_n)=
\frac{\nu_{\mathrm{str}}(T\mid\B_n)}{\kappa(T\mid\B_n)}.
\]
\end{definition}

\begin{definition}[Uniform novelty verification lane]
For depth horizon \(h\), the independent uniform-novelty evaluator is
\[
\nu^{\mathrm{unif}}_h(T\mid\B_n)
=
s_h(T\mid\B_n)+f(T\mid\B_n)+a(T\mid\B_n),
\]
where \(s_h\) counts nontrivial schema families induced by newly inhabited types up to depth \(h\), \(f\) counts genuinely new type formers, and \(a\) counts adjoint-completion credit. This evaluator verifies ordering and novelty decomposition without entering the primary structural score path.
\end{definition}

\begin{definition}[Executable PEN objective]
Given \(S_n=(\B_n,H_n)\), the executable selector chooses
\[
T^*\in
\argmin
\{\rho(T\mid\B_n)-\SelBar_{n+1}:
T\in\overline{\A}_{\mathrm{exec}}(S_n),\
\rho(T\mid\B_n)\ge\SelBar_{n+1}\}.
\]
If the displayed set has more than one representative with the same overshoot, ties are resolved lexicographically by smaller \(\kappa\), larger interface density, and then canonical key. The selected entry is sealed:
\[
\B_{n+1}=F_{T^*}(\B_n)
=\mathrm{Cl}_{\vdash}
\bigl(\B_n\mathbin{\raisebox{0.1ex}{\(\smallfrown\)}}(T^*)\bigr),
\qquad
H_{n+1}=H_n\mathbin{\raisebox{0.1ex}{\(\smallfrown\)}}(\nu(T^*),\kappa(T^*)).
\]
The persisted telescope is the finite presentation of this relative free closure; the selector does not thereby prove that \(T^*\) was the initial completion of a weaker preselected requirement.
\end{definition}

\begin{definition}[Executable selection loop]
One executable PEN step is the composite
\[
S_n
\longmapsto
\Cand_{\MBTT}(S_n)
\longmapsto
\A_{\mathrm{exec}}(S_n)
\longmapsto
\overline{\A}_{\mathrm{exec}}(S_n)
\longmapsto
T^*
\longmapsto
S_{n+1}.
\]
Its operational factors are:
\begin{center}
\small
\begin{tabularx}{\textwidth}{@{}l Y Y@{}}
\toprule
Factor & Input & Output \\
\midrule
Generation & \(\B_n\), budget bounds, active grammar & finite anonymous \(\MBTT\)-telescope surface \\
Type checking & generated telescopes and active interface & well-formed connected candidates satisfying \(\mathrm{Ref}_2\) \\
Canonicalization & checked candidates & quotient representatives in \(\overline{\A}_{\mathrm{exec}}(S_n)\) \\
Scoring & quotient representative and \(\B_n\) & \((\nu_G,\nu_C,\nu_H,\kappa,\rho)\) \\
Bar computation & history \(H_n\) and depth-two debt recurrence & threshold \(\SelBar_{n+1}\) \\
Selection & scored quotient surface & least-supercritical representative \(T^*\) \\
Free sealing & \(T^*\) and \(S_n\) & relative term-model closure \(S_{n+1}=(\B_{n+1},H_{n+1})\) \\
Verification & sealed sequence and independent evaluators & theorem-name witnesses and uniform-\(\nu\) ordering checks \\
\bottomrule
\end{tabularx}
\end{center}
\end{definition}

\begin{proposition}[Target-free synthesis]
The executable PEN engine searches over typed theory extensions rather than proofs of externally supplied theorems.
\end{proposition}

\begin{proofsketch}
The evaluator receives \(S_n\), generates \(\MBTT\)-telescopes over \(\B_n\), filters them by type checking, connectedness, and depth-two reference discipline, quotients them by canonical key, and computes \(\nu\), \(\kappa\), \(\rho\), and \(\SelBar\). No term in the objective contains a conjecture, proof state, external theorem name, or semantic label. Search control may bound the finite candidate surface, but acceptance is determined by structural bar clearance and deterministic least-supercritical selection.
\end{proofsketch}

\begin{proposition}[Canonical quotient invariance]
If \(T\sim_{\mathrm{can}}T'\), then \(T\) and \(T'\) determine the same executable acceptance value:
\[
\kappa(T\mid\B_n)=\kappa(T'\mid\B_n),\qquad
\nu_{\mathrm{str}}(T\mid\B_n)=\nu_{\mathrm{str}}(T'\mid\B_n),\qquad
\rho(T\mid\B_n)=\rho(T'\mid\B_n).
\]
\end{proposition}

\begin{proofsketch}
Canonical equivalence identifies candidates after elaboration and deterministic normalization. The structural evaluator reads only normalized clauses, interface references, path-dimensional capability flags, and induced schema consequences. These data are invariant under equality of canonical keys, so cost, novelty, and efficiency factor through \(\overline{\A}_{\mathrm{exec}}(S_n)\).
\end{proofsketch}

\begin{proposition}[Lane separation]
Structural selection and uniform-novelty verification define distinct maps
\[
\nu_{\mathrm{str}},
\nu^{\mathrm{unif}}_h:
\overline{\A}_{\mathrm{exec}}(S_n)\to\mathbb{N}.
\]
The selected \(T^*\) is computed from \(\nu_{\mathrm{str}}\), while \(\nu^{\mathrm{unif}}_h\) is an external ordering check on the same quotient surface.
\end{proposition}

\begin{proofsketch}
The structural lane computes \(\nu_G+\nu_C+\nu_H\) directly from the candidate's rule, interface, and homotopical consequences. The uniform lane recomputes novelty by inhabited-type density, type-former increase, and adjoint-completion credit. Since the second map is applied after selection rather than inside the objective, agreement between the two maps is an independent check of ordering rather than a hidden component of the score.
\end{proofsketch}

\begin{theorem}[Executable recovery of the Genesis trajectory]
Assume the fixed \(\MBTT\) grammar, the depth-two bar recurrence, the canonical Genesis ledger values, conservative type checking, canonical quotienting, and the least-supercritical PEN objective above. Iterating the executable selector from \(S_0=(\varnothing,\varnothing)\) recovers the fifteen-entry trajectory
\[
\begin{array}{rcl}
1&:&\text{Universe},\\
2&:&\text{Unit},\\
3&:&\text{Witness},\\
4&:&\Pi/\Sigma,\\
5&:&S^1,\\
6&:&\tau_{\le0},\\
7&:&S^2,\\
8&:&S^3,\\
9&:&\text{Hopf},\\
10&:&\text{Cohesion},\\
11&:&\text{Connections},\\
12&:&\text{Curvature},\\
13&:&\text{Metric},\\
14&:&\text{Hilbert/operator shell},\\
15&:&\DCT.
\end{array}
\]
\end{theorem}

\begin{proofsketch}
The proof is an induction on \(n\). At each stage the quotient surface \(\overline{\A}_{\mathrm{exec}}(S_n)\) is finite under the active generation bounds. The first three steps are constitutive founding acts; the third exports the first window demand. From Step 4 onward the induction is the demand chain: at each guarded stage the live obligation is stated, and the certified census establishes exactly one acceptable total discharger --- except at Step 4, where four genuinely distinct dischargers survive the quotient and the induction proceeds on the enacted branch, with cross-branch invariance certified separately (per-stage ledgers from Step 5, byte-identity from Step 8, halt on all branches). Step 5's circle pays the structured-identity demand that discrete candidates do not address; Steps 6--9 pay the exact homotopical chain of control, enclosure, operative lift, and binding; Steps 10--14 pay the framework demands of aspect, transport, route-dependence, magnitude, and analysis; Step 15 pays the temporal demand as the unique discharger among thousands stated (engine-register signature \((\nu,\kappa,\Delta)=(103,8,610)\), sealed testimony; audited semantic worth \(6\) on \(\kappa=8\)). Termination at \(\DCT\) is the certified halt package: \(\mathrm{O}(16)=\emptyset\) at instance granularity, internal continuations score \(\nu=0\) by univalent closure (derived, probe-tested), and certified external earning is capped by \(\nu\le4\kappa\) on the open band --- on every branch of the cone.
\end{proofsketch}

\begin{proposition}[Trace stability under the derived evaluator]
Replacing the reference evaluator's counting formulas by the rules derived in the Schema Calculus --- including the corrected \(\sum_i d_i^2\) form of the L1 law and the canonical operation count for H-space chaining --- reproduces the strict fifteen-step trace hash-identically in the accepted lane. The corrections are behavior-identical on all selected steps (each has a single constructor) and divergent only off-trace, where the corrected upper bound repairs real over-pruning of multi-constructor candidates without changing the accepted sequence.
\end{proposition}

\begin{proofsketch}
The derivation-facing rerun compiled against the derived rules passes the full evaluator suite with the accepted and canonical lane hashes unchanged through all fifteen steps. This converts the evaluator from a disclosed policy into a derived object: the trace survives the derivation of its own referee, closing the evaluator degrees-of-freedom exposure at sketch level and empirically, with machine-checking of L1 and L2 the remaining obligation.
\end{proofsketch}

\begin{proposition}[Mechanized theorem interface]
The executable realization interfaces with the coherence-depth mechanization through named theorem surfaces whose formal roles are the following.
\begin{center}
\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.34\textwidth}Y@{}}
\toprule
Mechanized name & Formal role \\
\midrule
\mname{cubical-\allowbreak{}coherence-\allowbreak{}depth-\allowbreak{}exactly-\allowbreak{}two}
& exact depth-two theorem for cubical structural extension; \\
\mname{primitive-\allowbreak{}obligations-\allowbreak{}factor-\allowbreak{}through-\allowbreak{}last-\allowbreak{}two}
& chronological factorization through the two most recent public interfaces; \\
\mname{full-\allowbreak{}coupling-\allowbreak{}depth-\allowbreak{}two-\allowbreak{}affine-\allowbreak{}law}
& full-coupling recurrence law at depth two; \\
\mname{constant-\allowbreak{}payload-\allowbreak{}fibonacci-\allowbreak{}scaling}
& Fibonacci scaling under constant payload cost; \\
\mname{higher-\allowbreak{}arity-\allowbreak{}computational-\allowbreak{}replacement}
& replacement of higher-arity primitive trace by computationally generated canonical trace. \\
\bottomrule
\end{tabularx}
\end{center}
\end{proposition}

\begin{proofsketch}
The theorem names correspond to the exact-depth, chronological-window, affine-recurrence, Fibonacci-scaling, and computational-replacement components used in Sections 5 and 6. The executable selector imports these as structural contracts: the depth-two window determines \(\SelBar\), the recurrence determines integration debt, and computational replacement justifies quotienting higher apparent trace into canonical derived fields.
\end{proofsketch}

Beyond the coherence-depth surface, the halt question's cheapest engine item --- the \(\mathrm{O}(16)\)-emptiness check --- has been executed and passed (July 2026): unlike an exhaustive Step-16 candidate search (infeasible at current bounds), the directive demand profile is finite and small, and the verified timeline exhibits the guard ladder directly --- one demand per structural stage, discharged in sequence, empty at Step 16. Instance-level enumeration of the final window's demands remains open as the finer check. No Koide-specific mechanization artifact was found in the supplied workspace. Its exact geometric identities, counterfamily, and conditional bridge are presently paper-level results; constructive mechanization is planned, not claimed.

\appendix

\section{Notation Index}

\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.24\textwidth}>{\raggedright\arraybackslash}p{0.68\textwidth}@{}}
\toprule
Symbol & Meaning \\
\midrule
\endhead
\(\CL\) & Constitutive Law \\
\(\SLaw\) & Selective Law \\
\(\PEN\) & Principle of Efficient Novelty, operational encoding of \(\SLaw\) \\
\(\DCT\) & Dynamical Cohesive Topos \\
\(H\) & realized history \\
\(\B_n\) & sealed library at stage \(n\) \\
\(\A(H)\) & admissible cone at history \(H\) \\
\(I(H,x)\) & integration map \\
\(F_x(H)\) & relative free sealing completion of the already selected API \(x\) over history \(H\) \\
\(\nu_H(x)\) & generative gain: marginal derivation-schema expansion of \(x\) over history \(H\) \\
\(\nu_{G,H}\), \(\nu_{C,H}\), \(\nu_{P,H}\) & gain components: introduction, cross-interface, path/computation \\
\(\kappa_H(x)\) & irreducible integration cost: irreducible specification-clause count over \(H\) \\
\(\Delta_H(x)\) & sealing latency: normalized atomic coherence witnesses required to seal \(x\) against the active historical interface \\
\(\rho\) & efficiency ratio \(\nu/\kappa\) \\
\(\mathcal{D}\) & decohered conjugate self-pairing \(\Psi\times_X\overline{\Psi}\) over public outcomes \\
\(C_{\phi,\psi}(B)\) & continuous event cross-pairing \(\int_B\overline{\phi(x)}\psi(x)\,d\mu(x)\) \\
\(\mathsf{Born}_\psi(B)\) & diagonal public support measure \(\int_B|\psi(x)|^2\,d\mu(x)\) \\
\(\SelBar_n\) & history-sensitive selection threshold \\
\(O^{(k)}(X)\) & structural obligations at historical depth \(k\) \\
\(\Supp(o)\) & irreducible historical support of obligation \(o\) \\
\(d_{\mathrm{obl}}\) & exact obligation depth \\
\(d_\mu\) & minimal-signature depth \\
\(\mu_n,\Delta_n\) & presentation-invariant sealing cost / integration debt (quantitative debt) \\
\(\mathrm{O}(n)\) & directive debt: unmet window demands of the two most recent sealed interfaces, guarding the admissible field; \(\mathrm{O}(16)=\emptyset\) \\
\(\Omega_n\) & cumulative realized efficiency \(\sum_{i\le n}\nu_i/\sum_{i\le n}\kappa_i\); diagnostic, engine-register, branch-indexed (the \(\Omega\)-ratchet is retired: audited worth is non-monotone, with accepted \(\nu=0\) steps) \\
\(U_n\) & normalized full-coupling cost \(U_n=\mu_n+2c\), satisfying \(U_{n+1}=U_n+U_{n-1}\) under constant payload \(c\) \\
\(\varphi\) & golden ratio \((1+\sqrt5)/2\) \\
\(\MBTT\) & Minimal Binary Type Theory specification grammar \\
\(\ATwoTT\) & abstraction-aware two-level type theory layer for separating strict structure from abstracted public interface \\
\(\CCHM\) & cubical type theory formal shadow used for the constitutive side \\
\(\Flat,\Sharp,\Disc,\Next,\Eventually\) & cohesive and temporal modalities \\
\(\mathcal{C}_{\mathrm{ord}}\) & minimal internal clock class: guarded succession and orientation modulo strictly increasing reparameterization \\
\(\mathsf{Closed}(U)\) & adopted axiom that the physical universe is spatially closed with no asymptotic exterior boundary \\
\(\mathsf{IRA}\) & Internal Record Actualization Axiom: public physical actuality requires a stable decodable internal record \\
\(\mathsf{PC}\) & Public Confluence: compatible stable overlap records have a joint nondemolition refinement; incompatible records have no common seal \\
\(\mathcal{Q}_p\) & partial public-event extractor for internal context \(p\) \\
\(\mathcal{H}_{\mathrm{fund}}(U)\) & exterior fundamental Hilbert account of a closed universe \(U\) \\
\(\mathcal{H}_{\mathrm{Ob}}\) & observer-indexed effective Hilbert space available to an internal observer \(\mathrm{Ob}\) \\
\(S_{\mathrm{Ob}}\) & observer entropy controlling the effective-description error \\
\(n_p,n_g\) & pointer and gravitational Renyi-2 resource entropies measured in bits \\
\(C_2(K),K_{\mathrm{cap}}\) & annealed effective ledger capacity and its leading Page-like crossover location \\
\(G_0\) & Standard Model local product \(SU(3)\times SU(2)\times U(1)_y\), before a physical global-form choice \\
\(K_{\mathrm{SM}}=\ker\rho\) & exact \(\mathbb Z_6\) kernel of the sealed point-field representation; \(G_0/K_{\mathrm{SM}}\cong\operatorname{Im}\rho\) \\
\(b_i\) & one-loop gauge coefficient in \(\mu\,dg_i/d\mu=b_i g_i^3/(16\pi^2)\) \\
\(P_U,P_W\) & projectors onto the trivial and standard sectors of the permutation flavor carrier \\
\(r,p_U,Q_N\) & Koide geometric coordinates: \(r=\|P_Wv\|^2/\|P_Uv\|^2\), \(p_U=\|P_Uv\|^2/\|v\|^2\), and \(Q_N=(1+r)/N=1/(Np_U)\) \\
\(\mathcal Z\) & central radial sector algebra \(\mathbb C P_U\oplus\mathbb C P_W\) used by Rank-Blind Central Actualization \\
\(\mathcal M_\ell\) & still-required gauge-invariant, infrared-safe QED-dressed charged-lepton mass-seal functional \\
\bottomrule
\end{longtable}

\section{Genesis Ledger}

The canonical ledger is the table in Section 8. Its phase decomposition is:
\[
\begin{array}{ll}
1\text{--}4 & \text{Bootstrap},\\
5\text{--}9 & \text{Geometric ascent},\\
10\text{--}14 & \text{Framework abstraction},\\
15 & \text{Temporal-cohesive terminal synthesis}.
\end{array}
\]

\begin{longtable}{@{}r>{\raggedright\arraybackslash}p{0.21\textwidth}>{\raggedright\arraybackslash}p{0.29\textwidth}>{\raggedright\arraybackslash}p{0.33\textwidth}@{}}
\toprule
Step & Row name & Formal contribution & Immediate successor capacity \\
\midrule
\endhead
1 & Universe \(\U_0\) & first typed carrier for admissible formation & typed candidates rather than untyped marks \\
2 & Unit \(\mathbf{1}\) & terminal inhabited type and null payload baseline & inhabitance with zero branching burden \\
3 & Witness \(\star:\mathbf{1}\) & explicit term-level realization of inhabitance & constructive evidence objects \\
4 & \(\Pi/\Sigma\) shell & dependent abstraction, dependent pairing, and interface transport over terms & compositional typed libraries \\
5 & Circle \(S^1\) & first nontrivial path constructor and topological generator & homotopical payload and loop-sensitive novelty \\
6 & Propositional truncation & proof-irrelevant compression of witnesses to support & public support without full witness exposure \\
7 & Sphere \(S^2\) & two-dimensional homotopy carrier & surface coherence and obstruction accounting \\
8 & H-space \(S^3\) & associative multiplication pattern and action object & internal symmetry action inside the depth-two window \\
9 & Hopf fibration & nontrivial base/fiber/clutching interface & bundle geometry and higher geometric reuse \\
10 & Cohesion/modal shell & shape/discrete/flat/sharp modal adjunction package & synthetic spatial cohesion over the geometric core \\
11 & Connection shell & parallel transport and comparison of cohesive fibers & local differential comparison \\
12 & Curvature shell & loop-defect measurement for connection transport & intrinsic field-strength and characteristic-class interface \\
13 & Metric/operator bundle & metric, frame, Hodge, Laplacian, and Ricci comparison package & measurement geometry and operator-valued comparison \\
14 & Hilbert-functional shell & complete inner-product state space and spectral/operator calculus & quantum-functional state dynamics \\
15 & Temporal-cohesive shell & guarded temporal modalities and cohesive exchange laws & internal runtime, history-bearing flow, and \(\DCT\) semantics \\
\bottomrule
\end{longtable}

\section{Dependency Graph}

\[
\begin{array}{c}
\text{Two-role exhaustiveness}\\
\Downarrow\\
\CL+\SLaw\\
\Downarrow\\
\CCHM+\PEN\\
\Downarrow\\
d_{\mathrm{obl}}=2,\ d_\mu\le2\\
\Downarrow\\
\text{two-layer chronological window}\\
\Downarrow\\
\text{affine Fibonacci recurrence}\\
\Downarrow\\
\text{PEN bar dynamics}\\
\Downarrow\\
\text{Genesis Sequence}\\
\Downarrow\\
\text{relative free sealing of each selected API}\\
\Downarrow\\
\DCT\text{ and ordered runtime ignition}\\
\Downarrow\\
\mathcal{C}_{\mathrm{ord}}
\quad+\quad
\mathsf{Closed}(U)+\mathsf{IRA}+\mathsf{PC}+\mathcal{Q}_p\\
\Downarrow\\
\text{observer-indexed public physics}\\
\Downarrow\\
\text{mind, consciousness, artificial systems, moral status}.
\end{array}
\]

\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.28\textwidth}>{\raggedright\arraybackslash}p{0.30\textwidth}>{\raggedright\arraybackslash}p{0.34\textwidth}@{}}
\toprule
Dependency node & Required invariant & Output used downstream \\
\midrule
\endhead
Two-role exhaustiveness & every determinate history requires admissibility and actualization roles & primitive split into \(\CL\) and \(\SLaw\) \\
\(\CL/\SLaw\) requirements & coherent realizability plus selective integration of continuations & CCHM realization and PEN objective \\
CCHM realization & constructive univalence, Kan filling, canonicity, finite public interfaces & sealed structural extensions with computable obligations \\
PEN objective & ratio score, bar clearance, least-supercritical selection & canonical next-API rule for library growth; not general categorical initiality \\
Relative free sealing & least congruence generated by the selected API and inherited equations & initial exact realization of that API, with no junk or extra equations \\
Exact depth two & lower binary obstruction and upper horn contraction & two-layer chronological window \\
Affine recurrence & full-coupling obligations depend on last two public interfaces & Fibonacci debt and normalized cost \(U_n\) \\
PEN bar & debt growth and realized efficiency history & rising threshold for constructively prime continuation \\
Genesis Sequence & repeated PEN selection from \(\B_0=\varnothing\) & fifteen-step formal library ending in \(\DCT\) \\
\(\DCT\) & temporal-cohesive guarded runtime structure and consistent semantic fragment & internal ordered flow and the clock class \(\mathcal{C}_{\mathrm{ord}}\), but no metric calibration \\
Closed-universe observer layer & \(\mathsf{Closed}(U)\) plus the Harlow--Usatyuk--Zhao observer code & trivial exterior account; entropy-limited internal effective Hilbert spaces \\
Physical sealing bridge & Internal Record Actualization, Public Confluence, and a capacity-respecting extractor \(\mathcal{Q}_p\) & stable Born-weighted public records; common seal or no common seal on overlaps \\
Ledger-capacity law & pointer entropy \(n_p\), gravity bottleneck \(n_g\), and balanced record entropy \(K\) & conditional Page-like retained-record transition and operational rank bound \\
Domain consequences & application of \(\CL+\SLaw\) inside selected runtime geometry and declared physical bridges & physics, mind, consciousness, artificial active contexts, and moral-status ordering \\
\bottomrule
\end{longtable}

\section{Source Map}

\begin{description}[leftmargin=*,style=nextline]
\item[Two primitive roles, CL, SL, PEN]
Book Appendix A and foundations manuscript \cite{lande-book-appendices,lande-foundations}.
\item[Depth two, horn reduction, chronology, Fibonacci]
Coherence-window and LMCS/Zenodo coherence manuscripts \cite{lande-coherence,lmcs-coherence}.
\item[PEN axioms, MBTT cost, schema novelty, automated synthesis]
PEN library-growth, generative-calculus, and automated-theory-synthesis manuscripts \cite{lande-pen,lande-generative,lande-automated}.
\item[Genesis and temporal-cohesive completion]
Architecture, synthetic-framework, and algorithmic-big-bang manuscripts \cite{lande-architecture,lande-synthetic,lande-bigbang}.
\item[Minimal-Extension Theorem (Step-15 stall, minimality, uniqueness)]
Book Appendix J \cite{lande-minimal-extension}.
\item[DCT no-go theorem, model fragments, jet-filtration repair]
Companion model note and the Genesis-mathematics chapter postscript \cite{lande-dct-note,lande-book-appendices}.
\item[Two debts, obligation locality, instance-granular debt exhaustion, certified halt on the cone]
Guard-Rail companion note and the certified audit record: \(\mathrm{O}(16)=\emptyset\) at instance granularity, falsifier executed and excluded, act-local provenance ceiling, four-branch invariance \cite{lande-guard-rail,lande-book-appendices}.
\item[Schema Calculus, lemmas L1/L2, derived-evaluator trace stability]
Evaluator derivation documents in the mechanization repository \cite{lande-evaluator}.
\item[\(\Omega\)-ratchet (retired) and runtime richness from incompleteness]
Fate-of-the-universe chapter, revised per the register audit \cite{lande-fate}.
\item[Quantum kinematics, Born weights, GR, Standard Model alphabet]
Born-rule appendix, local GR/QM mechanization paper, and standard no-cloning/GR references \cite{lande-born-rule,lande-grqm,wootters-zurek,dieks,lovelock}.
\item[Holography and dark sector]
Local dark-sector manuscript and book dark-sector chapter \cite{lande-dark,lande-book-appendices}.
\item[Specific physics predictions]
Book audit chapter and scorecard consolidation \cite{lande-audit}.
\item[Standard Model representation closures and perturbative controls]
The sequential charge seal, exact \(\mathbb Z_6\) kernel, faithful point-field quotient, tree-level \(g=2\), one-loop coefficients, minimal Yukawa carrier, Higgs-multiplicity gap, and conditional \(\rho_{\mathrm{tree}}=1\) are derived or audited in this synthesis against the predictions-program layer. The distinction between a local-field quotient and a complete global gauge group is controlled by line and topological probes \cite{lande-constants,tong-lines}.
\item[Koide geometry, underdetermination no-go, and conditional bridge]
The exact geometry and counterfamily are proved in this synthesis. Book Appendix M records the pre-registration and the now-superseded bridge attempt, not a proof of equipartition \cite{lande-koide}. Pole-versus-running placement and the mass-seal problem are cross-checked against \cite{xing-koide,sumino-koide,weldon-shell,kniehl-sirlin}.
\item[Weak mixing angle at the structural normalization point]
Constants-program document in the mechanization repository \cite{lande-constants}.
\item[Closed-universe observers]
Quantum-observer construction for a closed universe and observer-indexed Hilbert spaces \cite{harlow-observers}.
\item[Minimal clock class and calibration no-go]
Runtime-ignition and open-problems analyses: the temporal shell supplies guarded order and tangent update, while reparameterization blocks a definition-derived conversion from the structural ledger to a metric physical clock \cite{lande-bigbang,lande-book-appendices}.
\item[Relative free sealing, internal records, and Public Confluence]
Relative free sealing is the term-model universal property applied after PEN selects an API. Internal Record Actualization and Public Confluence are adopted physical clauses in this synthesis. The extractor gap follows by comparing the Two-Law/PEN data with the decoder freedom left by the closed-universe observer construction \cite{harlow-observers}.
\item[Kinematic reconstruction target]
The countermodel classification and the Genesis-to-Jordan, natural-monoidal-phase, free-environment, and public-descent program are consolidated in this synthesis as an open theorem package; they are not outputs of the current Genesis trace.
\item[Public-ledger capacity transition]
Harlow--Usatyuk--Zhao Renyi-purity formulas and Appendix-B real contractions \cite{harlow-observers}, specialized and checked in the reproducible companion calculation \texttt{public\_ledger\_capacity/}.
\item[Finite thermal-repetition public-event extractor]
Exact model-level derivation in this synthesis: likelihood-equivalence classes force the Hamming-weight statistic; commuting weight projectors give the extractor and Public-Confluence refinement; binary hypothesis testing gives the sealing-error floor; collision probabilities give \(n_p\), \(n_g\), and the capacity diagnostic. The imported closed-universe observer construction motivates the cloned pointer and second-Renyi accounting but does not uniquely select this finite dynamics \cite{harlow-observers}.
\item[Mind, consciousness, artificial systems, moral status]
Book appendices and chapters as normalized terminology layer \cite{lande-book-appendices}.
\end{description}

\section{Theorem Source Index}

\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.34\textwidth}>{\raggedright\arraybackslash}p{0.58\textwidth}@{}}
\toprule
Claim family & Source layer \\
\midrule
\endhead
Two-role exhaustiveness; primitive law factorization
& Book Appendix A and foundations manuscript \cite{lande-book-appendices,lande-foundations}. \\
Constitutive judgment clauses; CCHM formal shadow; canonicity
& Book Appendix A, foundations manuscript, and standard HoTT/CCHM background \cite{lande-book-appendices,lande-foundations,hottbook,cohen-cubical}. \\
Selective Law; PEN admissibility; generative gain; irreducible integration cost; ratio score; bar; least-supercritical selection; relative free sealing
& Book Appendix A, PEN library-growth manuscript, automated-theory-synthesis manuscript, and generative-calculus manuscript \cite{lande-book-appendices,lande-pen,lande-automated,lande-generative}. Relative free sealing is post-selection term-model initiality, not a theorem that PEN selects the initial completion of a weaker requirement. Internal Record Actualization and Public Confluence are separately marked as adopted physical clauses. \\
Lower bound \(d_{\mathrm{obl}}\ge2\); binary swap obstruction; adjunction barrier
& Coherence-depth theorem index and LMCS/Zenodo coherence manuscript \cite{lande-coherence-artifact,lmcs-coherence}. \\
Upper bound \(d_{\mathrm{obl}}\le2\); horn reduction; contractible remote factors
& Coherence-depth theorem index and LMCS/Zenodo coherence manuscript \cite{lande-coherence-artifact,lmcs-coherence}. \\
Primitive trace elimination; \(d_\mu\le2\); computational replacement
& Coherence-depth theorem index, trust-boundary documentation, and coherence manuscript \cite{lande-coherence-artifact,lande-coherence}. \\
Chronological Markov blanket; two-layer history factorization
& Coherence-depth theorem index and coherence-window manuscript \cite{lande-coherence-artifact,lande-coherence}. \\
Affine recurrence; constant-payload Fibonacci scaling; asymptotic \(\varphi\)
& Coherence-depth theorem index, LMCS/Zenodo manuscript, and coherence-window manuscript \cite{lande-coherence-artifact,lmcs-coherence,lande-coherence}. \\
Generative calculus; MBTT cost; schema novelty; circle selection; Hopf transition
& PEN library-growth, generative-calculus, and automated-theory-synthesis manuscripts \cite{lande-pen,lande-generative,lande-automated}. \\
Genesis trajectory; Step 15 novelty decomposition; temporal-cohesive completion
& Book Appendix B, Architecture of Genesis, Synthetic Framework Abstraction, and Algorithmic Big Bang manuscripts \cite{lande-book-appendices,lande-architecture,lande-synthetic,lande-bigbang}. \\
Minimal Temporal Extension theorem (stall lemma, arity rigidity, \(\kappa=8\) minimality, uniqueness)
& Book Appendix J \cite{lande-minimal-extension}. \\
DCT no-go; Cahiers and tower model fragments; jet-filtration repair
& Companion model note; Genesis-mathematics postscript trichotomy claim \cite{lande-dct-note,lande-book-appendices}. \\
Two debts; obligation locality; \(\mathrm{O}(16)=\emptyset\); relocated halt criterion; open-band probe record
& Guard-Rail companion note; T1 probe record; Genesis-mathematics postscript status \cite{lande-guard-rail,lande-book-appendices}. \\
Schema Calculus; L1 (\(d^2\)) and L2 (\(r^2\)) lemmas; H-space chaining resolution; derived-evaluator trace stability
& Evaluator derivation documents \cite{lande-evaluator}. \\
\(\Omega\)-ratchet (retired); runtime richness from incompleteness
& Fate chapter, revised per the register audit: the ratchet is refuted in the lawful register (accepted \(\nu=0\) steps); incompleteness now grounds runtime richness, not a stalling dichotomy \cite{lande-fate}. \\
Koide geometry \(Q_N=(1+r)/N=1/(Np_U)\); positive counterfamily; underdetermination and Born-counting correction
& Exact paper-level derivation in this synthesis. Appendix M is retained as the historical pre-registration and superseded bridge attempt \cite{lande-koide}; no Koide-specific mechanization artifact is presently claimed. \\
Rank-Blind Central Actualization; conditional \(Q_N=2/N\); phase, jurisdiction, hierarchy, and mass-seal obligations
& New bridge axiom and conditional theorem in this synthesis, informed by the Koide audit and mass-definition literature \cite{lande-koide,xing-koide,sumino-koide,weldon-shell,kniehl-sirlin}. \\
Weak mixing \(\sin^2\theta_W=3/8\) at the structural normalization point
& Constants-program document \cite{lande-constants}. \\
Sequential charge sealing and conditional \(q_\nu=0\); integer charge lattice; exact \(\mathbb Z_6\) representation kernel; faithful local quotient; physical global-form gap
& Exact enumeration and kernel proof in this synthesis, normalized against the constants and predictions programs; global-probe distinction cross-checked with line-operator classification \cite{lande-constants,tong-lines}. \\
Tree-level \(g=2\); one-loop \(b_i\) checks; minimal Higgs carrier; scalar-multiplicity gap; conditional \(\rho_{\mathrm{tree}}=1\)
& Standard perturbative evaluator calculations and explicit countermodel audit in this synthesis. The one-doublet coefficient row is conditional on \(N_H=1\), not a selection theorem. \\
Runtime ignition; two clocks; minimal internal clock class; calibration no-go; internalization of meta-evolution
& Algorithmic Big Bang and Architecture of Genesis manuscripts plus the runtime-calculus reparameterization audit recorded in the book's open-problems layer \cite{lande-bigbang,lande-architecture,lande-book-appendices}. \\
Quantum kinematics; no-cloning; finite/tame Born-square uniqueness; continuous \(L^2\) Born completion; spectral-measure Born form; measurement as public flattening
& Born-rule appendix, GR/QM manuscript, book Appendix H source layer, and standard no-cloning references \cite{lande-born-rule,lande-grqm,lande-book-appendices,wootters-zurek,dieks}. \\
Lorentzian four-dimensionality; Einstein-Hilbert minimality; Standard Model alphabet
& GR/QM manuscript, Lovelock theorem background, and local physics appendices \cite{lande-grqm,lovelock,lande-book-appendices}. \\
Holographic area accounting; topological dark matter; horizon dark energy; trigger-indexed \(\Lambda\) residual magnitude
& Dark-sector manuscript and book dark-sector material \cite{lande-dark,lande-book-appendices}. \\
Specific physics inventory; gauge desert; proton stability; Dirac-minimality claim; three-generation floor; conditional charged-lepton Koide target; withdrawn signed-neutrino exercise; dark-sector records; trigger-indexed \(\Lambda\) magnitude; Born completion; holographic ringdown comb; minimal primordial spectrum
& Audit chapter, book physics chapters, Koide pre-registration appendix as historical input, dark-sector manuscript, GR/QM manuscript, algorithmic-big-bang manuscript, and Kolmogorov-complexity background \cite{lande-audit,lande-book-appendices,lande-koide,lande-dark,lande-grqm,lande-bigbang,li-vitanyi}. The status corrections are made explicitly in this synthesis. \\
Closed-universe external triviality; observer-indexed effective Hilbert spaces; observer-ledger consistency
& Quantum-observer construction for gravity in a closed universe, integrated with the updated holography, constructive-idealism, and shared-branch chapters \cite{harlow-observers,lande-book-appendices}. \\
Public-ledger capacity crossover; structured real-code correction; operational rank bound; numerical validation
& Harlow--Usatyuk--Zhao equations (4.5), (4.18)--(4.19), and Appendix B \cite{harlow-observers}, specialized in the reproducible companion calculation \texttt{public\_ledger\_capacity/}. The public-ledger interpretation and \(\epsilon\)-seal definition are new bridge content. \\
Public-event extractor gap; finite thermal-repetition extractor; Public Confluence; capacity-guarded shared branch; kinematic underdetermination
& Logical and model-level results in this synthesis: the imported observer code fixes approximate inner products and purities but not a universal POVM, tolerance, coarse-graining, or lock-in rule. In the declared seven-cell thermal-repetition observer, the calibrated dynamics uniquely derives the Hamming-weight sufficient statistic, the optimal no-abstention error floor, exact nondemolition gluing, and the resource counts. Public Confluence constrains compatible seals but still does not select the hidden operational state cone or the universal observer dynamics. \\
Consciousness, judgment decomposition, objectivity, artificial candidacy, moral-status ordering
& Book appendices and moral-status appendix \cite{lande-book-appendices}. \\
Executable trajectory recovery; structural lane; uniform-\(\nu\) verification lane; canonical quotienting
& Automated-theory-synthesis manuscript and autonomous Rust appendix \cite{lande-automated,lande-autonomous-rust}. \\
\bottomrule
\end{longtable}

\section{Executable Theorem-Name Index}

\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.36\textwidth}>{\raggedright\arraybackslash}p{0.56\textwidth}@{}}
\toprule
Name & Role in the theory file \\
\midrule
\endhead
\mname{semantic-\allowbreak{}depth-\allowbreak{}one-\allowbreak{}impossible}
& lower-bound obstruction for depth one; \\
\mname{swap-\allowbreak{}path-\allowbreak{}forces-\allowbreak{}binary-\allowbreak{}trace}
& binary public trace requirement used in the lower-bound proof; \\
\mname{adjunction-\allowbreak{}barrier}
& adjunction-form obstruction to unary historical support; \\
\mname{primitive-\allowbreak{}trace-\allowbreak{}depth-\allowbreak{}at-\allowbreak{}most-\allowbreak{}two}
& upper bound for primitive trace depth; \\
\mname{structural-\allowbreak{}stabilizes-\allowbreak{}at-\allowbreak{}two}
& stabilization of structural obligations at the second historical layer; \\
\mname{cubical-\allowbreak{}foundations-\allowbreak{}primitive-\allowbreak{}coherence-\allowbreak{}depth-\allowbreak{}exactly-\allowbreak{}two}
& cubical semantic wrapper for exact primitive coherence depth two; \\
\mname{cubical-\allowbreak{}coherence-\allowbreak{}depth-\allowbreak{}exactly-\allowbreak{}two}
& compressed exact-depth theorem name; \\
\mname{primitive-\allowbreak{}obligations-\allowbreak{}factor-\allowbreak{}through-\allowbreak{}last-\allowbreak{}two}
& last-two-interface factorization of primitive obligations; \\
\mname{chronological-\allowbreak{}markov-\allowbreak{}blanket}
& two-layer chronological screening theorem; \\
\mname{higher-\allowbreak{}arity-\allowbreak{}fields-\allowbreak{}disappear-\allowbreak{}from-\allowbreak{}minimal-\allowbreak{}signature}
& removal of higher primitive fields from \(\mu\)-minimal public signatures; \\
\mname{higher-\allowbreak{}arity-\allowbreak{}computational-\allowbreak{}replacement}
& replacement theorem for generated higher-arity trace; \\
\mname{full-\allowbreak{}coupling-\allowbreak{}affine-\allowbreak{}recurrence}
& affine recurrence for full-coupling envelopes; \\
\mname{full-\allowbreak{}coupling-\allowbreak{}depth-\allowbreak{}two-\allowbreak{}affine-\allowbreak{}law}
& depth-two specialization of the full-coupling affine law; \\
\mname{constant-\allowbreak{}payload-\allowbreak{}fibonacci-\allowbreak{}scaling}
& Fibonacci scaling under constant payload; \\
\mname{universal-\allowbreak{}affine-\allowbreak{}recurrence-\allowbreak{}from-\allowbreak{}coherence}
& recurrence extraction from the coherence-depth law. \\
\bottomrule
\end{longtable}

\begin{thebibliography}{99}
\raggedright

\bibitem{lande-foundations}
H. Lande.
\newblock \emph{Foundations of Reality}.
\newblock Manuscript draft, local source file \texttt{foundations\_reality\_draft.pdf}.

\bibitem{lande-coherence}
H. Lande.
\newblock \emph{Coherence Windows and Foundational Trajectories}.
\newblock Manuscript draft, local source file \texttt{coherence\_windows\_foundational\_trajectories.pdf}.

\bibitem{lande-coherence-artifact}
H. Lande.
\newblock \emph{Coherence Depth Mechanization: Theorem Index and Trust Boundary}.
\newblock Local artifact documents \texttt{docs/theorem\_index.md} and \texttt{docs/coherence\_depth\_trust\_boundary.md}.

\bibitem{lmcs-coherence}
H. Lande.
\newblock \emph{Coherence Depth and Fibonacci Growth}.
\newblock LMCS/Zenodo manuscript draft, local source file \texttt{1\_coherence\_depth\_LMCS.pdf}.

\bibitem{lande-pen}
H. Lande.
\newblock \emph{Geometric Type-Theoretic Library Growth via the Principle of Efficient Novelty}.
\newblock Local mechanization manuscript, source file \texttt{0\_pen\_paper.tex}.

\bibitem{lande-generative}
H. Lande.
\newblock \emph{Generative Calculus: Geometric Core}.
\newblock LMCS manuscript draft, local source file \texttt{generative\_calculus\_geometric\_core\_lmcs\_draft.pdf}.

\bibitem{lande-synthetic}
H. Lande.
\newblock \emph{Synthetic Framework Abstraction}.
\newblock MSCS manuscript draft, local source file \texttt{synthetic\_framework\_abstraction\_mscs\_draft.pdf}.

\bibitem{lande-architecture}
H. Lande.
\newblock \emph{Architecture of Genesis}.
\newblock Manuscript draft, local source file \texttt{architecture\_of\_genesis.pdf}.

\bibitem{lande-automated}
H. Lande.
\newblock \emph{Automated Theory Synthesis}.
\newblock JAR manuscript draft, local source file \texttt{automated\_theory\_synthesis\_jar\_draft.pdf}.

\bibitem{lande-bigbang}
H. Lande.
\newblock \emph{The Algorithmic Big Bang}.
\newblock Manuscript draft, local source file \texttt{algorithmic\_big\_bang.pdf}.

\bibitem{lande-grqm}
H. Lande.
\newblock \emph{General Relativity and Quantum Mechanics from PEN}.
\newblock Local mechanization manuscript, source file \texttt{gr\_and\_qm.pdf}.

\bibitem{lande-dark}
H. Lande.
\newblock \emph{Dark Sector Consequences of the Two-Law Framework}.
\newblock Local manuscript source file \texttt{dark\_sector.tex}.

\bibitem{lande-book-appendices}
H. Lande.
\newblock \emph{The Answer Is Two}: book appendices and technical postscript files.
\newblock Local manuscript sources, \texttt{appendices/} and \texttt{chapters/}.

\bibitem{lande-born-rule}
H. Lande.
\newblock \emph{Born Rule from Proof-Space Cardinality}.
\newblock Book Appendix H, local source file \texttt{appendices/app\_h\_born\_rule.tex}.

\bibitem{lande-audit}
H. Lande.
\newblock \emph{The Audit}.
\newblock Book Chapter 38, local source file \texttt{chapters/ch38\_audit.tex}.

\bibitem{lande-autonomous-rust}
H. Lande.
\newblock \emph{Autonomous Rust Status}.
\newblock Book Appendix E, local source file \texttt{appendices/app\_e\_autonomous\_rust\_status.tex}.

\bibitem{lande-minimal-extension}
H. Lande.
\newblock \emph{The Minimal-Extension Theorem}.
\newblock Book Appendix J, local source file \texttt{appendices/app\_j\_minimal\_extension\_theorem.tex}.

\bibitem{lande-koide}
H. Lande.
\newblock \emph{Koide Sector Equipartition: Pre-Registration and Bridge Attempt}.
\newblock Book Appendix M, local source file \texttt{appendices/app\_m\_koide\_equipartition.tex}. Its former reduction is superseded by the underdetermination audit in this synthesis.

\bibitem{lande-guard-rail}
H. Lande.
\newblock \emph{The Debt Guard-Rail Theorem: Directive versus Quantitative Debt}.
\newblock Companion note, local source file \texttt{debt\_guard\_theorem.tex}; probe record \texttt{docs/t1\_result.md}.

\bibitem{lande-dct-note}
H. Lande.
\newblock \emph{DCT Model Existence: No-Go Theorem and Model Fragments}.
\newblock Companion note, local source file \texttt{dct\_model\_note.tex}.

\bibitem{lande-evaluator}
H. Lande.
\newblock \emph{Evaluator Derivation: The Schema Calculus}.
\newblock Mechanization-repository documents \texttt{docs/EVALUATOR\_DERIVATION.md}, \texttt{docs/LEMMA\_L1\_D\_SQUARED.md}, \texttt{docs/LEMMA\_L2\_R\_SQUARED.md}, \texttt{docs/HSPACE\_ENUMERATION.md}.

\bibitem{lande-fate}
H. Lande.
\newblock \emph{The Fate of the Universe}.
\newblock Book chapter, local source file \texttt{chapters/ch\_fate.tex}.

\bibitem{lande-constants}
H. Lande.
\newblock \emph{The Constants Program}.
\newblock Mechanization-repository document \texttt{docs/CONSTANTS\_PROGRAM.md}.

\bibitem{hottbook}
The Univalent Foundations Program.
\newblock \emph{Homotopy Type Theory: Univalent Foundations of Mathematics}.
\newblock Institute for Advanced Study, 2013.

\bibitem{cohen-cubical}
Cyril Cohen, Thierry Coquand, Simon Huber, and Anders Mortberg.
\newblock Cubical type theory: a constructive interpretation of the univalence axiom.
\newblock \emph{21st International Conference on Types for Proofs and Programs}, 2015.

\bibitem{girard}
Jean-Yves Girard.
\newblock Linear logic.
\newblock \emph{Theoretical Computer Science}, 50:1--101, 1987.

\bibitem{wootters-zurek}
William K. Wootters and Wojciech H. Zurek.
\newblock A single quantum cannot be cloned.
\newblock \emph{Nature}, 299:802--803, 1982.

\bibitem{dieks}
Dennis Dieks.
\newblock Communication by EPR devices.
\newblock \emph{Physics Letters A}, 92:271--272, 1982.

\bibitem{lovelock}
David Lovelock.
\newblock The Einstein tensor and its generalizations.
\newblock \emph{Journal of Mathematical Physics}, 12:498--501, 1971.

\bibitem{tong-lines}
David Tong.
\newblock Line operators in the Standard Model.
\newblock \emph{Journal of High Energy Physics}, 07:104, 2017; arXiv:1705.01853.

\bibitem{xing-koide}
Zhi-zhong Xing and He Zhang.
\newblock On the Koide-like relations for the running masses of charged leptons, neutrinos and quarks.
\newblock \emph{Physics Letters B}, 635:107--111, 2006; arXiv:hep-ph/0602134.

\bibitem{sumino-koide}
Yukinari Sumino.
\newblock Family gauge symmetry and Koide's mass formula.
\newblock \emph{Physics Letters B}, 671:477--480, 2009; arXiv:0812.2090.

\bibitem{weldon-shell}
H. Arthur Weldon.
\newblock Mass-shell behavior of electron propagator at low temperature.
\newblock \emph{Physical Review D}, 59:065002, 1999; arXiv:hep-ph/9810324.

\bibitem{kniehl-sirlin}
Bernd A. Kniehl and Alberto Sirlin.
\newblock Pole mass, width, and propagators of unstable fermions.
\newblock \emph{Physical Review D}, 77:116012, 2008; arXiv:0801.0669.

\bibitem{harlow-observers}
Daniel Harlow, Mykhaylo Usatyuk, and Ying Zhao.
\newblock Quantum mechanics and observers for gravity in a closed universe.
\newblock arXiv:2501.02359v2 [hep-th], 2025. Local source file \texttt{quantum\_observers}.

\bibitem{shannon1948}
Claude E. Shannon.
\newblock A mathematical theory of communication.
\newblock \emph{Bell System Technical Journal}, 27:379--423 and 623--656, 1948.

\bibitem{li-vitanyi}
Ming Li and Paul Vitanyi.
\newblock \emph{An Introduction to Kolmogorov Complexity and Its Applications}.
\newblock Springer, 3rd edition, 2008.

\end{thebibliography}

\end{document}
