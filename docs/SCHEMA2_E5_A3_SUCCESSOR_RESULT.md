# E-5 A3 successor result

**Date:** 2026-07-21. **Status:** **STOPPED FAIL-CLOSED**. The create-new
artifact is `docs/schema2_e5_a3_successor_v1.json`, with result digest
`blake3:900c79f51137a902e01f12f6270aebb3565bf3b0a86baad992345262c06e1307`.
Definition replay is valid.

## Result

The proposed candidate generator is finite, count-blind, nonvacuous, and
replays over all sixteen historical selection stages. Its structural
completion predicates are now evaluated directly from a raw two-window
snapshot, without importing the coarse focus labels. Those predicates
reproduce the complete coarse ladder and the Stage-3
demand-before-jurisdiction wrinkle.

That is not yet the full A3 theorem. Two load-bearing checks fail before F1
can lawfully execute.

1. **Natural-family quotient regression.** At Stage 16 the generator emits
   89 metadata instances and 89 singleton candidate orbits: 17 unary, 64
   direct chronological, and 8 pointwise chronological. The exact E-5
   predecessor already proves that the same 64 direct J3 specializations are
   instances of **eight** natural-family orbits. The new quotient therefore
   multiplies uniform specializations and contradicts its monotone-extension
   predecessor. This is the named gap
   `A3_DIRECT_J3_NATURAL_FAMILY_QUOTIENT_REGRESSION_64_SINGLETONS_VS_8_ORBITS`.
2. **No typed demand-output judgment.** `A3DemandOutputType` describes a
   requested output as metadata, but the generator does not construct a term
   or future-hole judgment denoting that output, type-check it, normalize it,
   or supply an eliminator into `D(B15)`. A typed source clause and a hash of
   its formation do not type the demanded action. In particular, source
   reflexivity is not a proof of a unary action. The generic
   motive-parametric theorem transports an already certified derivation; it
   cannot manufacture the missing source derivation.

The Stage-3 row is consequently a correct **label regression**, not yet an
instance-orbit transport theorem. Likewise, re-elaborating an accepted clause
under a larger library would show prefix stability of that clause, but would
not prove equality with a concrete demand instance that has not been
constructed.

## Exact machine disposition

- Restricted J3 remains positive: 64 typed and `D(B15)`-derivable direct
  instances, quotienting to 8 natural families.
- Candidate Stage-16 stock: 17 unary + 64 direct + 8 pointwise = 89 metadata
  occurrences; higher seed rejected for no typed path witness; no completion
  occurrence.
- Coarse ladder labels: reproduced.
- Stage-3 wrinkle: reproduced as a regression.
- Full A3 identification: **not proved**.
- Guard-Rail F1: **not executable; neither triggered nor excluded**.
- Semantic `O(16) = ∅`: **unset**, not false and not proved.
- Theorem 12 at instance granularity: neither proved nor refuted.
- T-BF2: not authorized and not executed.
- Bridge: not authorized and not executed.
- No bar-free adoption and no halt claim were issued.

The complete named obstruction set is serialized in the certificate:

- `A3_DEMAND_OUTPUT_TERM_AND_TYPING_JUDGMENT_UNDEFINED`
- `A3_RULE_CONSTRUCTOR_INVENTORY_EXHAUSTIVENESS_UNPROVED`
- `A3_DIRECT_J3_NATURAL_FAMILY_QUOTIENT_REGRESSION_64_SINGLETONS_VS_8_ORBITS`
- `A3_OUTPUT_LEVEL_D_B15_MEMBERSHIP_NOT_EXECUTABLE`
- `A3_STAGE3_TO_STAGE4_INSTANCE_ORBIT_TRANSPORT_UNPROVED`

## Parallel bar-free theorems

T-BF1 and T-BF3 both executed independently and stopped on the registered
F-BF1 falsifier. Stage 4 has four canonically distinct strict total
dischargers, all tied at the minimum parsimony key `(κ, certified ν)=(3,5)`.
No winner was selected. Their create-new artifacts are
`docs/t_bf1_prefix_v1.json` and
`docs/t_bf3_enactment_equivalence_v1.json`. The bar-free adoption block is
therefore locked independently of the E-5 obstruction. T-BF2 remains queued
behind semantic `O(16)=∅`; even a future positive T-BF2 cannot repair the
failed T-BF1/T-BF3 selector.

## Required successor

The smallest honest successor must:

1. define every A3 rule as an intrinsically typed demand expression or typed
   hole with a semantic interpretation;
2. prove the constructor inventory exhaustive rather than merely closed;
3. make the 64 direct cases instances of the eight established J3 families,
   with pointwise, unary, higher, and completion cases quotiented under the
   same natural-family law;
4. issue an actual per-output `D(B15)` eliminator certificate; and
5. construct and equate the Stage-3 and Stage-4 semantic demand orbits before
   claiming transport, locality, or expiration.

Only then may this create-new successor be rerun, F1 executed, and T-BF2 or
the bridge considered.

## Verification

- `cargo test -p pen-eval a3_demand_grammar::tests -- --nocapture` — 7/7.
- `cargo test -p pen-search e5_a3_successor::tests -- --nocapture` — 3/3.
- `cargo check -p pen-search` — passed (pre-existing warnings remain).
- Public create-new and independent public replay — valid, with the same
  89-instance/89-orbit negative result.

