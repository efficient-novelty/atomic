# XF Campaign Ledger

**Campaign:** XF-1, the External Falsifier Campaign
(`docs/xf1_external_falsifier_campaign.md`). **Opened:** 2026-07-24.
**Foundation:** MS-1 sealed milestone certificate
`docs/milestone_certificate_v1.json`, result digest
`blake3:37d96619734afb37125f3cc28fc3cac87b8b0b81c8c0a9945156bdba6c43bb0b`
(replay-verified at campaign opening: laws=27, frontier=150, drift=12,
M3 v1 authoritative, M4 unauthorized). Every input this campaign consumes is
bound there or does not exist for its purposes.

**Constitution:** F-XF1 (register eligibility), F-XF2 (prediction-blindness),
F-XF3 (burn semantics), F-XF4 (suppressing a miss voids the campaign),
F-XF5 (LMC frozen before any candidate evaluation) — verbatim in the brief.
**Hazard fence:** no pattern-spotting of φ, Fibonacci numbers, or ledger
integers in physical data without a derived bridge; the fine-structure
constant is declared out of reach; no curve toward it may be fit.

**Statuses:** registered / derived / corroborated / burned — no fifth state.
This ledger is append-only: the event log below is never edited, only
appended; the status index is a derived view regenerated from the log.

## Status index (derived view of the log)

| Id | Prediction | Status | Veto condition |
|---|---|---|---|
| XF-A | Sorkin hierarchy vanishes exactly at third order and above: κ₃ = 0, at every precision, forever | registered | any confirmed nonzero κ₃ at any order of precision |
| XF-B1 | Neutrino electric charge q_ν = 0 exactly | derived | any confirmed nonzero neutrino electric charge |
| XF-B2 | Exactly one fundamental Higgs multiplet | registered (conditional; bridge premise open) | a discovered second fundamental doublet, once the bridge premise is closed verdict-blind |
| XF-B3 | No fourth gauge factor outside the sealed alphabet's faithful image (exact ℤ₆ kernel) | registered | an LMC-adjudicated confirmed fundamental fourth gauge factor |
| XF-C | Koide Q = 2/3, conditional on four verdict-blind premise closures | registered | any of the four premises failing honestly burns the item; a derived Q ≠ measured Q at stated precision burns the prediction |
| XF-D | Any cumulative ledger quantity acquiring a derived physical reading is a global constant quantized to exactly two lawful values with a computed gap | registered (exploratory; no bridge exists) | a derived-bridge cumulative quantity measured outside the two lawful values |

Campaign artifacts (not predictions): XF-0 census (calibration experiment —
EXECUTED, mismatch pattern published, see event 004); XF-B0 Library
Membership Criterion (build — FROZEN, see event 003); XF-0b family
concordance (EXECUTED — Outcome U by one family, see event 006); XF-0b2
targeted re-examination of df4feb52882e (EXECUTED — NO-NAME CONFIRMED,
export WITHDRAWN, see event 007); XF-1 Hopf-bridge audit v1 of SSRN 6975959
(EXECUTED — Koide test vacuous, EFT matching has no solution, S⁹ census
succeeds verdict-blind under three declared premises and reaches the ℤ₆ group
by a second route, but the paper's own nested shell tower does not realise
SU(3)×SU(2) at all, see event 008); free environment completion (EXECUTED —
realization half derived, selection half independent by countermodel;
**ledger invariance** enters as a counted bridge premise, see event 009).

**Standing disclosure (constrains how the whole record is read):** the certified
fifteen-stage register is a hand-authored literal table whose recognizing search
gates postdate it. Four framings are SUSPENDED pending the PA-1 provenance
audit — autonomous recovery, the uniqueness census *as law*, non-circularity of
the recreation claim, and the halt *as a law-property* (event 010). Items
resting only on the Selective Law's realization face are unaffected.

**Counted bridge premises** (not predictions; each must be discharged or
declared before anything downstream is claimed): ledger invariance (event 009 —
a derivation route now exists via the self-paired layer and target 1's dagger
fragment, `docs/target1_ledger_invariance_joint_v1.md`; premise still counted,
status unchanged); O4 fixing the hypercharge scale and the ambient simple
`su(5)` trace form (event 008 — the third premise of that trio, the tier-C
free-sealing clause, was discharged in event 011); the four XF-C Koide premises
C1–C4; and the sealing-order premise behind `q_ν = 0`.

## Event log (append-only)

### 001 — 2026-07-24 — Campaign opened; XF-0 correspondence rule frozen

- MS-1 foundation replay-verified (valid; counts above).
- XF-0 correspondence rule v1 frozen at
  `docs/xf0_correspondence_rule_v1.md` /
  `blake3:fd20618c86d5e5c532bc84f5fcbf0218e970473fbb322cc543964179e595814a`,
  BEFORE any physics-column row was filled. Frozen content: the counting
  discipline (classes D/P/C/T/N with the existence boundary clause), the
  fifteen verbatim row inputs, the three brief-frozen calibration anchors
  (stages 8→1, 9→0, 12→0), the blinding protocol (isolated per-row analyst
  and verifier contexts receiving only the rule and their row input; targets
  quarantined to the mechanical join), the frozen tally partition (11 blind
  single-target rows; stage 4 blind dual-target, branch-indexed {enacted,
  cone minimum}; 3 calibration rows), and the void conditions.
- Predictions XF-A, XF-B1, XF-B2, XF-B3, XF-C, XF-D registered as in the
  status index; per-item registered-prediction artifacts follow as events
  002+ with digests. XF-B1 enters as derived per the campaign brief
  (sequential sealing; certified representation fact in the audited record).

### 002 — 2026-07-24 — Registered-prediction artifacts sealed

Recorded order: every artifact below was written and digest-sealed before
any comparison against data; no measured target was consulted in any
derivation or derivation-obligation statement.

- XF-A Born exactness (registered):
  `docs/xfa_born_exactness_registered_v1.md` /
  `blake3:84fdbcd09f53a0d048c3a8d52783a9e37690d7c039ce05a92e4d07ee862c40e6`.
  Includes the binding register caution: the derivation obligation may rest
  only on width-two structure and d_obl = 2, never on the engine-register
  d²/r² counting laws.
- XF-B1/B2/B3 completeness vetoes (B1 derived; B2 registered-conditional;
  B3 registered): `docs/xfb_completeness_vetoes_registered_v1.md` /
  `blake3:73cb61e2ba35172dafd44e90fc8d3caa4f0dad271480fbc9f451f0ee7103855f`.
- XF-C Koide closure, four verdict-blind premise work items (registered):
  `docs/xfc_koide_closure_registered_v1.md` /
  `blake3:7140eeeedad104c956ca685668716c67c3defaf06c1a2d6ec0d72fd33e784991`.
- XF-D two-valued constant (registered, exploratory, passive):
  `docs/xfd_two_valued_constant_registered_v1.md` /
  `blake3:254254881121b677976433b6beb8f695c146d57409258f65279fd7b8bbaf50c1`.

### 003 — 2026-07-24 — XF-B0 Library Membership Criterion frozen

- Pre-freeze adversarial stress panel (three lenses: register-leak,
  replayability, prejudgment; refutation-first confirmation; workflow run
  `wf_280902ea-c89`, 19 agents): six substantive findings CONFIRMED, all
  repaired before the freeze; repairs recorded verbatim in the criterion's
  Section 8. Headline repairs: LMC-3 veto made presumptive with the burden
  of production on the theory and a registered three-attempt bound;
  "carried by" defined; coverage clause for never-evaluated or
  NotFormalizable confirmed structures; machinery pinned by content
  digests, not version tags; the four certified falsifier shapes exact,
  no generalization; two-part dossier separating classification input
  from the sealed empirical annex.
- LMC v1 FROZEN, no candidate evaluated (F-XF5):
  `docs/xfb0_library_membership_criterion_v1.md` /
  `blake3:dcee3babd78c69711dd9d5fdb0f44df0aa3cd0363591a41d2c0c858ac0d2c29f`.
- Standing consequence: every confirmed fundamental discovery must end as
  corroboration, recorded veto, or tracked criterion-adequacy failure —
  never silence.

### 004 — 2026-07-24 — XF-0 census executed; mismatch pattern published

- Blind fill: workflow run `wf_586ade88-ef0`, 48 isolated agent contexts
  (15 analysts, 30 adversarial verifiers, adjudicators on rows 1, 9, 15),
  none receiving the certified register; 95 classified data with citation
  per datum. Column: `docs/xf0_physics_column_v1.json` /
  `blake3:c0c342977cebc6140eff94a3b6f2a67fdc5b5fb063b6ad8d29e0045e5aba57c1`
  (binds the frozen rule digest — F-XF2 custody chain).
- Mechanical join (replayable, `xf0_census_v1` example; MS-1 replay + result
  digest verified in-run): `docs/xf0_census_v1.json` /
  `blake3:85491fecb69ef0046a124d7825fc709ea11e9b205aef28c9f5af9dfcd566f1e0`.
- **Verbatim outcome: blind single-target 3/11 Match (stages 3, 7, 11);
  stage-4 dual-target D=2 matches the cone-minimum value class, not the
  enacted branch; calibration 3/3.** Not a systematic match. Localization
  published row-by-row in `docs/XF0_CENSUS_RESULT.md` /
  `blake3:578b8a54ca8cd705c1c197da4b4f90993edf3ac34c0978d0ee6cbd0f58ae8499`:
  exact agreement on every sparse-tail row including both certified zeros
  (9, 12, blind-derived); strict undercounts on every rich stratum;
  +1 overcounts on the two bootstrap rows; stage-4 inside the certified
  branch-value set.
- Consequence recorded: no later campaign item may lean on a claimed
  register ↔ degrees-of-freedom correspondence; rule v1 is spent; any
  successor census requires a preregistered v2 rule with disclosed,
  weaker blindness. No row suppressed (F-XF4).

### 005 — 2026-07-24 — XF-0b Family Concordance Study frozen and opened

- Brief frozen: `docs/xf0b_family_concordance_plan.md` /
  `blake3:b340555c7f80ac93b76b0fc5a6a590f46ff9b081e471653e9132a31b72e798da`.
  Campaign artifact, not a prediction. Scope extended to all 32 credited
  families (27 rich-stratum + 5 sparse singletons) so the C-4 inverse join
  is total.
- Contamination declaration (F-C4), recorded now: XF-0b reads the
  certified register directly and is not blind; its outputs are a
  disclosed input to any future blind exercise, including any XF-0 v2
  census.
- C-1 extraction source: `t_bi_nu1_semantic_provenance_v6.json`
  (`semantic_ledger_v6` in the MS-1 chain, embedded
  `blake3:d51d0543c32542200fff6e5cfd234da1bb2ffc12c058c805ac7974385727c8b4`),
  packages → credited family rows + resolving role occurrences; owner
  clauses rendered from the frozen telescope
  (`Telescope::reference`, `crates/pen-core/src/telescope.rs`). Credited
  counts per stage reproduce the certified vector exactly.

### 006 — 2026-07-24 — XF-0b executed: Outcome U by one family; sealed

- C-2/C-3: 32 naming analysts + 32 adversarial verifiers + adjudicators
  (workflow `wf_861d6c66-06c`; first run lost one adjudicator to a session
  limit; the complete resumed run — 0 errors, every dispute adjudicated —
  is authoritative). Adjudication-stability disclosure recorded verbatim
  in the sealed artifact: two verdicts flipped between runs at the
  NO-NAME boundary (s01 25a30e71 and s15 eedcb269, both NO-NAME → named).
- **Verbatim outcome: 31/32 families named under clause-preserving
  translations with citations; Outcome U by exactly one family —
  stage-14 df4feb52882e (`Pi(Lam(Var 1), Sigma(Var 1, Var 2))`),
  NO-NAME after four recorded attempts, undisputed — registered as a
  candidate novel mathematical object (potential export), with a
  recommended targeted versioned re-examination given the one-family-thin
  margin.** One Outcome-X row: the blind stage-15 guarded-fixpoint datum
  maps to no certified family (no sealed fix/Löb clause); referred back
  as a named question. The two XF-0 bootstrap overcounts are fully
  explained: both blind data land on uncredited internal families.
- C-4 inverse join total over all 19 blind class-N data: 11 exact, 2
  uncredited-internal, 5 partial, 1 orphan. Forward join explains every
  rich-stratum undercount: the register mints per named aspect (cell
  data, adjunction units/counits, mode-theory 2-cells, modal axioms)
  that the blind unit's P/C/D classes absorbed.
- Sealed deliverables (mutation-guarded; replay =
  `cargo run -p pen-search --example xf0b_concordance_v1 -- replay docs`):
  `docs/xf0b_concordance_v1.json` result digest
  `blake3:907b7a474dcc530fe5b332d3caec51758dc104ab3226e915a5c357f7388785d2`
  (file `blake3:b42a173248076f15f2ea740d2cf22d662cd9eac1f67726977c8f92e94cdb6a8b`);
  `docs/XF0B_CONCORDANCE_RESULT.md` /
  `blake3:7cbed696dc9a7e395b0cc96a9c9b3951e390c8075eedde4987422992591d6741`.
- Registered recommendation: a principled XF-0 v2 census unit (named
  classical structures at family granularity) exists conditional on
  resolving df4feb52882e; any v2 must disclose this study's contamination
  (F-C4) and v1's spent rule.

### 007 — 2026-07-24 — XF-0b2 executed: NO-NAME CONFIRMED; export WITHDRAWN

- Brief frozen and executed: `docs/xf0b2_df4feb52_reexamination_plan.md` /
  `blake3:576ca5f6c881b051e2d2682330d0ed6ed7d2e3471377bda4dc1aa592ea46a81f`.
  Single-family successor to XF-0b; gates nothing.
- **Phase R-0 (F-R1 discharged).** The elaboration was re-derived
  mechanically from the frozen kernel and the concordance gloss was
  corrected on four counts, recorded in the versioned note
  `docs/xf0b2_r0_dossier_v1.md` /
  `blake3:38d145f745c5c3c292605562169ac6bdfb154b61cba1756da05b39d565596909`
  (+ `docs/xf0b2_r0_addendum_v1.md`, post-freeze). The family is **not** a
  dependent product (both binders non-dependent); its domain is the
  **constant family** λ_.X; `Var 2` resolves to the **field of clause 0**,
  not an unexplained sort; and — unrecorded by the concordance — the
  formation is accepted only under **two coarse subsumptions**, the highest
  at stage 14, with Π-binder classifier `El(Lam(Var 1))`: the kernel does
  not certify it as well-sorted. Irreducibility certificate: **IRREDUCIBLE**
  (0 App nodes, no η rule, 0 computation-role clauses corpus-wide; bounded
  catalog of 99 064 expressions confirms no type-former presentation
  exists). Role dossier: exactly one occurrence in fifteen stages, generator
  only, no specializations; one third of stage 14's ν = 3 payment.
- **Verbatim outcome: NO-NAME CONFIRMED.** Eight independent contexts —
  five fresh armed lead analysts (one per registered lead L1–L5), two
  independent armed analysts, one adversarial verifier — concur, **zero
  flips**, all five leads disposed, ~35 classical targets attempted. The
  F-R3 stability quorum is satisfied. Near misses killed on named laws:
  coalgebra for (−)×Y (structure map's source is `El(Lam(Var 1))`, not
  `El(Var 1)`); illative Ξ(K X)(K(X×Y)) (type-free target cannot receive the
  source's sort content); simple product (requires the certified-impossible
  identification); Π_g in an LCCC (argument must live in the slice).
- **Export WITHDRAWN — a separate finding on separate evidence.** During
  R-1, analyst B observed and the coordinator and verifier confirmed that
  the subject clause's exact shape is a **hard-coded literal in the search's
  admissibility layer**, and the per-position gate admits exactly one
  expression at clause position 3. The causal direction is **settled**:
  `git log -L 335,363:crates/pen-core/src/telescope.rs` returns exactly one
  commit — `5dd8447`, 2026-03-13, "First rust implementation step" — and the
  stage-14 telescope has never been modified since, while the search gates
  postdate it (2026-03-14, 2026-03-15). The certified register is built by
  **reading** `Telescope::reference`, and step 14 "Hilbert, κ=9" was
  pre-declared in the repository's **Initial commit**. **No search in this
  repository ever found this object.** Record:
  `docs/xf0b2_provenance_finding_v2.md` /
  `blake3:1effd3e1d7f0de3b108fd6eecad9e2f39c911e6d2ec5fba65955bb9ba5e92b62`
  (v1 superseded, retained unedited because the verifier audited it).
  Per F-R5 a withdrawn export is a success of the discipline;
  `docs/df4feb52_specification_v1.md` is deliberately **not** written.
- Verifier corrections adopted, recorded verbatim in the artifact: the
  "η hinge" framing was a **coordinator error** (η's redex is `λx.(f x)`;
  this body is `Var 1`, so adding η would collapse nothing — there is no
  hinge, and a ruling on η-freeness would change nothing); analyst B's
  flip-condition **was met** by Automath's AUT-QE type inclusion at n=1,k=0
  and **did not flip** (a rule is not a structure; the licensed object is
  `(A→X)→(X×Y)` with A anonymous); and R-0 dossier §5 constraint 1
  (equation-freeness as a standing bar) **proves too much** and is demoted
  to a per-target observation. Sixteen of seventeen audited obstructions
  hold; no verdict rests on the one that fails.
- Sealed deliverables (mutation-guarded; replay =
  `cargo run -p pen-search --example xf0b2_df4feb52_v1 -- replay docs`,
  which recomputes the digest, **re-derives every R-0 claim from the frozen
  kernel**, enforces the R-1 recording discipline over all five leads,
  enforces the R-2 quorum, and binds the parent concordance by digest):
  `docs/xf0b2_df4feb52_v1.json` result digest
  `blake3:1abba00b35b7f8a94adef96f2f4fe986b812a4fba0067ede5ff7e01419550805`
  (file `blake3:a48543f64c2634bea68aaa915b794d946c130417f80f2e0bf25d4189cc282e60`);
  `docs/XF0B2_DF4FEB52_RESULT.md` /
  `blake3:8918cdef1b6ebca5d7bb16adbb4b235044a977f2e88fea83dd909cb5f8ecb43d`.
- **Registered consequences.** (i) XF-0b's Outcome U stands with its meaning
  changed: 31 of 32 named, and the 32nd is **not** a candidate novel object
  but "no classical name, best explained as an authoring artifact in a
  hand-transcribed reference table". (ii) The v2 census unit is unblocked
  but re-pointed: any v2 must additionally disclose that the register's
  content is read from a hand-authored table. (iii) **A campaign-level
  methods question is opened, wider than this study**: the whole certified
  fifteen-stage register is read from a hand-authored literal table, and
  every structural-family gate from stage 3 onward is a per-position
  literal — this bears on XF-0b's headline and on the register's standing as
  a search result; referred to the internal program. (iv) **XF-0 is
  untouched** (it was blind; its 3/11 mismatch stands). (v) One item left
  open and disclosed: `Telescope::is_connected` reads `Var` as a relative
  index while the kernel reads absolute levels — immaterial to the credited
  family and to every verdict, but it must be settled before any document
  describes the family "in situ". No row suppressed (F-XF4, F-C2).

### 008 — 2026-07-25 — XF-1 Hopf-bridge audit v1 executed: two decisive negatives, one derived positive

External bridge candidate audited: *The Complex Hopf Fibration as the Canonical
Space for Gauge–Gravity Unification*, SSRN 6975959, 18 June 2026
(`docs/ssrn-6975959.pdf` /
`blake3:7ca0eb1663643c811b522b4989e23f48956247eb1432f7037d41321715be86ad`).
Three commissioned tests. Result:
`docs/XF1_HOPF_BRIDGE_AUDIT_V1_RESULT.md` /
`blake3:0057ad83518b0bae8f584e03abe7c9f0166c6ca872fbacf518dd4b14f99d328e`.
Reproduction scripts under `scripts/xf1_hopf_bridge_audit/` (mpmath, 40–60
digits; every quoted number computed, none transcribed):
`a1_koide.py` /
`blake3:ef8edf1fde76a2f932904c65fa6b0572d31637443c6af97e8fa545ffcbf6c12b`,
`a2_census.py` /
`blake3:c9472c5fdbff20c3df4cbec0da38f4d0231543beafcc711c9d91465e57677e9d`,
`a3_eft.py` /
`blake3:4465385a875cf0f560bdbd0fcee5675e68f63a4c81a713e78945bce56ec6e7df`,
`a4_neutrino.py` /
`blake3:35845914363bbd80b84639f47a9b39520f8c1b106dbdcae7de83427a3bd58f75`,
`a5_addendum.py` /
`blake3:aea2f36d8df5ad37219fe4a44959336602ea0b88f50e3da163188ecb3c5d27d4`.

- **T1 — exact Koide cross-check: the test is vacuous as posed.** The paper's
  `D(1), D(2), D(3)` (Eq. 66) are not derivable from its own Eqs. (63)–(65) —
  those give `−ζ′_n(0)` negative and decreasing where `D(n)` is positive and
  increasing — and inverting Eq. (62) on the measured lepton masses returns the
  printed values with a **single common offset** `3.4114×10⁻⁵` across all three
  n (differences agree to `7.5×10⁻¹⁰`). A common offset in `D` is exactly
  degenerate with `Λ_Hopf`, so the printed coefficients are the measured masses
  re-expressed. Parameter count: 3 free quantities, 3 masses, zero residual.
  Hence `Q_Hopf = 0.6666605113664864` and `Q_exp = 0.6666605114655220`, differing
  by `−9.90×10⁻¹¹`: the advertised `−9.23` ppm gap from `2/3` **is** the
  experimental Koide deviation, not a second mechanism nearly meeting the first.
  The `a_Koide − a_Hopf = 6.615` ppm is a reparameterization (the shift
  `a→a+δ`, `D(n)→D(n)+nδ` is an exact invariance), and Remark 23's absorption of
  a linear-in-n term into an n-independent scale is an error.
  **Consequence: XF-C is untouched — no evidence flows in either direction, and
  this paper must not be cited as external Koide support.**
- **T2 — S⁹ minimality census: SUCCEEDS, verdict-blind, and yields more than
  S⁹.** Obligation profile O1–O8 frozen before the paper's S⁹ sections were
  read; no measured constant enters. The load-bearing correction is that the
  candidate's group is the fibration-preserving `U(n+1)`, not the isometry group
  `O(2n+2)`. Scored at three tiers: rank alone gives `{3,4,…}`; faithful
  **3**⊕**2** with exactly one commutant U(1) gives `{4,6,7,8,…}` (argmin 4);
  adding free sealing / no unforced generators gives **`{4}` uniquely**, since
  `C⁵ = 3 ⊕ 2` is the only decomposition carrying each defining representation
  exactly once. Forced by-products: structure group `U(5)` and minimal
  SM-carrying subgroup `S(U(3)×U(2))`. **Scoped down after adversarial review
  (two findings refuted on status, none on arithmetic):** (a) that
  `S(U(3)×U(2)) ≅ (SU(3)×SU(2)×U(1))/ℤ₆` is a **standard fact** (Baez–Huerta
  2010 = the paper's own ref [16]), independently reconfirmed here by a centre
  argument — `Z(S(U(3)×U(2)))` is a connected circle while
  `Z(SU(3)×SU(2)×U(1))` has 6 components, both groups being dimension 12 — so
  the census *reaches* the Two-Law group by a second independent route but does
  **not** newly derive the ℤ₆; (b) the `5/3` and `sin²θ_W = 3/8` are **not**
  pure subgroup indices: parametrising the one-dimensional commutant direction
  as `Y(y) = diag(2y,2y,2y,−3y,−3y)` gives `κ(y) = 60y²` and
  `sin²θ_W(y) = 1/(1+60y²)`, equal to `5/3` and `3/8` **only** at `y = ±1/6`,
  which is fixed by obligation **O4** (`q_ν = 1/2 − 3y = 0`) — a matter-content
  premise — and `κ = 5/3` is rep-independent (verified on **5**, **10**, **15**,
  **24**) only through the ambient **simple** `su(5)` trace form. `SU(5)` is
  still never *gauged* (no X/Y bosons, no GUT proton decay); its trace form is
  supplied by the shell's structure group `U(5)`. **This is a Two-Law
  derivation, not the paper's**: the paper argues S⁹ from `Spin(10)` spinor
  framing, but the winding decomposition is `U(1)`-equivariant hence
  `U(5) = O(10) ∩ GL(5,ℂ)`-covariant (dim 25) and not `SO(10)`-covariant
  (dim 45), and it derives α *from* S⁹, so its own route is circular.
  **Three registered premises**, all to be frozen before anything is claimed:
  the tier-C free-sealing clause (without it the census returns an argmin over
  `{4,6,7,8,…}`, not a uniqueness theorem); O4 fixing the hypercharge scale;
  and the ambient-`su(5)` trace form. **(c) The sharpest structural finding of
  the audit, and it cuts against the paper harder than the circularity does:
  the paper's own shell tower does not realise `SU(3) × SU(2)` at all.** The
  census uses the BLOCK embedding `C⁵ = 3 ⊕ 2` with the two factors commuting;
  the paper builds its shells by the standard NESTED inclusion
  `(z₁..z_n) ↦ (z₁..z_n, 0)`, so `S³ ⊂ S⁵` makes `SU(2)` the STABILISER inside
  `SU(3)` — Thm 6 verbatim, *"the unique compact Lie group G containing SU(2)
  such that G/SU(2) ≅ S⁵ is G = SU(3)"*. Verified with explicit 5×5
  anti-hermitian generators: block embedding — factors commute, commutant
  `dim 2` in `u(5)` and `dim 1` in `su(5)` (the single hypercharge); nested
  embedding — `su(2) ⊂ su(3)`, factors do **not** commute, commutant `dim 5` in
  `u(5)` and non-abelian. So the census does **not** retroactively license the
  paper's construction: same sphere, incompatible group actions. The paper's
  tower realises a nested chain `SU(2) ⊂ SU(3) ⊂ … ⊂ SU(5)`, which is not the
  Standard Model gauge group, and this defect is upstream of every numerical
  result in the paper.
- **T3 — four-dimensional EFT matching: no solution exists.** Treating the
  paper's `g = 0.6197717` and `g′ = 0.3470714` as MS-bar couplings at a common
  scale requires `4.542×10⁴` GeV and `3.819×10⁻¹` GeV respectively — **5.075
  decades apart**, because `b₂ < 0 < b_Y` forces them to run in opposite
  directions. Read correctly, `(α⁻¹ = 137.0361, sin²θ_W = 3/4π = 0.238732)` is
  the measured `Q²→0` pair (`+0.61` ppm and `+0.043 %`), carrying no information
  beyond it; the paper's own `Δ_spec(M_Z²) ≈ −0.0075` is the known SM running of
  the weak mixing angle re-imported under another name. Redone with correct
  placement, `g₁ = g₂` sits at `1.03089×10¹³` GeV (one loop) and
  **`1.08939×10¹³` GeV (two loop)** — confirming the July 2026 placement audit's
  `1.1×10¹³` GeV to under 1 %. The commissioning factor 3.7 resolves exactly as
  `3.9599 = 2.70014 (v/M_Z) × 1.46655 (undressed-vs-measured)`: **a placement
  artifact, not a diagnostic.** `sin²θ_W = 3/8` is identically `0.375` at every
  crossing in every placement and carries no information about `μ_X`.
  Eq. (78) gives `α_s^geom = 3.10×10⁻³` against Remark 24's claimed `≈1` — a
  factor `322.9`, with the wrong sign of running. Thm 49 (UV finiteness, "every
  shell action is Gaussian") and Thm 19 (explicit cubic vertex `g_klm`) are
  incompatible as written. **Further: Eq. (71) does not reproduce the paper's
  own Table 1.** Every intermediate checks out (`Λ_B = 46429.563` MeV,
  `r_f = 8.002012`), but the masses come out `+0.05508 %`, `+0.05513 %`,
  `+0.05520 %` high — one constant, hence a pure `Λ_B` normalisation gap
  requiring an undisclosed rescaling by `0.99944949`. Taken as written, Eq. (71)
  puts `m_Z` at **24.0σ** from PDG, not the tabulated `+0.11σ`, and `m_W` at
  `3.44σ`, not `+0.04σ`. The p. 41 concession that the residual is `O(α²)` fails:
  `α² = 0.0053 %` against a `0.055 %` gap. Unlike the lepton sector this absorbs
  only *one* quantity, so two genuine boson mass ratios survive as testable.
  The commissioned on-shell parameters reproduce exactly (`g₂ = 0.6528276683`,
  `g_Y = 0.3499382355`, `sin²θ_W(OS) = 0.2232002862`, `λ = 0.1293322002`,
  `y_e = 2.9350286×10⁻⁶`, `y_μ = 6.0686861×10⁻⁴`, `y_τ = 1.0205763×10⁻²`) but
  are matching results *of the tabulated masses*, not of the paper's mechanism.
- **Supplementary (not commissioned) — the S⁹ neutrino sector is the paper's one
  parameter-free prediction, and it is falsified in its derived form.** Every
  closed-form coefficient verifies independently: Eq. (116) is the
  Ikeda–Taniguchi coexact-2-form spectrum on `S⁹`, Eq. (117) is the Weyl
  dimension of `so(10)` `[k−1,0,1,0,0]` (`d(1) = 120 = dim Λ³(10)`), and
  `ζ′_Δ2(0)` recomputes to `−0.41364466` against the stated `−0.41364`. But
  Eqs. (119)–(121) derive `a₉ = exp(ζ′/16)√5 = 2.1790009`, then discard the
  exponential and declare `a₉ = √5` "exactly", absorbing an n-dependent factor
  into an n-independent prefactor. With the derived `a₉`,
  `Δm²₂₁ = 5.952×10⁻⁵` (**8.8σ**) and `Δm²₃₁ = 1.746×10⁻³` (**21.4σ**). The
  illegal truncation is the difference between the fit and a 21σ miss.
  Separately, `τ₃ = (1,1,√3)` on `S³` versus `τ₉ = (1,4,3)` on `S⁹` for the same
  three knots cannot both be right.
- **Registered consequences.** (i) XF-C untouched. (ii) The ℤ₆ is reached by a
  second independent route but **not** newly derived (the group-theoretic
  identification is textbook); this mildly strengthens XF-B3 without closing it,
  and must not be recorded as a second discovery. (iii) A new verdict-blind
  derivation is available for registration — the S⁹ census — carrying **three**
  premises (tier-C free sealing, O4 fixing the hypercharge scale, and the
  ambient-`su(5)` trace form), all of which must be frozen first. (iv) The two cosmological bridges (`w = −1` exactly versus a late
  interval with `w_eff < −1`) remain competing alternatives and must not be
  combined. (v) The paper enters the record as *Candidate Hopf-Spectral Physical
  Realization — external bridge model, not part of the sealed core*, with its
  error tier attached: its α formula and neutrino spectral machinery are worth
  retaining; its charged-lepton sector is not a derivation and must not be cited
  as one. No row suppressed (F-XF4).

#### 008a — 2026-07-25 — Addendum to 008: post-synthesis verification pass

Appended after event 008 was written, when the 36-agent adversarial pass (five
derivation lanes, 25 refutation agents, 0 errors) completed. **Three findings
refuted, all on status and none on arithmetic**; those corrections are folded
into event 008 above and into the result document's T2.3/T2.4/T2.6, marked in
place. No verdict changed. New material, each item re-derived from scratch
before recording (`scripts/xf1_hopf_bridge_audit/a5_addendum.py`):

- **The "quadratic piece" reading of D(n) is now closed, not merely unmet.**
  The second difference of `−ζ′_n(0)` has a closed form,
  `−(8 ln 3 − 3 ln 2) = −6.7094567676650416029`, so any three-point quadratic
  has curvature `c₂ = −3.3547283838325208015` against the `+1.2020569031595942854`
  that `ζ(3)n²` requires — **wrong sign, 2.79× too large**. Relatedly the paper's
  own note "for large n, D(n) ∼ ζ(3)n²" is false: `−ζ′_n(0)/(ζ(3)n²)` is
  `−6.34, −120.35, −1825.97` at `n = 10, 100, 1000`; the object grows as
  `−(1/3)n³ln n`.
- **Thm 46 names a coset whose volume it does not use.** p. 61 reads "two copies
  of `Vol(S⁴)` (the coset volume `Vol(SU(3)/SU(2))`)", but `SU(3)/SU(2) ≅ S⁵` —
  as the paper itself states on p. 23. Using `Vol(S⁵) = π³` gives
  `1/α = 190.194176592907` instead of `137.036082448164`. Further, splitting
  `ln α`: the n-INdependent prefactor `9/(8π⁴) = 0.0115492300365199` carries
  **90.669 %** and `N_B = (Vol(S⁹)/160)^(1/4)` only **9.331 %** — so "α is
  derived from S⁹" is thinner than presented. This tempers the one item the
  audit credited to the paper.
- **Table 1 contradicts the paper's own Thm 35 at 5 %, one level above the
  0.055 % Λ_B gap already recorded.** `m_W = g v/2 = 76.299990399` GeV from
  Thm 35's `g = 0.619771739575` and Axiom 1's `v`, against Table 1's `80.3695` —
  `−5.0635 %`. And Table-1 on-shell `sin²θ_W = 0.223200286206` against
  Eq. (75)'s `3/(4π) = 0.238732414638`, `−6.50608 %` (`−6.95875 %` normalised to
  the on-shell value). The boson table and the gauge-coupling theorem are two
  disconnected chains disagreeing about the same two observables.
- **Obligation O8 fails inside the paper's own p. 52 summary table.** The three
  massive shells use three extractors: operator order `1 → 1 → 2`, framing
  `6 (knot) → 6 (knot) → 16 (contact chirality)`, determinant exponent
  `−1/2 → −1/2 → +1/2`. Separately, Eq. (112) is a positive-definite quadratic
  form in a **commuting** 2-form field, so Gaussian integration gives
  `(det)^{−1/2}`; the paper's `Z = (det Δ₂)^{+1/2}` "fermionic sign" (p. 49) has
  no spinor field to justify it, and that sign propagates through `κ₉` and `Λ₉`
  into the entire neutrino spectrum.
- **Full-text search over all 78 pages:** `"koide"` 0, `"ppm"` 0,
  `"proton decay"` 0, `"georgi"` 0, `"grand unif"` 1 (bibliography [16],
  Baez–Huerta). Consequences: (i) **XF-C's standing is stronger than event 008
  recorded** — the paper is not a failed external test of the Koide target, it
  is silent on it; (ii) "consistent with no intermediate GUT group" is vacuous
  *as a statement about the paper*, which never raises the issue — the
  consistency is with the Two-Law theory alone; (iii) the paper's single ℤ₆
  mention is imported from [16], reinforcing the scoping already recorded.
- **Correction to event 008's quotation of `ℓ₉`.** Recorded as `2⁸/2 = 16`;
  that is a misreading of flattened PDF superscripts (`2⁸/2 = 128`). The paper's
  actual reading is `dim_C S± = 2^((10−2)/2) = 2⁴ = 16` and `ℓ₉ = 2^(8/2) = 16`.
  **The number is fine; only the justification fails** — `S⁹ = Spin(10)/Spin(9)`
  is odd-dimensional and `Spin(9)` has a unique 16-dimensional spinor irrep, so
  there is no chirality split to count. (16 is available `U(5)`-covariantly as
  `dim Λ^even(C⁵) = 1 + 10 + 5 = 16`, by a route the paper does not use.)
- **One census discrepancy left open, not silently resolved.** An independent
  lane's enumeration of O1–O4+O7 survivors gives `n ∈ {4,7,8,…}`; this audit's
  tier B gives `{4,6,7,8}`. They differ at `n = 5,6` on the encoding of "exactly
  one commutant U(1)". Immaterial: both give argmin 4, and tier C gives `{4}`
  uniquely under either. Also noted: at `n = 7` the same trace computation gives
  `sin²θ_W = 3/4` and at `n = 8` `7/16` — preferring `n = 4` because it yields
  `3/8` would break verdict-blindness and is **not** done; tier C excludes
  `n = 7,8` structurally, before any value is computed.
- **A publishable falsifier.** Any future derivation of `D(n)` must reproduce
  `D_required(n) − D(n) = 3.411418677×10⁻⁵ ± 6×10⁻¹⁰` simultaneously for
  `n = 1,2,3` — constant free (degenerate with `Λ_Hopf`), shape fixed at
  `D(2)−D(1) = 3.603534014` and `D(3)−D(1) = 9.615217254` — **without being told
  `m_τ`**. This is the one calculation that would flip the audit. No row
  suppressed (F-XF4).

### 009 — 2026-07-25 — Free environment completion: realization derived; ledger invariance registered as a counted bridge premise

Target 3 of the Genesis operational-completion package (*No fundamental
mixing*) worked by the tier-C free-sealing template — identify the API, define
the category of exact realizations under it, exhibit the initial object, check
that the selection is a universal property and not a value quantity. Artifacts:
`docs/free_environment_completion_attempt_v1.md` /
`blake3:21f9e51385fad537dffaa5b59e5d7aa1a51a7be2d70a717aa95e6f3e4e917cda`;
`docs/260725_immediate_open_problems.md` /
`blake3:da61d30610022462827ac36378e915b6e2143e792901ec490cebb6d070999ee2`;
`docs/260725_immediate_open_problems_solution.md` /
`blake3:0245f90372ab133525736c6ab7ad6ffee536b72e0362fc06216f2c941704cc9a`.
These are research notes, not sealed artifacts: there is no replay example and
no result digest, and they are cited by file digest only.

- **Realization half — DERIVED.** For an essentially small strict symmetric
  monoidal `P` and a wide symmetric monoidal class `J ⊆ P` of environment
  embeddings, the category `Env_J(P)` — objects those of `P`, morphisms
  `(E, f : A → B ⊗ E)` modulo `(E,f) ∼ (E′,(id_B ⊗ u)f)` for `u ∈ J`,
  composition `[D,g][E,f] = [D⊗E,(g⊗id_E)f]`, discard `ε_A = [A,id_A]` — is
  **initial** among exact `J`-realizations. Every morphism is a dilation by
  construction, so purification is a property of the free seal and not a
  postulate about states. Two qualifications recorded rather than absorbed:
  outside the strict lane the realization functor's tensorator and unit map must
  additionally be required causal, with an explicit counterexample showing the
  unqualified statement false; and the equivalence on dilations is in general a
  **zigzag** of embeddings, not a single reversible map, absent a filtered or
  common-cocone hypothesis. The synthesis's target-3 phrase "unique up to
  reversible environmental equivalence" is corrected accordingly.
- **Non-vacuity — the unrestricted completion collapses.** If environments are
  identified along *all* maps of `P`, then whenever `P` has a zero object with
  `B ⊗ 0 ≅ 0` every hom-set of the completion is a singleton and purification
  holds vacuously with nothing else surviving. An embedding class is therefore
  mandatory, not a convenience. The intended class `J = Isom(P)` is unavailable
  until the dagger is sealed, so **targets 1 and 3 of the reconstruction package
  are ordered, not coordinate** — the package is not four independent targets,
  and the synthesis is amended to say so.
- **Selection half — INDEPENDENT. New counted bridge premise: LEDGER
  INVARIANCE**, `ε_{E′} u = ε_E` for every isometry `u`. It is **not entailed**
  by Internal Record Actualization, Public Confluence, monoidal discarding and
  free sealing. Countermodel: nonnegative real matrices under Kronecker product
  with transpose as dagger, `ε_n = (1,…,1)`, and `u = 2^{-1/2}(1,1)ᵀ : 1 → 2`,
  for which `u†u = 1` while `ε₂u = √2 ≠ 1 = ε₁`; arithmetic independently
  recomputed. The near-miss is closed deliberately rather than by omission:
  every **unitary** of that category is a permutation matrix, so its discard is
  invariant under all unitary recoding and only isometric *enlargement*
  separates the two notions. The failure is exactly a mismatch between
  dagger-losslessness and ledger-losslessness. Public Confluence cannot repair
  it for a structural reason worth recording: PC is a conditional whose
  antecedent is a model-chosen compatibility relation and whose failure mode is
  the designated disjunct "no common seal"; an axiom of that shape cannot force
  an equation, so no strengthening of its consequent helps.
- **Scope of the negative, stated at the careful grade.** Independence is from
  the clauses **as abstractly stated**. Whether the specific sealed pure layer
  satisfies ledger invariance by a theorem using its own structure is untouched
  and open. The one identified route to a positive result: derive the dagger and
  the discard from a single record structure, in which case the mismatch the
  countermodel exploits is structurally unavailable — a joint target-1/target-3
  derivation rather than two separate ones.
- **Verdict-blindness.** The item produces no number and no measured quantity
  was consulted; F-XF2 is satisfied by construction. One trap is recorded and
  was avoided: Stinespring uniqueness is a theorem about Hilbert spaces and must
  not be used as an input here, since that would derive the abstract premise
  from the intended model. The isometry class was forced by the collapse
  argument **first** and only then checked against the external record.
- **External concordance, not an input.** Huot and Staton, *Quantum channels as
  a categorical completion* (LICS 2019, arXiv:1904.09600), complete
  finite-dimensional vector spaces and **isometries** to CPTP maps between
  finite-dimensional C\*-algebras. The agreement is on the *constraint*, not
  merely the endpoint. Recorded as concordance in the sense already used for the
  ℤ₆ endpoints in event 008 — it is not a second derivation and must not be
  cited as one.
- **Register independence.** This item does not consume the certified
  fifteen-stage register. It uses the Selective Law's realization face and the
  abstract structure of the pure layer only. The standing provenance disclosure
  (`docs/provenance_disclosure_v1.md`) therefore does not bear on it, and PA-1's
  outcome will not re-grade it.
- **Registered consequences.** (i) Target 3 is **conditionally derived**: ledger
  invariance is one counted bridge premise, and conditional on it the
  environment completion is a theorem of free sealing. This is the disposition
  the reconstruction conjecture's own falsifier clause prescribes for a target
  that resists derivation. (ii) The reconstruction package is ordered, not
  coordinate. (iii) No registered prediction changes status. **XF-A3**
  (complex-versus-Jordan discriminator) remains named and unregistered; this
  item concerns target 3 and supplies no part of XF-A3's derivation route. (iv)
  A separate ledger event remains owed for `docs/provenance_disclosure_v1.md`,
  which directs that the XF ledger carry it; it is not folded into this event.
  No row suppressed (F-XF4).

### 010 — 2026-07-25 — Standing provenance disclosure carried; synthesis swept for suspended claims

The disclosure directs that "the XF ledger carries this disclosure as an event."
It is carried here. `docs/provenance_disclosure_v1.md` /
`blake3:c4bcc733382423e70a4c2267d2c7fe612b5f3bbe6857414ef00a4bea059e90c2`.
Recorded late relative to its own date; the delay is disclosed rather than
backdated, and this event is appended in append order, not chronological order.

- **What the disclosure establishes** (from XF-0b2, event 007): the fifteen
  sealed telescopes sourcing the certified register are a hand-authored literal
  table committed in the first implementation step; the recognizing search gates
  postdate it and their position arms are syntactic singletons; Step 14 is
  pre-declared in the initial commit. Consequence, unsoftened: **no search in
  this repository has been shown to have found any sealed telescope clause.**
- **Four framings suspended** pending the PA-1 provenance audit, to be read as
  unproved and not as false: autonomous recovery; the uniqueness census *as a
  property of the laws*, including the Stage-4 "exactly four"; non-circularity
  of the recreation claim; and the halt *as a law-property*. The certified forms
  that stand are the corresponding statements about the sealed library.
- **Sweep executed.** The disclosure states that no downstream document may cite
  a suspended claim without citing the notice, and that silence is a violation
  of the constitution's suppression clause applied to the program itself. The
  synthesis was cited-in-full at five sites and repaired: the abstract (which
  carried both the uniqueness census and "the halt is therefore a theorem of the
  laws rather than of the enacted choice" unqualified); roadmap items on the
  Genesis chain and on foreclosure past Step 15; the terminality proof sketch;
  and the Halt-status proposition. A standing disclosure remark now precedes the
  formal spine and each site refers to it. **Finding worth recording: the two
  most prominent suspended claims in the whole document were in its abstract.**
- **Scope of the sweep.** Editorial only; no theorem, certificate, or sealed
  artifact was altered, and no claim was deleted — each is restated in its
  certified form with the stronger reading marked suspended. Items resting only
  on the Selective Law's realization face are explicitly excluded from the
  suspension, since free sealing and its initiality arguments do not consume the
  register; event 009 is the worked example.
- **Registered consequences.** (i) PA-1 is the outstanding gate on the program's
  spine; until it reports, "derived from two laws" framings that consume the
  register are to be read as "derived from the sealed library." (ii) No
  prediction status changes: the registered predictions rest on representation
  facts of the sealed content plus stated bridges, which the disclosure leaves
  standing. (iii) XF-0 remains untouched, having been blind by construction. No
  row suppressed (F-XF4).

### 011 — 2026-07-25 — Tier-C free-sealing clause DERIVED; the S⁹ census uniqueness ceases to be a declared premise

Status change to a premise registered in event 008. That event recorded three
premises for the S⁹ census, all to be frozen before anything is claimed: the
tier-C free-sealing clause, O4 fixing the hypercharge scale, and the ambient
simple `su(5)` trace form. The first is now derived.
`docs/tier_c_free_sealing_2_law_conjecture.md` /
`blake3:40205b0daa76fea728fe082098d0fdc548510741e6be515e877923482d8f04dd`
(note: that artifact still records its premise P1 as open and is stale on that
point; the closure is in the synthesis, not in the note).

- **What was derived.** The Local alphabet theorem selects its factors *with*
  their division-algebra carriers — now a lemma in the synthesis with a proof,
  which is exactly the premise P1 the note left open. Given it, the category of
  exact realizations of the carrier-bearing alphabet has `V₃ ⊕ V₂ ≅ C⁵` as
  initial object, so the free seal of the alphabet in a complex arena is `C⁵`,
  whence `n★ = 4`, `S¹ → S⁹ → CP⁴`, `G(H₄) = U(5)`. The complex rung contributes
  no summand, being typed as a scalar-action clause; had it required one, the
  coproduct would be `C⁶` and the shell `S¹ → S¹¹ → CP⁵`.
- **The `argmin` fallback recorded in event 008 was never lawfully available.**
  That event stated that without the tier-C clause the census returns an
  `argmin` and a monotone cost `κ(H_n)` must be justified independently. A
  monotone cost is a value quantity, and no value quantity legislates at a
  guarded stage. The lawful trichotomy is: free sealing derivable, and `n★ = 4`
  is selected; not derivable, and the discharging set is a **certified cone**; a
  cost, and the move is **forbidden**. Corrected in the synthesis.
- **Residue, stated rather than buried.** One interpretive claim carries the
  result: that initiality is a universal property and not a value quantity. It
  is invariant under equivalence, ranks nothing, and compares nothing, so the
  reading is defensible — but it is the theory's own to adjudicate, and if it
  fails the census returns a cone rather than a selection. Note also that the
  same value/universal-property distinction shows a dimension-minimum route
  reaching the same `C⁵`; that route is **illegal** at a guarded stage, so the
  carrier lemma buys not the value but the entitlement to assert it.
- **Register independence.** Like event 009, this item consumes the realization
  face only and not the certified register; the standing disclosure of event 010
  does not bear on it.
- **Registered consequences.** (i) Of the three premises registered in event 008,
  **one is discharged**; O4 and the ambient trace form remain declared. (ii)
  XF-B3 is not upgraded: the ℤ₆ identification remains textbook and reached by a
  second route, exactly as event 008 scoped it, and this changes nothing there.
  (iii) The census's own open items are unchanged — O4–O6 and O8 are still not
  adjudicated structurally, and O5 in particular still finds nothing equal to
  three in any invariant of `CP⁴` computed so far. (iv) A companion editorial
  sweep removed six sites at which the synthesis selected by comparing `κ` or
  `ρ` — empty desert, proton stability, the dark sector, no-cloning,
  Einstein–Hilbert, and the Step-15 extension — each restated as an
  unforced-generator, evidentiary-standard, initiality, or premise-based
  argument. No row suppressed (F-XF4).

### 012 — 2026-07-27 — PA-1b open-competition audit executed; no certified autonomous winner

PA-1b of the provenance audit is executed and registered. Artifacts:
`docs/PA1B_EXECUTION_PLAN.md` /
`blake3:043501055859423f37e4c596af74aaeb0dc6b34e3cfa91292121092c24cabd63`;
`docs/pa1_open_competition_v1.json` /
`blake3:51cc36babf9e638e97fe212932eefcba61b4bed5551e8fb38f97607e28012141`
(internal result digest
`blake3:53af941cd24dbcad9f94cad5315e90ef5c1985a16d38704dc73ca09455259bf7`);
`docs/PA1_OPEN_COMPETITION_RESULT.md` /
`blake3:feb06501fd37631928a2f80858338be35974dae50e1d6d9965e7ba6dd33109d5`.

- **Question and rule.** The audit asks whether any certified run actually
  enumerated a target-independent, outcome-complete competition and then
  selected a unique winner under a law that did not pin that winner in advance.
  All four conditions are conjunctive; raw breadth, singleton completeness,
  authored witness sets, and target-derived deterministic tie breaks do not
  qualify.
- **Global finding.** **No certified run in the record contains a genuinely
  open competition with a lawful winner.** The certificate contains 20 explicit
  surface rows and zero qualifying rows. PA-1b therefore identifies no
  "strongest autonomy evidence."
- **Positive openness evidence retained.** T-BF1 Stage 4 is a real, typed,
  admitted four-way cone whose candidates are co-minimal under the audited
  `(κ, ν)` law; it correctly selects none. The generic Step-16 gate is open, and
  the corrected 17-shape adversarial table contains four lawful bar clearers,
  but the table is authored and non-exhaustive and performs no full selection.
  These are the strongest gate-openness facts, not autonomous discoveries.
- **Negative cells retained.** Claim Step 1 reduces 2,144 raw expressions to a
  target-gated singleton; its special catalog and gate postdate the reference.
  Claim Step 15's 7,211 count is prefix-event telemetry, with heuristic and
  incumbent-rank drops and one full evaluation, not a complete competition.
  Phase-5b v2's apparent Stage-4 winner uses distinctions forbidden by the
  later two-register law. Phase-5b v3 and the BI-1/BI-1b successor lanes expose
  position-pinned singleton cones. SH-1 names its Step-16 survivor explicitly.
- **Replay and bounded failures.** A fresh-process replay reproduced the
  certificate and report hashes and the zero-row result. Nine focused PA-1b,
  claim, T-BF1, Step-16, semantic-exhaustion, and halt tests passed. The legacy
  BI branch-cone regression failed closed at the already recorded current-vs-
  frozen v3 reissuance drift and is disclosed as such. Two bounded current
  Step-15 attempts emitted no artifact; the known multi-gigabyte Step-16 OOM
  materialization was not repeated. Neither resource outcome is promoted to a
  halt, census, or winner claim.
- **Scope.** This event records PA-1b only. It assigns no PA-1c provenance
  grades, changes no prediction status, edits no sealed input, and does not
  weaken or supersede the standing disclosure in event 010. No row suppressed
  (F-PA4/F-XF4).
