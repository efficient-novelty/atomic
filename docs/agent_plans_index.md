# Agent plans index (post IP-1/TDC-1 v3)

> **HIST-CERT v3 continuation (2026-07-19):** the boundary-to-realizer join
> discharges F-B2. Raw values are `7/8/10/18`; the registered
> typed-and-marginal path is classified as `2/2/5/10`; the ordinary
> `5/6/5/8` remainder and independent novelty credit remain unresolved. Every
> full `certified_total` is `null`, F-T1 remains open, and no later acceptance,
> Step 16, or global halt follows. See `docs/HIST_CERT_V3_RESULT.md`.

> **DEMAND-COMPLETE v1 continuation (2026-07-19):** Agent D reaches the D-4
> gap branch. The coarse ladder and stage-3 wrinkle replay, but the extractor
> mirrors the caller/focus timeline and the sources do not operationally
> define the historical `C(W)` instance grammar, orbit quotient, or
> `D`-membership judgment. F1 and F5 are not triggered; semantic `O(16)`
> emptiness and the `d = 4` demand-orphan finding remain conditional. See
> `docs/DEMAND_COMPLETENESS_RESULT.md`. No halt conclusion follows.

**Date:** 2026-07-19. Four dedicated agent briefs, one per mechanization
item. Each is self-contained; hand each file to its own thread.

| agent | brief | owned modules | depends on |
| --- | --- | --- | --- |
| A — HIST-CERT | `agent_a_hist_cert_plan.md` | `pen-type::cubical` (historical fragment), `pen-eval` tdc1 | none to start; final unconditional status waits on B + adjudication |
| B — BOUNDARY-AUDIT | `agent_b_boundary_audit_plan.md` | `pen-type::cubical::boundary_variants` (new, gated) | none |
| C — KERNEL-BRIDGE | `agent_c_kernel_bridge_plan.md` | `pen-type::{infer,normalize,equality,substitution}`, `pen-search::candidate_join` schema 4 | none; long pole |
| D — DEMAND-COMPLETE | `agent_d_demand_completeness_plan.md` | `pen-eval::{debt_guard, extractor}`, new spec module | none |

**Parallelism:** A ∥ B ∥ C ∥ D. Module ownership is disjoint by design;
coordinate through types, never concurrent edits of the same file.

**Adjudications that remain the user's, not any agent's:**
1. The attachment boundary axiom (informed by B's table; A records
   conditionality until decided).
2. Composition of C's bridge with A5 into any Step-16 acceptance
   conclusion.
3. Anything F-IP2 (C-5) or Guard-Rail F1 (D-2/D-3) turns up — both are
   report-verbatim-and-stop events.

**Common discipline (binding on all four):** create-new artifact
semantics; full replay + mutation batteries; the bar value appears in no
derivation path; obstructions are named results, never workarounds;
historical records are never edited to fit machinery ("fix the machinery,
never the history").

## Agent E — SCHEMA2 (terminal long pole)

- **SCHEMA2 v1 partial execution (2026-07-19):** the current E-1
  operational fragment and Agda algebra replay, but global C1 remains open
  pending the `pen_core::Expr` elaboration and Agda/Rust correspondence
  bridges. E-2 preflight stopped on the mandatory Stage-1 Universe-package
  boundary and Step-8 post-path typed-semantics adjudications. E-3..E-8 were
  not executed; no ordinary token or halt conclusion was issued. See
  `SCHEMA2_RESULT.md` and `schema2_v1.json`.
- **SCHEMA2** (`agent_e_schema2_plan.md`, 2026-07-19): execution plan for
  the frozen spec `step_15_completion_open_problem.md`. Phases E-1..E-8
  retire C2/C1/D-4/C6/C8 and the HIST-CERT ordinary remainder; E-2 is an
  early ordinary-family handoff to Agent A; E-5 makes Guard-Rail F1
  executable at instance granularity for the first time (report-verbatim
  hazard). Owns new crate `pen-schema` + `agda/Schema2.agda`. No
  reselection, no Step-16 verdict, no bar. The stage-1 EGP divergence
  explanation is a registered E-2 deliverable feeding the Phase-5b fork.

## Agent C continuation briefs

- **TRUNC-ER** (`agent_c_trunc_endpoint_realizer_plan.md`, 2026-07-19):
  **executed.** `C6_TRUNC_DECLARED_ENDPOINT_PATHCON_REALIZER` is closed at
  d = 1 under a source-bound formal eliminator-premise context; the two-key
  Trunc bundle completes the `2/2/5/10` handoff to Agent A. See
  `TRUNC_ENDPOINT_REALIZER_RESULT.md`. General C6/C8/classification remain
  explicitly open.

## Agent C v3 continuation

The post-adjudication continuation is recorded in
`KERNEL_BRIDGE_V3_ELEMENT_OVERLAY_RESULT.md`. It adds a schema-5 sidecar over
the exact schema-4 archive, closes the three registered C7 bindings, and
hands Agent A exact-prefix constant-boundary C6 tokens. General C6, the Trunc
endpoint-dependent realizer, F-B2's create-new rerun, intended-schema
classification, and candidate-level C8 were open at that archival point.
TRUNC-ER subsequently closed the registered d = 1 Trunc realizer, and
HIST-CERT v3 subsequently discharged F-B2. General C6, intended-schema
classification, candidate-level C8, and F-T1 remain open.
