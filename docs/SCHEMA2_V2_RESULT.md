# SCHEMA2 v2: resumed E-2 quotient result

**Executed:** 2026-07-19  
**Disposition:** R1 and the registered Step-8 interpretation under R2 replay
count-blindly; the current fragment reaches F-Q4 before generator-membership
verdicts, historical ordinary-family handoff, or a revised count regression.
E-2 therefore remains **partial**. E-3 through E-8 were not executed.

## Artifact

- Certificate: `docs/schema2_v2.json`
- Schema: `schema2-e2-successor-v2`
- Bytes: `18978`
- SHA-256:
  `1e683c4fe14142347c08de6ac064bd449e11c18736a3a5e803527319f8ca6be7`
- Result digest:
  `blake3:2a292d6f0f785d178269af4c9a299ba14486a43ca4bdd31a1cede25ddad5b2de`
- Outcome:
  `partial_e2_registered_signatures_replay_fq4_generator_membership_pending`

The artifact is a create-new successor to `docs/schema2_v1.json`; it does not
overwrite or reinterpret the predecessor. It binds 18 inputs and implementation
sources by exact byte length and BLAKE3 digest. Strict replay of the emitted file
returned `valid: true` with no errors. A second emit exited unsuccessfully and
left the original artifact intact.

## Adopted quotient rules

The exact adoption text in `docs/e2_quotient_adjudications.md` is pinned and
replayed for:

1. `formation-completion-package-family-rule-v1` (R1);
2. `derived-action-generator-membership-rule-v1` (R2).

Both are treated as adopted policy, not as automatic independence verdicts.
Neither rule reads an archived count or the acceptance bar. Their exceptional
or independent cases remain delegated to typed generator membership, exactly as
the adopted text requires.

## R1: actual Stage-1 execution

The engine reconstructs the historical Stage-1 telescope from
`Telescope::reference(1)`, checks both clauses with the kernel elaborator, and
replays the exact source shape:

```text
carrier    = Univ
completion = App(Univ, Var(1))
canonical completed family = App(Univ, Var(1))
coverage direction         = completion covers carrier
```

The completed package family is issued. The carrier is recorded as provenance
and is not separately credited. This is a positive result about the base package,
but it is not yet a total Stage-1 count: R1's exception asks whether the carrier
has an independent `LocalRole` not generated by the package. The four frozen
roles—`KernelHead`, `AdjointMate`, `SupportAction`, and `Coherence`—all remain
pending because the required E-4 membership procedure is unavailable.

Consequently:

```text
completed_package_family_issued                         = true
carrier_exception_membership_decided                    = false
separate_carrier_family_issued                          = false
total_stage1_family_cardinality_omitted_pending_membership = true
historical_family_handoff_issued                        = false
```

No value of `1` or `2` is installed as an engine result. The sealed Stage-1
value is therefore neither contradicted nor recovered by this partial run, and
F-Q2 is not yet evaluated.

## R2: registered Step-8 execution

The source telescope is reconstructed from the exact Steps 1–7 predecessor and
the Step-8 candidate. Its five raw clause shapes replay as:

```text
App(Univ,Var(1)); Var(1); PathCon(3); Lam(Var(1)); Lam(Var(2))
```

Against the adopted registered interpretation, the checker forms and types:

- `mu : El(S3) × El(S3) -> El(S3)`, registered as natural in both arguments;
- the first-slot left-unit equality `mu(base, x) = x`, with the right-unit
  alternative retained as its D1 mate rather than multiplied;
- `mu`'s action on the Step-8 `PathCon(3)` cell.

The cell action is represented by a face-safe `MapCube`: its six output faces
are derived by applying the checked operation to the typed source cube's faces,
rather than accepted from caller-supplied boundary labels. The replay also joins
the exact S3 typed-boundary, wrapper-bundle, and historical child derivation
hashes from HIST-CERT v3.

F-Q1 is therefore **not triggered for this adopted registered-overlay checker**.
That statement is intentionally narrow. The direct positional
`pen_core::Expr`-to-`Schema2` term elaboration bridge remains unproved, and no
Agda-to-Rust operational correspondence or naturality-generator theorem is
claimed.

R2 still requires generator membership before the left-unit coherence or cell
action can be classified as generated versus independently exported. Both
verdicts remain pending, and no independent `SupportAction`, ordinary-family,
or Agent A handoff token is issued.

## Operational grammar scope

The Rust implementation adds a finite, typed registry for the nine currently
registered ordinary constructor kinds:

- formation;
- point/unit introduction;
- path-constructor introduction;
- recursor;
- inductor;
- Trunc parametric action;
- post-path operation;
- post-path coherence;
- cell action.

Every registered descriptor carries a depth proof of at most two, a typed
interpretation, support data, and replayable derivation hashes. Historical
clause anchors are checked against the referenced telescope's actual candidate
hash and clause count. Caller-selected presentations cannot bypass a pending
R1/R2 quotient, and a generic formation/completion pair does not receive R1
package coverage without the specific same-package dependency proof supplied
by the Stage-1 issuer.

This is a **historical-reference support fragment**, not the full arbitrary
`Schema2(W)` theorem. In particular:

```text
historical_reference_support_fragment_only       = true
arbitrary_sealed_support_window_grammar_proved    = false
generic_quotient_judgements_complete              = false
generator_completeness_proved                     = false
C2 retired                                        = false
```

The named generalization gap is
`C2_ARBITRARY_SEALED_SUPPORT_WINDOW_GRAMMAR_NOT_YET_IMPLEMENTED`.

## Agda boundary

`agda/Schema2E2.agda` imports the byte-pinned v1 `Schema2` module and checks
under `--safe --without-K`, with no postulates. It defines a six-face `Cube3At`,
the boundary-preserving `map-cube3`, the registered Step-8 carrier/operation/
left-unit records, and the typed Step-8 cell action.

This proves the stated Agda typing boundary only. It does not prove generator
independence, counts, Rust-issuer soundness, or Agda–Rust correspondence.

## F-Q4 and historical regression

The available naturality basis covers sort-preserving parameter renamings,
identity, composition, and weakening by one unused coarse parameter. It does
not yet supply:

- E-3 frozen typed normalization and univalent equality;
- typed-instance substitution generator decomposition;
- cubical face-map generator decomposition;
- parent-operation naturality-closure membership.

The current fragment therefore cannot decide the R1 carrier exception or the
R2 coherence/cell-action cases. F-Q4 is triggered and those rows remain pending.
No label, clause position, archived total, or desired historical outcome is
used to infer independence.

The earlier HIST-CERT v3 projection still replays as archival output:

| Step | Raw operational rows | Registered path typed-and-marginal subtotal | Ordinary unresolved |
| ---: | ---: | ---: | ---: |
| 5 | 7 | 2 | 5 |
| 6 | 8 | 2 | 6 |
| 7 | 10 | 5 | 5 |
| 8 | 18 | 10 | 8 |

These values are never grammar or quotient inputs. Because the pending ordinary
rows have no membership verdicts or handoff tokens, the revised E-2 output
regression was not run and F-Q2 was not evaluated.

## Strict-order ledger

| Phase | Result |
| --- | --- |
| E-1 | predecessor scoped fragment complete; global C1 and correspondence bridges open |
| E-2 | partial; registered signatures replayed; F-Q4 membership pending |
| E-3–E-8 | not executed under the governing strict-order plan |

Global C1, C2, general C6, D-4, domain-wide C3, and C8 remain open. No schema-3
row is promoted. No historical score is recomputed.

## Verification

The following passed against the emitted artifact and final source tree:

```text
cargo fmt -p pen-schema -- --check
cargo test -p pen-schema
    35 passed; 0 failed
cargo clippy -p pen-schema --all-targets --no-deps -- -D warnings
agda -i agda agda/Schema2.agda
agda -i agda agda/Schema2E2.agda
cargo run -p pen-schema --example schema2_e2 -- replay docs/schema2_v2.json
```

The successful Clippy claim is deliberately package-local (`--no-deps`); no
dependency-inclusive lint claim is made. Certificate tests reject source-pin
changes, unknown and duplicate JSON fields, altered counts, invented membership
or handoff tokens, later-phase promotion, and halt claims even after an attacker
recomputes the outer digest. The create-new test and the actual second CLI emit
both reject overwrite.

## Conclusion and next lawful action

This result contains no Step-16 acceptance computation, closing inequality,
semantic `O(16)` verdict, global-halt proof, or global-continuation proof.

The resumed run exposes a real dependency cycle in the current phase plan:
E-2's adopted R1/R2 quotients require the E-3 frozen equality and E-4 generator
membership machinery, while the strict-order plan forbids executing E-3/E-4
before E-2 completes. The next sound action is a versioned procedural
adjudication that either:

1. authorizes a minimal E-3/E-4 prerequisite kernel solely to decide E-2's
   named membership obligations; or
2. amends the phase order so E-2 typing and E-2 quotient closure are separate
   milestones.

After that prerequisite exists, rerun the four R1 exception cases and the two
R2 membership cases, mint only the resulting individual ordinary-family tokens,
perform the revised Steps 5–8 count-as-output regression, evaluate F-Q2, and
only then hand the completed ordinary inventory back to Agent A.
