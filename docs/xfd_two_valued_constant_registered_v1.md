# XF-D — The Two-Valued Constant: Exploratory Registration v1

**Campaign:** XF-1 (`docs/xf1_external_falsifier_campaign.md`). **Date
registered:** 2026-07-24. **Status:** registered (exploratory; passive —
no bridge exists). Append-referenced from `docs/XF_LEDGER.md`.

## Registered statement

The Stage-4 fork leaves exactly one permanent trace: cumulative interface
quantities are branch-indexed with a derived offset — the certified 2+2
value classes (BI-4 v2: two branches per class; canonical lawful-register
instance: the complete semantic Σν classes {32, 31}, gap 1, first
divergence at Stage 4 with branch values {3, 3, 2, 2}).

**If** any cumulative ledger quantity acquires a *derived* physical
reading through a future derived bridge, **then** the theory predicts the
corresponding global physical constant is quantized to exactly two lawful
values, with the gap computed from the certified branch tables — a
signature no rival framework predicts.

## Why this exists now

No bridge from cumulative ledger quantities to any physical observable
exists, and none is claimed. This item exists solely to ensure that if
such a bridge is ever derived, its prediction was registered first
(F-XF2): the two-valuedness and the computed gap predate any physical
reading.

## Hazard fence (binding)

This registration licenses **nothing** about the numbers 31, 32, or any
ledger integer appearing in physical data. Pattern-spotting of ledger
integers, φ, or Fibonacci structure in measurements without a derived
bridge is banned outright by the campaign's hazard fence; any such
"confirmation" offered without a derived bridge is void and is recorded
as a discipline violation, not as evidence.

## Register eligibility note (F-XF1)

The branch-indexed quantities cited here are semantic-register content
(complete Stage-1..15 semantic Σν, BranchIndexed, SemanticRegisterAuthority
in MS-1) and diagnostic-register traces cited as traces only. No
structural-register total (engine Σν = 358, Bar values, ε, ν = 103
decomposition) is an input to this registration.

## Activation and veto

- **Activation condition:** a derived (not fitted) bridge assigning a
  physical reading to a specific cumulative ledger quantity, itself
  register-eligible and verdict-blind.
- **Prediction upon activation:** the corresponding constant takes exactly
  one of two lawful values; the gap is computed before measurement.
- **Veto condition (armed only upon activation):** a measurement of the
  bridged constant outside the two lawful values at stated precision.
