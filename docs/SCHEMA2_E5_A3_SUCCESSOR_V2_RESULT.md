# E-5 A3 successor v2 result

**Date:** 2026-07-21. **Status:** **STOPPED FAIL-CLOSED AFTER A REAL
ADVANCE.** The create-new artifact is
`docs/schema2_e5_a3_successor_v2.json`, with result digest
`blake3:e650a5f6a2751dd5ba1d2c9f79c75da12b89d966b2c8741849fd0b26821f681c`.
Independent definition replay is valid.

## Result

The defect that stopped E-5 v1 is repaired on the chronological surface:

- the 64 direct Stage-16 specializations reproduce exactly eight natural
  families;
- the eight pointwise specializations are constructed and kernel-typed; and
- those pointwise specializations join the same eight families rather than
  being counted as new families.

This establishes an exact typed transport for all 72 chronological A3
occurrences and removes the old 64-singleton-versus-8-family regression.

The full A3 output grammar is nevertheless incomplete. Exactly 17 unary
action outputs remain undefined, despite all 17 source registrations joining
typed naturality records. Exactly 13 historical structural-completion outputs
remain typed-hole obligations rather than terms. Neither class may be replaced
by source reflexivity, a coarse focus label, or a later historical answer.

## F1 and Theorem 12

Because those outputs are not well-formed semantic demand terms, the complete
instance inventory cannot be tested against `D(B15)`. The machine therefore
records:

- F1 executable: **false**;
- F1 executed: **false**;
- F1 triggered: **false**, but not excluded;
- semantic `O(16)`: **unset** (`null`), not false and not proved empty;
- Theorem 12 at full instance granularity: neither proved nor refuted; and
- T-BF2 authorization: **false**.

This is not a rerun disappointment: it distinguishes a repaired quotient from
an absent semantic operation and refuses to convert metadata into a theorem.

## Why the remaining terms cannot be synthesized from the current axioms

`ActionAt` contains a source family and source type, but no operator, motive,
or result expression. The existing normalization, ambient-former, contextual,
and motive-parametric theorems can certify or transport an already chosen
expression; none selects which action the schema demands. Choosing `Next`,
`Eventually`, identity, or both would change the grammar and requires a
versioned semantic definition.

Likewise, `StructuralCompletion` contains a constructor tag and structural
snapshot but no carrier, point, endpoints, motive, or telescope-shaped hole.
The existing fresh-declaration and path-constructor APIs type supplied data;
they do not derive that data from the snapshot. A genuine successor therefore
needs an intrinsically typed `A3DemandExpr`/future-hole representation and an
answer-blind instantiation theorem.

## Hygiene and verification

The certificate carries the imported predecessor digest checks and the exact
live-replay drift record from the shared transport artifact. It is explicitly
relative to the frozen wrapped surface; no drift was laundered.

- focused E-5 successor tests: 3 passed, including forged promotion
  rejection;
- create-new emission: valid;
- public JSON replay: valid;
- artifact size: 30076 bytes;
- artifact SHA-256:
  `DE3F6C3F99FA26012DAC38502C4388DD02A4876873D510ECD390890AFF3DBE19`.

The bridge, bar-free adoption, and final halt certificate were not run because
their explicit E-5/T-BF2 gate did not open.
