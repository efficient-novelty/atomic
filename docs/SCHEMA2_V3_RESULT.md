# SCHEMA2 v3: E-3/E-4 partial development under procedural adjudication P1

**Executed:** 2026-07-20  
**Disposition:** The adopted P1 order was applied exactly. The sealed v2 typing
milestone is accepted as E-2a complete; a replayable E-3 normalization/equality
fragment and an E-4 four-generator development were constructed. One Step-8
cell-action row is finally `Generated` under M1. Global E-3 and E-4 remain
incomplete, so E-2b and E-5 through E-8 were not executed.

## Artifact

- Certificate: `docs/schema2_v3.json`
- Schema: `schema2-e3-e4-successor-v3`
- Bytes: `19987`
- SHA-256:
  `82ffeb5a9b4029d6abdc1f1b3a8e5a4eea3074163a463c54d5b67d9f40394acb`
- Result digest:
  `blake3:ced5aabffbc592100f09251f8eb4bfdfacbfe6e94b94a6dbc82736d9e00487fe`
- Outcome:
  `partial_e3_fragment_and_e4_development_one_m1_generated_five_membership_cases_pending`

This is a create-new successor to `docs/schema2_v2.json`. It binds and replays
23 exact inputs and implementation sources, including the v2 predecessor, the
adopted P1 text, the unchanged R1/R2 adjudications, the frozen Schema2 problem
and execution plan, the Rust issuers, and the safe Agda modules. Strict replay
of the emitted JSON returned `valid: true`. A second emit failed with the
platform's file-exists error and left the SHA-256 unchanged.

## P1 phase boundary

The adopted order is:

```text
E-1 -> E-2a (typing) -> E-3 -> E-4 -> E-2b (quotient closure) -> E-5 ... E-8
```

The certificate replays `docs/schema2_v2.json` and the verbatim P1 adoption
block. P1 declares E-2a complete because v2 already contains the completed
Stage-1 package typing, all registered Step-8 signatures, and the finite typed
ordinary-constructor registry. It does not convert the still-pending quotient
questions into answers.

P1's M1 clause is used only in its monotone direction: a row generated by a
proved sub-basis is generated by the eventual basis. M1 permits that positive
verdict before E-4 completeness, but it does not permit `Independent`, an
ordinary-family token, a score, F-Q2, or a handoff.

## E-3 result: a typed, provenance-aware fragment

The Rust implementation supplies deterministic beta normalization for the
current typed `SchemaTermExpr`/`SchemaTypeExpr` inventory. Every issued token:

- replays the formed context and typed local/dimension environment;
- infers and checks the source type;
- normalizes with a deterministic structural budget;
- re-infers the output type and checks type preservation;
- proves replay stability of the normal form;
- records both source and surviving normal-form provenance.

Frozen equality is positive exactly when the typed normal forms and their
surviving provenance agree. The full source lineages remain in the hashed
token, even when beta reduction erases an unused predecessor/library argument.
The relation does not silently reclassify a predecessor or library reference
as a new family. Bound lambda/Pi binders are removed from free provenance, and
variable cube coordinates remain recorded as dimension provenance.

The fragment also replays reflexivity, symmetry, transitivity, normalization
congruence, weakening congruence, and a genuine non-cubical substitution
example. Cubical substitution fails closed where registered-boundary-bundle
transport has not been proved. Ordinary-schema equality retains historical
anchors and source telescope references as provenance while keeping them out
of the semantic equality key.

This is not global E-3. The certificate keeps the following obligations open:

```text
E3_TOTAL_NORMALIZATION_FOR_ALL_WELL_TYPED_SCHEMA2_TERMS_NOT_PROVED
E3_UNIVALENT_EQUALITY_BEYOND_SCOPED_BETA_NORMAL_EQUALITY_NOT_PROVED
C6_V3_GENERAL_HISTORICAL_TERM_LEVEL_COMPLETION_NOT_PROVED_BY_E3_FRAGMENT
E3_REGISTERED_BOUNDARY_BUNDLE_PROVENANCE_TRANSPORT_UNDER_SUBSTITUTION_NOT_PROVED
E3_ARBITRARY_ALPHA_EQUIVALENT_CONTEXT_PRESENTATION_QUOTIENT_NOT_PROVED
```

Accordingly, `global_e3_complete`, general C6, and general univalent equality
are all false.

## E-4 result: four operational generators, no completeness claim

The Rust basis implements and replays the four prescribed generator kinds:

1. typed adjacent exchange;
2. weakening;
3. typed instance-image extension;
4. cubical face map.

For the current E-1 substitution syntax, legal morphisms are presented by
strict source-prefix images rather than admitted as an arbitrary catch-all
substitution. This is a complete presentation of that checked E-1 syntax only;
it is not the semantic naturality-completeness theorem for every intended
`Schema2(W)` family.

All eight class ledgers remain incomplete: Foundation, Former, Map, Axiomatic,
Modal, HIT/V2, Synthesis, and Unknown. Each retains its class-specific gap and
the three common gaps:

```text
E4_CLASS_INDEXED_SCHEMA2_CONSTRUCTOR_EXHAUSTIVENESS_NOT_PROVED
E4_GENERATORS_NOT_YET_BOUND_TO_E3_NORMALIZATION_NATURALITY_FOR_ALL_SCHEMA_TERMS
E4_GENERAL_DEPENDENT_CUBICAL_SCHEMA_ACTION_INDUCTION_NOT_PROVED
```

Thus `C2_DEPTH_TWO_SCHEMA_GRAMMAR_AND_GENERATOR_COMPLETENESS`, global E-4
completeness, and independent-membership issuance all remain false. This is the
required F-E1 behavior: resisted classes stay named gaps rather than being
smoothed into a global theorem.

## The one final M1 `Generated` verdict

The Step-8 cell action receives one final positive membership verdict. The
proof is not based on its label or archived count. A generic operational
judgment `ParentCubeActionGenerated(f,c,row)`:

- constructs the canonical row `MapCube(f,c)`;
- requires the proposed row to be that exact AST and to infer the same type;
- constructs every axis/endpoint equation
  `CubeAt(MapCube(f,c), coordinates) = f(CubeAt(c, coordinates))`;
- derives each right side through the registered `cube_at_map_cube`
  constructor-computation rule;
- typechecks both sides of all six equations;
- binds each equation to a replayed cubical-face-map generator;
- records an explicit inclusion of that sub-basis in the declared E-4 basis.

Step 8 reconstructs the exact registered term
`lambda x. MapCube (lambda z. mu(z,x)) p` and its registered type before
instantiating the generic judgment. Literal term equality fixes the cube
interior; the six face equations witness the boundary action. Frozen equality
is unnecessary for this exact-constructor case.

The resulting token records:

```text
subject                              = step8_cell_action
M1 Generated verdicts               = 1
typed face equations                 = 6
final_by_basis_monotonicity          = true
full_e4_completeness_used            = false
Independent verdict                 = false
ordinary-family credit issued       = false
stage count issued                   = false
```

This is membership evidence only. It must not be multiplied into the ordinary
family inventory or treated as a completed Step-8 count.

The five unresolved membership cases are:

- Stage-1 carrier exception: `KernelHead`;
- Stage-1 carrier exception: `AdjointMate`;
- Stage-1 carrier exception: `SupportAction`;
- Stage-1 carrier exception: `Coherence`;
- Step-8 left-unit coherence.

No `Independent` verdict is available for any of them before complete E-4.

## Agda boundary

`agda/Schema2E3E4.agda` checks under `--safe --without-K` with no postulates.
It proves structural normalization and replay stability for an intrinsically
typed toy fragment, renaming/substitution naturality for that fragment, a
strict provenance-preserving normal equality, decomposition of already checked
E-1 substitution syntax, M1 monotonicity, and a generic parent-cube action with
all six faces. The Step-8 Agda cell action instantiates that relation.

The Agda module deliberately exports no `Independent` verdict constructor.
Arbitrary extensional morphism completeness, finite cubical-basis completeness,
provenance-correct dependent substitution, per-class induction, general C6,
and Rust-Agda operational correspondence are explicit open gaps. The Agda proof
therefore supports the stated fragment but is not evidence of global E-3/E-4.

## Phase and falsifier ledger

| Phase | Result |
| --- | --- |
| E-1 | predecessor scoped fragment retained; global C1/correspondence open |
| E-2a | complete by adopted P1 and replayed v2 typing milestone |
| E-3 | scoped fragment complete; global E-3 false |
| E-4 | partial; one M1 `Generated`, five memberships pending; global E-4 false |
| E-2b | not executed because complete E-4 membership is absent |
| E-5 through E-8 | not executed by phase order |

- **F-P1:** not triggered. No premature independent verdict, family token,
  count, F-Q2 evaluation, or handoff exists.
- **F-P2:** not evaluated. There is no completed E-4 basis against which the
  final M1 positive verdict could conflict.
- **F-P3:** not evaluated. E-2b is not lawful yet, so its count regression was
  neither skipped after completion nor run against partial verdicts.
- **F-E1:** active as intended. Every resistant naturality class is recorded as
  incomplete with named gaps.

No historical score was recomputed. No Step-16 acceptance computation, closing
inequality, semantic `O(16)` decision, global-halt proof, or global-continuation
proof was issued.

## Verification

The final source tree and emitted artifact passed:

```text
cargo fmt -p pen-schema -- --check
cargo test -p pen-schema --no-fail-fast
    68 passed; 0 failed
cargo clippy -p pen-schema --all-targets --no-deps -- -D warnings
agda -i agda agda/Schema2.agda
agda -i agda agda/Schema2E2.agda
agda -i agda agda/Schema2E3E4.agda
cargo run -p pen-schema --example schema2_e34 -- replay docs/schema2_v3.json
    valid: true
```

Focused checks passed 17 E-3 tests, 10 E-4 tests, and 6 successor-certificate
tests. Mutation tests reject source-pin drift, unknown/duplicate JSON fields,
invented independent or completeness claims, removal of a pending case,
promotion of E-2b/later phases, family/count/F-Q2/handoff outputs, and halt
claims even after the outer digest is recomputed.

## Conclusion and next lawful action

This batch makes real progress but does not close C2 or the Genesis halt. Its
durable result is one typed, count-blind Step-8 `Generated` verdict plus a
replayable account of exactly why the other five quotient cases remain open.

The next lawful action is to extend E-3 from the audited expression fragment to
all intended typed `Schema2(W)` constructors, then prove E-4 normalization
naturality and dependent cubical action class by class. Only after every class
is complete may the five pending R1/R2 cases receive final membership verdicts.
At that point E-2b may mint the justified individual ordinary-family tokens,
run the revised Steps 5-8 count-as-output regression, evaluate F-Q2, and issue
any lawful Agent A handoff.
