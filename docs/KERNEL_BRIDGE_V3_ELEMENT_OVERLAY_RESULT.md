# KERNEL-BRIDGE v3 result: registered C7 closed, general C6 still open

> **HIST-CERT v3 successor (2026-07-19):** Agent A consumed this exact-prefix
> element-overlay handoff together with the registered Trunc endpoint bundle.
> F-B2 is discharged, and the registered typed-and-marginal path is classified
> as `2/2/5/10`. The ordinary `5/6/5/8` remainder is unresolved, independent
> novelty credit is not established, every full `certified_total` is `null`,
> and F-T1 remains open. General C6 also remains open. See
> `docs/HIST_CERT_V3_RESULT.md`; this schema-5 result remains archival.

> **Successor execution (2026-07-19):** TRUNC-ER now retires
> `C6_TRUNC_DECLARED_ENDPOINT_PATHCON_REALIZER` at the registered d = 1
> scope and supplies the exact B5 two-key bundle under a source-bound formal
> eliminator-premise context. This document and schema 5 remain archival;
> see `docs/TRUNC_ENDPOINT_REALIZER_RESULT.md` and the successor typed-bundle
> API. General C6 remains open.

**Date:** 2026-07-19. **Execution status:** scoped-success / obstruction
exit. This is the create-new continuation of `KERNEL_BRIDGE_RESULT.md` under
the adopted axiom
`pathcon-attachment-declared-bound-boundary-theory-axiom-v3-element-overlay`
and its `historical-element-declaration-overlay-v1` component.

The machine now proves the three registered historical base bindings and the
constant-boundary Step 5/7/8 realizer handoff. It does not prove the general
C6 theorem, the endpoint-dependent Trunc realizer, candidate-level C8
provenance, semantic-schema completeness, or any later acceptance result.

## Create-new schema-5 artifact

The successor certificate is
`docs/ip1_candidate_verdict_join_v5_element_overlay.json`:

- length: 484,717 bytes;
- SHA-256:
  `C4CFBB693BB3EB6F2CBBDD14495475EE856BE64EC42890A4FC5B2EA612EB1AA2`;
- Git blob: `734f9eb07b8f6dc009a85cb863e694acee2ee164`;
- internal digest:
  `blake3:ee5a0883ef0a81450035d472c855a81d259d6058498983eddc05707b8c3b637b`;
- exact schema-4 parent bytes definition-replayed: true;
- schema-4 rows joined by schema-3 row digest and schema-4 row derivation
  hash: 213 of 213;
- row promotions: zero.

Schema 4 remains byte-for-byte archival at 535,972 bytes, SHA-256
`4094B2E65E36507157029760C71FF62BA241E82D066CF590F9184DE66BEDB5F4`,
and internal digest
`blake3:6a6c3ccfe4731080bfaa944b98f50afe2bde1eb29fdd679b8f62e3a42f72d1e2`.
Schema 5 compile-time binds those bytes and the adopted adjudication file.
Its JSON replay also rejects unknown fields, including fields nested inside
the imported schema-4 row-status types.

## Element overlay and C7

The registry is closed and machine-checked: exactly Step 5 / S1 clause 1,
Step 7 / S2 clause 1, and Step 8 / S3 clause 1. For every entry the issuer
reconstructs, without using display names:

1. formation at clause 0;
2. the immediately following introduction at clause 1;
3. the registered path attachment at clause 2 with the expected dimension;
4. every registered face resolving to clause 1.

The raw shallow elaboration remains `KernelTy::Type`. The v3 token records
that fact and separately supplies the adopted reading
`KernelTy::El(owner)`. It then issues and replays the historical base-binding
and typed declared-boundary tokens. The same chain replays both in the full
H15 signature and in the exact historical predecessor signatures B4, B6,
and B7. Wrong prefixes and altered current telescopes fail before a token is
issued.

Therefore
`C7_ALL_REGISTERED_HISTORICAL_BASE_BINDINGS_UNDER_V3` is proved. This is a
closed three-entry result; it is not a rule that promotes arbitrary historical
or candidate clauses to element sort. The old v2 obstruction remains
available and continues to replay on the archival v2 path.

## C6 boundary and realizer ledger

| package | exact predecessor | registered boundary terms | inherited constant-basis bridge |
| --- | --- | --- | ---: |
| Step 5 / S1 | B4 | typed and replayed under v3 | 2 |
| Step 6 / Trunc | B5 | typed `x,y` context-variable endpoints; no overlay | unavailable |
| Step 7 / S2 | B6 | typed and replayed under v3 | 5 |
| Step 8 / S3 | B7 | typed and replayed under v3 | 10 |

For S1/S2/S3, an opaque prefix-bound bridge joins:

- the v3 typed declared boundary and overlay-backed base binding;
- the inherited v2 declared-boundary attachment and finite presentation;
- every existing typed path realizer; and
- the exact key bijection
  `Beta -> Beta`, `PrincipalTransport(p) -> Kan(p,p)`, and
  `TransportNaturality(p,q) -> Kan(p,q)` for `p != q`.

This earns the registered constant-boundary `2/5/10` bundles. It does not
claim independence, exhaustiveness, or classification of all intended path
schemas.

Trunc is the honest separating case. Its declared `x/y` boundary is typed,
but the current cubical `PathConstructor` and `PathMethod` compute with one
constant base. They cannot realize two endpoint-dependent methods. The exact
gap is:

```text
C6_TRUNC_DECLARED_ENDPOINT_PATHCON_REALIZER
```

The overall C6 status therefore remains:

```text
C6_V3_GENERAL_HISTORICAL_TERM_LEVEL_COMPLETION
```

Ordinary-parameter weakening/substitution joined to cubical contexts,
boundary-aware dependent `coe`/`hcom`, general face-indexed motive methods,
and semantic independence/exhaustiveness are still absent.

## C8 and candidate join

The schema-4 proof of the four registered reference-only diagrams and
`1 + d^2` with `c(b) = 0` replays unchanged. The overlay changes no boundary
charge. It does not add a total boundary-provenance inventory to the 213
candidate rows, so this gap remains:

```text
C8_CANDIDATE_BOUNDARY_PROVENANCE_JOIN
```

All 27 `Hit` rows receive the now-proved *registered historical C7 premise*,
but retain open C6/C8 evidence and every promotion flag remains false. The
other 186 rows record the registered HIT obligations as not applicable.

## Falsifier disposition

| falsifier | result |
| --- | --- |
| F-O1 operational-role witness | discharged for exactly the three overlay entries by full-signature and historical-prefix replay |
| F-O2 S1/S2/S3 term typing | discharged by overlay, base-binding, face, and overlap token replay |
| F-O3 conservativity | schema-4 scope discharged in schema 5; archival HIST-CERT, boundary-audit, and TDC cubical artifacts also replay unchanged |
| sharpened F-B2 | pending; the Agent A-owned create-new HIST-CERT v3 rerun has not been performed |

The unchanged archival replay digests are:

- HIST-CERT v1:
  `blake3:e840f99c519756bc6215fbf8db03a738dae7c9127043599c06eaafed25d77c27`;
- BOUNDARY-AUDIT v1:
  `blake3:76c389766a5c54582d1e2ffe9942db5c53cd3764137cfc62254b9857e1f1c435`;
- TDC cubical regression v3:
  `blake3:075791e9024a8c36a3f0dab593fa1460fb8288d3e9fc3345aa76c48cff978e56`.

F-B2 is not discharged by replaying an old artifact. Its required create-new
run must reproduce both operational presentations `7/8/10/18` and path
marginality `2/2/5/10` while consuming the new v3 prefix tokens.

## Reproduction

```powershell
cargo test -p pen-type --lib
cargo test -p pen-search candidate_join_v5 --lib

cargo run -p pen-search --example ip1_candidate_join_v5 -- replay `
  docs\ip1_candidate_verdict_join_v3.json `
  docs\ip1_candidate_verdict_join_v4.json `
  docs\ip1_candidate_verdict_join_v5_element_overlay.json

cargo run -p pen-eval --example hist_cert -- replay docs\hist_cert_v1.json
cargo run -p pen-eval --features boundary-audit --example boundary_audit -- replay docs\boundary_audit_v1.json
cargo run -p pen-search --example tdc1_cubical_regression -- replay docs\tdc1_cubical_regression_v3.json
```

The schema-5 replay result is:

```text
valid: true
exact_schema4_archive_bound_and_replayed: true
all_213_schema4_rows_joined_by_row_digest: true
c7_all_registered_historical_base_bindings_proved: true
c6_general_historical_term_completion_proved: false
c8_candidate_boundary_provenance_join_proved: false
no_row_promoted: true
```

## Honest conclusion

The v3 overlay resolves the specific C7 sort mismatch and supplies the
constant-boundary historical handoff that Agent A was waiting for. It does
not complete KERNEL-BRIDGE as a semantic classifier. The immediate next
machine task is the endpoint-dependent Trunc realizer, followed by the
create-new HIST-CERT v3 rerun. Separately, the depth-two intended-schema
classification theorem and candidate-level C8 join remain the long poles.
No Step-16 or global-halt conclusion follows from this artifact.
