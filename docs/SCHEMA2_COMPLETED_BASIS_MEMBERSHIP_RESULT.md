# Schema2 completed-basis membership verdicts

**Date:** 2026-07-21. **Status:** all five R1/R2 membership cases are
decided against the completed global E-4 v10 basis. No pending membership
case remains, and E-2b is now authorized.

The create-new artifact is
`docs/schema2_completed_basis_membership_verdicts_v1.json`, with result
digest
`blake3:34b566d668132a1f9cc464636ea15fbe6962abd740a10c43b6d4f794ff0a59ec`.
Artifact replay succeeds.

## Final verdicts

| Subject | Typed-role audit | Completed-basis verdict | Separate family authorized |
| --- | --- | --- | --- |
| Stage-1 carrier / `KernelHead` | carried | generated by the completed R1 package | no |
| Stage-1 carrier / `AdjointMate` | not carried | typed role absent | no |
| Stage-1 carrier / `SupportAction` | not carried | typed role absent | no |
| Stage-1 carrier / `Coherence` | not carried | typed role absent | no |
| Step-8 left-unit coherence | carried and typed | **Independent** | yes |

The Stage-1 carrier is exactly `Univ : Type`, registered as the
`FreshFormation`/`KernelHead` role. R1's replayed
completion-covers-carrier derivation generates that role inside the one
completed package. The other three LocalRoles require semantic roles the
formation carrier does not have, so they do not trigger R1's independent-role
exception. No separate Stage-1 carrier family is authorized.

The Step-8 target remains the typed schema

```text
Pi x:El(S3). Path El(S3) (mu(base,x)) x
```

The earlier M1 fixed point contains no noncircular derivation of that target.
Every later completed-basis extension is audited as nonseeding: the four
endpoint rules act on existing premises; all 43 trace signatures are anchored
at Steps 9-15; P6 exports no extra primitive; the six Internal closure rules
mint zero credit; classifier dispositions are not generators; and the
verdict-blind G-6 decision issued no unit-coherence generator. Global E-4
completeness therefore upgrades the prior non-generation result to the final
R2 verdict `Independent`.

This verdict classifies the typed left-unit schema. It does not fabricate a
closed path inhabitant.

## Frozen-surface replay note

Global E-4 v10 and the current Stage-1, Step-8, and motive-parametric issuers
replay directly. The older grammar-completion and M1 JSON artifacts retain
valid sealed self-digests, but their native live definition replay now reports
the previously introduced `pen-type/src/normalize.rs` source-binding drift:
recorded
`18309/blake3:b9ad659cf60175b87090b590ee0850b3635a4968a47fda5bbd3559588d57ba7a`
versus current
`17962/blake3:8b8089f38fab15923ab92521f45c79fe16d64d6ef2c59a70ac11d74a4004ae9d`.

The verdict certificate does not hide or generalize this exception. It
source-binds the current normalization file, accepts only that exact recorded
drift, verifies both predecessor self-digests, and consumes their frozen
projections. Thus the result is explicitly relative to the frozen wrapped
surface authorized by v10.

## Phase boundary

The verdict totals are one package-generated case, three typed-role-absent
cases, one Independent case, and zero pending cases. These are verdict counts,
not historical family counts.

No ordinary-family token, Stage-1 or Step-8 family count, counts-as-outputs
regression, F-Q2 result, Agent A handoff, E-5/F1 result, bridge output, fork,
or halt/continuation claim was issued. The next lawful action is E-2b
create-new, consuming this certificate: mint the justified ordinary tokens,
run counts-as-outputs, and evaluate F-Q2 against the sealed record.

## Verification

```text
cargo test -p pen-search completed_basis_membership -- --nocapture
cargo run -q -p pen-search --example schema2_completed_basis_membership -- replay docs/schema2_completed_basis_membership_verdicts_v1.json
```

Three focused tests pass, including mutations that forge either direction of
membership or execute E-2b prematurely. Existing unrelated `pen-search`
warnings remain unchanged.
