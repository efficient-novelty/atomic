# Autonomous Genesis: Generative-Capacity Plan and Status

Decision date: 2026-08-03

Last updated: 2026-08-04

Repository: `pen-atomic`

Status: **A4-R2d QG2, QG3-S1a/D1 through QG3-S1b3, the QG3 preservation
kernel, and QG3-S1c0 through QG3-S1c3 are closed at their stated
representation boundaries; QG3-S1c4 action lineage, footprint agreement, and
support packaging are the active frontier.**

Project decision: **Semantic-novelty work is on hold. All remaining
mechanization and experimentation will focus on generative capacity.**

This document is the forward execution plan. Detailed milestone history remains
in the phase records, crate READMEs, tests, and Git history; it is intentionally
not repeated here.

## 1. Binding project decision

The project will focus its remaining mechanization and experimentation on the
**generative-capacity interpretation of novelty**: whether a guarded act creates
new, reusable public capabilities for forming, transforming, comparing,
compiling, or discharging later obligations.

Work on **semantic novelty** is on hold. Until an explicit future project
decision resumes it, the project will not:

- extend the semantic-family novelty quotient `Sem`;
- seek positive semantic SR2 provenance or a numerical `nu`;
- mechanize the remaining nonempty semantic demand-orbit or realization cases;
- build new semantic-novelty transcript suites, fixtures, profile gates, or
  experiment readouts;
- use `nu`, semantic SR2, or a semantic-novelty proxy in admission, selection,
  ranking, halting, or profile comparison; or
- enlarge the semantic receipt codomain to evade the existing negative result.

Existing semantic-novelty results remain valid frozen evidence. In particular,
the exact negative SR2 theorem is retained; it is not weakened, reinterpreted,
or reported as `nu = 0`.

This hold does **not** prohibit semantic typing, normalization, equality,
quotient reconstruction, or subject-path work required to construct and verify
the generative-capacity authority. In active work, “semantic” may describe the
meaning of a typed object; “semantic novelty” means specifically the deferred
`Sem`/SR2/`nu` diagnostic.

Terminology for the forward path:

- unqualified **novelty** means generative novelty;
- its carrier is `GCap`;
- its marginal cardinality is `gamma`; and
- `nu` appears only when recording the frozen separation theorem or the hold.

## 2. Mission and theorem target

The long-term mission remains to reverse engineer known physics toward the most
likely first principles while preserving the two-law architecture:

1. a constitutive law determines which acts are admissible; and
2. a selective law determines which admissible acts actualize.

The first defensible autonomous target is blind, trace-independent discovery
over a fixed anonymous basis. The engine must:

1. validate the registered Law V2A bootstrap;
2. compile every positive activation from the sealed public grammar;
3. classify present derivability as witnessed derivable, certified
   underivable, or `Unknown`;
4. form live debt only from certified-underivable activations;
5. enumerate the complete finite set of constitutively admissible total
   dischargers in the declared fragment;
6. quotient them by a frozen, target-neutral equivalence;
7. retain every surviving class, preserving branching; and
8. halt only after a complete activation census proves that all activations are
   already derivable, with no unresolved `Unknown` disposition.

Names, expected structures, expected counts, the historical length fifteen,
future-trace viability, target hashes, and presentation-order tie-breakers must
remain outside generation, acceptance, quotienting, selection, and halting.
Recognizing a sealed anonymous output as familiar mathematics or physics is a
held-out decoding step, never an input to the engine.

## 3. Formal novelty separation retained as a lesson

The earlier plan incorrectly treated semantic novelty and generative capacity
as ordered measurements. They are independent finite quotients:

```text
Sem(H,A)  = RawSem(H,A)  / ~sem
GCap(H,A) = RawGCap(H,A) / ~gcap
```

`nu` counts a semantic marginal; `gamma` counts a generative-capability
marginal. No map, inequality, fallback, or substitution between them is
licensed without a future theorem.

The frozen semantic audit found that both registered vectors fail the exact
typed, role-preserving SR2 injection. Therefore positive semantic provenance is
unavailable and `nu` is undefined on those vectors. This is a precise theorem
about `Sem`; it says nothing about `GCap` or `gamma`.

The active quantity is now defined only from the independently constructed
generative lane. If `W_H` is the verified weakening image from the predecessor
quotient, then:

```text
MarginalGCap(H,A) = GCap(H,A) \ W_H
gamma(H,A)        = |MarginalGCap(H,A)|
```

An act may add a reusable integration capability without adding a semantic
distinction. Conversely, a new distinction need not create a reusable public
capability. The project decision is to mechanize and experiment with the first
question.

## 4. Non-negotiable boundaries

### 4.1 Oracle and held-out evidence

- The archived Genesis sequence is regression evidence only.
- No production dependency may observe historical names, expected stages,
  expected cardinalities, decoded physical interpretations, or future results.
- Profile hypotheses must be motivated, reviewed, and frozen before live
  Genesis exposure.
- Divergence from the historical sequence is a mathematical result, not an
  automatic software defect.

### 4.2 Authority and evidence

- A digest, Boolean, tag, count, caller assertion, or matching type name is not
  authority.
- Exact bytes must be compared before their digest is recorded.
- Caller-selected roots, capabilities, occurrences, substitutions, receipts,
  or constructor tags may not define a supposedly complete carrier.
- `Unknown` is the required outcome when evidence is incomplete or resources
  fail; it is not permission to guess.
- No incomplete subgate may issue live `gamma`, selection, or bootstrap
  authority.

### 4.3 Finite evidence versus universal law

- Particular checked substitutions do not prove substitution laws.
- A finite occurrence census does not prove generic naturality.
- Universal identity, composition, lifting, preservation, functor, selector,
  and constructor-square laws belong to the generic theorem package.
- Fixed-resource Rust replay proves only successful instances; resource
  exhaustion mints no fact.

### 4.4 History and provenance

- Complete history means complete through an opaque producer-designated
  finalized head, never globally latest or universal EOF.
- Caller events, counts, boundaries, roots, or digests cannot establish history
  completeness.
- Generative provenance uses only the frozen JG1 receipt codomain; semantic SR2
  receipts may not be reused.
- Complete local support is weaker than a globally injective provenance
  assignment; both must be proved separately.

### 4.5 Isolation and versioning

- Evidence-bound root manifests and lockfiles must not be mutated by
  experimental dependencies.
- Generative experiments remain in isolated workspaces.
- Protected wire, outcome, resource, or profile changes require explicit
  versioning, canonical fixtures, layer-specific mutants, and independent
  replay.

## 5. Current status

### 5.1 Discharged foundations

The following are stable prerequisites and require no further narrative in this
plan:

| Foundation | Status relevant to the forward path |
| --- | --- |
| Canonical Rust/safe-Agda bridge, semantic replay, typing bridge, inventory correspondence, independent transcript agreement, and private minting factory | Discharged |
| Native carrier, root inventory, canonical subject bundle, and typed-occurrence census | Discharged |
| Typed rewrite authority and the completed existing semantic-audit artifacts | Frozen reference evidence; not an active dependency or backlog |
| JG1 seven-role grammar and eight admission obligations | Discharged |
| JG2a constructor grammar and exact stage surface | Discharged |
| JG2b1a relative contiguous chain and declaration births | Discharged |
| JG2b1b0 sealed-log protocol, JG2b1b1 linear producer, and JG2b1b2 complete-through-head replay | Discharged |
| JG2b2a substitution/naturality protocol grammar | Discharged |
| JG2b2b0 structural occurrence grammar | Discharged |
| JG2b2b1 particular open typed substitution/context-morphism evidence | Discharged |
| JG2b2b2-P0 and A0 adequacy audits; A1 envelope protocol | Discharged/frozen |
| A2-O ordinary nine-sort substrate and A3-O seven constructor schemas | Frozen |
| A4-R1, R2a+C2, R2b, A3-C1, and R2c+C2 | Frozen |

The JG1 roles, in frozen order, are:

```text
Formation
Abstraction
Aggregation
Transport
Comparison
Compiler
DischargeTransformer
```

Admission requires all eight properties: stage genericity; naturality under
every admissible substitution; finite public typed interface; present-grammar
checkability; predecessor absence; successor presence; support by a new paid
clause or prior live generative output; and independence from later or held-out
evidence.

### 5.2 Active frontier: A4-R2d QG3

A4-R2d is the current ordinary-profile frontier.

Closed inputs:

- QG1 semantic mirrors for quotient groups 1–5;
- QG2 entry, receipt-free semantic views, obligation core grammar, and stable
  group rows;
- QG2-O2a group-2 carrier compiler with counts `[5,8,8,7,11,9,13]`;
- QG2-O2b group-3 constructor programs with counts
  `[0,0,0,2,0,3m_5+3,15+p_6+6m_6]`;
- QG2-O2c group-5 primary-route census with counts
  `[20,21,21,21,20,21,25+h_6]`, including byte-identical route vectors in
  group-5 rows 5–7; and
- QG2 item 2's complete unrooted `QuotientObligationsCandidateV1` compiler,
  with all eight groups present, checked group-local/global prefix sums, exact
  symbolic target schemas and support paths, preserved repetitions, closed
  empty-group behavior, fail-closed invalid-pair handling, and total counts
  `[d_0+34,d_1+37,d_2+38,d_3+42,d_4+42,d_5+41+3m_5,
  d_6+60+p_6+6m_6]`; and
- QG2 item 3's complete unrooted ten-role quotient source bundle, explicit
  punctured admission descriptor, exactly two bundle-local admission copies,
  one-domain symbolic R2c occurrence compiler, closed 16-role translation
  table, and exact-on-primary-image quotient path translator; and
- QG2 item 4's exact quotient-primary 8/11 subject compiler, with fresh
  TypeId-free binder/action/literal/node schemas, nine non-aliasing semantic
  output roles, neutral receipt-free square subjects retaining five endpoints,
  four adjacent propositions, and the end-to-end naturality subject, exact
  binder/value/clause arithmetic, a one-domain route scan, the full
  `5*PR_c` route-copy partition, and exactly two transitive admission values;
  and
- QG3-S1a's complete R2c support/disposition field inventory, seven-root
  quotient-support source boundary, fresh semantic graph/coverage ABI, and
  closed payload-free `Primitive` field-disposition compiler; and
- QG3-S1b1's provisional direct/dereferenced/primary-project support locus,
  canonical TypeId-free nominal-schema generator, exact smallest-manifest
  rule, nine-case expected-judgment surface, and explicit rejection of R2c
  presentation-only reconstruction/normalization provenance; and
- QG3-S1b2's append-only `PrimaryProjectGrammar=3` locus correction, exact
  seven-root pass-A traversal, field-term prefix adjudication, total ambient-
  pack and origin co-evaluators, complete dependency/grammar/operation
  companion-path census, finite referent/owner recipe algebra, full local edge
  image `1..21`, exact seventeen-node project-wide candidate `M_nf`, and
  deterministic pass-B lowering without numerical `TypeIdV1`; and
- QG3-S1b3's exact candidate-only binder/referent/owner/key sums, footprint-
  erased primary resolver, Q1-bound private history capability, typed semantic
  preflight, direct referent/owner execution, deterministic `Cl_b` closure
  suffix, sorted classes, seven-root coverage, projection, independently
  recomputed footprint agreement, and representative-independence theorem; and
- the QG3 preservation kernel's fresh route type and independent route census,
  one minimal contextual seed per subject, factored endpoint modes, exact
  reflexive quotient-normal-form meaning of `NormalizedSeed`, fresh
  clause wire, three-row template, unchanged QG2 `5*PR_c` census, separate
  QG3 `3*PR_c` census, and zero-new-admission theorem; and
- QG3-S1c0's correction of formal ownership from the preservation binder to
  the canonical instantiated naturality subject, exact empty/nonempty subject
  option law, standalone arbitrary-action binder at local ordinal 0, semantic
  binder anchor, one-per-primitive root-6 path/edge-28 spine, square-primitive
  atomicity, formal-footprint exclusion, and exact two-node primitive schema
  delta; and
- QG3-S1c1's fresh total-action component ABI, right-associated substitution
  suffix comb and total-action lifts, left-fold square vertical composites,
  endpoint-minimal canonical pasted epsilon, completed standalone naturality
  program/syntax with exact parametric node counts, independent source-path
  census, semantic one-per-primitive occurrence blocks, and the total/disjoint
  R2e-checkable representative-formal-ID erasure fibers. The candidate
  seventeen-node manifest remains unchanged; at the S1c1 boundary the
  nineteen-node target was proved through the subject/edge-28 prefix and left
  conditional only on the edge-27/action traversal; and
- QG3-S1c2's combined seven-root action stream, fresh instantiated-subject
  virtual loci, action-only provenance product, exact ordinary root-6
  descendant census, paired provenance-only `ActionIntroduced=27` overlay,
  class-neutral companion theorem, exact counts, complete action schema
  universe, generated nineteen-node shared symbolic enum, and total injective
  order-preserving symbolic candidate embedding; and
- QG3-S1c3's flat seven-case shared locus, contiguous ten-case judgment,
  fresh formal-action occurrence and formal-binder locators, exact shared
  binder/referent/location/owner/key sums, `Current < Older < Structural <
  FormalAction` comparator, shared graph ABI, total order-preserving candidate
  graph embedding, action pass-B/resolution/closure/class/projection compiler,
  concrete shared projection sort, and exact three-subject typing/denotation.
  Its truth audit proves that the current `Full` reindexing equality is
  uninhabited for every nonempty action because edge 28 forces distinct formal
  keys absent from the candidate endpoint. The subject is typed proposition
  syntax, not a proved preservation law.

Active deliverable:

> Independently replay the frozen paired native/edge-27 event census to derive
> candidate-empty and action-nonempty `ActionIntroduced` lineage. Recompute
> implementation, prior-support, and current-output footprints from the shared
> graph and the exact reindexed output-lineage slots, using stored footprints
> only as final equality targets. Then package the candidate/action group-6
> support around the shared graph, coverage/projection, footprint agreement,
> and three typed preservation descriptors. Preserve QG2, candidate `M_nf`,
> S1b3, the preservation kernel, and S1c0--S1c3 byte-for-byte. Admit no support-
> to-lineage or footprint-to-owner back-edge, representative, copied raw/
> source/owner/class ordinal, receipt, R3 payload, semantic SR2 result, or
> `nu`.

Not yet materialized:

- QG3 action-introduced lineage, independently recomputed action footprint
  agreement, and the final candidate/action group-6 support package;
- the final shared-typed placement of the instantiated action subject;
- the shared candidate/action eight-group projection;
- root `0xfd`;
- final numerical `TypeIdV1` assignments;
- executable JG2b2b2 authority; and
- a provable transported/equivariant replacement for the currently unproved
  full-projection renaming and reindexing proposition descriptors. That theory
  corrigendum is mandatory before JG2b2b3 can mint universal authority, but it
  is not an A4 wire-construction input.

### 5.3 Blocking chain

The critical path is:

```text
R2d QG3 support/action mirrors
  -> R2d roots and runtime closure
  -> A4-R3 failure/resource machine
  -> A4-R2e registry/source join
  -> A4-R4 fixtures and mutations
  -> JG2b2b2b executable ordinary calculus
  -> preservation-subject theory corrigendum and exact transported law
  -> JG2b2b3a generic substitution theorem and Rust/Agda binding
  -> JG2b2b3b nine functor laws
  -> JG2b2b3c seven constructor squares and combined authority
  -> JG2b2c complete occurrence/classifier census
  -> JG2b3 birth-local typed support
  -> JG2c complete RawGCap carrier
  -> JG3 -> JG4 -> JG5 -> JG6 -> JG7 -> JG8 -> JG9
  -> frozen generative-profile experiments
```

No live `gamma` or generative selective-profile authority exists yet.

## 6. Ordered forward plan

### 6.1 Close A4-R2d and the ordinary representation

1. **QG2 item 2 — closed.** The complete eight-group unrooted obligation value
   is materialized at the compiler-plan level, with exact row counts, checked
   prefix sums, support paths, symbolic result schemas, preserved repetitions,
   empty groups, invalid-pair aborts, and representative independence audited.
2. **QG2 item 3 — closed.** The complete ten-role source bundle, bundle-local
   two-copy invariant, symbolic R2c primary occurrence image, paired route
   table, and exact-on-image quotient translator are closed at compiler-plan
   level. Numerical TypeId lowering and physical representative-byte comparison
   remain deferred to R2e/R4.
3. **QG2 item 4 — closed.** The exact 8/11 quotient-primary subject families,
   formal action components, independent nominal/implementation output roles,
   five-endpoint square subjects, arithmetic, locators, `5*PR_c` route-copy
   partition, and full transitive two-copy admission invariant are closed at
   compiler-plan level. Numerical TypeId lowering and representative-byte path
   comparison remain deferred to R2e/R4.
4. **QG3 — active; S1a/D1 through S1b3, the preservation kernel, and
   S1c0--S1c3 are closed at their stated representation boundaries.** The
   exact R2c projection inventory, seven semantic roots, graph/coverage ABI,
   payload-free ordinary
   `Primitive` compiler, corrected four-way virtual-locus ABI, canonical
   symbolic-schema generator, no-formal expected-judgment surface, exact typed
   traversal and total ambient/origin compilers, companion census, generated
   seventeen-node candidate manifest, no-formal candidate graph, fresh
   preservation route/seed factorization, reflexive `NormalizedSeed`
   semantics, generic three-clause wire, corrected instantiated-subject owner,
   semantic action-binder anchor, exact root-6 primitive spine, total-action
   component forest, completed `Some` subject, primitive occurrence loci,
   combined action pass-A image, paired `ActionIntroduced=27` dispatcher,
   nineteen-node shared symbolic manifest, and symbolic candidate embedding
   are fixed. S1c3 additionally fixes the final shared support sums, graph ABI
   and pure core, candidate embedding, total action projection compiler, and
   concrete typing and denotation of the three preservation-subject
   descriptors. It proves no
   preservation theorem; its exact nonempty-action formal-key obstruction is a
   later theory-corrigendum gate. The active cut closes action-introduced
   lineage, footprint agreement, and the candidate/action support package.
   The remaining ordered QG3 cut then closes the shared eight-group shape.
   This is GCap infrastructure, not semantic-novelty measurement.
5. **R2d roots/runtime.** Close quotient projection and keys, checked outcomes
   and classes, coverage/dispositions, base-relative action rebuild, action
   trace/image, roots `0xfd`, `0xe7`–`0xe9`, `0xfe`, `0xff`, joined root `0xe3`,
   and slots 9–11.
6. **R3.** Freeze the exact failure, resource, and meter machine.
7. **R2e.** Assign the dynamic registry TypeIds and close the implementation-
   source join.
8. **R4.** Regenerate canonical fixtures and layer-specific mutations; require
   deterministic reminting and exact replay before release.

Exit criterion: A4 must be definition-closed, canonically encoded, resource-
bounded, mutation-tested, independently replayed, and still mint no universal
substitution or naturality theorem by itself.

### 6.2 Complete the generic indexed-interface theorem package

1. **JG2b2b2b.** After R4, implement the closed executable Rust grammar and
   particular fail-closed operations, beginning with aggregate kernel metering
   and receipt accounting. Bounded success is particular evidence only.
2. **Preservation-subject adjudication.** Before any universal proof consumes
   A4's retained proposition descriptors, freeze a provable transported law.
   Renaming must compare `h_*(Pi(B))` with `Pi(Rename_Q(B,h))`; action must
   compare an independently generated pre-quotient action transform
   `q_*(Pi(B))` with `Pi(QAction(B,q))`, including the mandatory formal-event
   delta. Candidate-key filtering and bare projection inclusion are invalid:
   action rebuilding may change, merge, remove, and add classes. If the exact
   theorem must be retained in the A4 wire, version the endpoint grammar and
   rerun the affected A3/R2c/R2d/R2e/R4 closures; otherwise bind the universal
   authority to a separate versioned theorem schema and treat the A4 equality
   descriptors only as unproved historical conjecture syntax.
3. **JG2b2b3a.** Prove capture-safe substitution, identity, composition, binder
   lifting, typing preservation, typed-equality preservation, and
   normalization/reindexing compatibility, then bind the proof to the exact
   Rust representation through safe Agda.
4. **JG2b2b3b.** Prove identity and composition for all nine indexed
   reindexing actions, including both whiskering actions of the bi-indexed
   substitution carrier.
5. **JG2b2b3c.** Prove selector totality/uniqueness and stability plus all seven
   constructor naturality squares. Mint combined universal authority only when
   every preceding grammar, proof, and representation identity agrees.

Exit criterion: the package proves universal laws over formal well-typed
derivations while keeping fixed-resource executable replays conditional and
fail-closed.

### 6.3 Derive the complete raw generative carrier

1. **JG2b2c.** Consume the opaque complete-through-head history and internally
   enumerate every birth occurrence. Derive exactly one typed disposition for
   every occurrence/constructor pair, prove exact `occurrence_count * 7`
   coverage, and aggregate each occurrence to one unique match, zero matches,
   or typed ambiguity.
2. **JG2b3.** Derive predecessor/successor closure at each member’s own birth
   and complete typed support from only a newly paid clause or a prior live
   generative-requirement output.
3. **JG2c.** Enumerate every public-surface/constructor tuple and retain exactly
   one admitted raw derivation or typed certified-negative disposition. A
   completely covered empty carrier is valid; incomplete coverage aborts.

`RawGCap(H,A)` is complete only when:

- no caller-selected capability, occurrence, substitution, schema, role, or
  expected count enters construction;
- every positive classifier row retains verifier-minted evidence binding its
  complete normalized implementation and local typed theorem instance;
- all seven constructor dispositions exist for every public occurrence;
- exact history-bound identity coexists with structural renaming and
  presentation invariance;
- unsupported, mistyped, predecessor-present, post-sealing-absent, duplicated,
  omitted, or oracle-dependent proposals fail closed; and
- the resulting opaque authority exposes no quotient, marginal, provenance,
  `gamma`, bootstrap, or selection authority.

Only then may JG3 become active.

### 6.4 Derive authoritative `gamma`

1. **JG3 — generative quotient.** Freeze and decide `~gcap`; quotient the exact
   raw carrier with complete coverage.
2. **JG4 — weakening/restriction.** Reconstruct the predecessor carrier and
   quotient independently; prove the successor embedding and inverse on its
   image.
3. **JG5 — marginal.** Derive the exact complement of the weakening image and
   define `gamma` as its finite cardinality.
4. **JG6 — strict enlargement.** Prove `gamma > 0` exactly when the marginal is
   nonempty, invariant under renaming, representatives, permutations, and
   presentation.
5. **JG7 — generative provenance.** Inject every marginal capability into the
   exact JG1 receipt codomain:

   ```text
   (NewPaidClause × GenerativeCapabilityRole)
     ⊎ PriorLiveGenerativeRequirementOutput
   ```

   Bind the carrier, quotient, weakening, marginal, and typed support. The
   generic bound is `7|K| + |GenReq|`. No semantic SR2 receipt is admissible.
6. **JG8 — Rust/safe-Agda correspondence.** Canonically encode and independently
   replay JG1–JG7, prove byte-identical transcripts, and expose only opaque
   generative authority.
7. **JG9 — bootstrap.** Independently derive the branch-local `(kappa,gamma)`
   registers for the two most recent positive-cost acts required by the first
   acceleration comparison. Values may not be copied from the historical
   ledger or inferred from `nu`.

Until each gate is discharged, `gamma` is `Unknown`.

### 6.5 Freeze and run generative-capacity experiments

The registered two-by-three programme remains the intended experiment:

```text
{C0,C1} × {S0,S+,S↑}
```

- `C0` is the existing constitutive profile.
- `C1` is the target-neutral contextual-internalization hypothesis.
- `S0` accepts every total demand-connected discharger.
- `S+` additionally requires `gamma > 0`.
- `S↑` additionally requires
  `gamma/kappa > Bar_gamma`, using only the independently certified branch-local
  two-positive-cost-act baseline.

The profiles are gates, never ranking rules: every inequivalent discharger that
passes the selected profile survives.

For continuity with the phase records: K0 freezes `C1`; K1 freezes the three
selective manifests; K2 runs the common six-profile factorial; and K3 is any
follow-on autonomous research justified by the sealed results.

Before execution:

1. finish the target-neutral `C1` demand schema, trigger, least-universal-
   interface statement, fragment decision, adversarial review, and pre-exposure
   freeze;
2. freeze separate `S0`, `S+`, and `S↑` manifests with fail-closed `Unknown`
   behavior for missing `gamma` or JG9 bootstrap authority;
3. freeze the common resource contract, outcome taxonomy, canonical manifest,
   generic tests, and falsifiers; and
4. seal all profile definitions before inspecting Genesis outputs.

The anticipated identification of `C1` dischargers with dependent sum and
product remains a held-out prediction. It may not influence compilation,
generation, acceptance, quotienting, or halting.

During the semantic-novelty hold, experiments will neither require nor report
`nu`. Comparisons will concern generative capability, cost, branching, halt,
resource behavior, and held-out post-sealing interpretation only.

### 6.6 Conditional cubical generative-capacity lane

A2-C is not on the ordinary-profile critical path. It may resume only as a
separately versioned generative-capacity ontology lane when a frozen
constitutive profile requires cubical carriers. Before it can feed any carrier
or classifier it must close all nine carriers, actions, canonical encodings,
finite presentation universes, Level-II checkers, and adequacy/preservation/
reflection fields. Opaque cubical globals and kernel definitional equality are
not substitutes for checked cubical semantics.

This conditional lane may extend `GCap` coverage; it may not restart semantic-
novelty measurement or alter the ordinary profile after observing results.

## 7. Retained lessons that govern future work

1. **`Unknown` is successful safety.** It identifies the missing proof or
   resource without manufacturing authority.
2. **The profile determines the sequence.** A short halt under a narrow profile
   cannot justify retrospective broadening. New constitutive structure needs
   independent motivation and a pre-result freeze.
3. **Semantic novelty and generative capacity are independent.** A negative or
   undefined `nu` has no conclusion about `gamma`.
4. **Support is not injective provenance.** Local support for every member does
   not produce a global receipt injection.
5. **Finite replay is not a universal theorem.** Derivation-local instances and
   generic algebraic laws require different authorities.
6. **Completeness is relative to a verified boundary.** A producer-finalized
   head can delimit a complete finite history; caller EOF/count/digest cannot.
7. **Correspondence requires shared bytes and meaning.** Similar Rust and Agda
   datatypes, tests, or hashes do not prove agreement.
8. **Soundness is not domain completeness.** “Every accepted certificate is
   sound” does not prove that all required subjects were included.
9. **Stability is one-step commutation.** Saturating a finite universe under
   open substitutions is usually the wrong theorem.
10. **Convertible is not exact.** Normal-form equality cannot replace exact
    identity where a protocol binds a declared type, path, field order, or
    digest.
11. **Negative classifications are completeness claims.** Every new input
    family must re-audit the checker’s full failure taxonomy.
12. **Formal executability shapes proof design.** Bounded normalization,
    checked arithmetic, explicit fuel, and fallible allocation are part of the
    theorem boundary.
13. **Each verification layer needs discriminating mutants.** New layers need
    fixtures that earlier layers accept and only the new claim rejects.
14. **Evidence-bound workspaces stay isolated.** Dependency drift must never
    silently alter previously issued evidence.

## 8. Semantic-novelty hold register

### 8.1 Frozen facts to preserve

- The existing `Sem` quotient and its completed supporting artifacts remain
  historical verified results.
- Both registered vectors fail the frozen typed, role-preserving semantic SR2
  injection.
- Failed provenance means `nu` is undefined, not zero.
- No theorem relates `nu` and `gamma`.
- Semantic receipts cannot be reused as generative receipts.

### 8.2 Deferred work

The following work is intentionally removed from the active backlog:

- nonempty semantic demand-orbit authority;
- nonempty typed semantic realization authority;
- positive semantic SR2 provenance;
- general semantic-novelty transcript agreement and external audit freeze;
- `nu` sidecar APIs, profile fixtures, quadrant experiments, dashboards, or
  selection integration; and
- reinterpretation of historical stages through a numerical `nu`.

Cost work required independently for `kappa` or `S↑` remains in scope, but it
must be formulated without reviving `nu` or semantic SR2.

### 8.3 Maintenance policy during the hold

Existing semantic-novelty artifacts may be read, regression-tested, or minimally
repaired to preserve build and evidence integrity. Such maintenance must not
expand their authority, add a new semantic-novelty milestone, or enter the
critical path.

Resumption requires a new explicit project decision, a stated scientific reason,
a clean authority boundary from `GCap`, and a revised pre-exposure plan. It may
not occur merely because a future generative result is surprising.

## 9. Canonical forward artifacts

The detailed contracts live in these records:

- `docs/260730_architecture.md` — repository architecture and dependency lanes;
- `docs/260802_jg2b2b2a_a4_r2d_quotient_outcome_coverage_action_schemas.md` —
  active A4-R2d schema and compiler frontier;
- `docs/260802_jg2b2b2a_a4_r2c_envelope_evidence_support_subject_schemas.md` —
  frozen envelope/evidence/support/subject schemas;
- `docs/260802_jg2b2b2a_a3_c1_expected_judgment_corrigendum.md` — frozen
  expected-judgment correction;
- `docs/260802_jg2b2b2a_a4_r2a_acyclic_schema_foundation.md` and
  `docs/260802_jg2b2b2a_a4_r2b_level_relation_schemas.md` — frozen A4 schema
  foundations;
- `docs/260802_jg2b2b2a_adequacy_and_envelope_protocol.md` — adequacy boundary
  and common proof-carrying envelope;
- `docs/260802_jg2b2b2a_indexed_ontology_profiles.md` and
  `docs/260802_jg2b2b2a_a3_ordinary_constructor_schemas.md` — ordinary indexed
  substrate and seven constructors;
- `docs/260801_jg1_pre_exposure_generative_capability_grammar.md` — JG1 grammar;
- `docs/260801_jg2a_constructor_and_stage_surface.md` — JG2a constructor/stage
  surface;
- `docs/260801_jg2b1b0_target_neutral_sealed_log_protocol.md`,
  `docs/260801_jg2b1b1_process_local_linear_append_producer.md`, and
  `docs/260801_jg2b1b2_complete_through_head_replay.md` — sealed-history chain;
- `docs/260801_jg2b2a_substitution_naturality_protocol.md`,
  `docs/260802_jg2b2b0_structural_occurrence_grammar.md`, and
  `docs/260802_jg2b2b1_particular_open_typed_substitution.md` — substitution and
  occurrence boundaries;
- `docs/260730_contextual_internalization_frontier.md` — `C1` research frontier;
- `docs/experimental_program_addition.md` — registered factorial programme; and
- `C:/DEV/book/chapters/ch02_two_law_architecture.tex`,
  `C:/DEV/book/chapters/ch03_constitutive_law.tex`, and
  `C:/DEV/book/chapters/ch04_selective_law.tex` — current theoretical
  hypotheses.

Detailed completion evidence belongs in those artifacts and their tests. This
plan records only status, dependencies, decisions, exit criteria, and the next
action.

## 10. Immediate execution packet

The next autonomous work item is **A4-R2d QG3-S1c4: action-introduced
lineage, footprint agreement, and candidate/action support packaging**.

Closed in QG3-S1a:

- the complete `OrdinarySupportV1`/`SupportCoverageV1`/
  `SupportProjectionV1`/`PublicFieldDispositionsV1` field inventory;
- the exact seven quotient-support roots
  `CanonicalPayload`, `IndexedInputs`, `IndexedOutput`,
  `PublicFields`, `Obligations`, `PrimarySubjects`, and
  `ActionArguments`;
- the fresh dependency/owner/class/coverage/projection outer ABI; and
- the ordered field-bound, payload-free `Primitive` disposition compiler,
  with every R2c certificate, transcript, replay, receipt, and resource field
  excluded.

Closed in QG3-S1b1:

- the provisional support path ABI at tags `0..2`, with `Direct`,
  `Dereferenced`, and `PrimaryProject` preserving physical descriptor
  occurrences and repeated reached values;
- the three exact schema-anchor families, five-step TypeId-free nominal-schema
  locator, canonicalization rule, and smallest no-formal manifest equation;
- the generated-finite-sum discipline for
  `QuotientSupportSymbolicTypePlanCandidateV1`, whose exact table is generated
  and frozen with S1b2 rather than admitted as an open locator;
- the exact nine admitted expected-judgment cases at tags `0..6,8,9`;
- the corresponding eventual contiguous ten-way wire shape, whose payloadful
  tag 7 has no admitted quotient-support value; and
- the empty no-formal direct image of R2c `SourceLeaf`, presentation-only
  `RecordReconstruction`, and `NormalizationStep` provenance.

Closed in QG3-S1b2:

- the exact value-preorder traversal of all seven roots, including root-4
  physical predicate plus virtual-source walks, root-5 literal/project source
  walks without source-bundle descent, and the empty same-tag root-6 identity;
- the append-only `PrimaryProjectGrammar=3` companion locus required by a
  projected, possibly reindexed Demand live value, with the old locus tags and
  all seventeen manifest nodes unchanged;
- the explicit prefix rule locating `TelescopePrefix` at the selected
  `Field(i).Type : Term` while retaining the full context and absolute boundary
  in `ContextPrefix`;
- the total compiler-only ambient algebra for every root-0--5 direct,
  dereferenced, and projected route, including exact contexts, section bases
  and targets, full carrier ordinals, grammar companions, and binder-aware term
  child frames;
- the complete compiler-only `Dependency`, `GrammarCompanion`, and
  `OperationRecordCompanion` path roles plus finite referent/owner recipes,
  total route-to-origin co-evaluation, `QFootprints` excluded, and no path left
  for S1b3 to invent;
- the exact project-wide candidate manifest
  `QuotientSupportSymbolicTypePlanCandidateV1` with `m=17`, fixed tags `0..16`,
  and no context/telescope/OpenJudgment wrapper variants; and
- deterministic pass-B lowering of every direct incidence and nested path,
  preserving the exact direct edge image `1..21`, repetitions, root order, and
  local semantic order without any numerical `TypeIdV1`.

Closed in QG3-S1b3:

- the exact candidate-only binder, referent, current-location, owner, and key
  sums, with no reserved formal or structural-binder placeholder;
- an identity-bound, nonencoded private-history capability and a footprint-
  erased primary resolver/preflight, with `QFootprints` confined to the final
  equality check;
- total direct referent and owner execution over the immutable S1b2 stream,
  including capture-safe binder provenance and decorated contract-set
  canonicalization;
- the deterministic `Cl_b` suffix, canonical candidate comparator
  `Current < Older < Structural`, classes, seven-root coverage, and projection;
  and
- independently recomputed current, older, and output footprints plus the
  complete representative-independence theorem. The later shared comparator
  must extend this candidate order through an injective order-preserving
  embedding.

Closed in the QG3 preservation kernel:

- a fresh `QuotientSupportPreservationSourcePathPlanCandidateV1` and an
  independently compiled route census that relabels one-for-one to the frozen
  QG2 primary census without reading or copying its stored path values;
- one subject-local seed containing only the evaluation identity and fresh
  endpoint-path vector, with constructor/action/graph state kept in nonencoded
  dependent compiler context and no source bundle or admission back-edge;
- four payload-free-or-binder-only endpoint modes, one physical seed per
  subject, and the exact three-row binder/mode template with two role-distinct
  literal nodes and one equality root;
- `NormalizedSeed` as a distinct mode interpreted through the identity
  normal-form eliminator on already-normalized quotient context, not a second
  quotient endpoint, a projection-byte normalization, or a tag alias;
- a parametric `SupportProjectionSort` over the future shared action-capable
  key schema and injective order-preserving candidate embedding; and
- QG2's exact `5*PR_c` path census and two admission values unchanged, plus a
  separate exact `3*PR_c` fresh-QG3 path census and zero new admission values.

Closed in QG3-S1c0:

- the source-level correction that the preservation reindexing binder is only
  a quantified preservation parameter and cannot own action support;
- `None` exactly for candidate/total identity and one `Some` skeleton exactly
  for a nonempty total action, with identity append preserving an existing
  `Some`;
- one standalone `Naturality=7` program skeleton with one
  `FormalAction(a_c,ArbitraryNormalized,SourceCandidate)` binder at local
  ordinal 0 and a semantic binder-declaration anchor rooted in that subject;
- contextual use of the existing base QG2 source environment, with the
  representative's repeated copy checked by R2e and erased rather than copied
  into the quotient action subject or duplicating its two admission values;
- exact root-6 paths and one leading `ActionArgument=28` incidence per complete
  normalized primitive, with one square record—not its three substitution
  fields—counted as one action argument; and
- exclusion of formal owners from `Cl_b` and all history footprints, plus the
  exact primitive-spine schema delta
  `{NormalizedSubstitution,QuotientNormalizedContextSquarePrimitive}`. The
  nineteen-node merge was deliberately left conditional there and is
  discharged by S1c2's complete action traversal.

Closed in QG3-S1c1:

- a fresh instantiated-action source/component/literal/node ABI with one
  `ArbitraryNormalized` binder and no QG2 or preservation-path alias;
- the exact right-associated substitution suffix-composite forest and one
  total-action lift for Abstraction, Aggregation, and DemandCompiler;
- square primitive-field blocks in `rho/sigma/theta_prime` order, explicit
  left-fold vertical composites, one final receipt-free pasted epsilon, final
  `LC`/`RC`, constructor-specific reindexed components, and one congruence
  endpoint, with every intermediate paste trace erased;
- the frozen ten-node endpoint suffix and parametric prefix/value/full/root
  counts, specializing exactly to QG2 slot 7 when `n=1`;
- independent program source-path counts `[2,3,3,3,2,3,7+h]`, zero new
  admission values, and byte-identical program/syntax prefix cross-checks; and
- one fresh semantic `CanonicalPrimitive(i)` occurrence per root-6 argument,
  resolving to program block `[i,i+1)` for substitutions or `[3i,3i+3)` for
  squares, with a total, pairwise-disjoint, fail-closed R2e-owned erasure
  contract from the representative `FormalActionIdV1`. The subject plus edge-28
  prefix adds only
  `{T_sub,T_sq}` beyond
  `U_nf`; at this cut the final nineteen-node manifest remained conditional on
  S1c2's `ActionIntroduced=27` and remaining descendant census.

Closed in QG3-S1c2:

- one combined seven-root action stream with the action-rebuilt roots `0..4`,
  byte-identical base primary prefix, standalone instantiated-action program
  suffix, and complete root-6 primitive vector in a fixed order;
- fresh compiler-only `ActionDereferenced`, `ActionProject`, and
  `ActionProjectGrammar` loci plus a typed pass-A coproduct with the frozen
  candidate locus, with no eighth root, candidate-locus mutation, or syntax-
  copy rescan;
- a representative-free action provenance product with inherited base frames,
  one canonical identity for every shared action boundary, distinct formal
  boundary/body binder referents, and one enclosing `CanonicalPrimitive(i)`
  owner per primitive; concrete globals remain under history resolution;
- the exact native primitive-descendant count
  `D_sub(s)=2*|s.images|+sum TR(s.images[k])`, square-field concatenation, and
  `n` whole-primitive edge-28 rows;
- the fresh R2d decision that every native root-6 descendant row is retained
  and followed by one edge-27 companion copying every field except the edge,
  so `N_27=D_A` and total root-6 direct rows are `n+2D_A`;
- the proof that every companion has the same resolved referent, owner,
  expected judgment, and irreducible key as its native row, hence edge 27 adds
  dependency/coverage/lineage multiplicity but no new class or distinct closure
  seed/occurrence;
- the noncircular boundary that group-4 lineage later replays and projects the
  common paired-event census rather than selecting published support; and
- the unconditional equation
  `U_action=U_nf union {T_sub,T_sq}`, exact nineteen-node
  `QuotientSupportSymbolicTypePlanSharedV1`, and schema-preserving symbolic
  candidate embedding `0->0`, `1..10->+1`, `11..16->+2` without a numerical
  `TypeIdV1`.

Closed in QG3-S1c3 at the representation boundary:

- the flat shared locus with candidate cases byte-stable at tags `0..3` and
  action-subject cases appended at `4..6`, plus exact candidate/action path
  lowering through the frozen nineteen-node manifest;
- the contiguous ten-way expected-judgment wire, including payloadful but
  quotient-uninhabited `NormalizationSubject=7`, and recursive candidate
  embedding of path and symbolic payloads;
- one fresh formal-action ID around the exact
  `CanonicalCompositeNaturalityBinder/CanonicalPrimitive(i)` occurrence,
  shared boundary/body formal-binder locators, and exact shared binder,
  referent, current-location, owner, and key sums with no `FormalGlobal`;
- the shared `Current < Older < Structural < FormalAction` comparator, fresh
  graph ordinals and containers, and a fieldwise candidate graph embedding
  that preserves every ordinal, vector order, class, coverage row, and
  projection position without sorting or deduplication;
- total action pass B, exhaustive inherited/formal/global resolution, native-
  first edge-27 copy/checking, current-owner-only `Cl_b`, shared classes,
  seven-root coverage, and a pure graph/projection compiler with literal
  candidate equality for the total identity action;
- the concrete shared support-projection sort and exact three-subject
  aggregate, with three physical seeds, `3*PR_c` fresh paths, five binder
  declarations, six endpoint modes, six projection literals, three equality
  nodes, zero new admissions, and zero proof values; and
- a truth audit separating typed syntax from theorem authority. Nontrivial
  exact-identifier renaming needs explicit transport, and every nonempty
  action contributes at least one distinct formal key per primitive, so the
  current `Full(ProjectSeed)=Full(ReindexedSeed)` proposition is uninhabited.
  A versioned theory corrigendum -- equivariant transport for renaming and an
  exact restricted, extension, or support-delta relation for nonempty actions
  -- is mandatory before JG2b2b3 may mint universal preservation authority;
  A4 does not assume either law.

Required sequence:

1. **Closed -- S1c1.** Freeze the fresh canonical-total-action component
   compiler, completed one-program `Some` subject, exact nodes/counts/source
   census, and semantic primitive-occurrence loci.
2. **Closed -- S1c2.** Freeze the combined action pass-A census, paired
   `ActionIntroduced=27` overlay, exact descendant/count/schema image, generated
   shared symbolic enum, and symbolic candidate embedding.
3. **Closed -- S1c3.** Close the final shared binder, referent, owner, key,
   path, expected-judgment, class, coverage, and projection sums together with
   the total injective order-preserving embedding of every candidate S1b3
   variant and total lowering of every action pass-A recipe; consume rather
   than regenerate the frozen shared symbolic enum. Then instantiate the
   generic preservation template and close `RenamedSeed` and `ReindexedSeed`
   against the shared projection type as typed, denoted proposition
   descriptors, without claiming equality or proof authority;
4. **Active -- S1c4.** Close candidate-side empty action-introduced lineage and
   action-side lineage/support/footprints rebuilt from the original base and
   total flattened action without support/raw/class-coordinate back-edges;
5. close the shared eight-group schema. Candidate and total identity-action
   projections are byte-identical. For a nonempty action, the only additional
   top-level action-owned fields are lineage and the optional action subject;
   every common position is deterministically rebuilt from the original base
   and may change or contain nested action-only variants;
6. run the second transitive no-R3/no-representative/no-forbidden-raw scan,
   reprove QG2's exactly two admissions and exact `5*PR_c` route partition,
   independently prove the QG3 `3*PR_c` route partition, and freeze fail-closed
   arithmetic, ordering, independence, and action-base binding; and
7. update the architecture/frontier ledgers only after all of QG3 passes, then
   advance to R2d roots/runtime.

“Identity” in item 5 means that the **total** flattened action is empty:

```text
QAction(B,[])      = QCandidate(B)
QAction(B,A ++ []) = QAction(B,A).
```

Thus appending an identity to an existing nonempty image preserves that image;
it does not reset the projection to the base candidate.

QG1, QG2, and S1c3's shared algebra/projection compiler are frozen inputs to
this packet and may not be changed. The three preservation descriptors are
typed proposition syntax only. Their section-10.19.6 truth audit is also
frozen: S1c4 may package the descriptors but may not use either unproved
equality as a compiler fact, class relation, footprint criterion, or authority.
The work must stop before root `0xfd` if the support/disposition projection, candidate/
action shape, or transitive independence audits fail. It must also stop on any
attempt to consume an R3 account, receipt, source/raw ordinal, presentation,
copied TypeId, evidence target, semantic SR2 result, or `nu`.

Fresh quotient semantic-dependency and class ordinals are permitted only as
checked positions in the independently reconstructed graph. The prohibition is
on copying R2c raw/source/owner coordinates or letting any such coordinate
select a quotient value.

R2e must later lower both sides' symbolic endpoint schemas, reconstruct and
byte-compare the physically encoded paths from a supplied validated
representative, and perform the registry/source join. R4 must regenerate the
canonical fixtures, repeat those checks, and mutation-test the closed
translation table. Those later checks are not QG3 inputs and cannot be used to
select its support, disposition, lineage, or action-subject values.

## 11. Definition of success for this plan

This plan succeeds when:

- A4-R2d, R3, R2e, and R4 close the ordinary representation without importing
  semantic-novelty authority;
- JG2 derives a complete, duplicate-free, internally enumerated
  `RawGCap(H,A)` carrier with exact positive and certified-negative coverage;
- JG3–JG7 derive the generative quotient, weakening image, marginal,
  strict-enlargement theorem, and injective generative provenance;
- JG8 proves exact Rust/safe-Agda transcript agreement for that authority;
- JG9 derives the independent branch-local startup registers;
- `gamma` is issued only from the complete chain and otherwise remains
  `Unknown`;
- `C0/C1 × S0/S+/S↑` is frozen before exposure and executed without `nu` as an
  input, gate, ranking value, or reported novelty measure;
- every survivor is retained, halting is debt-derived, and held-out decoding
  occurs only after sealing; and
- all semantic-novelty work remains on hold unless a later explicit project
  decision revises this plan.

The result need not reproduce the historical fifteen-stage sequence. It must be
blind, proof-producing, target-neutral, resource-explicit, and honest about
`Unknown`.

## Appendix A. Legacy citation map

A few frozen records cite the former long-form plan. Read those citations as:

- former plan §1.1a → §§2 and 4.1 here;
- former plan §5.7 and §12.3 → §§4.1 and 6.5 here; and
- former plan §5.8 → §6.6 here.

These aliases preserve old document references; they are not active milestones.
