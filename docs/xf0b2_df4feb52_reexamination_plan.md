# XF-0b2 — Targeted re-examination of family df4feb52

**Date:** 2026-07-24. **Status:** brief frozen; single-family successor
to the concordance study (ledger event on freeze). Subject: the sole
NO-NAME survivor, stage-14 functional head **df4feb52882e**, glossed in
the concordance as `Pi(Lam(Var 1), Sigma(Var 1, Var 2))` --- a
dependent product with a term-level abstraction in domain position.
Motivation for a targeted pass: the verdict is one-family thin (two
boundary verdicts flipped between the interrupted and complete
concordance runs --- disclosed judgment variance at exactly the NO-NAME
boundary), and the naming fleet was not armed with the specialist
literature this shape suggests. The re-examination must be able to go
either way: closing the concordance at 32/32 and withdrawing the export
is as valuable as confirming the program's first candidate novel
object.

## Phase R-0 — Internal characterization first (before any naming)

1. **Elaborate; never trust the gloss.** The informal reading
   ``abstraction in domain position'' must itself be re-derived: the
   exact kernel typing derivation of the clause \emph{in situ} at
   stage 14, under the frozen de Bruijn conventions, with every `Var`
   resolved to its actual binder or telescope field. The program has
   been burned by exactly this before (the sealed `Lam(Var(1))`
   misreading, where the machine refused a relabeling and was right);
   that precedent is this phase's charter. If the elaborated content
   differs from the concordance gloss, the re-examination proceeds on
   the elaborated content and the gloss is corrected in a versioned
   note.
2. **Irreducibility certificate.** Decide whether the domain
   abstraction is \(\beta/\eta\)-eliminable under the frozen kernel
   rules. Survival of the family quotient predicts irreducibility;
   certify it either way. If reducible after all, the family's
   distinctness collapses into a presentation question and the
   quotient machinery --- not the naming layer --- owns the next move.
3. **Role dossier.** Where the family acts: which sealed stage-14/15
   content references it, what the hilbert-functional payment uses it
   for, and what its instances look like. Naming a structure is easier
   and more honest when its work is on the table.

## Phase R-1 — Armed naming attempts (standard unchanged)

Fresh analysts (none from the concordance fleet), armed with
registered leads, under the \emph{same} clause-preserving translation
standard --- arming widens the search, never lowers the bar (F-R2).
Registered leads, to be tested and ideally killed:

- **L1.** Very dependent function types (Hickey, NuPRL tradition,
  mid-1990s) and Kopylov-style dependent intersections/records ---
  dependent products whose domain/codomain discipline involves the
  function being defined.
- **L2.** Self-referential record-type presentations.
- **L3.** Comprehension-category / CwF reading: \(\Pi\) applied to a
  family presented by a \emph{section} (a defined family, \(\Pi \circ
  \mathsf{Lam}\)) rather than by a display map --- possibly a
  composite classifier with a standard name in fibered category
  theory.
- **L4.** Polynomial-functor reading: exponent given by a term rather
  than a type; the associated container literature.
- **L5.** Anything the R-0 role dossier suggests.

Each lead: full translation attempt with citations, or a recorded
obstruction ("resembles L1 but violates clause X").

## Phase R-2 — Disposition (exactly one)

- **NAMED.** The concordance closes at 32/32; the export candidacy is
  withdrawn without regret (F-R5: keeping a NO-NAME alive to keep an
  export alive is the F-C2 violation in miniature); the v2-census unit
  is unconditionally available.
- **NO-NAME CONFIRMED.** Required stability: two independent armed
  analysts and an adversarial verifier concur, with all five leads
  disposed. The export firms: a DCT-pattern specification note is
  registered (the object's definition from the sealed content, its
  elementary properties from the role dossier, and its existence /
  consistency / relation-to-known-types obligations named --- the
  cheap half done honestly, the expensive half scheduled, exactly as
  with the DCT).
- **UNSTABLE.** If armed verdicts flip again, the finding is about
  the naming layer, not the family: the boundary-variance problem is
  referred to the campaign as a methods question (adjudication
  protocol strengthening) before any verdict issues.

## Falsifiers

- **F-R1.** Any naming or non-naming argument resting on the informal
  gloss rather than the R-0 elaboration → void (the v7-witness rule).
- **F-R2.** Any relaxation of the translation standard for an armed
  lead → the row is void; armed means informed, not lenient.
- **F-R3.** A verdict issued without the R-2 stability quorum → void.
- **F-R4.** Any splitting, merging, or reinterpretation of the family
  to fit a lead → void (F-C3 verbatim).
- **F-R5.** Any pressure of the export's attractiveness on a
  disposition → void on its face; a withdrawn export is a success of
  the discipline.

## Deliverables

`docs/XF0B2_DF4FEB52_RESULT.md` + `docs/xf0b2_df4feb52_v1.json`
(elaboration, irreducibility certificate, role dossier, per-lead
attempts, disposition, quorum record), mutation-guarded; ledger event
on result; on NO-NAME CONFIRMED, the specification note
`docs/df4feb52_specification_v1.md` as a registered export stub.

## Relation to the campaign

Gates nothing; runs parallel to the front (XF-A artifacts, XF-B5
census). Whatever the disposition, the concordance's headline
stands: the register speaks classical mathematics at aspect
granularity, with at most one new word --- and after this brief, the
"at most" resolves.
