# BC-1 depth-two completeness result

**Date (syntax identifier):** 2026-07-23. **Status:** `stopped_named_gaps`. **Certificate:** `blake3:7cc97e9f2e5ffd2140e0935a75f22c89537248285256e0be82b39d2450f7e99b`.

Labels in BC-1, M-3, E-1/E-4/E-7, Schema2, stage/clause identifiers, and artifact versions are syntax identifiers. Every quantitative statement below carries its register explicitly.

## Frozen baseline

M-3 v1 replays without a baseline shift. Its proved bridge conditions remain **4 [register: `artifact_metadata`]** of **9 [register: `artifact_metadata`]**, the enacted E-5/BI-2 regression reproduces exactly, and the baseline makes no promotion.

## What re-derived

The standalone depth-one campaign re-derives **43 [register: `structural_testimony`]** trace signatures and **9 [register: `structural_testimony`]** ordinary constructors, for **52 [register: `structural_testimony`]** scoped depth-one components. The frozen component stock additionally contains **12 [register: `structural_testimony`]** public cubical constructors, **17 [register: `structural_testimony`]** transparent-former rules, and **1 [register: `structural_testimony`]** dependent-context rule. Every component and each of the **5 [register: `structural_testimony`]** registered impossibilities has an explicit disposition.

The grammar-completion artifact's live source drift remains disclosed as sealed testimony: `source replay failed: G-1 replay failed: source invariant failed: archival M1 replay failed: v4 replay failed: predecessor replay failed: source binding mismatch for e4_generator_basis_source: expected 82053/blake3:fb19b2ed000cf23dad0b2de4a844cd2244c30613a7885607474ca4c4c7e54877, observed 82321/blake3:149d69787b408a5eaf9c9756227f44465dd446f42237776875e4bf933a687316`. BC-1 reconstructs the exact trace rows directly and does not repair or conceal that drift.

## Why C2 does not close

The component stock is not promoted into an independently defined semantic domain. Doing so by definition would identify the intended domain with generator closure and make generator completeness circular. The typed context-morphism decomposer, transparent-former matrix, dependent-context rule, A3 relative theorem, and wrapped-candidate exhaustion all retain their narrower scopes.

| Named gap | Exact obstruction |
|---|---|
| `BC1_INDEPENDENT_DEPTH_TWO_SEMANTIC_SIGNATURE_SURFACE_NOT_DEFINED` | The adopted ledger independently enumerates trace-bound seeds and proves scoped constructor rules, but it does not define an inductive Sem2/Marg2/SLNF depth-two signature surface or a total typed interpretation of every composite. Defining the intended domain to equal generator closure would make completeness circular and is forbidden. |
| `BC1_DEPTH_TWO_GENERATOR_COVERAGE_INDUCTION_NOT_PROVED` | The registered typed context-morphism generators decompose morphisms already admitted by the current E1 language, but there is no class-indexed structural induction proving coverage for every member of an independently defined depth-two semantic domain. |
| `BC1_DEPTH_ONE_RESTRICTION_OF_DEPTH_TWO_THEOREM_UNFORMABLE` | Every sealed depth-one trace row re-elaborates exactly and the ordinary registry replays, but without a depth-two semantic datatype there is no restriction function whose image can be proved equal to that campaign. |

Promoted rows: **0 [register: `artifact_metadata`]**. New semantic-family credits: **0 [register: `semantic_family_authority`]**. C2 closed: **false**. M-3 v2 prerequisite satisfied: **false**.

## Permitted conclusion

The M-3 v1 baseline is unchanged. The sealed depth-one trace campaign re-derives exactly, every frozen ordinary/cubical/transparent/dependent component has an explicit scoped disposition, and grammar closure mints no semantic-family credit. These scoped results do not constitute an independently defined depth-two semantic domain or a noncircular generator-completeness theorem. C2 remains open and no row is promoted.

## Required successor

Adjudicate or construct an independent inductive depth-two semantic signature domain with total typed interpretation, without defining it by the generator closure. Then prove trace/adopted-former coverage, the class-indexed generator induction, and the depth-one restriction theorem in a versioned successor before rerunning M-3.
