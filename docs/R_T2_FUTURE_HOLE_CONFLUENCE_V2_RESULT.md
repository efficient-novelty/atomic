# R-T2 future-hole confluence v2 result

**Date:** 2026-07-22. **Status:** **EXECUTED — NO CONFLUENCE VERDICT.**

The create-new run is recorded in
`docs/r_t2_future_hole_confluence_v2.json` with result digest
`blake3:5633bae218a520c25b3a5570ce41c67cc03c5050f05a0179d60f044db8cc8747`.
Definition replay succeeds. The result is
`relative_coverage_or_named_gap_blocker`.

## Constructed surface

All four genuinely distinct Stage-4 minimizers were carried to their exact
Stage-5 prefixes. Each branch attempts the same five successor schemes and
constructs:

- three unary registrations;
- one structural `InitialHit` registration;
- one replayable unary registration gap.

Thus the branch signature is `3 unary + 1 structural + 1 gap` for each of
the four branches. Enumeration order and branch-local family identifiers are
not used as semantic selectors.

The missing scheme is not classified nonexistent. Its third canonical
parameter is `Opaque`, reconstructed from a prior Stage-4 clause with kernel
type `Function(Neutral, Type)`. The exact full declaration requires ambient
arity 3, exceeding the frozen kernel maximum of 2. The isolated diagnostic
then fails motive formation because `Neutral` is not a B_n-formable motive.
This is an **API expressivity F-FH4 gap**, not a proof that the successor
scheme has no semantics.

## Why R-T2 does not speak yet

Pairwise scheme-set comparison is gated on complete, prefix-local,
law-level-authoritative successor formation. Those premises fail before the
comparison stage:

- one named future-hole gap survives in every branch;
- the successful rows inherit the motive-v2 bare-`Univ` totality
  counterexample and are not yet theorem-level authoritative;
- the run remains explicitly scoped relative to the archived A3-v2 /
  Global-E4-v10 surface and its recorded live-source drift.

Accordingly the pairwise comparison count is zero. The artifact proves
neither equivalence nor divergence:

| decision | result |
| --- | --- |
| R-T2 confluence proved | false |
| R-T2 confluence refuted | false |
| R-T3 opened | false |
| branch selected | none |
| order-reversal invariance promoted | false |

This is the disciplined outcome required by the tie-resolution protocol.
The four partial successor surfaces are retained as typed evidence, but no
choice, gauge-equivalence claim, or free-decision claim is inferred from
them.

## Lawful successor

R-T2 needs the same versioned dependent-context machinery exposed by E-5,
plus proof-strength source-domain totality. Once every one of the five
schemes forms on every branch and the A3/Global-E4 scope is refreshed, rerun
this v2 issuer create-new. Only then may it compare the six branch pairs and
decide confluence or refutation.

Because R-T2 did not issue a verdict and E-5 did not issue semantic `O(16)`,
the bridge and final certificate remain closed.
