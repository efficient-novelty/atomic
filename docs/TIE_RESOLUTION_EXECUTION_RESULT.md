# Tie-resolution execution result

**Date:** 2026-07-21. **Status:** **EXECUTED THROUGH THE FIRST LAWFUL
STOPS.**

The prescribed sequence produced three create-new, replay-valid artifacts:

| phase | artifact | result |
| --- | --- | --- |
| shared transport | `naturality_orbit_transport_v1.json` | direct `64 -> 8` proved; pointwise `8 -> same 8` proved; Stage-4 R-T1 leaves four semantic classes |
| E-5 successor | `schema2_e5_a3_successor_v2.json` | stops on 17 undefined unary actions and 13 undefined structural typed holes; F1 unexecuted; semantic `O(16)` unset |
| T-BF1/T-BF3 rerun | `t_bf1_t_bf3_tie_protocol_rerun_v3.json` | enters R-T2; stops at the undefined typed Stage-5 `InitialHit` successor; confluence neither proved nor refuted; R-T3 closed |

The shared build therefore fixed the exact quotient defect it was meant to
fix, but it also exposed a semantic-definition boundary that transport alone
cannot cross. The current APIs move or certify an already supplied term; they
cannot infer which unary action or structural future-hole term A3 means.

Because semantic `O(16)` was not issued, T-BF2 was not run. Because neither
T-BF2 nor T-BF1/T-BF3 completed, the bridge and final certificate were not
run. Doing so would have violated the adopted gates.

The next lawful construction is a versioned, intrinsically typed A3 demand
expression grammar. It must specify unary operators and structural package
signatures independently of the historical answers. The first regression
target is the Stage-5 `InitialHit` hole over all four Stage-4 branches.
