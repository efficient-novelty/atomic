# MS-1 — The Milestone Certificate: sealed state, named frontier

**Date:** 2026-07-24. **Status:** brief frozen; versioned successor to
`docs/mainline_completion_plan.md` in one respect only: it authorizes a
**milestone certificate** that is *not* M-4 and claims nothing M-4's
gate protects. Every gate of the mainline plan is untouched: M-3 v1
remains authoritative, `m4_authorized` remains false, and M-4 issues
only on a 9/9 bridge with zero named gaps, exactly as adopted. The
milestone certificate is a sealed photograph of the program's certified
state together with its exactly-named frontier — the program's
characteristic move (claim what is proved, name what is not) applied to
the program itself.

**Standing rules.** Create-new, replay + mutation falsifiers; register
discipline (F-NR6) on every number; quantifiers per the BI-4
disposition table; drift disclosed as testimony; no content of any kind
generated for this certificate — it *binds*, it does not *prove*. Any
claim found in it that is not bound by digest to a sealed certificate
is invalid on its face.

## MS-1a — Contents (bound by digest, nothing else)

1. **The law.** The 27 signed adjudications, verbatim adoption blocks
   included, as the two-law theory's complete legislative record.
2. **The certified chain.** E-5 dependent-context finale; BI-0 v6;
   BI-1/BI-1b sweeps; the four BI-2 finales; BI-4 v2 cone report
   (Z-SPLIT-4, disposition table); UC-1 scoring (P1/P2 pass, P3
   refuted, Z-LEDGER, `U_T2_UNATTEMPTED`); M-1 transport maps and the
   two order-axis obstructions; M-3 v1 (4/9, fail-closed); the Stage-4
   semantic parsimony v3 cross-binding; the semantic ledger
   ([1,0,1,3,6,3,1,1,0,4,1,0,2,3,6], Σ=32) with the published
   structural divergence table.
3. **The theory's headline claims, stated with their exact quantifiers:**
   cone-level — demand ladder, halt at 15, semantic O(16)=∅ at
   instance granularity, F1 excluded, per-stage ledgers 5–15;
   branch-indexed — Stage-4 act and value, all cumulative quantities;
   adopted doctrine — two-register acceptance, value never legislates
   at guarded stages, choice secured against value not proof.
4. **The frontier, first-class.** The DNF-4 disposition table verbatim:
   150 named gaps, 0 projected rows, decomposed exactly (61 family
   rows lacking complete-context/universal-naturality evidence; 72 A3
   rows with concrete replayable squares but no family-level closure;
   17 unary rows lacking exported dependent declarations and bodies);
   the T-D2-1 v3 frozen bounds (15 entries, 64 clauses, free scope 9,
   binder depth 2, dimension 3, semantic depth 2) with their
   provenance citations; and the precise statement of the wall: a
   full-grammar canonicalization and universal-naturality metatheory
   for the frozen kernel, registered below as the KM program.
5. **Open items, silence-is-not-disposition:** the KM program; U-T2
   (the Π/Σ act equivalence — the cone's one open identity); WB-1's
   W-T1/W-T2; the parked observational-univalence conjecture; the R2
   injective join; the bridge chain (T-D2-2 → BC1 → 431-gap rerun →
   M-3 v2 → M-4) as specified and closed; and the external-falsifier
   frontier as the program's declared next front.
6. **Drift manifest.** Every known live-replay drift, with archived
   self-digests validated and exact live errors recorded — testimony,
   not repair.

## MS-1b — The KM program, registered (named, not scheduled)

**KM (kernel metatheory):** full context-grammar canonicalization;
universal naturality (family membership commutes with every legal
substitution, proved over the whole calculus); decidability and
finiteness for the bounded fragment re-derived against the full
grammar. Discharging KM converts the 150 named gaps into projections
or refutations, reopens T-D2-1 → T-D2-2 → BC1, and is the sole lawful
road to M-4. It is a formalization result of independent standing and
is registered here with no deadline: naming it precisely is the
milestone's obligation; scheduling it is the adopter's prerogative.

## Falsifiers

- **F-MS1.** Any statement in the certificate not bound by digest to a
  sealed artifact → invalid; the milestone proves nothing.
- **F-MS2.** Any language implying bridge completion, M-4 issuance, or
  discharge of any named gap → invalid on its face; the M-4 gate is
  binary and untouched (F-BC4 verbatim).
- **F-MS3.** Any register or quantifier violation — an unindexed
  cumulative quantity, a structural total presented as semantic, a
  branch fact stated cone-level → invalid (F-NR6/F-B4-4 lineage).
- **F-MS4.** Any gap of the DNF-4 table omitted, summarized, or
  softened → invalid; the frontier is content, not an appendix.
- **F-MS5.** The milestone may not be cited as ground for de-scoping,
  weakening, or re-gating any adopted rule or frozen brief; it records
  the state, it does not amend it.

## Deliverables

`docs/MILESTONE_CERTIFICATE_V1.md` + `docs/milestone_certificate_v1.json`
with mutation falsifiers (flipping any digest, quantifier, register
tag, or gap row must invalidate replay); a short
`docs/MS1_INDEX.md` mapping every bound claim to its artifact.

## Relation to the standing program

After MS-1 seals: the mainline's remaining chain stays exactly as
frozen, gated behind KM; the program's declared active front shifts to
the external-falsifier question (a derived, parameter-free quantity
exposed to measurement), for which the milestone certificate is the
citable foundation; and the book's Part-I claims acquire a single
sealed reference. Nothing in MS-1 forecloses returning to KM at any
time — the wall is named, mapped, and waiting.
