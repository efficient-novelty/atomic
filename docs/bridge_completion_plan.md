# Bridge completion — C2 / C8 / E8 theorem program

**Date:** 2026-07-23. **Status:** brief frozen; construction only, no
new law. Successor to the M-3 fail-closed stop
(`docs/schema2_e7_e8_bridge_v1.json`, `blake3:a61fe809…`): 4/9 bridge
conditions proved over 213 aggregate rows, zero promotions,
`m4_authorized = false`. This program proves the three named blockers
and reruns the bridge; it changes nothing else. The M-4 specification
in `docs/mainline_completion_plan.md` is untouched.

**Standing rules (all phases).** Create-new artifacts, replay +
mutation falsifiers; sealed bytes never edited; drifts disclosed,
never repaired; register discipline per `nu-register-split-v1` —
semantic-family ν is the only law-level value, structural quantities
appear as tagged testimony only; quantifiers per the BI-4 v2
disposition table — cone-level statements quantify over all four
branches, branch-level statements name branch and register; no value,
bar, hash, or enumeration order as selector; any semantics beyond the
adopted 25-rule ledger → named gap, versioned adjudication, stop.

**Frozen baseline (hard gate).** The four proved conditions of bridge
v1 and the exact enacted E-5/BI-2 regression are the baseline. Every
rerun must reproduce them identically before any new condition is
attempted. A baseline shift is not progress in disguise; it voids the
run (F-BC1).

## BC-1 — C2: depth-two schema grammar and generator completeness

Extend the depth-one Schema2 completion (the sealed
grammar-completion campaign: trace-faithful signatures, derived
closure, zero exports) to depth two.

- Enumerate the depth-two signature surface from the sealed trace and
  adopted formers only; every signature proven trace-faithful or
  named impossible — zero silent residue.
- Generator completeness: the depth-two generator set closes under
  the adopted transparent-former matrix and dependent-context rules;
  completeness is an exhaustiveness theorem with replay, never an
  inventory assertion.
- Regression: the depth-one campaign's results re-derive as the
  depth-one restriction of the depth-two theorem.
- Nothing minted: depth-two schemas are grammar, not credit
  (standing zero-charge law).

## BC-2 — C8: candidate boundary provenance join

Join act-local provenance to the bridge's aggregate rows across the
candidate boundary.

- The join consumes only: the candidate, its sealed prefix, and the
  exported transport maps (M-1, `blake3:b9d84d89…`). Archive joins
  are regression comparators, never authority (F-AL2 verbatim — a
  row whose provenance traces to a historical certificate join is
  not proved, it is testimony).
- Every aggregate row's provenance resolves act-locally or the row is
  a named gap; the join must be transportable (replay-identical on
  any branch containing the same act over the same prefix), since
  bridge statements quantify over the cone.
- The two certified order-axis obstructions are inputs here, not
  problems: rows separated by an obstruction stay separated; any join
  merging across a certified obstruction is invalid on its face.

## BC-3 — E8: aggregate parent-row disposition

The registered target is disjunctive: per-row refinement proofs, or
one universal theorem, for the Schema3 aggregate parent rows.

- Strategy choice (row-by-row versus universal) is proof engineering,
  free to the agent; **coverage is not**: every one of the 213 rows
  ends in exactly one of {refined, covered-by-universal-theorem,
  named-impossibility}. No mixture presenting partial coverage as
  complete; no row silently out of scope.
- If the universal theorem is attempted and fails, the failure point
  is published and the program continues row-wise from it — a failed
  universal is information, not a stop.
- Refinement must not consult desired bridge outcomes: rows are
  refined from their own typed content (count-blind, F-E4 lineage).

## Exit gate

All three blockers proved → **M-3 v2 rerun**, create-new, same
issuer lineage: frozen baseline reproduced, all nine conditions
evaluated, 213/213 rows disposed. Only a 9/9 result with zero named
gaps sets `m4_authorized = true`; anything less is a new fail-closed
stop with its remaining gaps named. M-4 then executes exactly per the
mainline plan — no drive-by edits to its specification from inside
this program.

## Falsifiers

- **F-BC1.** Baseline shift: any of the four proved v1 conditions or
  the enacted regression failing to reproduce → run void; fix the
  machinery, never the baseline.
- **F-BC2.** Silent residue: any signature, generator, or row without
  an explicit disposition → the owning phase is incomplete regardless
  of its other output.
- **F-BC3.** Register violation: any structural-register quantity in
  a law-level derivation, or any published number untagged → invalid
  (F-NR1/F-NR6).
- **F-BC4.** Partial promotion: any bridge condition, row class, or
  claim promoted before 9/9 → invalid; the M-4 gate is binary.
- **F-BC5.** Expressivity gap: any theorem needing semantics beyond
  the adopted ledger → named gap, versioned adjudication, stop; no
  improvisation inside the theorem program.
- **F-BC6.** Scope creep: U-T2, WB-1 tests, the R2 strengthening,
  diagnostic policy, or the parked conjecture appearing anywhere in
  this program's artifacts → invalid; parked means parked.
- **F-BC7.** Quantifier error: any bridge statement whose
  cone/branch quantifier disagrees with the BI-4 disposition table →
  invalid regardless of proof status.

## Deliverables

`docs/BC1_DEPTH_TWO_COMPLETENESS_RESULT.md`,
`docs/BC2_BOUNDARY_PROVENANCE_JOIN_RESULT.md`,
`docs/BC3_PARENT_ROW_DISPOSITION_RESULT.md` (each + certificate),
then `docs/SCHEMA2_E7_E8_BRIDGE_V2_RESULT.md` +
`docs/schema2_e7_e8_bridge_v2.json` from the M-3 v2 rerun. Mutation
falsifiers on all certificates; the v1 bridge artifacts remain sealed
testimony of the stop.
