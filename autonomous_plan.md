# Autonomous Claim Lane Plan

Last updated: 2026-04-01
Status: active

This file is the staged path from the current step-`4` wall to final claim
signoff. It is intentionally strategic and forward-looking rather than an
experiment diary.

## Objective

Produce one stored `desktop_claim_shadow` bundle from the disclosed desktop
that:

- finishes through step `15`
- preserves accepted-hash parity and honest breadth accounting
- passes compare, benchmark, and certification on stored evidence

Until that bundle exists, keep the paper wording at `bounded live recovery`.

## Current Strategic Position

- The early RSS cliff is gone; the lane is now blocked by step-`4` throughput.
- The current full-profile speed winner is
  `runs/codex-claim-release-full-aggregation-open-band-structural-nu-facts-v1`.
  It beats the prior winner at matched later checkpoints through `533`, keeps
  the honest retained-prefix story through `576`, and still does not reach
  step `5`.
- The current deeper continuation target is
  `runs/codex-claim-release-full-aggregation-open-band-prefix-nu-context-v2`,
  because it still owns the farthest stored wall at `1038`.
- The decisive later surfaces are now no-miss surfaces:
  admissibility and fallback connectivity checks are zero there on stored
  evidence.
- The replay harness is now a permanent iteration layer for this lane.

## Runtime Thesis

The next wins should come from specializing the winning hit path, not from
reopening broad cache machinery.

The working order is:

1. Make the local continuation cone the primary scoring object.
2. Keep clause facts predecoded and mandatory on the hot path.
3. Split the no-miss plateau kernel from the general fallback kernel.
4. Compress `lib_refs` more aggressively.
5. Use a tiny survivor sketch before waking dormant reopen machinery.

## Decision Rules

- Trust stored artifacts over terminal impressions.
- Treat "same honest shape, slower clock" as regression.
- Replace the current speed winner only if a later slice:
  - preserves the honest retained-prefix story
  - beats the current winner honestly through the stored `484/533/576` region
  - and stays well below the old allocator-failure band
- Replace the deeper continuation target only if a later slice:
  - keeps the speed win through the current winner's region
  - and then moves materially past `576` toward `1038` or reaches step `5`
- Require replay-harness parity plus either fewer exact-`nu` evaluations or
  lower measured aggregation time before any serious full-profile rerun.
- Retire a hypothesis after one honest stored loss, non-engagement, or pure
  cost shift.

## Active Phase: Step-`4` Runtime Improvement Loop

Goal:

- keep improving the current full-profile winner step by step until the lane
  moves materially past the current step-`4` wall or reaches step `5`

Loop:

1. land one narrow code slice on top of the current winner
2. rerun only the claim-focused tests needed by that slice
3. validate it on the replay harness in release mode
4. require parity plus either fewer exact-`nu` evaluations or lower measured
   aggregation time
5. only then launch a full-profile rerun
6. re-earn the stored later checkpoints in this order:
   - `140/163`
   - `332/335`
   - `408/437/454/484`
   - move materially past `576`
   - then chase `1038` or step `5`

Current slice order:

1. prefix-local interval pruning on the local continuation cone
2. mandatory `TerminalClauseNuFacts` sidecar on the hot path
3. explicit no-miss hit-path kernel versus fallback kernel split
4. tiered `lib_refs` representation inside
   `SingleClauseStructuralNuContext`
5. tiny survivor sketch if second-pass duplication remains visible

Do not reopen first:

- cached-summary reopen wake-up work
- contender-rank helper rewrites
- connectivity-first/cache-first rewrites
- deterministic batched parallel reduction
- broad metadata-only or bookkeeping-only cleanup

Done when:

- a later full-profile rerun moves materially past the current step-`4` wall
  or reaches step `5`

## Phase 2: Finish One Intended-Profile Claim Run

Goal:

- complete one intended-profile claim run through step `15`

Required output:

- one canonical run directory from the disclosed desktop
- accepted parity through step `15`
- honest breadth evidence through the required floors
- complete reason and prune accounting

## Phase 3: Freeze Signoff Artifacts

Goal:

- turn the finished run into the auditable claim bundle

Required output:

- one compare report against the guarded reference
- one benchmark bundle
- one passing `claim_certificate.json`
- one certified runtime threshold tied to stored evidence

## Phase 4: Open The Language Gate

Goal:

- allow stronger wording only after the technical gate is closed

Required output:

- user-facing and paper-facing wording updated only after certification passes

## Non-Goals Until The Runtime Wall Moves

- allocator or memory-compaction work first
- broad frontier rewrites
- waking dormant general reopen machinery first
- compare, benchmark, or certification work before a runtime winner exists
- stronger user-facing language

## Success Condition

This plan is done only when one stored `desktop_claim_shadow` bundle from the
disclosed desktop shows all of the following at the same time:

- full-profile completion through step `15`
- no silent guarded, replay, realistic-shadow, or demo-only fallback
- accepted parity through step `15`
- breadth gates passed honestly from stored evidence
- complete reason and prune accounting
- the step-`4` compute wall is no longer the blocker on the intended profile
- benchmark and manifest data sufficient for certification
- passing compare and certification outputs
