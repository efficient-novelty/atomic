# Autonomous Claim Lane: Next Operational Slice

Last updated: 2026-04-17

This file owns the single active work order for `desktop_claim_shadow`.

## Contract

- Keep exactly one active objective and one current hypothesis here.
- Move completed or ruled-out probes to
  [autonomous_ledger.md](autonomous_ledger.md).
- Do not restate medium-horizon phases or historical context here. Use
  [autonomous_plan.md](autonomous_plan.md) and
  [autonomous_progress.md](autonomous_progress.md).

## Objective

Preserve
`runs/codex-claim-release-full-aggregation-open-band-clause-accept-rank-facts-long-rerun-v15`
as the current certified stored head and spend the next claim-lane turn below
the largest repaired-head blocker: the direct top-level step-`15`
`reference` frontier remainder now narrowed one layer deeper to a
mismatch-`1` surface at
`177 = 145` remaining-two plus `32` remaining-three partial-prefix captures,
ahead of only a `64 = 54 + 10` reference/reference tail. Within that
remaining-two slice, the largest single pairing is still
`reference / demo_flat_codomain = 61`, and that branch now decomposes one
layer deeper too: remaining-two clause-`4` `claim_next_bridge = 33`,
remaining-two clause-`4` `reference = 28`, and a `12`-capture
remaining-three spill that stays entirely on clause-`4` `reference`. The
broadest repaired-head code-side reopening below that `33` side is now frozen
too: the exact claim-variant-pair plus clause-`4` `claim_next_bridge`
reland lands at
`generated_raw_prefixes = 7485`,
`partial_prefix_bar_failure = 539`,
direct `reference` remainder `= 227`,
`reference / demo_flat_codomain = 45`,
slot split `27 / 18 / 14`,
and
`small_cluster = 2112 / 542 / 542 / 0`.
The tied exact single-sheet controls there are frozen too: both exact
`claim_flat_domain` and exact `claim_sharp_codomain` relands now land at
`generated_raw_prefixes = 7317`,
`partial_prefix_bar_failure = 545`,
direct `reference` remainder `= 233`,
`reference / demo_flat_codomain = 51`,
slot split `30 / 21 / 14`,
and
`small_cluster = 2076 / 530 / 530 / 0`.
That smaller clause-`2` `reference = 9` sheet is now frozen too as the first
non-widening safe side-control on the repaired head: it lands at
`generated_raw_prefixes = 7149`,
`partial_prefix_bar_failure = 551`,
direct `reference` remainder `= 239`,
`reference / demo_flat_codomain = 57`,
slot split `33 / 24 / 14`,
and
`small_cluster = 2040 / 518 / 518 / 0`.
It does not touch the live clause-`4` `claim_next_bridge = 33` blocker;
instead it trims only the clause-`4` `reference` companion from `28` to `24`
while widening the remaining-three spill from `12` to `14`. That attempted
exact reference-sheet landing path is now blocked too: reopening that same
exact clause-`2` `reference` sheet together with its clause-`4`
`reference` companion relands the exact same
`7149 / 551 / 2040 / 518 / 518 / 0`
surface, the same direct `reference` remainder `239`, the same targeted
pairing `57`, and the same `33 / 24 / 14` slot split. That neutral
extension also pins the safe-control grid itself:
remaining-two clause-`4` `claim_next_bridge` stays
`12 / 12 / 9`,
remaining-two clause-`4` `reference` stays
`9 / 9 / 6`,
and the remaining-three clause-`4` `reference` spill stays
`5 / 5 / 4`
across clause-`2`
`claim_flat_domain / claim_sharp_codomain / reference`.
The first repaired-head exact `claim_flat_domain` and exact
`claim_sharp_codomain` reopenings under that broader clause-`4`
`reference` companion are now frozen too:
both land at
`generated_raw_prefixes = 7236`,
`partial_prefix_bar_failure = 550`,
direct `reference` remainder `= 238`,
targeted pairing `57`,
branch-local slot split `33 / 24 / 13`,
and
`small_cluster = 2058 / 524 / 524 / 0`.
They leave the remaining-two clause-`4` `reference` row frozen at
`9 / 9 / 6`
and trim only one remaining-three spill capture, landing
`4 / 5 / 4`
or
`5 / 4 / 4`
across clause-`2`
`claim_flat_domain / claim_sharp_codomain / reference`.
The next active slice is therefore the smaller exact clause-`2`
`reference = 6` slice below that same broader clause-`4`
`reference` companion, ahead of the whole remaining-three spill or any higher
branch. Do not reopen step-`1`, storage / tooling work, or the now-closed
certification question.

## Hypothesis

The stored step-`1` breadth and certification questions are now closed.
Stored `v15` keeps accepted-hash parity through step `15`, restores stored
step `1` to `2144 / 1285 / 1 / 475`, preserves the repaired stored step-`15`
surface at `7211 / 553 / 2052` with a zero-admitted `broad = 3600`, and now
stores ready compare / benchmark / certification artifacts beside the run.
Regression-backed repaired-head localization now shows that the unchanged
`553` wall lives only on queued frontier remainder branches `4 .. 11`, with
the direct top-level `reference` remainder alone carrying `241`, the
top-level `claim_eventual_domain` remainder carrying `156`, and the six
`claim_flat_domain` descendants carrying the other `156`. That direct
`reference` remainder now decomposes cleanly too: mismatch `1` carries
`177 = 145 + 32`, mismatch `2` carries `50 = 42 + 8`, mismatch `3` carries
`14 = 12 + 2`, and the largest single remaining-two pairing inside the
branch stays `reference / demo_flat_codomain = 61`. Inside that pairing, the
next exact lead is now clearer too: remaining-two clause-`4`
`claim_next_bridge = 33` stays larger than remaining-two clause-`4`
`reference = 28`, while the branch's remaining-three spill is another `12`
captures and stays entirely on clause-`4` `reference`, with the
  `claim_next_bridge = 33` side itself splitting
  `12 / 12 / 9` across clause-`2`
  `claim_flat_domain / claim_sharp_codomain / reference`. The broadest exact
claim-pair reopening under that `33` side still narrows only as a tradeoff
control, moving the repaired head to
`7485 / 539 / 2112 / 542 / 542 / 0`
while shrinking the direct `reference` remainder to `227` and the targeted
pairing to `45`. The tied exact single-sheet claim reopenings under that same
side are now frozen too as matched smaller tradeoff controls at
`7317 / 545 / 2076 / 530 / 530 / 0`
with direct `reference` remainder `233`, targeted pairing `51`, and slot split
`30 / 21 / 14`. The exact clause-`2` `reference` sheet below that side is
now frozen too as a safe side-control at
`7149 / 551 / 2040 / 518 / 518 / 0`
with direct `reference` remainder `239`, targeted pairing `57`, and slot
split `33 / 24 / 14`, but it trims only the clause-`4` `reference`
companion rather than the live clause-`4` `claim_next_bridge = 33` blocker.
That exact reference-sheet landing attempt is now ruled out too: adding its
clause-`4` `reference` companion is completely neutral and relands the same
`7149 / 551 / 2040 / 518 / 518 / 0` surface with the same direct
`reference` remainder `239`, targeted pairing `57`, and slot split
`33 / 24 / 14`. The first exact claim-flat and exact claim-sharp reopenings
under that broader clause-`4` `reference` companion are now frozen too at
`7236 / 550 / 2058 / 524 / 524 / 0`
with direct `reference` remainder `238`, targeted pairing `57`, the
remaining-two clause-`4` `reference` row still pinned at `9 / 9 / 6`, and a
single spill capture removed to
`4 / 5 / 4`
or
`5 / 4 / 4`.
The next honest leverage is therefore no longer another exact reference-sheet
probe or a return to those tied claim-sheet reopenings; it is the smaller
exact `reference = 6` slice below that same clause-`4` `reference`
companion before the whole remaining-three spill or any higher branch.

## Start From

- previous audited stored head:
  `runs/codex-claim-release-full-aggregation-open-band-clause-accept-rank-facts-long-rerun-v14`
- current certified stored head:
  `runs/codex-claim-release-full-aggregation-open-band-clause-accept-rank-facts-long-rerun-v15`
- `v14 -> v15` stored evidence now shows:
  - accepted-hash parity through all `15` steps
  - stored step `1` closes from `546 / 288 / 1 / 435` to
    `2144 / 1285 / 1 / 475`
  - stored step `15` stays on the repaired
    `7211 / 553 / 2052` surface with
    `broad = 3600 / 0 / 0 / 0` and the same fenced `single` pocket
  - search-policy metadata stays honest for the claim lane, and compare still
    reports a consistent step-`15` claim boundary
- refreshed native evidence now exists beside stored `v15`:
  - `claim-compare.{json,txt}` reports `Comparison Signoff: ready`,
    accepted-hash parity through step `15`, and `claim lane audit: ready`
  - `claim_benchmark.{json,txt}` covers `v12` / `v13` / `v14` / `v15` with
    runtime `min = 3976`, `median = 4165`, `p90 = 4642`, `max = 4642` ms,
    parity success `4 / 4`, late-floor success `2 / 4`, early-breadth
    success `1 / 4`, and runtime-threshold success `4 / 4`
  - `claim_certificate.{json,txt}` reports `Claim Certification: ready` with
    stored runtime `4165 ms`; accepted parity, search-policy honesty,
    fallback honesty, narrative completeness, early breadth, late floors,
    exact-screen completeness, prune-class completeness, and manifest
    completeness all pass
- targeted current-head verification is green:
  - `cargo test -p pen-search desktop_claim_shadow_smoke_config_ -- --nocapture`
  - `cargo test -p pen-search demo_early_steps_restore_full_clause_catalog_generation -- --nocapture`
  - `cargo test -p pen-search desktop_claim_shadow_step_five_keeps_claim_generic_surface_under_demo_budget -- --nocapture`
  - `cargo test -p pen-search current_claim_step_fifteen_post_reference_exact_two_step_ -- --nocapture`
  - `cargo test -p pen-search current_claim_step_fifteen_live_reference_ -- --nocapture`
  - `cargo test -p pen-search current_claim_step_fifteen_live_repair_partial_prefix_wall_localizes_to_eight_frontier_remainder_branches_on_the_repaired_head -- --nocapture`
  - `cargo test -p pen-type claim_next_bridge_side_on_exact_reference_sheet -- --nocapture`
  - `cargo test -p pen-type outside_exact_reference_clause_two_sheet -- --nocapture`
  - `cargo test -p pen-cli claim_evidence -- --nocapture`
- repaired-head wall localization is now explicit too:
  - all `553` clean partial-prefix captures map to queued branches `4 .. 11`
  - branch totals are
    `4 / 5 = 7`,
    `6 / 7 = 19`,
    `8 / 9 = 52`,
    `10 = 156`,
    `11 = 241`
  - inside queued branch `11`, mismatch `1 / 2 / 3` now split the direct
    `reference` remainder as
    `177 = 145 + 32`,
    `50 = 42 + 8`,
    and
    `14 = 12 + 2`
  - that direct `reference` remainder keeps the same five remaining-two
    pairings explicit, with `reference / demo_flat_codomain = 61` still the
    single largest pairing
  - inside that `reference / demo_flat_codomain` branch, remaining-two
    clause-`4` pressure now splits as
    `claim_next_bridge = 33` versus `reference = 28`, while the
    remaining-three spill is another `12` captures and stays entirely on the
    clause-`4` `reference` side
  - inside that `claim_next_bridge = 33` side, clause-`2`
    `claim_flat_domain = 12`,
    `claim_sharp_codomain = 12`,
    and
    `reference = 9`
  - the first repaired-head exact claim-pair reopening under that side is
    now frozen too:
    `generated_raw_prefixes = 7485`,
    `partial_prefix_bar_failure = 539`,
    direct `reference` remainder `= 227`,
    `reference / demo_flat_codomain = 45`,
    slot split `27 / 18 / 14`,
    and
    `small_cluster = 2112 / 542 / 542 / 0`
  - the tied repaired-head exact single-sheet claim reopenings under that
    same side are frozen too:
    both exact `claim_flat_domain` and exact `claim_sharp_codomain`
    relands land at
    `generated_raw_prefixes = 7317`,
    `partial_prefix_bar_failure = 545`,
    direct `reference` remainder `= 233`,
    `reference / demo_flat_codomain = 51`,
    slot split `30 / 21 / 14`,
    and
    `small_cluster = 2076 / 530 / 530 / 0`
  - the exact repaired-head clause-`2` `reference` sheet under that same
    side is frozen too:
    `generated_raw_prefixes = 7149`,
    `partial_prefix_bar_failure = 551`,
    direct `reference` remainder `= 239`,
    `reference / demo_flat_codomain = 57`,
    slot split `33 / 24 / 14`,
    and
    `small_cluster = 2040 / 518 / 518 / 0`
  - the attempted exact reference-sheet landing candidate is frozen too:
    adding the clause-`4` `reference` companion under that same exact
    clause-`2` `reference` sheet is neutral and relands the same
    `7149 / 551 / 2040 / 518 / 518 / 0`
    surface, with branch-local grid
    `12 / 12 / 9` on remaining-two clause-`4` `claim_next_bridge`,
    `9 / 9 / 6` on remaining-two clause-`4` `reference`,
    and
    `5 / 5 / 4`
    on the remaining-three clause-`4` `reference` spill
- queued branches `0 .. 3` contribute no repaired-head wall captures
- direct script execution is still blocked here because `python`, `python3`,
  `py`, and `uv` are unavailable on `PATH`, but native `pen-cli`
  `compare-claim-lane`, `certify-claim-lane`, and `benchmark-claim-lane`
  remain sufficient for current-head evidence refresh

## Active Hypothesis

- Do not spend another turn re-proving stored step `1`, certification status,
  or the native evidence path unless a later code change regresses `v15`.
  The only open binary gate left is the clean step-`15` partial-prefix wall
  dropping below `553` on top of the certified head, and the first live
  blocker inside that wall is now the mismatch-`1` surface inside the direct
  top-level `reference` remainder at `177 = 145 + 32`, not the already
  wall-free queued branches `0 .. 3` or another whole-branch scout. The
  broadest existing repaired-head code-side reopening under its
  `claim_next_bridge = 33` side is already frozen as the
  `7485 / 539 / 2112 / 542 / 542 / 0` tradeoff control, so the next move must
  start below that pair-level reland. The tied exact
  `claim_flat_domain` / `claim_sharp_codomain` single-sheet reopenings are now
  frozen too at `7317 / 545 / 2076 / 530 / 530 / 0`. The exact clause-`2`
  `reference = 9` sheet is now frozen too at
  `7149 / 551 / 2040 / 518 / 518 / 0`,
  and adding its clause-`4` `reference` companion is now ruled out as a
  neutral extension on the same exact surface. The first exact
  `claim_flat_domain` / `claim_sharp_codomain` reopenings under the broader
  clause-`4` `reference` companion are now frozen too as spill-only tradeoff
  controls at `7236 / 550 / 2058 / 524 / 524 / 0`, so the next move is below
  them on the smaller exact `reference = 6` slice rather than re-proving the
  same exact reference sheet or revisiting the already-matched claim sheets.

## Do This Next

- Keep `v15` as the current certified stored head and refresh compare /
  benchmark / certify artifacts after each material rerun.
- Start the next local code slice from the repaired-head branch localization:
  - preserve canonical accepted `103 / 8`
  - preserve stored step-`15`
    `7211 / 553 / 2052` plus
    `small_cluster = 2052 / 522 / 522 / 0`
  - attack the direct top-level `reference` remainder first via its narrowed
    mismatch-`1` surface at `177 = 145 + 32`
  - start that mismatch-`1` slice from the largest single remaining-two
    pairing, `reference / demo_flat_codomain = 61`, and inside that pairing
    freeze the repaired-head exact claim-pair plus clause-`4`
    `claim_next_bridge` reopening as a tradeoff control at
    `7485 / 539 / 2112 / 542 / 542 / 0` with direct `reference` remainder
    `227`, targeted pairing `45`, and slot split `27 / 18 / 14`
  - keep the tied repaired-head exact `claim_flat_domain` and
    `claim_sharp_codomain` single-sheet reopenings frozen too as matched
    smaller tradeoff controls at
    `7317 / 545 / 2076 / 530 / 530 / 0`
    with direct `reference` remainder `233`, targeted pairing `51`, and slot
    split `30 / 21 / 14`
  - freeze the exact clause-`2` `reference` sheet below that same side as the
    first safe side-control at
    `7149 / 551 / 2040 / 518 / 518 / 0`
    with direct `reference` remainder `239`, targeted pairing `57`, and slot
    split `33 / 24 / 14`
  - freeze the attempt to add its clause-`4` `reference` companion too:
    that exact reference-sheet extension stays completely neutral and relands
    the same `7149 / 551 / 2040 / 518 / 518 / 0` surface with the same
    `33 / 24 / 14` slot split
  - freeze the first exact `claim_flat_domain` and exact
    `claim_sharp_codomain` reopenings under that broader clause-`4`
    `reference` companion too:
    both land at `7236 / 550 / 2058 / 524 / 524 / 0`,
    keep the remaining-two clause-`4` `reference` row pinned at `9 / 9 / 6`,
    and trim only one remaining-three spill capture to
    `4 / 5 / 4`
    or
    `5 / 4 / 4`
  - step below those tied spill-only controls to the smaller exact
    `reference = 6` slice before the whole remaining-three spill
  - reopen the sibling
    `reference / claim_next_codomain = 42` and
    `reference / claim_sharp_codomain = 42` pairings only if that broader
    clause-`4` `reference` slice stays neutral
  - treat the deeper reference/reference tail at `64 = 54 + 10` as the second
    priority inside the direct `reference` branch if the mismatch-`1` slice
    stays neutral
  - keep the top-level `claim_eventual_domain` remainder
    `156 = 126 + 30` as the next whole-branch priority only if the narrowed
    direct `reference` slice stays neutral
  - treat queued branches `0 .. 3` as wall-free on the repaired head and
    queued branches `4 .. 9` as lower-priority descendants rather than fresh
    frontier-wide scout leads
- Record any landed probe or ruled-out control in
  [autonomous_ledger.md](autonomous_ledger.md), and only promote a repair once
  it is regression-backed.
- Keep the docs honest about the new state: the claim lane now has one stored
  compare / benchmark / certify-passing bundle, and any stronger wording must
  be tied directly to that stored certificate and disclosed bundle.

## Keep Green

- accepted step `15` winner stays canonical `103 / 8`
- stored step-`1` surface stays explicit at `2144 / 1285 / 1 / 475` unless it
  is intentionally reopened
- stored `small_cluster` stays no worse than
  `2052 / 522 / 522 / 0` unless a stronger audited bundle replaces `v15`
- the isolated `single` pocket stays fenced
- stronger-than-canonical lifted `89 / 8` terminals do not become fully scored
  or live
- any stronger wording stays tied directly to the stored certificate and
  disclosed bundle

## Not The Next Move

- reopening step-`1` breadth work before the certified `v15` surface regresses
- rerunning storage / evidence only to re-prove `v15` without a material code
  change
- reopening tooling work while native `pen-cli` evidence remains sufficient
- downgrading `v15` back to local-only or pre-certification language

## Success For This Slice

- one focused local step-`15` repair or tighter diagnosis exists on top of the
  certified `v15` head
- the exact clause-`2` `reference` sheet below the repaired-head
  `claim_next_bridge = 33` side is frozen as the last safe inert control
  before the broader clause-`4` `reference = 28` companion
- the first exact `claim_flat_domain` / `claim_sharp_codomain` reopenings
  under that broader clause-`4` `reference` companion are frozen as
  spill-only tradeoff controls at `7236 / 550 / 2058 / 524 / 524 / 0`
- the clean step-`15` partial-prefix wall drops below `553`, or the direct
  top-level `reference` remainder falls below `241`, or the mismatch-`1`
  surface inside that branch falls below `177`, or the
  `reference / demo_flat_codomain` branch falls below `73`, or its
  remaining-two clause-`4` `claim_next_bridge` side falls below `33`, or the
  broader clause-`4` `reference` companion falls below `28`, or the smaller
  exact `reference = 6` slice falls below `6`, or the remaining-three spill
  below the now-frozen claim-sheet controls falls below `13`, or one narrower
  live blocker below the now-frozen pair-level, spill-only claim-sheet, and
  exact-reference-sheet controls is documented without regressing accepted
  parity, the repaired stored step-`15` surface, or the certified evidence
  path
